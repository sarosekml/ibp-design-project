---
component: Chip
title: "Chip"
version: "1.018"
updated: "11.09.2026"
page: pages/atoms/Chip.html
page_js: scripts/chip.page.js
runtime: scripts/ds-chip.js (+ scripts/ds-tooltip.js — тултип усечённой подписи)
css: styles/chip.css
deps: [label-helper, avatar, spinner]
status: auto
---

> Автоспека для быстрого контекста. Источник истины — CSS-файл и страница компонента. При изменении компонента обновляй эту спеку.

## Назначение
Чип — компактный интерактивный элемент для фильтрации и группировки данных. Применяется как тег, маркер и статус, входит в состав чиплиста и InputAutocomplete. Два типа — Edit и ReadOnly, два стиля — Fill и Outline.

## Инварианты
- Корень компонента объявляет парное `[hidden] { display: none }`: браузерное правило имеет специфичность (0,0,0) и приходит из UA-стиля, а `display` компонента — (0,1,0) и перебивает его, из-за чего атрибут `hidden` молча перестаёт работать. Соглашение ДС от 05.09.2026, охраняется правилом B11 линтера.
- `max-width:320px` фиксирован на всех размерах — длинный текст обрезается эллипсисом (`.chip__label`), не растягивает чип.
- Чип СЖИМАЕМ (`min-width: 0`): он несёт данные переменной длины, поэтому в чужой flex-строке (ячейка таблицы, тулбар) уступает место и усекает подпись внутри плашки. Без этого автоминимум inline-flex равен min-content — чип вылезает за границу контейнера, и его подпись обрезает непрозрачный фон соседа, без многоточия.
- **Несжимаемый вариант `.chip--fit`** — для коротких фиксированных значений, которые обязаны показаться целиком (код валюты RUB/USD/EUR/JPY, флаг-тег PE, «Применено: N»): `flex: 0 0 auto; min-width: auto` — чип не сжимается в чужой flex-строке, подпись не усекается (код не обрезается до «R…»).
- Ведущий слот — один из icon/marker/avatar, не сочетаются одновременно; левый паддинг зависит от того, какой из них есть.
- Hover/selected — только у типа Edit; Readonly не реагирует на курсор (`cursor:default`, без hover-переходов).
- `.chip__info` — настоящая кнопка, фокусируется независимо от чипа даже внутри readonly-чипа (база `.chip`; модификатора «chip--readonly» в CSS нет — сам чип не несёт tabindex/hover).
- Disabled/`[aria-disabled]` не блокирует тултип на обрезанном тексте — `pointer-events:auto` держится через `.chip--has-tooltip`. Класс ставит рантайм и только при реальном усечении: у disabled-чипа `pointer-events: none`, событий он не даёт, поэтому ленивая привязка до него не дотягивается — его обслуживает проход `DSChip.refresh(root)`.
- **Усечённая подпись объясняет себя тултипом — это правило чипа, а не его окружения.** Владелец — `ds-chip.js` (с 1.017): делегированием по всей странице, где бы чип ни стоял — в поле ввода, в баре фильтра, в тайле, в ячейке таблицы. Раньше правило было реализовано только внутри таблицы (`ds-table.js`), и в остальных местах чип обрезался молча.
- Размер L — только для readonly; в Edit используются M/S/XS.
- Атрибут `hidden` скрывает чип по-настоящему: `.chip[hidden] { display: none }` стоит рядом с базовым `.chip { display: inline-flex }`. Этим свёрнутые в «+N» чипы прячет `ds-input.js`.
- Чип «Применено: N» у TableFilter (`.tfilter__applied`) — **не зона ответственности этого рантайма**: его снимает `ds-table-filter.js`, он же шлёт наружу `tfilter:reset`. `ds-chip.js` такой чип пропускает и по клику на крестик, и по Backspace/Delete.

## Диагностика
- «Чип обрезан многоточием, а тултипа нет» → не подключён `scripts/ds-chip.js` или `scripts/ds-tooltip.js`. С 1.017 тултип навешивает рантайм Chip по первому наведению/фокусу — руками `data-tooltip` на подпись писать не нужно
- «Тултип показывается и у короткого чипа» → у подписи проставлен свой `data-tooltip` без `data-tooltip-truncated="only"`; рантайм ставит оба атрибута сам
- «У чипа всплывают два тултипа сразу» → на чипе остался `title`: рантайм забирает его текст и снимает атрибут, но только когда сам навешивает тултип (то есть при подключённом `ds-tooltip.js`)
- «Тултип не показывается на обрезанном disabled-чипе» → рантайм ставит `.chip--has-tooltip` при загрузке; чип, построенный позже, попадает под правило только после `DSChip.refresh(root)`
- «Инфо-кнопка внутри readonly-чипа не фокусируется» → она должна быть отдельным `<button class="chip__info">`, не декоративным span
- «Крестик чипа «Применено: N» убирает чип, но фильтр остаётся применённым» → рантайм старше 1.014: `ds-chip.js` грузится раньше `ds-table-filter.js` и снимал чип первым; у оторванного от DOM чипа `closest(.tfilter)` уже `null`, и событие `tfilter:reset` не уходило. С 1.014 чип `.tfilter__applied` пропускается

## Ключевые правила (из разделов страницы)
- **Использование** — Чипы используются в составе чиплиста для отображения параметров фильтра, в InputAutocomplete при множественном выборе, а также как статус-метки в таблицах и карточках.
- **Анатомия** — Чип — это капсула-контейнер с текстом и опциональными слотами. Ширина зависит от контента, высота фиксирована по размеру. Максимальная ширина — 320px; при переполнении текст уходит в многоточие.
- **Варианты · Типы** — Компонент имеет два типа. Edit — интерактивный чип, реагирует на наведение и поддерживает удаление. ReadOnly — для показа значения без редактирования (например, в ReadOnlyField) и на наведение не реагирует.
- **Варианты · Стиль** — Fill (Border + Fill) — заливка плюс обводка, базовый стиль для чиплистов и инпутов. Outline (Border) — только обводка, без заливки фона (прозрачный фон даже у тонов — тон задаёт только цвет рамки); при наведении появляется заливка.
- **Размеры** — Четыре размера. L — крупный ReadOnly-показ (зарезервирован). M и S — базовые для чиплистов и фильтров. XS — для компактных мест (плотные таблицы, инлайн-метки, значения-чипы в ReadOnlyField).
- **Размеры · Радиус скругления** — Радиус задан размером; Rounded делает чип пилюлей в любом размере. L — 8px · M — 8px (--radius-control) · S — 6px · XS — 6px (--radius-control-s) · Rounded — 999px (--radius-pill) · слоты аватар/крестик/информер — 50%.
- **Состояния** — Базовые состояния чипа и их цветовые токены. При наведении меняется цвет фона. Чип в состоянии Disabled на наведение не реагирует — кроме случаев с длинными текстовками, когда показываем тултип.
- **Варианты · Ведущие элементы** — Слева к тексту можно добавить дополнительный элемент. .Chip_Content — иконка-маркер; Иконка — смысловая пиктограмма; Аватар — компонент Avatar с выбираемым содержимым (текст / иконка / фото). Отдельный слот «бейдж» убран — объединён с аватаром. Справа — действие: крестик удаления ИЛИ шеврон выпадающего списка (.Chip_Action) — взаимоисключающие, вместе не ставятся. Отступы — как у кнопок: сторона с иконкой/маркером/аватаром/действием получает меньший padding (аватар прижат сильнее), значение своё на каждый размер.
- **Тон и статусы** — Статусные чипы делятся на две категории. **Семантические** (success/info/warning/error) — цвет привязан к состоянию (успех/инфо/предупреждение/ошибка). **Тональные** (green/lblue/orange/red/dpurple/grey/primary) — цвет без смысла, имя = имя рампы палитры статусов; используются как простой маркер категории. Окраска одинакова для всех: фон = `-light`, обводка = `-light` (тот же), текст = `-dark`, иконка/маркер = base-тон рампы. Статусные чипы **всегда** `.chip--rounded` (пилюля). Текстовая аватарка в статусном чипе: фон = `-mid` той же рампы, текст/иконка — белые (`--text-on-dark`). **Теги и флаг-категории** (PE, валюта, «Применено: N») — НЕ статусы: не скругляются, цвет кастомный по кейсу (только токены ДС), не через тон-вариант.
- **Тон — открытый список рамп** — Тон не ограничен категориями выше: чип может использовать любую рампу из «Local» (palette.css). success/info/warning/error и green/lblue/orange/red/dpurple/grey/primary — алиасы одних и тех же локальных рамп (StGreen, StLBlue, StOrange, StRed, StDpurple, StGrey, StPrimary); **dark** — алиас StGrey, добавлен для нейтрально-«чёрной» семантики (например, Чёрная зона в RiskMetric), где ситуативных error/warning недостаточно.
- **Solid (модификатор)Новое** — Для критичных статусов, где светлой заливки недостаточно, есть solid-заливка: это модификатор, работающий с любым тоном (класс `chip--<tone>-solid`, например `chip--error-solid`). Фон и обводка = base-тон рампы, текст и иконка — белые (`--mgrey-50`), hover — `-dark`. Включается отдельным свитчом, а не вариантом в списке тонов. Использует RiskMetric (Красная/Чёрная зона — `chip--error-solid` / `chip--dark-solid`).
- **Info-trigger (`.chip__info`)Новое** — Третий вариант trailing-действия (наряду с крестиком удаления и шевроном списка): кнопка-иконка, открывающая Popover с деталями по чипу (использует RiskMetric). В отличие от `.chip__remove`/`.chip__dropdown` — это настоящий `<button>`, самостоятельно фокусируемый даже внутри readonly-чипа (база `.chip`; модификатора «chip--readonly» в CSS нет — сам чип при этом остаётся вне таб-порядка).
- **Поведение · Удаление** — Если нужно дать пользователю управлять набором чипов, добавляется крестик. Крестик — единственная интерактивная зона помимо самого чипа; на наведении у него появляется собственный стейт-слой. Удаление доступно мышью и с клавиатуры (Backspace / Delete на сфокусированном чипе).
- **Контент · Переполнение текста** — Размер чипа зависит от контента, максимальная ширина — 320px. Если длина контента превышает максимум, текст уходит в многоточие, а при наведении показываем тултип с полной версией. Это работает и для Disabled-чипа с длинной текстовкой.
- **Скругление (Rounded)Новое** — Новые чипы объединили с маркером и добавили свойство Rounded для включения полного скругления (капсула 999px). В дизайн-системе пока не перешли на них — будем переводить после внутренних тестов нового компонента.
- **Бэклог** — Сверяясь с Material Design и практиками других ДС, в документации недостаёт ряда состояний и кейсов. Ниже — предлагаемые дополнения; все собраны на существующих токенах.
- **Типографика** — Текст чипа набирается гарнитурой SB Sans Text. Кегль зависит от размера и совпадает с токенами Body. Letter-spacing — 0.
- **Контент** — Текстовое содержимое чипа — метка, опциональный числовой суффикс и текст в альтернативах (title/aria-label). Краткий свод правил (подробно — в «Переполнение текста» и «Правила» выше/ниже):
- **Использование · Правила** — Чип несёт короткую метку или статус. Не превращайте его в кнопку и не помещайте длинные предложения — для этого есть другие компоненты.
- **Доступность** — Чип Edit — фокусируемый элемент (tabindex="0"); крестик удаления — вторая интерактивная зона внутри него. ReadOnly-чип неинтерактивен и из таб-порядка исключён. Disabled-вид красится и по `[aria-disabled="true"]` напрямую (у чипа нет нативного `disabled`) — класс `.chip--disabled` нужен только для форс-состояния витрины (RulesAudit W1, 12.08.2026).
- **Цвета** — Компонент использует только семантические и локальные токены статусов. Базовый системный тон — на рампе StSystem; статусные тона — на локальных рампах.

## Для разработчиков (выжимка)

### Таблица

| Параметр | L | M | S | XS |
|---|---|---|---|---|

### Разметка · HTML (эталонная реализация ДС)

```
<!-- Edit-чип с маркером и крестиком удаления -->
<span class="chip chip--edit chip--m chip--rounded" tabindex="0">
  <span class="chip__marker"><i data-icon="circle-filled"></i></span>
  <span class="chip__label">Активна</span>
  <span class="chip__remove" role="button" aria-label="Удалить Активна"><i data-icon="close"></i></span>
</span>

<!-- ReadOnly-чип: без tabindex и без крестика -->
<span class="chip chip--m"><span class="chip__label">Договор</span></span>

<!-- чиплист: контейнер с авто-переносом и единым зазором -->
<div class="chiplist" role="group" aria-label="Фильтры">…</div>
```

### Поведение · псевдокод (framework-agnostic)

```
// 1. Hover — только для Edit и только когда не disabled: чистый CSS (:hover)

// 2. Обрезка текста: когда scrollWidth подписи > clientWidth (текст обрезан
//    ellipsis) — показать тултип с полным текстом. Из коробки (ds-chip.js): по первому
//    наведению/фокусу подпись получает data-tooltip + data-tooltip-truncated="only" и
//    привязку ds-tooltip.js; показ — только при реальном усечении. Работает и для Disabled
//    (chip--has-tooltip возвращает pointer-events, класс ставит рантайм)

// 3. Удаление: клик по chip__remove ИЛИ Backspace/Delete на сфокусированном чипе →
//    анимация исчезновения (opacity+scale) → удалить из DOM → перенести фокус на соседний чип

// 4. Переполнение чиплиста (Предложение): измерить, сколько чипов вмещается в одну строку,
//    остальные свернуть в чип-счётчик «+N», открывающий полный список по клику

// 5. Клавиатура: Tab фокусирует чип (если Edit); :focus-visible → outline 2px --primary, offset 2px

// 6. Disabled: pointer-events: none, кроме chip--has-tooltip (тултип должен работать и в disabled)
```

### Chip.types.ts

```
type ChipType  = 'edit' | 'readonly';
type ChipStyle = 'fill' | 'outline';
type ChipSize  = 'l' | 'm' | 's' | 'xs';
type ChipTone  = 'system' | 'success' | 'info' | 'warning' | 'error' | 'green' | 'lblue' | 'orange' | 'red' | 'dpurple' | 'grey' | 'primary' | 'accent' | 'dark'; // открытый список — любая Local-рампа; accent=primary, dark=grey

interface ChipProps {
  type?: ChipType;                 // по умолчанию 'edit'
  style?: ChipStyle;                // по умолчанию 'fill'
  size?: ChipSize;                  // по умолчанию 'm'; 'l' — только readonly
  tone?: ChipTone;                  // по умолчанию 'system'
  label: React.ReactNode;
  leading?: 'marker' | 'icon' | 'avatar';
  avatarContent?: 'text' | 'icon' | 'image'; // содержимое ведущего Avatar
  count?: React.ReactNode;          // числовой суффикс (реализован)
  removable?: boolean;              // только для type='edit'; взаимоисключается с dropdown
  onRemove?(): void;
  dropdown?: boolean;               // взаимоисключается с removable
  infoTrigger?: boolean;            // Предложение — кнопка ⓘ, открывает Popover с деталями
  onInfoClick?(): void;
  rounded?: boolean;
  solid?: boolean;                // base-заливка тона + белый текст (chip--<tone>-solid)
  selected?: boolean;              // Предложение — тоггл-фильтр
  loading?: boolean;                // Предложение
  invalid?: boolean;                // Предложение
  disabled?: boolean;
  tooltip?: string;                 // полный текст при обрезке
  'aria-label'?: string;           // передаётся крестику удаления как «Удалить {label}»
}

interface ChiplistProps {
  children: React.ReactNode;        // Chip[]
  size?: 'm' | 's';              // влияет на зазор между чипами
  'aria-label'?: string;
}
```

### Таблица

| Класс / атрибут | На чём | Назначение |
|---|---|---|
| .chip | span | Корень-капсула; размер через CSS-переменные --chip-* |
| .chip--edit / --readonly | span | Тип |
| .chip--outline | span | Стиль Border (без заливки) |
| .chip--l / --m / --s / --xs | span | Размер |
| .chip--accent / --primary / --success / --green / --info / --lblue / --warning / --orange / --error / --red / --dark / --grey / --dpurple | span | Статусный тон: семантический (success/info/warning/error) или тональный (по имени рампы: green/lblue/orange/red/dpurple/grey/primary); accent=primary, dark=grey; фон `-light`, текст `-dark`, иконка base |
| .chip--rounded | span | Полное скругление 999px — статусы/стадии; статусные чипы всегда `.chip--rounded` |
| .chip--fit | span | Не сжимается: короткое фиксированное значение (код валюты, PE, «Применено: N») показывается целиком, подпись не усекается (`flex: 0 0 auto; min-width: auto`) |
| .chip--dark | span | Алиас `.chip--grey` — нейтрально-«чёрная» семантика (Чёрная зона RiskMetric) |
| .chip--<tone>-solid (например --success-solid / --error-solid / --dark-solid / --green-solid / --dpurple-solid) | span | Solid-модификатор на любой тон: base-заливка + белый текст/иконка (Новое; RiskMetric) |
| .chip--selected / .is-focus / --invalid | span | Состояния (Предложение) |
| .chip--disabled | span | pointer-events:none, цвет в --st-disabled-* |
| .chip--has-tooltip | span | Возвращает pointer-events disabled-чипу с обрезанным текстом |
| .chip__label | span | Текст с ellipsis при переполнении |
| .chip__marker / __icon | span | Ведущий слот: маркер-статус / смысловая пиктограмма |
| .chip__avatar | span | Ведущий аватар — компонент Avatar (.av .av--circular), содержимое текст/иконка/фото, уменьшен под чип |
| .chip__count | span | Числовой суффикс |
| .chip__dropdown | span | Шеврон выпадающего списка |
| .chip__remove | span[role=button] | Крестик удаления — собственный hit-стейт; парный aria-label |
| .chip__info | button | Info-trigger — открывает Popover с деталями (Предложение). Настоящий `<button>`, а не span[role=button] |
| .spin | span | Индикатор загрузки (Предложение) — общий Spinner (styles/spinner.css) |
| .chiplist | div | Flex-контейнер с авто-переносом и единым зазором (8px, --s — 6px) |
| tabindex="0" / role="button" / aria-label | span | Доступность: фокусируемый Edit-чип и озвучивание крестика |
