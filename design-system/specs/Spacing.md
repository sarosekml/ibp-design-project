---
component: Spacing
title: "Сетка и отступы"
version: "1.005"
updated: "05.09.2026"
page: pages/foundations/Spacing.html
css: styles/spacing.css
status: curated
---

> Спека для быстрого контекста. Источник истины — styles/spacing.css и страница. При изменении обновляй эту спеку и блок в specs/_cheatsheet.md.

> Корень компонента объявляет парное `[hidden] { display: none }`: браузерное правило имеет специфичность (0,0,0) и приходит из UA-стиля, а `display` компонента — (0,1,0) и перебивает его, из-за чего атрибут `hidden` молча перестаёт работать. Соглашение ДС от 05.09.2026, охраняется правилом B11 линтера.

## Назначение
Модуль 4 px и колоночная сетка. Все отступы, зазоры и размеры кратны 4 px; горизонтальную структуру задают 12 резиновых колонок контентной области с постоянным зазором 16 px. Значения берутся из токенов `--space-*` / `--grid-*`.

## Ключевые правила (из разделов страницы)
- **Использование** — колоночная сетка = горизонтальная структура (ширина блока в колонках), шкала отступов = расстояния внутри и между блоками; вертикаль сетка не задаёт. Нарушения: значения вне шкалы (13/18/22/30), ширина колонки в px, контент в зазоре.
- **Модуль 4 px** — база 4 px, основной шаг ритма 8 px. Полушаги 2/6/10/14 — исключения только для микро-оптики (фокус-обводка, XS-контролы, круглые формы); новых не вводить.
- **Колоночная сетка** — 12 колонок, зазор 16 px постоянный, колонки резиновые (`minmax(0,1fr)`), поля внутри области по умолчанию 0 (задаёт лайаут экрана). Формула: `col = (W − 2·margin − (N−1)·gutter) / N`; дробная ширина колонки — норма. Типовые раскладки: 12 · 8+4 · 6+6 · 4+4+4 · 3+3+3+3. Вложенная сетка наследует зазор.
- **Ширина блока** — любое число колонок 1–12 (`.col-N`), кратность не требуется. Ряд набирается до 12, недобор — норма. Раскладки экранов продукта: Главная 3+3+3+3, реестр 12, карточка сущности 3+3+6 / 6+3+3 / 6. Исключение: в редких случаях плитка может иметь кастомный фиксированный размер или ширину по контенту — осознанное исключение, помечается в разметке `data-off-grid="причина"` (без пометки — ошибка линтера L2).
- **Перестроение при сужении** — блок объявляет две ширины: основную `.col-N` и узкую `.colw-N` (`class="col-3 colw-6"`). Сам `.colw-N` ширину не меняет — только объявляет `--colw`; включает узкие ширины экран — своим порогом `@container screen (max-width: …) { .grid12 > [class*="colw-"] { grid-column: span var(--colw) } }` или классом `.grid12--narrow` на сетке (когда режим зависит от состояния, а не от ширины). Общего для ДС набора брейкпоинтов нет, ступеней две (широкая/узкая). Обособленные боковые панели в 12 колонок не входят — описываются на уровне экрана.
- **Где применяется** — таблица соответствий «расстояние → алиас → примитив».
- **Правила** — только из шкалы; близость = связь (внутри группы меньше, чем между группами); отступ задаёт контейнер через `gap`; чем крупнее блок, тем больше воздух; не складывать margin поверх padding.
- **Для разработчиков** — redline токенов (читается с `:root` через getComputedStyle) + псевдокод расчёта сетки и правило округления `v = round(v/4)*4`.
- **Бэклог** — общие брейкпоинты (число колонок фиксировано 12, порог задаёт экран), обособленные боковые панели, baseline-ритм, плотности, утилиты `.stack-*`/`.inline-*`, аудит компонентов на соответствие шкале.

## Токены

**Модуль**: `--space-unit` (4px)

**Шкала (кратно 4)**: `--space-0` `--space-4` `--space-8` `--space-12` `--space-16` `--space-20` `--space-24` `--space-32` `--space-40` `--space-48` `--space-64` `--space-80` `--space-96`

**Полушаги (исключения)**: `--space-2` `--space-6` `--space-10` `--space-14`

**Семантические алиасы**: `--gap-icon-text` (8) `--gap-label-field` (4) `--gap-inline` (8) `--gap-stack` (16) `--gap-group` (24) `--gap-section` (40) `--pad-control-s` (8) `--pad-control` (12) `--pad-card` (16) `--pad-panel` (24) `--pad-page` (24, поля контентной области экрана — см. Каркас экрана) (32)

**Сетка**: `--grid-columns` (12) `--grid-gutter` (16px) `--grid-margin` (0) `--colw` (узкая ширина блока, объявляется классом `.colw-N`)

## Разметка · утилиты

```html
<div class="grid12">
  <section class="col-8">контент</section>
  <aside class="col-4">панель</aside>
</div>
```

| Класс | Назначение |
|---|---|
| `.grid12` | CSS Grid на `--grid-columns` колонок с зазором `--grid-gutter` и полями `--grid-margin` |
| `style="--grid-gutter:var(--space-16)"` | локальная подмена зазора (инлайн на контейнере) |
| `.col-1` … `.col-12` | ширина блока в колонках (`grid-column: span N`) |
| `.colw-1` … `.colw-12` | узкая ширина блока: только объявляет `--colw`, сама по себе ширину не меняет |
| `.grid12--narrow` | на сетке: включает узкие ширины дочерних блоков (альтернатива — `@container`-порог экрана) |
