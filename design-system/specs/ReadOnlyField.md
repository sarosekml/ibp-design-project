---
component: ReadOnlyField
title: "ReadOnlyField"
version: "1.007"
updated: "05.09.2026"
page: pages/molecules/ReadOnlyField.html
page_js: scripts/read-only-field.page.js
runtime: scripts/ds-copy.js, scripts/ds-readonlyfield.js (+ scripts/ds-tooltip.js — тултип переполнения)
css: styles/read-only-field.css
deps: [label-helper, chip, link, tooltip, segment-control, splitter]
status: auto
---

> Автоспека для быстрого контекста. Источник истины — CSS-файл и страница компонента. При изменении компонента обновляй эту спеку.

## Назначение
Поле для отображения данных без возможности редактирования. Показывает значение — текст, чипы или ссылку — с опциональными лейблом и хэлпером (см. Label / Helper), иконками слева/справа и префиксом/постфиксом. Не имеет собственного контейнера: без рамки и фона, это пара «подпись → значение» для карточек сущностей и детальных панелей.

## Инварианты
- Корень компонента объявляет парное `[hidden] { display: none }`: браузерное правило имеет специфичность (0,0,0) и приходит из UA-стиля, а `display` компонента — (0,1,0) и перебивает его, из-за чего атрибут `hidden` молча перестаёт работать. Соглашение ДС от 05.09.2026, охраняется правилом B11 линтера.
- Копирование по иконке-действию `copy` — общий рантайм `scripts/ds-copy.js` (`DSCopy.write`/`.flash`); визуальный морф иконки в галочку остаётся за компонентом.
- Нет собственного контейнера (без рамки/фона) — это пара «подпись → значение», встраивается в чужой контейнер.
- Префикс/постфикс — короткие по дизайну (~10–12 символов, символ/аббревиатура), не полное слово; длинная строка обрезается, не растягивает строку.
- Значение типа `link` сохраняет цвета компонента Link — семантические цвета (positive/negative/muted) применяются только к text/chips-значениям.
- Переполнение — либо одна строка (ellipsis), либо N строк (line-clamp), обе версии с тултипом на переполнении.

## Диагностика
- «Ссылка-значение окрашена в error/success» → семантический цвет применяется только к text/chips, ссылка держит свой цвет Link
- «Префикс распирает строку» → должен обрезаться эллипсисом при `max-width:12ch`, не расти

## Ключевые правила (из разделов страницы)
- **Использование** — ReadOnlyField используется исключительно для отображения данных — в карточках сущностей и детальных панелях, когда значение приходит из системы и не редактируется пользователем напрямую в этом месте экрана.
- **Анатомия** — Поле — вертикальный стек из трёх зон: подпись, строка значения и пояснение. Строка значения может нести до пяти слотов одновременно; всё, кроме самого значения, — опционально.
- **Размеры** — У ReadOnlyField нет собственной шкалы размеров (M/S/…) — типографика каждого слота фиксирована токенами: Label и Helper — Body XS, значение — Body M. Единственная зависимая переменная — размер вложенного Chip для значений-чипов — всегда XS. Значения ниже измерены на живом экземпляре.
- **Размеры · Радиус скругления** — Своей плашки у поля нет — радиус только у вложенных элементов. поле — 0 · аватар — 50% · чип-значение S/XS — 6px (--radius-control-s).
- **Варианты · Типы значения** — Три типа значения. Текст — произвольная строка, может обрезаться или переноситься. Чипы — список меток размера XS (см. компонент Chip, тип ReadOnly). Ссылка — значение-переход, набирается компонентом Link.
- **Варианты · Опции** — Дополнительные слоты включаются независимо друг от друга и сочетаются в любой комбинации: иконка слева, префикс/постфикс, иконка справа (любой глиф из библиотеки иконок; действие — копирование, тултип или без действия), скрытие лейбла, выравнивание значения. Все доп. слоты доступны и типу «Чипы» — кроме префикса/постфикса: аффиксы только у типа «Текст».
- **Поведение · Переполнение и перенос** — Тип «Текст» может течь свободно, обрезаться до одной строки или до N строк — во всех случаях обрезанный текст уходит в многоточие, а при наведении курсора показывается тултип с полным содержанием. Тип «Чипы» переносится по-другому: чипы как целый список уходят на следующую строку, а при ограниченной высоте лишние сворачиваются в счётчик «+N». Тултип у счётчика обязателен: по hover и focus перечисляет через запятую названия всех скрытых чипов.
- **Варианты · Цвет значения** — Только для типа «Текст» (в конструкторе контрол скрыт для чипов и ссылки). Текст значения может быть окрашен для передачи динамики — например, положительного (зелёный) или отрицательного (красный) изменения показателя. Остальные слоты (лейбл, хэлпер, аффиксы) цвет не наследуют.
- **СостоянияДополнено** — Кроме обычного заполненного значения, компоненту нужны состояния отсутствия данных и ожидания ответа от системы — в материалах ДС они не описаны, добавляем на существующих токенах.
- **Типографика** — Label и Helper набираются токеном --type-body-xs (как в компоненте Label/Helper), значение — --type-body-m. Letter-spacing — 0.
- **Контент** — Текстовое содержимое компонента — четыре независимых слота: подпись, значение, пояснение и опциональные аффиксы.
- **Использование · Правила** — ReadOnlyField сообщает, что значение принципиально недоступно для правки в этом месте интерфейса — в отличие от задизейбленного инпута, который лишь временно не активен.
- **Доступность** — ReadOnlyField — пара «подпись → значение» из статичного текста; сам по себе он не интерактивен и не участвует в таб-порядке, кроме собственных точечных интерактивных элементов (иконка-копирование, значение-ссылка).
- **Цвета** — Компонент использует только семантические токены текста; для положительной/отрицательной динамики — ситуативные токены Success/Error.

## Для разработчиков (выжимка)

### Таблица

| Параметр | Значение |
|---|---|

### Разметка · HTML (эталонная реализация ДС)

```
<!-- значение-текст, с иконкой слева и копированием справа -->
<div class="rof">
  <span class="ds-label"><span class="ds-label__text">ID во внешней АС</span></span>
  <div class="rof__row">
    <span class="rof__icon"><i data-icon="info-circle"></i></span>
    <span class="rof__value">123456789012</span>
    <span class="rof__icon rof__icon--interactive" role="button" tabindex="0" aria-label="Скопировать значение"><i data-icon="copy"></i></span>
  </div>
</div>

<!-- значение-чипы -->
<div class="rof">
  <span class="ds-label"><span class="ds-label__text">Регионы присутствия</span></span>
  <div class="rof__row"><div class="rof__value rof__value--chips"><div class="chiplist chiplist--s">…</div></div></div>
</div>
```

### Поведение · псевдокод (framework-agnostic)

```
// 1. Обрезка/тултип: когда scrollWidth/scrollHeight элемента значения > clientWidth/clientHeight
//    (текст обрезан ellipsis или line-clamp) — на hover/focus показать tooltip с полным текстом.
//    Проверка выполняется заново при каждом наведении — обрезка зависит от текущей ширины layout.

// 2. Копирование: клик/Enter/Space по rof__icon--interactive → navigator.clipboard.writeText(value) →
//    иконка временно меняется на «check», всплывает тултип «Скопировано» (~1.3с), затем возврат

// 3. Переполнение чип-листа (chipsMaxRows): после layout измерить offsetTop чипов, сгруппировать
//    по строкам; если строк больше лимита — скрыть лишние чипы и добавить чип-счётчик «+N»
//    с ОБЯЗАТЕЛЬНЫМ тултипом, перечисляющим скрытые значения через запятую (hover + focus)

// 3a. Все тултипы поля (переполнение, аффикс, «+N», «Скопировано») монтируются в body,
//    position:fixed, по центру НАД целью с зазором 8px, pointer-events:none;
//    если сверху не помещается — переворот вниз; скрываются по scroll/leave

// 4. Empty: если данных нет — показать value = «—» приглушённым цветом вместо пустой строки

// 5. Loading: aria-busy="true" на корне; вместо label/value — шиммер-плейсхолдеры фиксированной высоты
```

### ReadOnlyField.types.ts

```
type ROFValueType = 'text' | 'chips' | 'link';
type ROFTone      = 'default' | 'positive' | 'negative' | 'empty';

interface ReadOnlyFieldProps {
  label: React.ReactNode;
  showLabel?: boolean;                // по умолчанию true
  helper?: React.ReactNode;
  helperStatus?: 'default' | 'error';
  type?: ROFValueType;                     // по умолчанию 'text'
  value?: React.ReactNode;                 // text / link
  chips?: string[];                     // type='chips'
  chipsMaxRows?: number;                // свёртка в «+N» (Предложение)
  linkHref?: string;                    // type='link'
  iconLeft?: React.ReactNode;
  prefix?: string;
  postfix?: string;
  iconRight?: React.ReactNode;
  iconRightAction?: 'none' | 'copy' | 'tooltip';
  iconRightTip?: string;                 // обязателен при iconRightAction='tooltip'
  tone?: ROFTone;                           // 'empty' — плейсхолдер «—» (Предложение)
  clampMode?: 'none' | '1' | '2' | '3';    // число строк перед line-clamp
  align?: 'left' | 'right';
  state?: 'default' | 'loading';      // (Предложение)
}
```

### Таблица

| Класс / атрибут | На чём | Назначение |
|---|---|---|
| .rof | div | Корень — без рамки и фона; вертикальный стек label → row → helper |
| .rof--align-right | div | Правое выравнивание — для числовых/денежных значений |
| .rof__row | div | Строка значения — до пяти слотов (иконка/префикс/значение/постфикс/иконка) |
| .rof__icon | span | Иконка слева (декоративная) или справа (действие); --interactive — копирование |
| .rof__affix | span | Префикс/постфикс; обрезается многоточием при превышении ~12 символов |
| .rof__value | span / div / a | Единственный обязательный слот; тег зависит от типа (span/div/a) |
| .rof__value--positive / --negative / --muted | span | Цвет значения — динамика / пусто (Предложение) |
| .rof__value--clamp-1 / --clamp-n | span | Обрезка в одну строку (ellipsis) / N строк (line-clamp, --rof-clamp) |
| .rof__value--chips | div | Обёртка чип-листа — внутри реальные .chip.chip--xs |
| .sk-line / .sk-line--caption | span | Loading-плейсхолдер — общий компонент Skeleton (Предложение) |
| .ds-label / .ds-helper | span | Подпись и пояснение — общие классы компонента Label/Helper |
| aria-labelledby / <dl><dt><dd> | div | Связь подписи и значения для скрин-ридера (см. «Доступность») |
