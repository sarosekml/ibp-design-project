---
type: workspace
title: Правила работы с воркспейсом IBP (GigaCode)
scope: корневой файл контекста для GigaCode
updated: "16.09.2026"
---

# IBP ai-Designer — правила для GigaCode

Этот файл читает GigaCode из корня воркспейса `aiDesigner`. Он описывает
структуру проекта и то, как с ним работать, чтобы выполнять главную задачу —
**генерацию HTML-экранов на компонентах дизайн-системы IBP**.

Язык общения и комментариев — **русский**. Контент репозитория —
UTF-8.

## Что это за проект

Дизайн-система продукта **IBP (Investment Banking Platform)** — внутренней
платформы инвестиционного банкинга. **Ванильный HTML/CSS, без React и без
сборщика.** Экран — это один самодостаточный `.html`, который открывается
в браузере двойным кликом. Интерфейс — плотный, деловой, на русском;
акцентный цвет — изумрудно-зелёный.

Главная задача агента — **генерация экранов из компонентов дизайн-системы**:
взять ТЗ/скриншот/описание, прочитать нужные спеки ДС, собрать экран как
HTML + markdown-спеку экрана, проверить и сдать.

## Четыре части проекта

В проекте четыре смысловые части, и они **не взаимозаменяемы**:

### 1. `.opencode/` — рабочая среда агента ai-designer

Папка, где лежит всё для работы агента `ai-designer`: правила его работы,
агенты, скиллы, команды и весь процесс генерации HTML-прототипов. Это
инструментарий, а не продукт и не дизайн-решения.

Ключевые файлы:

| Файл | Что содержит |
|---|---|
| `.opencode/opencode.json` | Конфиг: подключение `ds-rules.md`, права на инструменты |
| `.opencode/rules/ds-rules.md` | **Главные правила работы с ДС** — читать всегда, целиком |
| `.opencode/agents/ai-designer.md` | Ведущий агент: маршруты, распределение по субагентам, границы |
| `.opencode/agents/screen-builder.md` | Сборка экрана `<Имя>.html` + `<Имя>.screen.md` |
| `.opencode/agents/screen-reviewer.md` | Приёмка экрана по чек-листу (PASS / NEEDS-WORK) |
| `.opencode/agents/ux-researcher.md` | Исследование: база знаний продукта, выжимка под задачу |
| `.opencode/commands/*.md` | `/screen`, `/research`, `/concepts`, `/handoff`, `/resume`, `/screen-check` |
| `.opencode/skills/screen-assembly/SKILL.md` | Пошаговая сборка экрана на ДС |
| `.opencode/skills/ds-lookup/SKILL.md` | Как найти компонент в ДС и достать разметку, не читая большие файлы |
| `.opencode/skills/screen-review/SKILL.md` | Полная приёмка экрана |
| `.opencode/skills/screen-assembly/references/skeleton.html` | **Каркас экрана** — стартовая копия при сборке |
| `.opencode/skills/screen-assembly/references/patterns.md` | Готовые рецепты разметки (крошки, шапка, таблица, модалка) |
| `.opencode/skills/screen-assembly/references/runtime-hooks.md` | Таблица «что работает само / что требует атрибута» |

### 2. `DS-IBP/` — дизайн-система и компоненты

Источник истины: токены, классы компонентов и документация. **Файлы этой
папки агент не меняет** (исключение — явное поручение человека). Экран
подключает ДС двумя тегами: `ds.css` (все стили) и `scripts/ds.js` (все
рантаймы).

Ключевые файлы:

| Файл | Что содержит |
|---|---|
| `DS-IBP/ds.css` | Единая точка подключения стилей (@import всех `styles/*`) |
| `DS-IBP/scripts/ds.js` | Единая точка подключения рантаймов (сам догружает иконки, табы, модалки, таблицы) |
| `DS-IBP/specs/_index.md` | Манифест: компонент → спека → CSS → версия |
| `DS-IBP/specs/_cheatsheet.md` | Чит-шит: по блоку на каждый компонент. **Большой — читать только точечно** |
| `DS-IBP/specs/<Имя>.md` | Полная спека компонента |
| `DS-IBP/specs/Icons.md` | Все имена глифов (вставляются как `<i data-icon="имя">`) |
| `DS-IBP/styles/colors.css` (базовые цветовые рампы), `palette.css` (семантика цвета), `typography.css`, `spacing.css`, `radius.css`, `shadow.css` | Токены фундамента |
| `DS-IBP/templates/screen/Screen.html` | Стартовый шаблон экрана |
| `DS-IBP/pages/` | Документация компонентов (не читать целиком) |

### 3. `Projects/` — готовые экраны проекта

Лежат **уже готовые экраны проекта**, собранные в строгом соответствии с
дизайн-системой и текущим состоянием кода — **актуальные версии продуктовых
экранов**. Сюда агент не пишет: туда человек переносит то, что соответствует
настоящей системе. Всё здесь — строго на компонентах ДС.

### 4. `Concepts/` — концепты и разработки (R&D)

Лежат **концепты и новые разработки** — в том числе на новых компонентах и
без них. Это **R&D-папка**: отсюда берут идеи для разработки новых идей и
новых компонентов для ДС. **Сюда агент кладёт собранные экраны**: папка
концепта `<Имя>/` с `<Имя>.html` + `<Имя>.screen.md`.

Дополнительно:

| Файл | Что содержит |
|---|---|
| `hub.js` (корень) | Реестр хаба: одна запись на проект или концепт. Новый концепт регистрируется здесь |
| `index.html` (корень) | Хаб проектов: меню и три колонки строятся из `hub.js`, сама страница не правится |
| `AGENTS.md` (корень) | Правила проекта для внешних агентных CLI (opencode и др.) |

## Как работать: главная задача — сборка экрана

Сборку ведёт агент `ai-designer` по маршрутам из
`.opencode/agents/ai-designer.md`. Ключевые моменты:

### Порядок чтения перед задачей

Чтобы успешно выполнять генерацию экранов, **в начале задачи** подключи в
контекст:

1. `GIGACODE.md` (этот файл) — карта проекта. ✅
2. `.opencode/rules/ds-rules.md` — правила ДС, **целиком**.
3. `.opencode/agents/ai-designer.md` — роли и маршруты (по своей роли).
4. По необходимости — скиллы: `screen-assembly` (сборка),
   `screen-review` (приёмка), `ds-lookup` (поиск компонента),
   `concept-design` (концепты), `session-plan` (смета захода).
5. Каркас экрана — `.opencode/skills/screen-assembly/references/skeleton.html`
   (копия при старте сборки) + `patterns.md` + `runtime-hooks.md`.

### Главное правило: ничего не выдумывать

Цвет, размер, отступ, радиус, шрифт, класс, имя иконки — **только из
дизайн-системы**. Нет нужного — **спросить, а не изобрести**. Доменный термин
из ТЗ, не описанный в базе знаний, — тоже вопрос пользователю.

### Экономия контекста

ДС читать **только точечно**: `grep -n "^## "` по оглавлению чит-шита,
`sed -n '/^## <Имя>$/,/^## /p'` по блоку компонента, `read` с `offset/limit`.
Запрещено читать целиком:

- `DS-IBP/scripts/icons-data.js` (671 KB в одну строку);
- `DS-IBP/specs/_cheatsheet.md` (~170 KB);
- страницы `DS-IBP/pages/**/*.html`;
- собранный экран при правке — только по якорям секций (`#sec:head`,
  `#sec:content`, `#sec:modals`).

### Инварианты экрана

- Скроллится **только контент**, горизонтального скролла не бывает никогда.
- `body { margin: 0 }` обязателен (в шаблоне уже есть).
- Иконки — только `<i data-icon="имя">`, никакого инлайнового `<svg>`.
- Нет `#hex`/`rgb()`/`px` в цветах и типографике — только токены и классы ДС.
- Состояния компонентов (`--selected`/`--disabled`/`:hover`) задаёт только сам
  компонент; свой `<style>` — только раскладка (grid/flex/gap) на токенах.
- Собственный JavaScript вместо рантайма ДС не пишется. Хуки рантаймов —
  в `runtime-hooks.md`.
- Таблица-реестр занимает всю свободную высоту (`screen--app` + `dtable--fill`).
- После каждой сборки — приёмка `screen-reviewer`, без исключений.

### Куда класть результат

Результат сборки — папка концепта `Concepts/<Имя>/`:
`<Имя>.html` + `<Имя>.screen.md`, плюс запись в `hub.js`
(`group: 'concepts'`). Крошки/меню ведут на хаб `../../index.html`.
Файлы ДС и `Projects/` агент не трогает.

### Проверка в конце захода

Одна команда `lessons-cli gate` — прогоняет сторожей изменившегося, строка
`ВЕРДИКТ:` идёт в отчёт. Контекст между сессиями — `/handoff` и
`/resume <Задача>` (файл `Concepts/<Задача>/<Задача>.handoff.md`).