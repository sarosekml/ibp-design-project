---
screen: Фикстура Б32
zone: тестовый корпус сенсора
role: —
source: не экран продукта; спутник существует, чтобы внести в спеку ровно один дефект — разделы-журнал
---

# Фикстура Б32

Файл не описывает продуктовый экран. Дефект внесён ниже: спека ведёт журнал
правок по датам вместо описания текущего состояния. Такой журнал копится
заходами и платится заново каждой сессией, которая открывает файл.

Разметка `Б32.bad.html` — копия эталона и дефектов не содержит: проверяется
спутник, а не HTML.

## 1. Назначение

Проверить, что Б32 видит журнальные разделы.

## 2. Правки 03.09.2026

Дефект: раздел-журнал с датой в заголовке.

## 3. Правки 04.09.2026 (пагинация)

Второй такой же — сторож обязан посчитать оба.

## История правок

Третья форма журнала: без даты в заголовке, но по сути тот же журнал.
