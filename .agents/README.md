---
type: doc
title: Настройка opencode для сборки экранов на IBP DS
audience: человек, а не агент
updated: "21.09.2026"
---

# ai-designer — агент-дизайнер для opencode

Что это: настройка **opencode** (агентный CLI для корпоративного контура,
работает на DeepSeek / Qwen и любых других моделях), которая превращает ТЗ
в собранный на дизайн-системе IBP экран.

Собирать ничего не нужно — opencode сам находит эти файлы, когда запускается
из корня репозитория.

---

## 1. Как это устроено

Вы работаете **с одним агентом — `ai-designer`**. Он выбирается один раз и
дальше сам решает, кого из специалистов подключить под задачу. Переключать
агентов вручную не нужно.

```
                    ВЫ
                     │
              ┌──────▼──────┐
              │ ai-designer │   ← единственный агент, который выбирается вручную
              │  (primary)  │     говорит с вами, задаёт вопросы, отвечает за итог
              └───┬─────┬───┘
                  │     │         вызывает субагентов инструментом task
        ┌─────────┘     └─────────┐
        ▼                         ▼
┌──────────────┐          ┌────────────────┐
│screen-builder│          │screen-reviewer │
│  (subagent)  │          │   (subagent)   │
│ сборка экрана│          │ приёмка, PASS  │
└──────────────┘          └────────────────┘

  ux-researcher (база знаний продукта) — выключен, пока база не подключена
```

Три маршрута, которые `ai-designer` запускает сам:

| Маршрут | Что происходит |
|---|---|
| Собери экран | вопросы вам → `screen-builder` → `screen-reviewer` → результат |
| Спроектируй концепты | текстовые спеки нескольких концептов → ваш выбор → «Собери экран» |
| Проверь экран | `screen-reviewer` → вердикт |

Маршрут «Исследуй и собери» и команда `/research` выключены с 21.09.2026:
базы знаний продукта нет, и маршрут всегда возвращал пустой отчёт. Роль
`ux-researcher` и скилл `knowledge-lookup` оставлены и включаются вместе с базой
знаний (раздел 6).

**Почему субагентам нужны подробные задания.** Субагент не видит ваш разговор
с `ai-designer` — он получает только текст задания и начинает с нуля. Поэтому
в промпте `ai-designer` отдельно прописано, что в задание обязательно кладутся
пути к файлам, принятые решения и границы работы. Если субагент делает не то,
почти всегда дело в задании, а не в самом субагенте.

---

## 2. Что где лежит

Харнес — роли, команды, скиллы, правила и оснастка — лежит в нейтральном
каталоге `.agents/`. Всё, что понимает только opencode, — в тонком адаптере
`.opencode/`: конфиг и файлы-указатели на роли и команды харнеса. Оба пути
записаны в манифесте проекта `project.json` (`agentKit.mount`,
`agentKit.adapter`).

| Файл | Что делает | Кто читает |
|---|---|---|
| `AGENTS.md` (в корне репозитория) | вход: где харнес, ДС и манифест, одна команда проверки; открытый формат, читается и другими агентными CLI | opencode при старте |
| `.opencode/opencode.json` | конфигурация: права на файлы и команды, подключение правил. Модели здесь нет — см. раздел 4 | opencode при старте |
| `.opencode/agents/*.md` | указатели на роли: в шапке — режим, температура и права роли, в теле — путь к роли в `.agents/agents/` | opencode |
| `.opencode/commands/*.md` | указатели на команды: в шапке — описание и роль-исполнитель, в теле — путь к сценарию в `.agents/commands/` и аргументы | opencode |
| `.agents/rules/process.md` | правила процесса: проект, границы записи, работа с файлами, проверки, экономия контекста, самообучение | подключается через `instructions`, попадает в контекст всегда |
| `DS-IBP/AGENTS.md` | правила ДС для агента: карта ДС, чтение, запреты, каркас экрана, каталог компонентов | подключается через `instructions`, попадает в контекст всегда |
| `.agents/agents/ai-designer.md` | **ведущий агент** — маршрутизация и разговор с вами | opencode |
| `.agents/agents/screen-builder.md` | субагент: собирает экран | вызывается `ai-designer` |
| `.agents/agents/screen-reviewer.md` | субагент: приёмка, ничего не правит | вызывается `ai-designer` |
| `.agents/agents/ux-researcher.md` | субагент: выжимка из базы знаний продукта — **выключен**, пока база не подключена | никем, до подключения (раздел 6) |
| `.agents/commands/screen.md` | команда `/screen` | opencode |
| `.agents/commands/screen-check.md` | команда `/screen-check` | opencode |
| `.agents/commands/concepts.md` | команда `/concepts` — несколько концептов текстовыми спеками | opencode |
| `.agents/commands/handoff.md`, `resume.md` | команды `/handoff` и `/resume <Задача>` — перенос контекста между сессиями через `Concepts/<Задача>/<Задача>.handoff.md` | opencode |
| `.agents/skills/ds-lookup/` | как искать компоненты в ДС, не сжигая контекст | агент, по своему решению |
| `.agents/skills/screen-assembly/` | пошаговая сборка; в `references/` — рецепты разметки. Каркас экрана и хуки рантаймов — в ДС: `DS-IBP/templates/screen/Screen.html`, `DS-IBP/specs/_runtime-hooks.md` | агент |
| `.agents/skills/screen-spec/` | формат `<Имя>.screen.md` + шаблон | агент |
| `.agents/skills/screen-review/` | **чек-лист приёмки** | агент |
| `.agents/tools/` | оснастка: сенсор `layout-check.mjs`, `lessons-cli.mjs` (журнал уроков, итоговая проверка `gate`), сторожа, фикстуры | агент — запуском через `node`, в контекст идёт только вывод |
| `.agents/skills/layout-composition/`, `composition-review/` | композиция и сетка экрана, её приёмка | агент |
| `.agents/skills/concept-design/` | шкала «консервативно ↔ концептуально» и формат `<Имя>.concept-<A\|B\|C>.md` | агент |
| `.agents/skills/lessons/` | самообучение: урок → закрепление → доказательство откатом | агент |
| `.agents/skills/docs-split/` | раскатка страниц документации ДС под «сплиттер + табы» | агент |
| `.agents/skills/knowledge-lookup/` | путь к базе знаний, карта её папок, правила поиска и подключения — **выключен**, пока не задан `KB` | `ux-researcher` после подключения |
| `.agents/skills/session-plan/` | смета контекстного окна до старта (`ctx-budget.mjs`), паспорт этапов, границы сессий | агент, в начале крупной задачи |
| `Concepts/<Имя>/` | сюда складываются готовые экраны (папка концепта) | — |
| `index.html`, `hub.js` (корень) | хаб проектов и его реестр: одна точка входа в ДС, проекты и концепты | человек, агент дописывает запись реестра |

**Почему конфиг в `.opencode/`, а не в корне.** opencode читает проектный
конфиг из обоих мест: `opencode.json` / `opencode.jsonc` в корне и те же
файлы в `.opencode/` (проверено на 1.18.30). Слой `.opencode/` грузится
позже и перекрывает корневой. Вся настройка opencode держится в адаптере,
поэтому конфиг один — `.opencode/opencode.json`. Второй файл в корне не
заводить: права начнут складываться из двух слоёв, и запрет, снятый в одном
из них, никто не увидит. Конфиг в `.agents/` opencode не читает вовсе. Гейт
ловит оба случая (шаг `agent-config`).

**Почему в адаптере указатели, а не копии ролей.** opencode ищет агентов и
команды только в `.opencode/` (документация установленной версии 2.0.11), а
скиллы находит и в `.agents/skills` сам. Копия роли в адаптере разошлась бы
с оригиналом молча, а симлинк на Windows без режима разработчика git
выписывает текстовым файлом. Поэтому в адаптере — указатель: агент первым
делом читает свою роль из `.agents/`. Если CLI не подхватывает роли и скиллы
вовсе, работа идёт так же — чтением файла из `.agents/` (правило в
`AGENTS.md`). Сходимость адаптера с харнесом — у каждой роли и команды есть
указатель, описание одно, аргументы команды передаются — проверяет гейт,
шаг `agent-config`, код КФ5.

Относительные пути в `instructions` opencode ищет от папки запуска вверх, а
не от места конфига, поэтому `.agents/rules/process.md` записан от корня
репозитория.

**Разница между агентом, скиллом и правилами.**
*Правила* (`process.md`) — в контексте всегда, поэтому короткие.
*Агент* — роль и порядок работы, загружается при выборе или вызове агента.
*Скилл* — инструкция, которую агент подгружает сам, когда доходит до нужного
шага; так тяжёлые детали не занимают контекст всё время. Это важно для
DeepSeek и Qwen с их окном контекста.

---

## 3. Как запускать

```bash
cd "путь к репозиторию ДС"
opencode
```

Дальше:

| Способ | Как |
|---|---|
| Выбор агента | `Tab` → выбрать `ai-designer`. Он единственный в списке — остальные субагенты и вручную не выбираются |
| Собрать экран | `/screen DS-IBP/uploads/ТЗ-реестр-сделок.md` |
| Проверить готовое | `/screen-check Concepts/DealRegistry/DealRegistry.html` |
| Несколько концептов до сборки | `/concepts DS-IBP/uploads/ТЗ-реестр-сделок.md` |
| Посчитать смету захода | `node .agents/skills/session-plan/tooling/ctx-budget.mjs --stage build --tz <ТЗ> --cheat Table,Modal --out-lines 900` — строка `ВЕРДИКТ:` |
| Посмотреть состав этапов | `node .agents/skills/session-plan/tooling/ctx-budget.mjs --stages` |
| Сверить смету с фактом захода | `node .agents/skills/session-plan/tooling/ctx-budget.mjs --calibrate --stage build --fact <токенов>` + аргументы сметы |
| Сохранить контекст / продолжить в новой сессии | `/handoff` · `/resume DealRegistry` |
| Закрыть заход | `node .agents/tools/lessons-cli.mjs gate` — строка `ВЕРДИКТ:` |
| Проверить хаб проектов | `node .agents/tools/registry-check.mjs` — реестр `hub.js`, страницы вне реестра и ссылки на хаб, строка `ВЕРДИКТ:` (в гейте — шаг `registry`) |
| Просто спросить | пишите словами: «есть ли в ДС компонент для…», «чем Chip отличается от Badge» |

Скриншот макета прикладывается в сообщение как файл — если выбранная модель
умеет смотреть картинки. DeepSeek-Chat картинки не понимает; в этом случае
описывайте макет текстом или используйте мультимодальную модель.

Результат: папка концепта `Concepts/<Имя>/` — `<Имя>.html` открывается
двойным кликом в браузере (или с хаба `index.html`), `<Имя>.screen.md` уходит
агенту-разработчику.

**Как посмотреть, что делает субагент.** Когда `ai-designer` запускает
субагента, создаётся дочерняя сессия. Войти в неё — `Leader + Down`, дальше
переключаться стрелками, вернуться наверх — `Up`.

---

## 4. Как подключить модель

Модель в `.opencode/opencode.json` намеренно не задана — её выбирают под
контур. Провайдер и имя модели на каждой машине свои: дома — публичный API, на
рабочем контуре — корпоративная копия у другого провайдера. Если в репозитории
записать пару «провайдер/модель», которой на машине нет, opencode не
переключится на модель по умолчанию, а остановится с ошибкой
`Model not found`. Поэтому гейт краснеет на ключах `model`, `small_model`,
`provider` и `agent.<имя>.model` в конфиге репозитория (шаг `agent-config`).

**Куда писать модель — в глобальный конфиг машины**:
`~/.config/opencode/opencode.json`. Точный путь на своей машине печатает
`opencode debug paths` (строка `config`). Этот файл грузится первым, проектный
конфиг его дополняет, а модель не перекрывает — её там нет.

Если модель не задана нигде, opencode берёт последнюю выбранную командой
`/models` (запоминает её между сессиями), а если такой нет — модель
провайдера по умолчанию. Итоговую конфигурацию показывает
`opencode debug config`.

**Публичный DeepSeek или Qwen:** авторизоваться командой `opencode auth login`,
затем дописать в глобальный конфиг:

```json
{
  "model": "deepseek/deepseek-chat"
}
```

**Внутренний шлюз корпорации** (OpenAI-совместимый API — самый частый случай),
тоже в глобальный конфиг:

```json
{
  "provider": {
    "corp-gateway": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "Внутренний шлюз",
      "options": {
        "baseURL": "https://llm.corp.local/v1"
      },
      "models": {
        "deepseek-v4-flash": { "name": "DeepSeek V4 Flash" }
      }
    }
  },
  "model": "corp-gateway/deepseek-v4-flash"
}
```

Идентификаторы моделей должны совпадать с тем, что возвращает
`GET /v1/models` вашего шлюза. Другие модели шлюза (Qwen, GLM) — ещё строки в
`models`, переключение командой `/models`. Ключ — в переменной окружения или
через `opencode auth login`.

**Разные модели под разные роли.** Субагент без своей настройки работает на
модели того агента, который его вызвал. Чтобы развести — допишите в тот же
глобальный конфиг `"agent": { "screen-reviewer": { "model": "corp-gateway/…" } }`.
Не в шапку указателя (`.opencode/agents/`) и не в роль (`.agents/agents/`):
оба уезжают вместе с репозиторием, и модель, которой нет на другой машине,
остановит роль. Гейт это ловит (шаг `agent-config`, КФ3). Разумная раскладка:
сборку (`screen-builder`) отдать более сильной модели, приёмку
(`screen-reviewer`) — дешёвой и быстрой.

---

## 5. Права: что агент может, а что нет

Права складываются из двух слоёв: общий конфиг `.opencode/opencode.json`
действует на **всех** агентов, включая встроенные, а шапки указателей четырёх
своих ролей в `.opencode/agents/` его сужают. Ниже — то, что записано в файлах и
что opencode 2.0.11 из них читает (`opencode debug config`, проверено
21.09.2026). При расхождении этого раздела с файлами верить файлам.

**Общий конфиг — разрешено почти всё.**

| Действие | Права |
|---|---|
| чтение и поиск (`read`, `glob`, `grep`) | разрешены |
| правка файлов (`edit`) | разрешена везде, в том числе в `DS-IBP/` |
| команды (`bash`) | разрешены все без подтверждения, кроме трёх запретов по началу команды: `rm …`, `git push…`, `git reset…` |
| интернет (`webfetch`, `websearch`) | разрешён |
| субагенты (`task`) и скиллы (`skill`) | разрешены |
| пути вне рабочей директории (`external_directory`) | ключ пуст — действует умолчание opencode: запрос подтверждения. Это точка подключения базы знаний (раздел 6) |
| `"share": "disabled"`, `"autoupdate": false` | переписка на opencode.ai не выгружается, обновления не тянутся из интернета |

**Шапки своих агентов — сужение.**

| Агент | Правка файлов | Интернет |
|---|---|---|
| `ai-designer` | без подтверждения — `Concepts/**` и `hub.js`, остальное — с подтверждением | запрещён |
| `screen-builder` | без подтверждения — `Concepts/**`, остальное — с подтверждением | запрещён |
| `screen-reviewer` | запрещена | запрещён |
| `ux-researcher` | запрещена | запрещён |

Команды (`bash`) шапки агентов не сужают — у всех агентов они общие.

**Чего эти права НЕ дают — важно знать до поручения.**

- **Встроенные вспомогательные агенты** (`general`, `explore` — ведущий зовёт
  их для поиска по репозиторию) работают с общими правами: они **могут править
  файлы ДС** и ходить в интернет без подтверждения. Защита ДС от них держится
  на задании, которое им дают, а не на правах.
- **Команда обходит правку.** Сужение `edit` у своих агентов касается
  инструмента правки; `sed -i`, перенаправление в файл или скрипт `node`
  пишут файл без подтверждения.
- **Запрет удаления — по началу команды.** Закрыты `rm …`, `git push…`,
  `git reset…`; другие способы удалить файл (`git rm`, скрипт) не закрыты.
- **`opencode --auto`** одобряет всё, что не запрещено явно: подтверждение
  правки вне `Concepts/` превращается в разрешение. Остаются только запреты —
  три команды выше, правка у приёмщика и исследователя, интернет у своих
  агентов.

Описать фактические права, а не сузить конфиг до прежнего описания, — решение
владельца от 21.09.2026 (`docs/agent-imp.md`, У7). Если его пересмотрят,
начинать с общего конфига: сужение в нём закроет и встроенных агентов.

---

## 6. Как это дорабатывать

**Появился новый компонент в ДС.** В харнесе править ничего не нужно: знание о
ДС живёт в самой ДС (`DS-IBP/AGENTS.md`, в контексте агента всегда). Имя
компонента — в каталог `DS-IBP/AGENTS.md`, раздел 6 (его сверяет с манифестом
аудит ДС), рантайм с хуком — строкой в `DS-IBP/specs/_runtime-hooks.md`.
Порядок — `DS-IBP/MAINTAINING.md`, «Новый компонент».

**Появились локальные компоненты проекта** (тайл «КНР», «Финансовые метрики»).
Заведите отдельный скилл: `.agents/skills/local-components/SKILL.md` с
`name: local-components` (имя папки и поле `name` обязаны совпадать) и
`description` одной строкой — что в скилле и когда его грузить; опишите там,
где эти компоненты лежат и когда их брать вместо общих. Агент подхватит скилл
автоматически — в код агента лезть не нужно.

**Появился репозиторий базы знаний.** Он живёт **отдельно от этого
репозитория** — в ДС её материалов нет и не будет. Подключение описано в
`knowledge-lookup/SKILL.md`, приложение «Как подключить репозиторий»: две
правки с путём и возврат выключенного маршрута:

1. `.opencode/opencode.json` → `permission.external_directory` — сейчас там пустой
   объект, это заготовленная точка расширения:

   ```json
   "external_directory": { "~/ibp-knowledge/**": "allow" }
   ```

   Путь пишется абсолютным или от домашней папки; относительный `../…` здесь
   не разворачивается.

2. `knowledge-lookup/SKILL.md` → строка `KB = <путь не задан>` в начале файла.

3. Вернуть маршрут «Исследуй и собери» и команду `/research` (удалены
   21.09.2026, лежат в истории git) и снять пометки «выключен» с роли
   `ux-researcher` и скилла — шаги 4–5 того же приложения.

Больше нигде путь не зашит. Требования к самим файлам базы (YAML-шапка, одна
тема — один файл, пометка «факт / гипотеза», запрет на проценты при выборке
1–5 человек) — там же, в приложении «Требования к файлам».

**Нужен новый специалист** (например, агент UX-копирайтинга). Добавьте роль
в `.agents/agents/` с внятным `description` — по нему `ai-designer` поймёт,
когда звать, — и указатель в `.opencode/agents/` по образцу соседних: тот же
`description`, `mode: subagent`, права роли, в теле — путь к роли. Гейт
(`agent-config`, КФ5) проверит, что они сходятся; новая команда заводится
так же — сценарий в `.agents/commands/`, указатель в `.opencode/commands/`. Затем допишите строку в таблицу «Твоя
команда» в `ai-designer.md` и, если нужно, новый маршрут.

**Агент делает что-то не так.** Правится текстом, не кодом:
- путает компоненты → дополните «Частая путаница в именах» в `ds-lookup/SKILL.md`;
- халтурит с контентом → усильте «Контент: рисуем, а не заглушаем» в
  `screen-assembly/SKILL.md`;
- макет не кликается → проверьте `DS-IBP/specs/_runtime-hooks.md`;
- пропускает проверку → добавьте пункт в блокеры `screen-review/SKILL.md`;
- субагент делает не то → почти всегда виновато задание: усильте раздел
  «Главное про субагентов» в `ai-designer.md`.

Правило то же, что и в самой ДС: разобрали инцидент — тем же заходом впишите
класс ошибки в чек-лист, иначе он повторится.

**Проверить, что opencode всё увидел:** `/agents` покажет список агентов,
`/skills` — список скиллов. Роли или команды нет — у неё нет указателя в
`.opencode/` (это ловит гейт, КФ5). Скилла нет — проверьте, что файл называется
`SKILL.md` заглавными, что в шапке есть `description` и что `name` совпадает с
именем папки. opencode v2 берёт ID скилла из пути, а скилл без `description`
модели не показывает: он зарегистрирован, но агент о нём не знает. Оба
условия проверяет гейт (шаг `agent-config`, КФ4).

---

## 7. Известные ограничения

- **Макет живой, но не приложение.** Штатное поведение компонентов работает:
  поповеры, модалки, аккордеоны, сортировка, табы, тултипы, сворачивание
  панели навигации. Не работает то, что относится к данным: реальная
  фильтрация строк, пересчёт пагинации, сохранение, переходы между экранами.
  Такое описано словами в `<Имя>.screen.md`.
- **Репозитория базы знаний ещё нет**, поэтому исследовательский маршрут
  выключен: `/research` нет, `ux-researcher` не вызывается. Вопросы о
  пользователях агент задаёт вам — выдумывать пользователей по интерфейсу ему
  запрещено. Когда репозиторий появится — раздел 6, кода трогать не нужно.
- Слабые модели плохо держат длинные файлы. Если экран получается очень
  большим — просите собирать его частями (сначала каркас и шапка, потом
  таблица, потом модалки).
- Точность вёрстки по скриншоту у DeepSeek/Qwen ниже, чем у топовых моделей.
  Приёмка для них не формальность, а необходимый шаг — поэтому `ai-designer`
  и не имеет права её пропустить.
