---
screen: DealRegistry
kind: screen
title: Реестр сделок
file: apps/postrade/deals-app/pages/DealRegistry.html
module: postrade/deals-app
frontend: postrade/deals-app/pages/Deals
route: не решено (24.09.2026)
source: ТЗ — design-system/uploads/ТЗ-реестр-сделок.docx
version: "1.000"
created: "24.09.2026"
design_system: IBP DS
components: [Layout, NavPanel, Breadcrumbs, PageHeader, Tile, ReadOnlyField, TableFilter, Modal, Chip, IconButton, Buttons]
widgets: [tables/CurrentPortfolioDidTable, modals/DealFilterModal]
---

# Page: DealRegistry — Реестр сделок

<!-- Шапка: screen — имя файла экрана; kind — screen | document; module —
     <раздел>/<модуль> этого приложения; frontend — пара во фронтенде (README
     модуля, раздел «Имена фронтенда», или дерево фронтенда), нет пары — new;
     route — адрес страницы в продукте, если он известен; components —
     компоненты ДС; widgets — виджеты, которые страница вшивает
     (<группа>/<Имя>: свои и соседей по разделу). Экрану нужны свои разделы
     («Версии материала», «Скрипт экрана», «Дефекты ДС») — вставляются между
     стандартными, нумерация сквозная. Этот комментарий в спеку не
     переносится. -->

## 1. Назначение (Purpose)

Одно-два предложения: чью задачу решает экран и в какой момент процесса
пользователь на него попадает. Формулировка из ТЗ, не придуманная.

**Роль пользователя:** менеджер сделок, риск-менеджер
**Права:** чтение всех сделок подразделения, редактирование своих

## 2. Адрес и навигация (Route)

- **Адрес в продукте:** не решено (24.09.2026) — вопрос в разделе 13.
- **Пункт бокового меню** в состоянии `nav__item--selected`: «Сделки».

| Уровень крошек | Текст | Ссылка |
|---|---|---|
| 1 | Главная | `/` |
| 2 | Сделки | `/deals` |
| 3 (текущий) | Реестр сделок | — |

## 3. Раскладка (Layout)

```
Page (Layout, nav--rail)
├── PageHeader            — заголовок, дата обновления, «Выгрузить», «Создать сделку», меню «Ещё»
└── Grid 12, gap 16/24
    ├── Tile ×3 (4 + 4 + 4) — сводка по портфелю, по 3 ReadOnlyField
    └── CurrentPortfolioDidTable (12) — виджет tables/, реестр сделок
```

**Шапка страницы (PageHeader):**

| Элемент | Значение | Компонент / класс |
|---|---|---|
| Заголовок | Реестр сделок | `h1.phead__title` |
| Подзаголовок | Обновлено 26.08.2026 | `.phead__meta` + глиф `calendar` |
| Действие 1 | Выгрузить | `btn btn--outline btn--m`, глиф `download` |
| Действие 2 (главное) | Создать сделку | `btn btn--accent btn--m`, глиф `add` |
| Меню «Ещё» | Настройки реестра, История изменений | `btn--icon-only`, глиф `more-dots` |

**Блоки контента** — 12 колонок, зазор 16px, вертикальный зазор блоков 24px:

| № | Блок | Компонент или виджет | Колонок из 12 | Содержимое |
|---|---|---|---|---|
| 1 | Сводка по портфелю | Tile ×3 в одном ряду | 4 + 4 + 4 | по 3 показателя ReadOnlyField |
| 2 | Реестр сделок | `widgets/tables/CurrentPortfolioDidTable` | 12 | колонки — в паспорте виджета |

Вертикальный скролл: есть, скроллится контентная область, крошки залипают.
Горизонтального скролла нет.

## 4. Компоненты (Components)

| Компонент | Откуда | Где на экране | Варианты и ключевые параметры |
|---|---|---|---|
| Layout | ДС — `design-system/specs/Layout.md` | каркас | `nav--rail` |
| Breadcrumbs | ДС — `design-system/specs/Breadcrumbs.md` | зона крошек | — |
| PageHeader | ДС — `design-system/specs/PageHeader.md` | шапка | без иконки, без статуса |
| Tile | ДС — `design-system/specs/Tile.md` | сводка | обычный, 4 колонки |
| `CurrentPortfolioDidTable` | виджет модуля — `../widgets/tables/CurrentPortfolioDidTable/CurrentPortfolioDidTable.md` | реестр | метка `class="col-12"` |
| `DealFilterModal` | виджет модуля — `../widgets/modals/DealFilterModal/DealFilterModal.md` | по кнопке «Фильтр» | `.modal--w6` |

Виджет соседнего модуля раздела указывается путём от этой страницы
(`../../payments-app/widgets/…`). Параметры виджета на странице — атрибуты
метки `<ds-include>`: `class` (место в сетке), `id`, `state`, `mode`.

## 5. Таблицы (Tables)

Раздел нужен, только если таблица собрана прямо в разметке страницы. Таблица
в `widgets/tables/` описывается в своём паспорте — здесь ссылка на него.

| Ключ | Заголовок | Тип | Ширина | Выравн. | Сортировка | В фильтре |
|---|---|---|---|---|---|---|
| `counterpartyName` | Контрагент | текст | 2fr | слева | да | да, автокомплит |
| `amount` | Сумма, RUB | число | 1fr | справа | да, активна по убыванию | да, диапазон |
| `statusCode` | Статус | Chip | 120px | слева | нет | да, множественный выбор |

**Формат значений:** суммы — `1 240 500,00` с неразрывными разрядами;
даты — `ДД.ММ.ГГГГ`; пустое значение — прочерк `—`.

## 6. Поведение (Behavior)

| Триггер | Результат |
|---|---|
| Клик «Фильтр» | открывается `DealFilterModal` (`.modal--w6`) |
| Применён фильтр | в баре появляется чип «Применено: N»; крестик чипа сбрасывает фильтр целиком |
| Двойной клик по строке | открывается страница сделки `Deal.html?id=<dealId>` |
| Сужение рабочей области до 1240px | ряд сводки перестраивается 4+4+4 → 6+6+12 |

## 7. Модальные окна (Modals)

Окна, собранные прямо в разметке страницы. Окно в `widgets/modals/` — строка
со ссылкой на паспорт.

| id | Заголовок | Ширина | Содержимое | Кнопки подвала |
|---|---|---|---|---|
| `filter-scrim` | Фильтр | `--w6` | виджет `DealFilterModal` | см. паспорт |
| `del-scrim` | Удалить сделку? | `--w3`, вложенный | текст подтверждения | справа «Отмена», «Удалить» (`btn--danger`) |

## 8. Состояния (States)

### Loading
Тело таблицы — строки-скелетоны (`.tc--skeleton`), тулбар и футер на месте;
сводка — Tile со скелетоном ReadOnlyField.

### Empty
`.dtable__empty`: «Ничего не найдено» + «Сбросить фильтр».

### Error
Alert тона `error` над таблицей, текст — раздел 9.

### Disabled
Нет прав на редактирование: кнопки действий скрыты, таблица только на чтение.

Свои состояния экрана — отдельными подразделами того же вида.

## 9. Тексты интерфейса (Texts)

| Место | Текст |
|---|---|
| Заголовок страницы | Реестр сделок |
| Кнопка создания | Создать сделку |
| Пусто — заголовок | Ничего не найдено |
| Пусто — пояснение | Измените параметры фильтра или сбросьте его |
| Ошибка загрузки | Не удалось загрузить сделки. Обновите страницу |

## 10. Данные (Data dependencies)

### API — что нужно от бэкенда

Имена полей — те же, что в typedef демо-данных (подраздел Mock): из DTO, когда
он известен, иначе придуманы в стиле API и помечены `invented`.

| Поле | Тип | Обязательное | Смысл и источник |
|---|---|---|---|
| `dealId` | число | да | номер сделки |
| `counterpartyName` | строка | да | наименование контрагента |
| `amount` | число | да | сумма, 2 знака |
| `currencyCode` | строка | да | код валюты ISO 4217, `RUB` |
| `statusCode` | перечисление | да | значения — паспорт `CurrentPortfolioDidTable` |

Имена методов и хуков API здесь не придумываются: их выбирает разработка.

### Mock — демо-данные прототипа

| Файл | Глобальная переменная | Тип | Источник имён |
|---|---|---|---|
| `../data/deals.js` | `window.MOCK_DEALS` | `DealRsDto[]` | invented — заменить, когда придёт DTO |
| `../data/deals-store.js` | `window.DealsStore` | — | помощник: выборка, фильтр, тоны статусов |

### Permissions — роли, права, флаги

| Роль | Просмотр | Действия |
|---|---|---|
| Менеджер сделок | все сделки подразделения | создание, редактирование своих |
| Риск-менеджер | все сделки подразделения | только просмотр |

## 11. Соответствие файлов (Implementation mapping)

| Элемент | Прототип | Фронтенд (предложение) |
|---|---|---|
| Страница | `pages/DealRegistry.html` | `src/pages/Deals/DealsPage.tsx` |
| Реестр | `widgets/tables/CurrentPortfolioDidTable/CurrentPortfolioDidTable.html` | `src/widgets/tables/CurrentPortfolioDidTable/CurrentPortfolioDidTable.tsx` |
| Фильтр | `widgets/modals/DealFilterModal/DealFilterModal.html` | `src/features/modals/DealFilterModal/DealFilterModal.tsx` |
| Типы данных | `data/deals.js` (JSDoc typedef) | DTO-типы API; сам мок в продукт не переносится |

Пути фронтенда — по соглашениям его модуля (README модуля, «Имена
фронтенда»); окончательный путь выбирает разработка.

## 12. Доступность

- `<main>` для контентной области, `<nav aria-label="Хлебные крошки">`,
  `<nav aria-label="Главное меню">`;
- заголовок страницы — единственный `h1`;
- сортировка колонки объявлена через `aria-sort`;
- у всех кнопок-иконок есть `aria-label`;
- порядок в DOM совпадает с порядком чтения;
- контраст — не ниже WCAG AA (обеспечивается токенами ДС).

## 13. Открытые вопросы и допущения (Open questions)

| № | Вопрос / допущение | Статус |
|---|---|---|
| 1 | Набор статусов взят из ТЗ, п. 4.2 | из ТЗ |
| 2 | Адрес страницы в продукте не задан | вопрос разработке |
| 3 | Пара во фронтенде `Deals` выбрана по смыслу — **допущение** | нужно подтверждение |
