---
screen: DealRegistry
title: Реестр сделок
file: Concepts/DealRegistry/DealRegistry.html
source: ТЗ — DS-IBP/uploads/ТЗ-реестр-сделок.docx
version: "1.000"
created: "26.08.2026"
design_system: IBP DS
components: [Layout, NavPanel, Breadcrumbs, PageHeader, Table, TableCell, TableFilter, Pagination, Modal, Chip, Checkbox, IconButton, Buttons]
---

# Реестр сделок

## 1. Назначение

Одно-два предложения: чью задачу решает экран и в какой момент процесса
пользователь на него попадает. Формулировка из ТЗ, не придуманная.

**Роль пользователя:** менеджер сделок, риск-менеджер
**Права:** чтение всех сделок подразделения, редактирование своих

## 2. Навигация

| Уровень | Текст | Ссылка |
|---|---|---|
| 1 | Главная | `/` |
| 2 | Сделки | `/deals` |
| 3 (текущий) | Реестр сделок | — |

Пункт бокового меню в состоянии `nav__item--selected`: «Сделки».

## 3. Шапка страницы (PageHeader)

| Элемент | Значение | Компонент / класс |
|---|---|---|
| Заголовок | Реестр сделок | `h1.phead__title` |
| Статус | — | — |
| Подзаголовок | Обновлено 26.08.2026 | `.phead__meta` + глиф `calendar` |
| Действие 1 | Выгрузить | `btn btn--outline btn--m`, глиф `download` |
| Действие 2 (главное) | Создать сделку | `btn btn--accent btn--m`, глиф `add` |
| Меню «Ещё» | Настройки реестра, История изменений | `btn--icon-only`, глиф `more-dots` |

## 4. Структура контента

Контентная область — 12 колонок, зазор 16px, вертикальный зазор блоков 24px.

| № | Блок | Компонент | Колонок из 12 | Содержимое |
|---|---|---|---|---|
| 1 | Сводка по портфелю | Tile ×3 в одном ряду | 4 + 4 + 4 | по 3 показателя ReadOnlyField в каждом |
| 2 | Таблица сделок | Table | 12 | 8 колонок, фильтр, пагинация |

Вертикальный скролл: есть, скроллится контентная область, крошки залипают.
Горизонтального скролла нет.

## 5. Таблица «Сделки»

**Тулбар:** заголовок «Сделки», счётчик записей, TableFilter, кнопка настройки
колонок, кнопка «Добавить».

| Ключ | Заголовок | Тип | Ширина | Выравн. | Сортировка | В фильтре |
|---|---|---|---|---|---|---|
| `select` | — | чекбокс | 44px | центр | нет | нет |
| `counterparty` | Контрагент | текст | 2fr | слева | да, по умолчанию нет | да, автокомплит |
| `product` | Продукт | текст | 1.5fr | слева | да | да, множественный выбор |
| `amount` | Сумма, RUB | число | 1fr | справа | да, активна по убыванию | да, диапазон |
| `status` | Статус | Chip | 120px | слева | нет | да, множественный выбор |
| `date` | Дата изменения | дата | 140px | слева | да | да, диапазон дат |
| `actions` | Действия | кнопки | 96px | центр | нет | нет |

**Формат значений:** суммы — `1 240 500,00` с неразрывными разрядами;
даты — `ДД.ММ.ГГГГ`; пустое значение — прочерк `—`.

**Статусы колонки `status`:**

| Значение | Chip |
|---|---|
| Черновик | `chip--s` (нейтральный) |
| Активна | `chip--s chip--success` |
| На доработке | `chip--s chip--warning` |
| Отклонена | `chip--s chip--error` |

**Выбор строк:** есть. При выборе появляется панель массовых действий
(`.pgn-bulk`): «Выгрузить», «Удалить». Удаление продублировано в колонке
«Действия» на уровне строки.

**Пагинация:** 50 строк на страницу, переключатель размера страницы, счётчик
диапазона.

## 6. Поведение

| Триггер | Результат |
|---|---|
| Клик по кнопке сортировки в шапке колонки | цикл: нет → по возрастанию → по убыванию → нет; активна максимум одна колонка |
| Перетаскивание правой границы шапки колонки | изменение ширины колонки, минимум 96px |
| Клик «Фильтр» | открывается модальное окно фильтра (`.modal--w6`) |
| Применён фильтр | в баре появляется чип «Применено: N»; крестик чипа сбрасывает фильтр целиком |
| Двойной клик по строке | открывается модальное окно «Редактирование сделки» с данными строки |
| Клик по кнопке «Изменить» в строке | то же окно |
| Выбор одной и более строк | появляется панель массовых действий над пагинацией |
| Прокрутка тела таблицы | шапка таблицы остаётся на месте (липкая) |
| Сужение рабочей области до 1240px | ряд сводки перестраивается 4+4+4 → 6+6+12 |

## 7. Модальные окна

| id | Заголовок | Ширина | Содержимое | Кнопки подвала |
|---|---|---|---|---|
| `filter-scrim` | Фильтр | `--w6` | вертикальные табы разделов + поля фильтра в 2 колонки | справа «Очистить фильтр», «Применить» |
| `cols-scrim` | Настройка колонок | `--w4` | список колонок с чекбоксами и перетаскиванием | слева «Сбросить»; справа «Применить» |
| `row-scrim` | Редактирование сделки | `--w6` | форма полей строки | справа «Отмена», «Сохранить» |
| `del-scrim` | Удалить сделку? | `--w3`, вложенный | текст подтверждения | справа «Отмена», «Удалить» (`btn--danger`) |

## 8. Состояния экрана

| Состояние | Что показывается |
|---|---|
| Загрузка | тело таблицы — строки-скелетоны (`.tc--skeleton`), тулбар и футер на месте |
| Данных нет | `.dtable__empty`: «Ничего не найдено» + «Сбросить фильтр» |
| Ошибка загрузки | Alert тона `error` над таблицей |
| Нет прав на редактирование | кнопки действий скрыты, таблица только на чтение |

## 9. Тексты интерфейса

| Место | Текст |
|---|---|
| Заголовок страницы | Реестр сделок |
| Кнопка создания | Создать сделку |
| Плейсхолдер поиска | Контрагент, номер сделки |
| Пусто — заголовок | Ничего не найдено |
| Пусто — пояснение | Измените параметры фильтра или сбросьте его |
| Подтверждение удаления | Удалить сделку D-007? Действие нельзя отменить |

## 10. Данные

Что экран запрашивает у бэкенда — по одному полю на строку.

| Поле | Тип | Обязательное | Источник / примечание |
|---|---|---|---|
| `id` | строка | да | номер сделки, формат `D-000` |
| `counterparty` | строка | да | наименование контрагента |
| `amount` | число | да | в рублях, 2 знака |
| `status` | перечисление | да | значения см. раздел 5 |

## 11. Компоненты ДС

| Компонент | Где на экране | Варианты | Спека |
|---|---|---|---|
| Layout | каркас | `nav--rail` | `DS-IBP/specs/Layout.md` |
| Breadcrumbs | зона крошек | — | `DS-IBP/specs/Breadcrumbs.md` |
| PageHeader | шапка | без иконки, без статуса | `DS-IBP/specs/PageHeader.md` |
| Tile | сводка | обычный, 4 колонки | `DS-IBP/specs/Tile.md` |
| Table | реестр | тулбар + футер | `DS-IBP/specs/Table.md` |
| TableCell | строки и шапка | сортировка, выбор строк, чипы | `DS-IBP/specs/TableCell.md` |
| TableFilter | тулбар | с чипом «Применено» | `DS-IBP/specs/TableFilter.md` |
| Modal | 4 окна | `--w3`, `--w4`, `--w6` | `DS-IBP/specs/Modal.md` |

## 12. Доступность

- `<main>` для контентной области, `<nav aria-label="Хлебные крошки">`,
  `<nav aria-label="Главное меню">`;
- заголовок страницы — единственный `h1`;
- сортировка колонки объявлена через `aria-sort`;
- у всех кнопок-иконок есть `aria-label`;
- порядок в DOM совпадает с порядком чтения;
- контраст — не ниже WCAG AA (обеспечивается токенами ДС).

## 13. Открытые вопросы и допущения

| № | Вопрос / допущение | Статус |
|---|---|---|
| 1 | Набор статусов взят из ТЗ, п. 4.2 | из ТЗ |
| 2 | Ширины колонок подобраны по содержимому — **допущение**, в ТЗ не заданы | нужно подтверждение |
| 3 | Что происходит при удалении сделки в статусе «Активна» — в ТЗ не описано | вопрос заказчику |
