---
screen: Portfolio
title: Текущий портфель ДИД
file: Projects/post/Portfolio-did/Portfolio.html
source: ТЗ — Projects/post/deal_post/Текущий портфель.md
version: "1.026"
created: "03.09.2026"
updated: "16.09.2026"
design_system: IBP DS
components: [Layout, NavPanel, Breadcrumbs, Link, LabelHelper, PageHeader, Table, TableCell, TableFilter, Pagination, Modal, Tab, Chip, Avatar, Checkbox, InputText, InputAutocomplete, InputDateRange, EmptyState, Divider, IconButton, Buttons]
---

# Текущий портфель ДИД

## 1. Назначение

Реестр сделок направления ДИД: пользователь видит полный список сделок текущего
портфеля, фильтрует его по параметрам сделки, настраивает видимые колонки и
создаёт новую сделку.

**Роль пользователя:** Финансист ДИД
**Права:** просмотр портфеля; сделки с ограниченным доступом видны только при
наличии права (в макете одна строка с «Да»).

## 2. Навигация

| Уровень | Текст | Ссылка |
|---|---|---|
| 1 | Главная | `../mainPage/index.html` |
| 2 (текущий) | Текущий портфель ДИД | — |

Крошка «Главная» и пункт бокового меню «Главная» ведут на главную страницу
(`mainPage/index.html`) — двусторонняя связь: на главной пункт меню и тайл
«Текущий портфель» открывают эту страницу (см. mainPage.screen.md).

Пункт бокового меню в состоянии `nav__item--selected` + `aria-current="page"`:
«Текущий портфель» (группа «Текущий портфель ДИД», иконка `current-depo`).

## 3. Источник данных навигации

Меню панели строится экранным скриптом из `window.IBPHome` (`scripts/ibp-home.js`)
по роли `'Финансист ДИД'` — вызовом `IBPHome.itemsFor(role)`. Пункт
`current-portfolio` помечается selected; пункты с `tile.links` («Корпоративные
запросы», «Отчёты 1C/Navision», «Отчёты для Рисков») рендерятся как
аккордеон-родители `.nav__item--acc`; «Поиск клиентов в ЕПК» (`tile: null`) — обычный
пункт меню без тайла. Строка пользователя в футере — Александров Петр
Константинович, «Финансист ДИД» (вынесена в `IBPHome.footerHTML(role)`).
**В макете** строка пользователя ведёт на хаб проектов (корневой `index.html`,
`aria-label="Хаб проектов"`) — служебный переход, в продукт не переносится.

## 4. Шапка страницы (PageHeader)

| Элемент | Значение | Компонент / класс |
|---|---|---|
| Заголовок | Текущий портфель ДИД | `h1.phead__title` |
| Действие (главное) | Новая сделка | `btn btn--accent btn--m`, глиф `add`, триггер `data-modal="new-deal-scrim"` |

Статус экрана и подзаголовок отсутствуют.

## 5. Раскладка

Контентная область — 12 колонок, зазор 16px. Единственный блок контента —
таблица на всю ширину. Горизонтального скролла у рабочей области нет —
горизонтальный скролл только внутри тела таблицы (`.tbl--scroll`). Отступ
таблицы от нижней границы экрана — 24px: это нижнее поле контентной области,
собственного `margin-bottom` у таблицы нет (правило каркаса — низ одинаков для
всех типов контента).

**Высота таблицы — правило каркаса** (Layout `.screen--app` + Table `.dtable--fill`):
таблица как единственный/основной блок занимает **всю свободную высоту экрана**
за вычетом полей контента, крошек, шапки страницы и тулбара/фильтра. На рабочей
области стоит `.screen--app` (фикс. вьюпорт `100vh`), на таблице — `.dtable--fill`:
тело таблицы скроллится внутри, крошки/шапка/тулбар/пагинация закреплены.
Вертикальный скролл страницы при этом отсутствует — скроллится только тело таблицы.

| № | Блок | Компонент | Колонок из 12 | Содержимое |
|---|---|---|---|---|
| 1 | Зона тулбара | Table (`.dtable-toolbar`) | 12 | слева TableFilter, справа настройка колонок |
| 2 | Таблица «Сделки» | Table (`.dtable--fill`, headless) | 12 | 23 колонки, без тулбара (сразу шапка), пагинация |

Вертикальный ритм — **12px**: заголовок страницы → зона тулбара → таблица. Это
описанный вариант каркаса (Layout, раздел «Экран-реестр: состав и отступы»), а не
самодеятельность экрана; значение экран задаёт единственной раскладочной строкой
в своём `<style>`: `.screen__content { gap: var(--space-12) }`.

## 6. Таблица «Сделки»

**Источник данных (05.09.2026):** таблицу рендерит скрипт экрана из общей базы
направления, а не статичная разметка:

| Файл | Что | Подключён на этом экране |
|---|---|---|
| `../data/mock-deals.js` | `window.MOCK_DEALS` — 50 сделок (плоские поля таблицы и фильтра) + `window.DEALS_ENUMS` — перечни для фильтра | да |
| `../data/deals-store.js` | `window.DealsStore` — `list()/byId()/exists()/create()/update()/on()`, единая точка доступа к базе | да |
| `../data/mock-deal-trees.js` | `window.MOCK_DEAL_TREES` — дерево продуктов ДИД по сделкам, ключ — `id` | нет (нужно только будущей странице сделки) |

**Ключ колонки (`data-col`).** Каждая содержательная `.th` в шапке (23 колонки)
и каждая ячейка, которую собирает `renderRow`, несут `data-col="<поле сделки>"` —
ключ пишет общий `cellOpen`. Это хук рантайма таблицы: по ключу `DSTable.adoptRow`
сопоставляет ячейку строки, дописанной экраном, с колонкой шапки и приводит
строку к текущей форме колонок — порядок после переноса, треки после ресайза,
ячейка скрытой колонки уходит на склад строки. Инсеты закрепления новой строке
проставляет `tbl-pin.js` (он ловит вставку строки сам). Своего JavaScript под это
экран не пишет: обработчик «Новая сделка» только вставляет строку
(TableCell 2.019, Table 1.016).

Скрипт стоит **после** `ds.js` (нужны `window.DSTable`/`window.DSPagination`,
которые `ds.js` догружает через `document.write`); рантаймы таблицы сами
поднимаются на `DOMContentLoaded` — позже, чем этот синхронный скрипт, поэтому
50 вставленных строк учитываются ими штатно при первом `wire()` (порядок для
`data-sort-rows`, тултипы усечения — без обращения к `rowsChanged()`, он нужен
только для строк, добавленных ПОСЛЕ загрузки страницы — см. Table 1.012).

Плоские поля записи — `id`, `name`, `status`/`statusTsup`/`statusOps`/`statusMon`
(кортежи §7), `restricted`, `desk`, `director`/`manager`/`tsupEmployee`, `knr[]`,
`gsz`, `jointness`, `collateral`, `corpGovernance[]`, `boardRep`, `finTpl`,
`signDate`/`firstDisb`/`endDate`/`repayDate`, `monEntity`, и производные из
дерева продуктов — `mainProductDid`, `productsDidNames[]`, `productsNames[]`,
`balances[]`, `currencies[]`, `isPE`. Производные поля вычислены из
`MOCK_DEAL_TREES` один раз при генерации и заморожены в записи — в проде их
считает бэкенд, реестр не пересчитывает.

**Допущения по форматам** (нет однозначного ответа в ТЗ): `jointness` — строка
из справочника (`DEALS_ENUMS.jointness`: «Совместная»/«Не совместная»);
`corpGovernance` — список строк; `boardRep` — Да/Нет.

**Распределение 50 сделок** покрывает крайние случаи вёрстки: 5 сделок-черновиков
без дат (прочерк во всех трёх датах), сделка с максимально длинным
наименованием, сделка со всеми 4 валютами ДС сразу (RUB/USD/EUR/JPY — больше в
системе не заведено), сделка с 6 балансами, сделка с 4 КНР, инструмент без
транша. 3 сделки — `restricted: Да`, 6 — `isPE: true`.

**Вариант «тулбар над таблицей»** (`.dtable-toolbar` + headless-таблица):
- зона `.dtable-toolbar` — БЕЗ подложки, над таблицей: слева TableFilter (кнопка
  «Фильтр» → `filter-scrim`, чип «Применено: N» — с 05.09.2026 реальный, скрыт при N=0), справа кнопка «Настроить
  колонки» (`ibtn` с глифом `settings` → `cols-scrim`);
- таблица `.dtable--fill` — **headless**: без `.dtable__toolbar`, сразу шапка
  колонок; заголовка «Сделки» и счётчика «128» наверху нет (счётчик остаётся
  только в пагинации).

**Каркас:** `.tbl[data-table]` + `.tbl--scroll`. Шапка и все строки имеют
одинаковый `grid-template-columns` (8px-разделитель, 23 колонки данных,
замыкающий `minmax(8px,1fr)`). Колонки «Номер сделки» и «Наименование сделки»
закреплены (`.th--pinned` / `.tc--pinned`, `aria-pressed="true"` + глиф
`pin-filled` на `th__pin`).

**Закрепление — из коробки (TableCell 2.013, рантайм `tbl-pin.js` в `ds.js`):**
клик по `.th__pin` в шапке закрепляет/открепляет любую колонку (без переноса
в начало). При горизонтальном скролле закреплённые колонки удерживаются у
левого края нативным `position: sticky`; рантайм считает только инсеты
`left`/`right` и пишет их при изменении состава или ширин колонок — на скролл
не подписан. Собственного правила смещения у экрана нет.

До 04.09.2026 удержание шло через `transform: translateX` на событии `scroll` —
на MacBook закреплённые колонки дрожали (компоновщик применяет скролл раньше
JS-обработчика, коррекция всегда на кадр позади; дискретное колесо мыши на
Windows это скрывало). Экран не правился — исправление целиком в ДС.

| Ключ | Заголовок | Тип | Ширина | Выравн. | Сортировка | Закреплена |
|---|---|---|---|---|---|---|
| `number` | Номер сделки | идентификатор | 150px | слева | да | да |
| `name` | Наименование сделки | текст | 280px | слева | да | да |
| `status` | Статус | Chip | 140px | слева | нет | нет |
| `statusTsup` | Статус ЦУП | Chip+аватар «Ц» | 176px | слева | нет | нет |
| `statusOps` | Статус Operations | Chip+аватар «O» | 176px | слева | нет | нет |
| `statusMon` | Статус Мониторинг | Chip+аватар «М» | 320px | слева | нет | нет |
| `restricted` | Ограниченный доступ | Да/Нет | 120px | слева | да | нет |
| `desk` | Деск | текст | 150px | слева | нет | нет |
| `balances` | Балансы сделки | текст через запятую | 220px | слева | нет | нет |
| `pe` | Private Equity | Chip «PE» | 88px | слева | да (по `data-sort-value` 1/0) | нет |
| `mainProductDid` | Основной продукт ДИД | текст | 180px | слева | нет | нет |
| `productsDid` | Продукты ДИД | текст через запятую | 240px | слева | нет | нет |
| `products` | Продукты | текст через запятую | 240px | слева | нет | нет |
| `knr` | КНР | текст | 170px | слева | нет | нет |
| `gsz` | ГСЗ | текст | 170px | слева | нет | нет |
| `currencies` | Валюты | Chip ×N, свёртка в «+N» | 150px | слева | нет | нет |
| `signDate` | Дата подписания | дата | 130px | слева | да, активна по убыванию | нет |
| `firstDisb` | Дата первой выдачи | дата | 130px | слева | да | нет |
| `endDate` | Дата окончания | дата | 130px | слева | да | нет |
| `director` | Директор | ФИО | 120px | слева | нет | нет |
| `manager` | Менеджер | ФИО | 120px | слева | нет | нет |
| `tsupEmployee` | Сотрудник ЦУП | ФИО | 120px | слева | нет | нет |
| `finTpl` | Фин. расчеты в шаблоне | Да/Нет | 140px | слева | нет | нет |

**Формат значений:** номер сделки — идентификатор, а не величина: выравнивается
по левому краю как текст (`.tc--numbers` в реестре не используется — правый край
и табулярные цифры остаются за суммами и долями); даты — `ДД.ММ.ГГГГ`; пустое
значение — прочерк `—`; валюты — чип на валюту: 3-буквенный код ISO 4217
прописными (`RUB`, `USD`, `EUR`, `JPY`), без символов ₽/$/€/¥ и без «руб.»,
чипы не сжимаются (`.chip--fit` — код показывается целиком, не «R…»);
ФИО — «Фамилия И.О.».

**Усечение и тултип:** все текстовые значения строк несут `.tc__text--truncate` —
одна строка, многоточие по границе колонки. Полное значение показывает тултип по
наведению; он навешивается рантаймом таблицы (`ds-table.js`) на значения ячеек,
подписи колонок и подписи чипов и появляется только тогда, когда текст реально
не помещается. Своей разметки тултипа на экране нет.

**Выбор строк:** не предусмотрен ТЗ (нет колонки чекбоксов и панели массовых
действий).

**Пагинация (реальная, 05.09.2026):** колонтитул собирается скриптом экрана
напрямую через `DSPagination.footer({ info, pager })` (не автопривязкой
`data-pagination` — она не заполняет левый слот) и пересобирается заново при
каждом изменении страницы/размера. Левый слот — `pgn-info` со значением
«Сделок: N» (фактическое число строк реестра — сейчас всегда 50, поскольку
фильтр по-прежнему вёрстка-заглушка, см. открытый вопрос №7; при реализации
Этапа 4 число станет отражать результат фильтра). Размер страницы по
умолчанию — **20** (при 50 сделках — реальные 3 страницы: «1 из 3» → «2 из 3»
→ «3 из 3»); доступны все размеры Pagination (5/10/15/20/50/Все). Строки не
пересоздаются — видимость переключает `hidden` на каждом `.tbl__row` (правило
`.tbl__row[hidden]`, TableCell 2.015): строка видима, если попадает в окно
текущей страницы по своей позиции в актуальном DOM-порядке (учитывает
сортировку — окно считается ПОСЛЕ перестановки строк).

## 7. Статусы

Чипы статусов — `chip chip--s chip--rounded` + тон (**статусные чипы всегда
скруглённые**, правило чипа). Чипы с аватаркой (ЦУП/Operations/Мониторинг)
используют штатный паттерн: `.chip__avatar.av.av--circular` с буквой `Ц`/`O`/`М`.
Аватар тонируется по тону чипа (фон `-mid`, белая буква) — штатное поведение Chip 1.012.
Буквы: ЦУП = «Ц», Operations = «O», Мониторинг = «М».

### Общий маппинг (Статус, Статус ЦУП, Статус Operations)

| Статус | Класс-тон |
|---|---|
| Черновик | `chip--dark` |
| Ожидает подтверждения | `chip--warning` |
| Подтверждение изменений | `chip--warning` |
| Ожидает назначения ответственного (ЦУП) | `chip--warning` |
| Корректировка | `chip--info` |
| Ввод изменений | `chip--info` |
| Корректировка изменений | `chip--info` |
| Активная | `chip--success` |
| Утверждена | `chip--success` |
| На сопровождении (Operations) | `chip--success` |
| Погашена | `chip--dpurple` |

### Статус Мониторинг

| Статус | Класс-тон |
|---|---|
| На утверждении | `chip--dark` |
| Утверждена | `chip--dark` |
| Ожидает передачи на мониторинг | `chip--warning` |
| Направлена на передачу | `chip--warning` |
| Передана | `chip--warning` |
| Изменения ожидают передачи на мониторинг | `chip--warning` |
| Изменения направлены на передачу | `chip--warning` |
| Передача изменений | `chip--warning` |
| На мониторинге ГК СБИ | `chip--success` |
| На мониторинге ПАО Сбербанк | `chip--success` |
| Погашена | `chip--dpurple` |

### Private Equity

Чип-тег `chip chip--s chip--pe chip--fit` с текстом «PE». **PE — флаг-категория, а
не статус:** не скругляется, цвет кастомный по кейсу (правило «теги» в Chip 1.013),
к тон-вариантам чипа не привязан, не сжимается (`.chip--fit` — значение
показывается целиком). Окраска — только токены ДС: фон/обводка
`--st-orange-mid` (StOrange_mid), текст `--text-primary`. Класс `.chip--pe`
задан в собственном `<style>` экрана.

## 8. Модальные окна

| id | Заголовок | Ширина | Содержимое | Кнопки подвала |
|---|---|---|---|---|
| `new-deal-scrim` | Создание новой сделки | `--w4` | «Номер сделки» (InputText, `inputmode="numeric"`, хелпер валидации), «Наименование сделки» (InputText) | справа «Сохранить» (`btn--accent`) |
| `filter-scrim` | Фильтр | `--w6` | штатная структура TableFilter `.tfm` (Modal `--flush` + `.tfm__body`): слева `.tfm__nav` — вертикальные табы-якоря разделов, справа `.tfm__panel` с пяти `.tfm__sec` («Общая информация», «Продукты», «Сроки», «Команда сделки», «Прочее»), форма `.tfm__grid` (2×minmax(0,1fr)), широкие поля `.tfm__span-2`; поля `ds-label` + контрол, автокомплит-мультивыбор — `.inp` с `.inp__chips` + `data-ddl` и `.ddl`-списком | справа «Очистить фильтр» (`btn--transparent`) + «Применить» (`btn--accent`) |
| `cols-scrim` | Настройка таблицы | `--w4` | стандартная модалка (рантайм `ds-table-settings.js` по хуку `data-table-settings`): описание «Выберите столбцы для отображения», чекбокс «Выбрать все» (`cb` indeterminate), серый `divider`, список `[checkbox + pin + drag]` 23 колонок | справа «Отменить» (`btn--transparent`) + «Применить» (`btn--accent`) |

**Состав полей фильтра:**
- Основная информация: Номер сделки (numeric), Наименование сделки, КНР — текстовые;
  Статус, Статус operations, Статус ЦУП, Статус Мониторинг, Деск, Валюта сделки,
  Балансы сделки — автокомплит-мультивыбор.
- Продукты: Продукты сделки, Продукты ДИД, Основной продукт ДИД — мультивыбор.
- Сроки: Дата подписания, Дата первой выдачи, Дата окончания — `InputDateRange`.
- Команда сделки: Директор, Менеджер, Сотрудник ЦУП — одиночный автокомплит.
- Прочее: Совместность (мультивыбор), Обеспечение (Да/Нет), Корпоративное
  управление (мультивыбор), Представительство в СД (Да/Нет), Фин. расчеты в
  шаблоне (Да/Нет), Сделки Private Equity (Да/Нет), чекбокс «Только сделки
  ограниченного доступа».

## 9. Поведение

| Триггер | Результат |
|---|---|
| Клик «Новая сделка» | открывается модальное окно `new-deal-scrim` |
| Клик «Фильтр» | открывается модальное окно `filter-scrim` |
| Клик «Настроить колонки» | открывается стандартная модалка «Настройка таблицы» (рантайм `ds-table-settings.js`): «Выбрать все» (indeterminate), чекбоксы видимости, мультизакрепление (`pin`), перетаскивание за `drag`-иконку (линия вставки `--primary`). «Применить» применяет — скрывает/показывает, переставляет и закрепляет реальные колонки таблицы (событие `columnsettings`); «Отменить»/Escape/клик по скриму — закрытие без применения |
| Клик по `th__sort` в шапке | цикл сортировки: нет → по возрастанию → по убыванию → нет; активна максимум одна колонка. Меняются глиф стрелки, подсветка колонки (`.th--sorted`) и порядок строк. Сортируемые колонки — семь: «Номер сделки» (`number`, `data-sort-type="number"`), «Наименование сделки» (`name`, `text`), «Ограниченный доступ» (`restricted`, `text`), «Private Equity» (`pe`, `number` — значение берётся из `data-sort-value` ячейки, 1/0) и три даты (`signDate`, `firstIssueDate`, `endDate`, `date`). Ключ поля — в `data-sort` кнопки, тип — в `data-sort-type` на `.th`. Порядок строк переставляет рантайм ДС по `data-sort-rows` на `.tbl` — с 05.09.2026 по **всем 50 записям** реестра (не только по видимой странице): все строки стоят в DOM, скрыты только вне текущего окна пагинации; после события `sort` скрипт экрана сбрасывает пагинацию на первую страницу и пересчитывает видимость по новому порядку. С реальным бэкендом атрибут `data-sort-rows` снимается, а сортирует сервер по событию `sort` с тем же ключом |
| Перетаскивание `.th__resize` | изменение ширины колонки, минимум 96px |
| Перетаскивание за `.th__label` | изменение порядка колонок (закреплённые переносятся — класс pin едет с колонкой) |
| Клик по `th__pin` | закрепление/открепление колонки (на макете закреплены «Номер» и «Наименование») |
| Горизонтальная прокрутка тела | закреплённые колонки остаются на месте (sticky) |
| Создание сделки при изменённых колонках | строка встаёт в текущую форму колонок: закреплённые колонки держатся наравне с остальными строками, треки и порядок ячеек — как в шапке, скрытая настройкой колонка в строке не показывается. Делает рантайм ДС (`DSTable.adoptRow` + `tbl-pin.js`), хук — `data-col` |
| Смена страницы / размера страницы в пагинации | скрипт экрана пересчитывает окно видимых строк (`hidden`) и перерисовывает колонтитул (`DSPagination.footer`) с фактическим `total`; переключение реальное, не вёрстка |
| Клик «Сохранить» в «Новая сделка» | с 05.09.2026 — реально: валидация «Номер сделки» (только цифры, уникальность по `DealsStore.exists({id})`) и «Наименование сделки» (непустое, уникальность регистро- и пробело-независимая); ошибка — `.inp--error` + тултип (`DSTooltip`, тип error, показывается на фокусе поля, текст не дублируется в хелпере — правило InputText/LabelHelper); фокус уходит на первое невалидное поле. При успехе: `DealsStore.create({id, name})` (статусный кортеж — Черновик, остальные поля пустые), новая строка вставляется первой, `DSTable.wire(tbl).rowsChanged()` включает её в `__dsRowOrder` (участвует в сортировке), пагинация сбрасывается на страницу 1 и пересчитывается, модалка закрывается. При открытии модалки форма и состояние ошибок всегда сбрасываются (`DSModal.bind(trigger,{onOpen})`, перехватывает автопривязку `ds-modal.js`) |
| Выбор значения в поле фильтра | с 05.09.2026 — реально: списки мультивыбора/одиночного выбора перестроены из `window.DEALS_ENUMS` (полный домен, не черновик из нескольких демо-значений); чип добавляется/убирается в поле (`DSInputKit.makeChip`), крестик поля снимает выбор в списке и наоборот; счётчик на вкладке слева (`.tab__badge`) — живой, растёт/падает при каждой правке черновика формы, скрыт при 0 (`.tab__badge[hidden]`, правка Tab 1.010) |
| Клик «Применить» | реальный отбор: `DealsStore.list()` фильтруется по всем активным полям (текст — регистронезависимая подстрока, мультивыбор — вхождение в множество/пересечение массивов, диапазон дат — `parseRuDate` в границах, чекбокс — точное совпадение); пагинация и «Сделок: N» пересчитываются по результату, страница сбрасывается на 1-ю, модалка закрывается. Чип «Применено: N» в шапке тулбара — число активных полей на момент этого клика (не число найденных строк), создаётся/обновляется только здесь и при «Очистить». Нулевой результат — EmptyState вместо таблицы и колонтитула (см. новую вёрстку `#deals-empty`), а не пустая таблица |
| Клик «Очистить фильтр» / крестик на чипе «Применено: N» / «Очистить фильтр» в EmptyState | сброс всех полей формы и повторное применение (эквивалент «Применить» с пустым черновиком — снова все 50 сделок). Крестик чипа — стандартный `ds-table-filter.js`: убирает чип и шлёт `tfilter:reset` (bubbles) на `.tfilter`, экран слушает это событие для сброса параметров |

## 10. Тексты интерфейса

| Место | Текст |
|---|---|
| Заголовок страницы | Текущий портфель ДИД |
| Кнопка создания | Новая сделка |
| Заголовок таблицы | — (headless, варианта нет) |
| Кнопка фильтра | Фильтр |
| Чип фильтра | Применено: N (реальное число, чипа нет при N=0) |
| EmptyState — заголовок | Сделки не найдены |
| EmptyState — текст | По заданным условиям фильтра ничего не найдено. Измените параметры и попробуйте снова. |
| EmptyState — действие | Очистить фильтр |
| Кнопка настройки | Настроить колонки |
| Заголовок модалки 1 | Создание новой сделки |
| Заголовок модалки 2 | Фильтр |
| Заголовок модалки 3 | Настройка таблицы |
| Подвал модалки 2 (вторичная) | Очистить фильтр |
| Подвал модалки 2 (основная) | Применить |
| Подвал модалки 3 (вторичная) | Отменить |
| Подвал модалки 3 (основная) | Применить |
| Хелпер «Номер сделки» | Только числовые символы |

## 11. Компоненты ДС

| Компонент | Где на экране | Варианты |
|---|---|---|
| Layout | каркас | `nav--rail` |
| NavPanel | боковое меню | `nav--rail`, из `ibp-home.js` |
| Breadcrumbs | зона крошек | `data-breadcrumbs` |
| PageHeader | шапка | без иконки/статуса, 1 действие |
| Table | таблица | тулбар + футер, `.tbl--scroll` |
| TableCell | строки и шапка | сортировка, закрепление, чипы, числа; строка шапки — `tbl__row--head` (обязателен внутри `.dtable__body`, на нём липкая шапка, Table 1.013) |
| TableFilter | тулбар + модалка | бар (кнопка + чип «Применено: N», с 05.09.2026 реальный — создаётся/скрывается по факту применения) и модалка `.tfm` — неотъемлемые части [уточнено 04.09.2026] |
| Pagination | футер | размер страницы + номера |
| Modal | 3 окна | `--w3`, `--w4`, `--w6` + `--flush` |
| Chip | статусы, PE, валюты, фильтр | `chip--s` + тона, `chip--edit` |
| Avatar | аватарки чипов | `av--circular` в `.chip__avatar` |
| Checkbox | настройка колонок, фильтр | `cb`, `cb--indeterminate` |
| InputText | «Новая сделка», текстовые поля фильтра | `.inp`, ошибка через `DSTooltip` (не хелпер); крестик очистки — рантайм `ds-input.js` (виден только при значении), плейсхолдеров нет |
| InputAutocomplete | поля фильтра (мультивыбор и одиночный выбор) | переполнение чипов сворачивается в «+N» рантаймом `ds-input.js` (InputAutocomplete 1.013) · `.inp__chips` / `.ddl`, `data-ddl` + `DSDropdownList.bind(field,{multiple,onToggle/onSelect})` — списки перестроены из `DEALS_ENUMS` |
| InputDate / InputDateRange | поля «Сроки» | `.inp-range--date`, маска ДД.ММ.ГГГГ из коробки (DatePicker 1.007; до этого маска жила только в демо-скрипте страницы ДС). Единственные поля экрана, где плейсхолдер оставлен — он несёт формат |
| EmptyState | тело таблицы при нулевом результате фильтра | `.es--m`, иллюстрация `empty-folder`, действие «Очистить фильтр»; переключается атрибутом `hidden` (работает с EmptyState 1.002) |
| Divider | настройка колонок | `dvd--h` |

## 12. Открытые вопросы и допущения

| № | Вопрос / допущение | Статус |
|---|---|---|
| 1 | Ширины колонок таблицы в ТЗ не заданы — подобраны по содержимому | допущение, нужна валидация |
| 2 | ~~Две закреплённые колонки смещены на 150px собственным правилом~~ — закрыто (TableCell 2.007): удержание закреплённых у левого края делает рантайм `tbl-pin.js` (накопление отступов); правило экрана удалено | закрыто |
| 2а | Закрепление у правого края (хвостовой блок), прилипание сепаратора и перетаскивание закреплённых — закрыто (TableCell 2.008) | закрыто |
| 3 | Тон «Погашена» → `chip--dpurple` (фиолетовый, ТЗ) и «PE» → кастомный тег-цвет `.chip--pe` (жёлтый→StOrange, ТЗ) — рампы появились в ДС (Chip 1.012) | решено 04.09.2026 |
| 4 | Заливка аватарки чипа — tone-matched `-mid` той же рампы (фон `--st-<color>-mid`, белая буква) — штатное поведение Chip 1.012; заменило нейтральную `--st-system-midlight` | решено 04.09.2026 |
| 5 | «Представительство в СД» в фильтре показано как Да/Нет (тип в ТЗ не указан) | допущение |
| 6 | ~~Пагинация «1 из 3» — реальный пересчёт строк не реализуется~~ — закрыто 05.09.2026: колонтитул собирает скрипт экрана через `DSPagination.footer`, `total`/номера страниц фактические (50 сделок, размер по умолчанию 20 → «1 из 3»), переключение страницы/размера реально прячет/показывает строки | закрыто |
| 7 | ~~Создание сделки и фильтрация — вёрстка состояния, без реального перехода~~ — закрыто 05.09.2026: Этап 3 (создание сделки, валидация через `DealsStore.exists`/`create`) и Этап 4 (реальный отбор по ~26 полям формы фильтра, честный «Применено: N», EmptyState на нулевой результат) плана `mock-deals.plan.md` выполнены | закрыто |
| 10 | Новая сделка, созданная при активном фильтре, может не совпасть с текущим отбором (пустые поля черновика редко проходят условия) и окажется скрыта до «Очистить фильтр» — не переопределяем фильтр автоматически, чтобы не удивлять пользователя незапрошенным сбросом его условий | допущение, решить при проектировании страницы сделки |
| 8 | ~~Активная сортировка не соответствует фактическому порядку строк~~ — закрыто 05.09.2026: начальный рендер 50 строк отсортирован по дате подписания по убыванию (соответствует `aria-sort="descending"` в шапке). Механизм цикла (`none→asc→desc→none`) не изменился: первый клик после загрузки уводит в `none` — это возврат к исходному порядку разметки, который теперь ЕСТЬ убывание по дате, поэтому визуально ничего не меняется; второй клик даёт настоящее возрастание, третий — снова убывание (по всем 50 записям, не только по видимой странице) | закрыто |
| 1а | Ширины колонок статусов («Статус» 140px, «Статус ЦУП» / «Статус Operations» 176px) не вмещают формулировки статусной модели. Решение 04.09.2026: колонки не расширяем и формулировки не сокращаем — статус усекается многоточием внутри чипа, полное значение по наведению даёт тултип. Следить: в колонке «Статус» соседние состояния различаются почти только цветом | решено |
| 9 | Модалка «Настройка таблицы» — стандарт (рантайм `ds-table-settings.js`): «Выбрать все» (indeterminate при частичном), мультизакрепление, drag-перестановка с линией вставки, отложенное применение («Отменить»/«Применить»). «Применить» реально скрывает/показывает, переставляет и закрепляет колонки — соответствие «галочка ↔ видимость» проигрывается. Колонки без подписи (выбор/действие) — служебные, в настройке не участвуют | стандарт ДС (см. specs/Table.md «Настройка таблицы») |
