---
screen: Portfolio
title: Текущий портфель ДИД
file: Projects/post/Portfolio-did/Portfolio.html
source: ТЗ — Projects/post/deal_post/Текущий портфель.md
version: "1.026"
created: "03.09.2026"
updated: "16.09.2026"
design_system: IBP DS
components: [Layout, NavPanel, Breadcrumbs, Link, LabelHelper, PageHeader, Table, TableCell, TableFilter, Pagination, Modal, Tab, Chip, Avatar, Checkbox, InputText, InputAutocomplete, InputDateRange, EmptyState, Divider, IconButton, Buttons]
---

# Текущий портфель ДИД

## 1. Назначение

Реестр сделок направления ДИД: пользователь видит полный список сделок текущего
портфеля, фильтрует его по параметрам сделки, настраивает видимые колонки и
создаёт новую сделку.

**Роль пользователя:** Финансист ДИД
**Права:** просмотр портфеля; сделки с ограниченным доступом видны только при
наличии права (в макете одна строка с «Да»).

## 2. Навигация

| Уровень | Текст | Ссылка |
|---|---|---|
| 1 | Главная | `../mainPage/index.html` |
| 2 (текущий) | Текущий портфель ДИД | — |

Крошка «Главная» и пункт бокового меню «Главная» ведут на главную страницу
(`mainPage/index.html`) — двусторонняя связь: на главной пункт меню и тайл
«Текущий портфель» открывают эту страницу (см. mainPage.screen.md).

Пункт бокового меню в состоянии `nav__item--selected` + `aria-current="page"`:
«Текущий портфель» (группа «Текущий портфель ДИД», иконка `current-depo`).

## 3. Источник данных навигации

Меню панели строится экранным скриптом из `window.IBPHome` (`scripts/ibp-home.js`)
по роли `'Финансист ДИД'` — вызовом `IBPHome.itemsFor(role)`. Пункт
`current-portfolio` помечается selected; пункты с `tile.links` («Корпоративные
запросы», «Отчёты 1C/Navision», «Отчёты для Рисков») рендерятся как
аккордеон-родители `.nav__item--acc`; «Поиск клиентов в ЕПК» (`tile: null`) — обычный
пункт меню без тайла. Строка пользователя в футере — Александров Петр
Константинович, «Финансист ДИД» (вынесена в `IBPHome.footerHTML(role)`).
**В макете** строка пользователя ведёт на хаб проектов (корневой `index.html`,
`aria-label="Хаб проектов"`) — служебный переход, в продукт не переносится.

## 4. Шапка страницы (PageHeader)

| Элемент | Значение | Компонент / класс |
|---|---|---|
| Заголовок | Текущий портфель ДИД | `h1.phead__title` |
| Действие (главное) | Новая сделка | `btn btn--accent btn--m`, глиф `add`, триггер `data-modal="new-deal-scrim"` |

Статус экрана и подзаголовок отсутствуют.

## 5. Раскладка

Контентная область — 12 колонок, зазор 16px. Единственный блок контента —
таблица на всю ширину. Горизонтального скролла у рабочей области нет —
горизонтальный скролл только внутри тела таблицы (`.tbl--scroll`). Отступ
таблицы от нижней границы экрана — 24px: это нижнее поле контентной области,
собственного `margin-bottom` у таблицы нет (правило каркаса — низ одинаков для
всех типов контента).

**Высота таблицы — правило каркаса** (Layout `.screen--app` + Table `.dtable--fill`):
таблица как единственный/основной блок занимает **всю свободную высоту экрана**
за вычетом полей контента, крошек, шапки страницы и тулбара/фильтра. На рабочей
области стоит `.screen--app` (фикс. вьюпорт `100vh`), на таблице — `.dtable--fill`:
тело таблицы скроллится внутри, крошки/шапка/тулбар/пагинация закреплены.
Вертикальный скролл страницы при этом отсутствует — скроллится только тело таблицы.

| № | Блок | Компонент | Колонок из 12 | Содержимое |
|---|---|---|---|---|
| 1 | Зона тулбара | Table (`.dtable-toolbar`) | 12 | слева TableFilter, справа настройка колонок |
| 2 | Таблица «Сделки» | Table (`.dtable--fill`, headless) | 12 | 23 колонки, без тулбара (сразу шапка), пагинация |

Вертикальный ритм — **12px**: заголовок страницы → зона тулбара → таблица. Это
описанный вариант каркаса (Layout, раздел «Экран-реестр: состав и отступы»), а не
самодеятельность экрана; значение экран задаёт единственной раскладочной строкой
в своём `<style>`: `.screen__content { gap: var(--space-12) }`.

## 6. Таблица «Сделки»

**Источник данных (05.09.2026):** таблицу рендерит скрипт экрана из общей базы
направления, а не статичная разметка:

| Файл | Что | Подключён на этом экране |
|---|---|---|
| `../data/mock-deals.js` | `window.MOCK_DEALS` — 50 сделок (плоские поля таблицы и фильтра) + `window.DEALS_ENUMS` — перечни для фильтра | да |
| `../data/deals-store.js` | `window.DealsStore` — `list()/byId()/exists()/create()/update()/on()`, единая точка доступа к базе | да |
| `../data/mock-deal-trees.js` | `window.MOCK_DEAL_TREES` — дерево продуктов ДИД по сделкам, ключ — `id` | нет (нужно только будущей странице сделки) |

**Ключ колонки (`data-col`).** Каждая содержательная `.th` в шапке (23 колонки)
и каждая ячейка, которую собирает `renderRow`, несут `data-col="<поле сделки>"` —
ключ пишет общий `cellOpen`. Это хук рантайма таблицы: по ключу `DSTable.adoptRow`
сопоставляет ячейку строки, дописанной экраном, с колонкой шапки и приводит
строку к текущей форме колонок — порядок после переноса, треки после ресайза,
ячейка скрытой колонки уходит на склад строки. Инсеты закрепления новой строке
проставляет `tbl-pin.js` (он ловит вставку строки сам). Своего JavaScript под это
экран не пишет: обработчик «Новая сделка» только вставляет строку
(TableCell 2.019, Table 1.016).

Скрипт стоит **после** `ds.js` (нужны `window.DSTable`/`window.DSPagination`,
которые `ds.js` догружает через `document.write`); рантаймы таблицы сами
поднимаются на `DOMContentLoaded` — позже, чем этот синхронный скрипт, поэтому
50 вставленных строк учитываются ими штатно при первом `wire()` (порядок для
`data-sort-rows`, тултипы усечения — без обращения к `rowsChanged()`, он нужен
только для строк, добавленных ПОСЛЕ загрузки страницы — см. Table 1.012).

Плоские поля записи — `id`, `name`, `status`/`statusTsup`/`statusOps`/`statusMon`
(кортежи §7), `restricted`, `desk`, `director`/`manager`/`tsupEmployee`, `knr[]`,
`gsz`, `jointness`, `collateral`, `corpGovernance[]`, `boardRep`, `finTpl`,
`signDate`/`firstDisb`/`endDate`/`repayDate`, `monEntity`, и производные из
дерева продуктов — `mainProductDid`, `productsDidNames[]`, `productsNames[]`,
`balances[]`, `currencies[]`, `isPE`. Производные поля вычислены из
`MOCK_DEAL_TREES` один раз при генерации и заморожены в записи — в проде их
считает бэкенд, реестр не пересчитывает.

**Допущения по форматам** (нет однозначного ответа в ТЗ): `jointness` — строка
из справочника (`DEALS_ENUMS.jointness`: «Совместная»/«Не совместная»);
`corpGovernance` — список строк; `boardRep` — Да/Нет.

**Распределение 50 сделок** покрывает крайние случаи вёрстки: 5 сделок-черновиков
без дат (прочерк во всех трёх датах), сделка с максимально длинным
наименованием, сделка со всеми 4 валютами ДС сразу (RUB/USD/EUR/JPY — больше в
системе не заведено), сделка с 6 балансами, сделка с 4 КНР, инструмент без
транша. 3 сделки — `restricted: Да`, 6 — `isPE: true`.

**Вариант «тулбар над таблицей»** (`.dtable-toolbar` + headless-таблица):
- зона `.dtable-toolbar` — БЕЗ подложки, над таблицей: слева TableFilter (кнопка
  «Фильтр» → `filter-scrim`, чип «Применено: N» — с 05.09.2026 реальный, скрыт при N=0), справа кнопка «Настроить
  колонки» (`ibtn` с глифом `settings` → `cols-scrim`);
- таблица `.dtable--fill` — **headless**: без `.dtable__toolbar`, сразу шапка
  колонок; заголовка «Сделки» и счётчика «128» наверху нет (счётчик остаётся
  только в пагинации).

**Каркас:** `.tbl[data-table]` + `.tbl--scroll`. Шапка и все строки имеют
одинаковый `grid-template-columns` (8px-разделитель, 23 колонки данных,
замыкающий `minmax(8px,1fr)`). Колонки «Номер сделки» и «Наименование сделки»
закреплены (`.th--pinned` / `.tc--pinned`, `aria-pressed="true"` + глиф
`pin-filled` на `th__pin`).

**Закрепление — из коробки (TableCell 2.013, рантайм `tbl-pin.js` в `ds.js`):**
клик по `.th__pin` в шапке закрепляет/открепляет любую колонку (без переноса
в начало). При горизонтальном скролле закреплённые колонки удерживаются у
левого края нативным `position: sticky`; рантайм считает только инсеты
`left`/`right` и пишет их при изменении состава или ширин колонок — на скролл
не подписан. Собственного правила смещения у экрана нет.

До 04.09.2026 удержание шло через `transform: translateX` на событии `scroll` —
на MacBook закреплённые колонки дрожали (компоновщик применяет скролл раньше
JS-обработчика, коррекция всегда на кадр позади; дискретное колесо мыши на
Windows это скрывало). Экран не правился — исправление целиком в ДС.

| Ключ | Заголовок | Тип | Ширина | Выравн. | Сортировка | Закреплена |
|---|---|---|---|---|---|---|
| `number` | Номер сделки | идентификатор | 150px | слева | да | да |
| `name` | Наименование сделки | текст | 280px | слева | да | да |
| `status` | Статус | Chip | 140px | слева | нет | нет |
| `statusTsup` | Статус ЦУП | Chip+аватар «Ц» | 176px | слева | нет | нет |
| `statusOps` | Статус Operations | Chip+аватар «O» | 176px | слева | нет | нет |
| `statusMon` | Статус Мониторинг | Chip+аватар «М» | 320px | слева | нет | нет |
| `restricted` | Ограниченный доступ | Да/Нет | 120px | слева | да | нет |
| `desk` | Деск | текст | 150px | слева | нет | нет |
| `balances` | Балансы сделки | текст через запятую | 220px | слева | нет | нет |
| `pe` | Private Equity | Chip «PE» | 88px | слева | да (по `data-sort-value` 1/0) | нет |
| `mainProductDid` | Основной продукт ДИД | текст | 180px | слева | нет | нет |
| `productsDid` | Продукты ДИД | текст через запятую | 240px | слева | нет | нет |
| `products` | Продукты | текст через запятую | 240px | слева | нет | нет |
| `knr` | КНР | текст | 170px | слева | нет | нет |
| `gsz` | ГСЗ | текст | 170px | слева | нет | нет |
| `currencies` | Валюты | Chip ×N, свёртка в «+N» | 150px | слева | нет | нет |
| `signDate` | Дата подписания | дата | 130px | слева | да, активна по убыванию | нет |
| `firstDisb` | Дата первой выдачи | дата | 130px | слева | да | нет |
| `endDate` | Дата окончания | дата | 130px | слева | да | нет |
| `director` | Директор | ФИО | 120px | слева | нет | нет |
| `manager` | Менеджер | ФИО | 120px | слева | нет | нет |
| `tsupEmployee` | Сотрудник ЦУП | ФИО | 120px | слева | нет | нет |
| `finTpl` | Фин. расчеты в шаблоне | Да/Нет | 140px | слева | нет | нет |

**Формат значений:** номер сделки — идентификатор, а не величина: выравнивается
по левому краю как текст (`.tc--numbers` в реестре не используется — правый край
и табулярные цифры остаются за суммами и долями); даты — `ДД.ММ.ГГГГ`; пустое
значение — прочерк `—`; валюты — чип на валюту: 3-буквенный код ISO 4217
прописными (`RUB`, `USD`, `EUR`, `JPY`), без символов ₽/$/€/¥ и без «руб.»,
чипы не сжимаются (`.chip--fit` — код показывается целиком, не «R…»);
ФИО — «Фамилия И.О.».

**Усечение и тултип:** все текстовые значения строк несут `.tc__text--truncate` —
одна строка, многоточие по границе колонки. Полное значение показывает тултип по
наведению; он навешивается рантаймом таблицы (`ds-table.js`) на значения ячеек,
подписи колонок и подписи чипов и появляется только тогда, когда текст реально
не помещается. Своей разметки тултипа на экране нет.

**Выбор строк:** не предусмотрен ТЗ (нет колонки чекбоксов и панели массовых
действий).

**Пагинация (реальная, 05.09.2026):** колонтитул собирается скриптом экрана
напрямую через `DSPagination.footer({ info, pager })` (не автопривязкой
`data-pagination` — она не заполняет левый слот) и пересобирается заново при
каждом изменении страницы/размера. Левый слот — `pgn-info` со значением
«Сделок: N» (фактическое число строк реестра — сейчас всегда 50, поскольку
фильтр по-прежнему вёрстка-заглушка, см. открытый вопрос №7; при реализации
Этапа 4 число станет отражать результат фильтра). Размер страницы по
умолчанию — **20** (при 50 сделках — реальные 3 страницы: «1 из 3» → «2 из 3»
→ «3 из 3»); доступны все размеры Pagination (5/10/15/20/50/Все). Строки не
пересоздаются — видимость переключает `hidden` на каждом `.tbl__row` (правило
`.tbl__row[hidden]`, TableCell 2.015): строка видима, если попадает в окно
текущей страницы по своей позиции в актуальном DOM-порядке (учитывает
сортировку — окно считается ПОСЛЕ перестановки строк).

## 7. Статусы

Чипы статусов — `chip chip--s chip--rounded` + тон (**статусные чипы всегда
скруглённые**, правило чипа). Чипы с аватаркой (ЦУП/Operations/Мониторинг)
используют штатный паттерн: `.chip__avatar.av.av--circular` с буквой `Ц`/`O`/`М`.
Аватар тонируется по тону чипа (фон `-mid`, белая буква) — штатное поведение Chip 1.012.
Буквы: ЦУП = «Ц», Operations = «O», Мониторинг = «М».

### Общий маппинг (Статус, Статус ЦУП, Статус Operations)

| Статус | Класс-тон |
|---|---|
| Черновик | `chip--dark` |
| Ожидает подтверждения | `chip--warning` |
| Подтверждение изменений | `chip--warning` |
| Ожидает назначения ответственного (ЦУП) | `chip--warning` |
| Корректировка | `chip--info` |
| Ввод изменений | `chip--info` |
| Корректировка изменений | `chip--info` |
| Активная | `chip--success` |
| Утверждена | `chip--success` |
| На сопровождении (Operations) | `chip--success` |
| Погашена | `chip--dpurple` |

### Статус Мониторинг

| Статус | Класс-тон |
|---|---|
| На утверждении | `chip--dark` |
| Утверждена | `chip--dark` |
| Ожидает передачи на мониторинг | `chip--warning` |
| Направлена на передачу | `chip--warning` |
| Передана | `chip--warning` |
| Изменения ожидают передачи на мониторинг | `chip--warning` |
| Изменения направлены на передачу | `chip--warning` |
| Передача изменений | `chip--warning` |
| На мониторинге ГК СБИ | `chip--success` |
| На мониторинге ПАО Сбербанк | `chip--success` |
| Погашена | `chip--dpurple` |

### Private Equity

Чип-тег `chip chip--s chip--pe chip--fit` с текстом «PE». **PE — флаг-категория, а
не статус:** не скругляется, цвет кастомный по кейсу (правило «теги» в Chip 1.013),
к тон-вариантам чипа не привязан, не сжимается (`.chip--fit` — значение
показывается целиком). Окраска — только токены ДС: фон/обводка
`--st-orange-mid` (StOrange_mid), текст `--text-primary`. Класс `.chip--pe`
задан в собственном `<style>` экрана.

## 8. Модальные окна

| id | Заголовок | Ширина | Содержимое | Кнопки подвала |
|---|---|---|---|---|
| `new-deal-scrim` | Создание новой сделки | `--w4` | «Номер сделки» (InputText, `inputmode="numeric"`, хелпер валидации), «Наименование сделки» (InputText) | справа «Сохранить» (`btn--accent`) |
| `filter-scrim` | Фильтр | `--w6` | штатная структура TableFilter `.tfm` (Modal `--flush` + `.tfm__body`): слева `.tfm__nav` — вертикальные табы-якоря разделов, справа `.tfm__panel` с пяти `.tfm__sec` («Общая информация», «Продукты», «Сроки», «Команда сделки», «Прочее»), форма `.tfm__grid` (2×minmax(0,1fr)), широкие поля `.tfm__span-2`; поля `ds-label` + контрол, автокомплит-мультивыбор — `.inp` с `.inp__chips` + `data-ddl` и `.ddl`-списком | справа «Очистить фильтр» (`btn--transparent`) + «Применить» (`btn--accent`) |
| `cols-scrim` | Настройка таблицы | `--w4` | стандартная модалка (рантайм `ds-table-settings.js` по хуку `data-table-settings`): описание «Выберите столбцы для отображения», чекбокс «Выбрать все» (`cb` indeterminate), серый `divider`, список `[checkbox + pin + drag]` 23 колонок | справа «Отменить» (`btn--transparent`) + «Применить» (`btn--accent`) |

**Состав полей фильтра:**
- Основная информация: Номер сделки (numeric), Наименование сделки, КНР — текстовые;
  Статус, Статус operations, Статус ЦУП, Статус Мониторинг, Деск, Валюта сделки,
  Балансы сделки — автокомплит-мультивыбор.
- Продукты: Продукты сделки, Продукты ДИД, Основной продукт ДИД — мультивыбор.
- Сроки: Дата подписания, Дата первой выдачи, Дата окончания — `InputDateRange`.
- Команда сделки: Директор, Менеджер, Сотрудник ЦУП — одиночный автокомплит.
- Прочее: Совместность (мультивыбор), Обеспечение (Да/Нет), Корпоративное
  управление (мультивыбор), Представительство в СД (Да/Нет), Фин. расчеты в
  шаблоне (Да/Нет), Сделки Private Equity (Да/Нет), чекбокс «Только сделки
  ограниченного доступа».

## 9. Поведение

| Триггер | Результат |
|---|---|
| Клик «Новая сделка» | открывается модальное окно `new-deal-scrim` |
| Клик «Фильтр» | открывается модальное окно `filter-scrim` |
| Клик «Настроить колонки» | открывается стандартная модалка «Настройка таблицы» (рантайм `ds-table-settings.js`): «Выбрать все» (indeterminate), чекбоксы видимости, мультизакрепление (`pin`), перетаскивание за `drag`-иконку (линия вставки `--primary`). «Применить» применяет — скрывает/показывает, переставляет и закрепляет реальные колонки таблицы (событие `columnsettings`); «Отменить»/Escape/клик по скриму — закрытие без применения |
| Клик по `th__sort` в шапке | цикл сортировки: нет → по возрастанию → по убыванию → нет; активна максимум одна колонка. Меняются глиф стрелки, подсветка колонки (`.th--sorted`) и порядок строк. Сортируемые колонки — семь: «Номер сделки» (`number`, `data-sort-type="number"`), «Наименование сделки» (`name`, `text`), «Ограниченный доступ» (`restricted`, `text`), «Private Equity» (`pe`, `number` — значение берётся из `data-sort-value` ячейки, 1/0) и три даты (`signDate`, `firstIssueDate`, `endDate`, `date`). Ключ поля — в `data-sort` кнопки, тип — в `data-sort-type` на `.th`. Порядок строк переставляет рантайм ДС по `data-sort-rows` на `.tbl` — с 05.09.2026 по **всем 50 записям** реестра (не только по видимой странице): все строки стоят в DOM, скрыты только вне текущего окна пагинации; после события `sort` скрипт экрана сбрасывает пагинацию на первую страницу и пересчитывает видимость по новому порядку. С реальным бэкендом атрибут `data-sort-rows` снимается, а сортирует сервер по событию `sort` с тем же ключом |
| Перетаскивание `.th__resize` | изменение ширины колонки, минимум 96px |
| Перетаскивание за `.th__label` | изменение порядка колонок (закреплённые переносятся — класс pin едет с колонкой) |
| Клик по `th__pin` | закрепление/открепление колонки (на макете закреплены «Номер» и «Наименование») |
| Горизонтальная прокрутка тела | закреплённые колонки остаются на месте (sticky) |
| Создание сделки при изменённых колонках | строка встаёт в текущую форму колонок: закреплённые колонки держатся наравне с остальными строками, треки и порядок ячеек — как в шапке, скрытая настройкой колонка в строке не показывается. Делает рантайм ДС (`DSTable.adoptRow` + `tbl-pin.js`), хук — `data-col` |
| Смена страницы / размера страницы в пагинации | скрипт экрана пересчитывает окно видимых строк (`hidden`) и перерисовывает колонтитул (`DSPagination.footer`) с фактическим `total`; переключение реальное, не вёрстка |
| Клик «Сохранить» в «Новая сделка» | с 05.09.2026 — реально: валидация «Номер сделки» (только цифры, уникальность по `DealsStore.exists({id})`) и «Наименование сделки» (непустое, уникальность регистро- и пробело-независимая); ошибка — `.inp--error` + тултип (`DSTooltip`, тип error, показывается на фокусе поля, текст не дублируется в хелпере — правило InputText/LabelHelper); фокус уходит на первое невалидное поле. При успехе: `DealsStore.create({id, name})` (статусный кортеж — Черновик, остальные поля пустые), новая строка вставляется первой, `DSTable.wire(tbl).rowsChanged()` включает её в `__dsRowOrder` (участвует в сортировке), пагинация сбрасывается на страницу 1 и пересчитывается, модалка закрывается. При открытии модалки форма и состояние ошибок всегда сбрасываются (`DSModal.bind(trigger,{onOpen})`, перехватывает автопривязку `ds-modal.js`) |
| Выбор значения в поле фильтра | с 05.09.2026 — реально: списки мультивыбора/одиночного выбора перестроены из `window.DEALS_ENUMS` (полный домен, не черновик из нескольких демо-значений); чип добавляется/убирается в поле (`DSInputKit.makeChip`), крестик поля снимает выбор в списке и наоборот; счётчик на вкладке слева (`.tab__badge`) — живой, растёт/падает при каждой правке черновика формы, скрыт при 0 (`.tab__badge[hidden]`, правка Tab 1.010) |
| Клик «Применить» | реальный отбор: `DealsStore.list()` фильтруется по всем активным полям (текст — регистронезависимая подстрока, мультивыбор — вхождение в множество/пересечение массивов, диапазон дат — `parseRuDate` в границах, чекбокс — точное совпадение); пагинация и «Сделок: N» пересчитываются по результату, страница сбрасывается на 1-ю, модалка закрывается. Чип «Применено: N» в шапке тулбара — число активных полей на момент этого клика (не число найденных строк), создаётся/обновляется только здесь и при «Очистить». Нулевой результат — EmptyState вместо таблицы и колонтитула (см. новую вёрстку `#deals-empty`), а не пустая таблица |
| Клик «Очистить фильтр» / крестик на чипе «Применено: N» / «Очистить фильтр» в EmptyState | сброс всех полей формы и повторное применение (эквивалент «Применить» с пустым черновиком — снова все 50 сделок). Крестик чипа — стандартный `ds-table-filter.js`: убирает чип и шлёт `tfilter:reset` (bubbles) на `.tfilter`, экран слушает это событие для сброса параметров |

## 10. Тексты интерфейса

| Место | Текст |
|---|---|
| Заголовок страницы | Текущий портфель ДИД |
| Кнопка создания | Новая сделка |
| Заголовок таблицы | — (headless, варианта нет) |
| Кнопка фильтра | Фильтр |
| Чип фильтра | Применено: N (реальное число, чипа нет при N=0) |
| EmptyState — заголовок | Сделки не найдены |
| EmptyState — текст | По заданным условиям фильтра ничего не найдено. Измените параметры и попробуйте снова. |
| EmptyState — действие | Очистить фильтр |
| Кнопка настройки | Настроить колонки |
| Заголовок модалки 1 | Создание новой сделки |
| Заголовок модалки 2 | Фильтр |
| Заголовок модалки 3 | Настройка таблицы |
| Подвал модалки 2 (вторичная) | Очистить фильтр |
| Подвал модалки 2 (основная) | Применить |
| Подвал модалки 3 (вторичная) | Отменить |
| Подвал модалки 3 (основная) | Применить |
| Хелпер «Номер сделки» | Только числовые символы |

## 11. Компоненты ДС

| Компонент | Где на экране | Варианты |
|---|---|---|
| Layout | каркас | `nav--rail` |
| NavPanel | боковое меню | `nav--rail`, из `ibp-home.js` |
| Breadcrumbs | зона крошек | `data-breadcrumbs` |
| PageHeader | шапка | без иконки/статуса, 1 действие |
| Table | таблица | тулбар + футер, `.tbl--scroll` |
| TableCell | строки и шапка | сортировка, закрепление, чипы, числа; строка шапки — `tbl__row--head` (обязателен внутри `.dtable__body`, на нём липкая шапка, Table 1.013) |
| TableFilter | тулбар + модалка | бар (кнопка + чип «Применено: N», с 05.09.2026 реальный — создаётся/скрывается по факту применения) и модалка `.tfm` — неотъемлемые части [уточнено 04.09.2026] |
| Pagination | футер | размер страницы + номера |
| Modal | 3 окна | `--w3`, `--w4`, `--w6` + `--flush` |
| Chip | статусы, PE, валюты, фильтр | `chip--s` + тона, `chip--edit` |
| Avatar | аватарки чипов | `av--circular` в `.chip__avatar` |
| Checkbox | настройка колонок, фильтр | `cb`, `cb--indeterminate` |
| InputText | «Новая сделка», текстовые поля фильтра | `.inp`, ошибка через `DSTooltip` (не хелпер); крестик очистки — рантайм `ds-input.js` (виден только при значении), плейсхолдеров нет |
| InputAutocomplete | поля фильтра (мультивыбор и одиночный выбор) | переполнение чипов сворачивается в «+N» рантаймом `ds-input.js` (InputAutocomplete 1.013) · `.inp__chips` / `.ddl`, `data-ddl` + `DSDropdownList.bind(field,{multiple,onToggle/onSelect})` — списки перестроены из `DEALS_ENUMS` |
| InputDate / InputDateRange | поля «Сроки» | `.inp-range--date`, маска ДД.ММ.ГГГГ из коробки (DatePicker 1.007; до этого маска жила только в демо-скрипте страницы ДС). Единственные поля экрана, где плейсхолдер оставлен — он несёт формат |
| EmptyState | тело таблицы при нулевом результате фильтра | `.es--m`, иллюстрация `empty-folder`, действие «Очистить фильтр»; переключается атрибутом `hidden` (работает с EmptyState 1.002) |
| Divider | настройка колонок | `dvd--h` |

## 12. Открытые вопросы и допущения

| № | Вопрос / допущение | Статус |
|---|---|---|
| 1 | Ширины колонок таблицы в ТЗ не заданы — подобраны по содержимому | допущение, нужна валидация |
| 2 | ~~Две закреплённые колонки смещены на 150px собственным правилом~~ — закрыто (TableCell 2.007): удержание закреплённых у левого края делает рантайм `tbl-pin.js` (накопление отступов); правило экрана удалено | закрыто |
| 2а | Закрепление у правого края (хвостовой блок), прилипание сепаратора и перетаскивание закреплённых — закрыто (TableCell 2.008) | закрыто |
| 3 | Тон «Погашена» → `chip--dpurple` (фиолетовый, ТЗ) и «PE» → кастомный тег-цвет `.chip--pe` (жёлтый→StOrange, ТЗ) — рампы появились в ДС (Chip 1.012) | решено 04.09.2026 |
| 4 | Заливка аватарки чипа — tone-matched `-mid` той же рампы (фон `--st-<color>-mid`, белая буква) — штатное поведение Chip 1.012; заменило нейтральную `--st-system-midlight` | решено 04.09.2026 |
| 5 | «Представительство в СД» в фильтре показано как Да/Нет (тип в ТЗ не указан) | допущение |
| 6 | ~~Пагинация «1 из 3» — реальный пересчёт строк не реализуется~~ — закрыто 05.09.2026: колонтитул собирает скрипт экрана через `DSPagination.footer`, `total`/номера страниц фактические (50 сделок, размер по умолчанию 20 → «1 из 3»), переключение страницы/размера реально прячет/показывает строки | закрыто |
| 7 | ~~Создание сделки и фильтрация — вёрстка состояния, без реального перехода~~ — закрыто 05.09.2026: Этап 3 (создание сделки, валидация через `DealsStore.exists`/`create`) и Этап 4 (реальный отбор по ~26 полям формы фильтра, честный «Применено: N», EmptyState на нулевой результат) плана `mock-deals.plan.md` выполнены | закрыто |
| 10 | Новая сделка, созданная при активном фильтре, может не совпасть с текущим отбором (пустые поля черновика редко проходят условия) и окажется скрыта до «Очистить фильтр» — не переопределяем фильтр автоматически, чтобы не удивлять пользователя незапрошенным сбросом его условий | допущение, решить при проектировании страницы сделки |
| 8 | ~~Активная сортировка не соответствует фактическому порядку строк~~ — закрыто 05.09.2026: начальный рендер 50 строк отсортирован по дате подписания по убыванию (соответствует `aria-sort="descending"` в шапке). Механизм цикла (`none→asc→desc→none`) не изменился: первый клик после загрузки уводит в `none` — это возврат к исходному порядку разметки, который теперь ЕСТЬ убывание по дате, поэтому визуально ничего не меняется; второй клик даёт настоящее возрастание, третий — снова убывание (по всем 50 записям, не только по видимой странице) | закрыто |
| 1а | Ширины колонок статусов («Статус» 140px, «Статус ЦУП» / «Статус Operations» 176px) не вмещают формулировки статусной модели. Решение 04.09.2026: колонки не расширяем и формулировки не сокращаем — статус усекается многоточием внутри чипа, полное значение по наведению даёт тултип. Следить: в колонке «Статус» соседние состояния различаются почти только цветом | решено |
| 9 | Модалка «Настройка таблицы» — стандарт (рантайм `ds-table-settings.js`): «Выбрать все» (indeterminate при частичном), мультизакрепление, drag-перестановка с линией вставки, отложенное применение («Отменить»/«Применить»). «Применить» реально скрывает/показывает, переставляет и закрепляет колонки — соответствие «галочка ↔ видимость» проигрывается. Колонки без подписи (выбор/действие) — служебные, в настройке не участвуют | стандарт ДС (см. specs/Table.md «Настройка таблицы») |

## 13. Правки 03.09.2026

1. Двусторонняя связь с главной страницей (`mainPage/index.html`): крошка
   «Главная» и пункт бокового меню «Главная» получили реальный href
   (`../mainPage/index.html`) вместо `#`. На главной пункт меню и тайл
   «Текущий портфель» открывают эту страницу (см. mainPage.screen.md, раздел 9).
2. Высота таблицы — по правилу каркаса (Layout 1.009 + Table 1.007): на рабочую
   область добавлен `.screen--app`, на таблицу — `.dtable--fill`. Таблица
   занимает всю свободную высоту экрана, тело скроллится внутри (крошки/шапка/
   тулбар/пагинация закреплены); вертикальный скролл страницы отсутствует.
3. Вариант Table «тулбар над таблицей» (Table 1.008): тулбар вынесен из таблицы
   в detached-зону `.dtable-toolbar` (без подложки: слева фильтр, справа настройка
   колонок), таблица стала headless (без заголовка «Сделки» и счётчика «128» —
   сразу шапка). Вертикальный ритм 12px задан в `<style>` экрана
   (`.screen__content { gap: var(--space-12) }`).
4. Закрепление колонок — из коробки (TableCell 2.007, рантайм `tbl-pin.js`):
   клик по `.th__pin` закрепляет/открепляет любую колонку; при горизонтальном
   скролле закреплённые удерживаются у левого края JS-ом. Удалено собственное
   правило смещения второй pinned-колонки на 150px (было для битого CSS-sticky);
   hover-затемнение фона закреплённой шапки в ДС убрано (TableCell 2.007).
5. Этап 2 закрепления (TableCell 2.008): закреплённая колонка из хвостового
   блока удерживается у правого края (right-pin); примыкающий сепаратор
   прилипает вместе с закреплённой колонкой (левый/правый край);
   закреплённые колонки перетаскиваемы (класс pin едет с колонкой).
6. Закрепление TableCell 2.009 (фикс в ДС/рантайме, HTML экрана не менялся):
   сепаратор получил явную заливку `--bg-table-default` и при прилипании к закреплённой
   колонке красится `--bg-table-pinned` — сливается с колонкой в сплошную полосу;
   `.tbl__row { min-width: max-content }` — удержание закреплённых через transform
   больше не сужает ширину прокрутки, поэтому правая закреплённая колонка не
   перекрывает соседние незакреплённые слева при возврате в крайнее правое положение.

## 14. Правки 04.09.2026

1. Статусы переведены на новую таксономию Chip 1.012: статусные чипы получили
   `chip--rounded` (статусы всегда скруглённые), тон остался по маппингу.
   «Погашена» — `chip--dpurple` (фиолетовый, ТЗ) вместо аппроксимации `chip--error`;
   до появления рампы StDpurple в ДС фиолетовый был недоступен (см. открытый вопрос 3).
2. Аватарки чипов (ЦУП/Operations/Мониторинг) теперь тонируются по тону чипа:
   фон `-mid` той же рампы, белая буква — штатное поведение Chip 1.012 (заменило
   нейтральную `--st-system-midlight`; см. открытый вопрос 4).
3. Чип-тег PE переведён с `chip--warning` (аппроксимация) на кастомный тег-цвет
   `.chip--pe` (фон/обводка `--st-orange-mid`, текст `--text-primary`). PE — флаг-
   категория, а не статус: не скругляется, цвет кастомный по кейсу (правило «теги»).
   Класс `.chip--pe` задан в собственном `<style>` экрана.
4. Версия экрана `1.007` → `1.008`.

## 15. Правки 04.09.2026 (валюта)

1. Валюты в колонке «Валюты» и в фильтре «Валюта сделки» переведены с символов
   (`₽`, `$`, `€`, `¥`) на 3-буквенные коды ISO 4217 прописными: `RUB`, `USD`,
   `EUR`, `JPY`. Заменены подписи чипов и aria-label кнопок удаления.
2. Формат значений (раздел 6) обновлён под правило редакционной политики ДС
   «валюта — всегда код» (см. страницу «Редполитика»).
3. Чипы валют и PE переведены на несжимаемый вариант `.chip--fit` (Chip 1.013):
   код показывается целиком, не обрезается до «R…».
4. Версия экрана `1.008` → `1.010`.

## 16. Правки 04.09.2026 (пагинация)

1. Колонтитул таблицы переведён со статичной разметки на рантайм
   `data-pagination` (`data-total="128" data-page="1" data-page-size="50"`):
   сборку. Рантайм рисует `.pgn-footer > .pgn-row` сам — «Показывать строк: 50»
   с рабочим дропдауном, диапазон «1 из 3», номера страниц 1–2–3.
2. Диапазон приведён к формату ДС «N из M» (текущая страница «из» всего страниц)
   вместо «1–8 из 128». Раздел 6 и открытый вопрос 6 обновлены.
3. Версия экрана `1.010` → `1.013`.

> Примечание: в журнале правок этой спеки оставалось расхождение версии
> (раздел 15 фиксировал `1.010`, YAML — `1.012`). Версия в YAML приведена
> к `1.013`; хронология до `1.012` в разделах правок не задокументирована.

## 17. Правки 04.09.2026 (модалка фильтра — структура TableFilter `.tfm`)

1. Модалка фильтра пересобрана с самодельных `.fil-group/.fil-title/.fil-grid`
   на штатную структуру TableFilter `.tfm` (TableFilter 1.008): Modal `--w6`
   получила `.tfm` + `--flush` = `.tfm__body` (сетка `232px | minmax(0,1fr)`),
   слева `.tfm__nav` (вертикальные табы-якоря разделов), справа `.tfm__panel`
   с пяти `.tfm__sec`. Причина: модалка — неотъемлемая часть TableFilter, а не
   модель потребителя (урок Л39). Собственный CSS `.fil-*` из `<style>` удалён.
2. Якорные табы частично оформлены на рантайм `data-ddl` (автокомплиты) и
   коротким экранным скриптом scroll-spy (клик таба → скролл `.modal__body`
   к секции, `aria-current` при прокрутке) — поведение потребителя, эталон
   `table-filter.page.js:497–531`, рантайма ДС для якорей нет.
3. Поля автокомплита получили `data-ddl="<id>"` + `.ddl`-список: шеврон/поле
   открывают список (автопривязка `DSDropdownList.bindAll` в `ds.js`).
   Со «Статуса» снят статичный `is-open`/`aria-expanded="true"`.
4. Заполненным полям (Статус, Статус operations, Статус ЦУП, Деск, Валюта)
   добавлен крестик очистки `.inp__act[aria-label="Очистить поле"]` (порядок
   действий ДС: крестик → шеврон).
5. Подвал: обе кнопки справа — «Очистить фильтр» (`btn--transparent`) +
   «Применить» (`btn--accent`) вместо «Сохранить». Вторичная кнопка названа
   «Очистить фильтр» по конвенции Modal (в ДС TableFilter значилась «Очистить» —
   исправлено в ДС, см. дc-lessons).
6. Разделы 8, 10, 11 обновлены под новую структуру.
7. Версия экрана `1.013` → `1.014`.

## 18. Правки 04.09.2026 (модалка настройки — стандарт ДС)

1. Модалка «Настройка таблицы» переведена на **стандарт ДС**: удалена статичная
   разметка `cols-scrim` и хук `data-modal`, вместо них на кнопке-шестерёнке —
   `data-table-settings`. Модалку собирает рантайм `scripts/ds-table-settings.js`
   (в `ds.js`) по колонкам из шапки таблицы.
2. Стандартная структура: описание «Выберите столбцы для отображения» (Body M,
   `--text-secondary`) → «Выбрать все» (`cb` indeterminate) → серый `divider` →
   список колонок `[checkbox + pin + drag]` (M neutral). Футер — «Отменить»
   (`btn--transparent`) + «Применить» (`btn--accent`) справа.
3. Поведение: «Выбрать все», чекбоксы видимости, **мультизакрепление**,
   перетаскивание за drag-иконку (линия вставки `--primary`), отложенное
   применение. «Применить» реально скрывает/показывает, переставляет и
   закрепляет колонки таблицы; «Отменить»/Escape/клик по скриму — закрытие
   без применения.
4. Удалён собственный CSS `.cols-list`; хром модалки — `styles/table-settings.css`.
5. Разделы 8, 9, 10, 12 обновлены. Открытый вопрос №9 закрыт (соответствие
   «галочка ↔ видимость» проигрывается).
6. Версия экрана `1.014` → `1.015`.

## 19. Правки 05.09.2026 (реестр из общей базы: рендер, реальные пагинация и сортировка)

Этап 2 плана `mock-deals.plan.md` (см. `Projects/post/data/`).
Этапы 1 (правка ДС `.tbl__row[hidden]` + `api.rowsChanged()`, TableCell 2.015 /
Table 1.012) — в `DS-IBP/CHANGELOG.md` за 05.09.2026; этапы 3 (создание сделки
с валидацией) и 4 (реальная фильтрация) — не выполнены, см. открытый вопрос №7.

1. **Источник данных.** 8 статичных демо-строк удалены из разметки. Таблицу
   рендерит скрипт экрана (после `ds.js`, нужны его глобали) из
   `window.MOCK_DEALS` (`../data/mock-deals.js`, 50 записей) через
   `window.DealsStore` (`../data/deals-store.js`). Дерево продуктов
   (`../data/mock-deal-trees.js`) на этот экран не подключается — нужно только
   будущей странице сделки.
2. **Пагинация переведена с автопривязки `data-pagination` на прямую сборку**
   `DSPagination.footer({ info, pager })`: реальные `total`/номера страниц,
   слот «Сделок: N», размер по умолчанию 20 (50 сделок → «1 из 3»). Видимость
   строк — `hidden` на `.tbl__row` (требует правки ДС `.tbl__row[hidden]`,
   TableCell 2.015), строки не пересоздаются при смене страницы.
3. **Сортировка (`data-sort-rows`) работает по полному набору из 50 записей**,
   а не только по видимой странице — все строки стоят в DOM. После события
   `sort` пагинация сбрасывается на первую страницу и видимость пересчитывается
   по новому порядку.
4. **Начальный порядок строк отсортирован по дате подписания по убыванию** —
   приведён в соответствие с `aria-sort="descending"`, уже заявленным в шапке
   колонки «Дата подписания» (закрывает открытый вопрос №8 по-настоящему, а не
   допущением). Сделки без даты (черновики) — в конце независимо от направления.
5. Разделы 6, 9, 12 обновлены; открытые вопросы №6 и №8 закрыты, №7 уточнён
   (что именно ещё не реализовано и в каком этапе плана).
6. Версия экрана `1.015` → `1.016`.

## 20. Правки 05.09.2026 (создание сделки — валидация и запись в общую базу)

Этап 3 плана `mock-deals.plan.md` §4. Фильтр (Этап 4) по-прежнему не выполнен.

1. **Валидация «Номер сделки»** — только цифры и уникальность
   (`DealsStore.exists({ id })`); **«Наименование сделки»** — непустое и
   уникальное, сверка регистро- и пробело-независимая
   (`DealsStore.exists({ name })`). Проверка на клик «Сохранить» и повторно —
   при правке поля (`input`).
2. **Ошибка показывается тултипом на фокусе поля**, а не в хелпере — правило
   InputText/LabelHelper («текст ошибки… не дублируется в хелпере»,
   `specs/InputText.md`). Поле оборачивается в `.inp__box` (якорь тултипа,
   раскладочно прозрачен) — заведён напрямую в разметку модалки, а не через
   автообёртку `DSTooltip`'а (`.tip-anchor` вокруг `input` забрала бы флекс-рост
   поля, обёртка рассчитана на table-cell/меню, не на поле формы). Тултип
   строится и обновляется вручную (`DSTooltip.make` + `DSTooltip.bind(input,
   { tip })`), не через автопривязку по `data-tooltip`. Постоянный нейтральный
   хелпер «Только числовые символы» под «Номер сделки» остаётся видимым и в
   Error-состоянии (константа формата, не текст ошибки).
3. **Успех** — `DealsStore.create({ id, name })`: статусный кортеж Черновик
   (Черновик/Черновик/Черновик/На утверждении), остальные поля пустые (план
   §4 п.4). Новая строка вставляется первой в DOM тем же `renderRow()`, что и
   реестр из 50 записей — без пересоздания остальных строк. `DSTable.
   wire(tbl).rowsChanged()` (правка Table 1.012, Этап 1) включает новую строку
   в `__dsRowOrder`, иначе `data-sort-rows` не увидел бы её при следующей
   сортировке. Пагинация сбрасывается на страницу 1 и пересчитывается —
   «Сделок: N» растёт на единицу.
4. **Открытие модалки всегда сбрасывает форму** (пустые поля, снятые ошибки):
   `window.DSModal.bind(trigger, { onOpen: resetForm })` вызывается из скрипта
   экрана раньше автопривязки `ds-modal.js` (та идёт на `DOMContentLoaded`,
   позже синхронного скрипта экрана) — `bind()` увидит `trigger.
   __dsModalBound` уже выставленным и не навесит второй обработчик.
5. Раздел 9 обновлён, открытый вопрос №7 частично закрыт (создание сделки;
   фильтр остаётся за Этапом 4).
6. Версия экрана `1.016` → `1.017`.

## 21. Правки 05.09.2026 (реальная фильтрация + правка ДС InputDate/Tab)

Этап 4 плана `mock-deals.plan.md` §8 — последний этап плана, все четыре
выполнены.

1. **Списки полей фильтра перестроены из `window.DEALS_ENUMS`** (реальный
   домен генератора), а не из статичного черновика на 2–4 значения: «Статус» —
   9 значений вместо 3, «Директор»/«Менеджер»/«Сотрудник ЦУП» — все 32 ФИО
   вместо 3 демо. Мультивыбор (`DSDropdownList.bind(field, {multiple:true,
   onToggle})`) сам не даёт чипы и крестик очистки «из коробки» (см. план §8) —
   экран строит их (`DSInputKit.makeChip`), навешивает крестик там, где его не
   было в разметке (`.inp__acts`), и держит чипы и чекбоксы списка в
   синхроне в обе стороны (снятие чипа снимает чекбокс и наоборот).
2. **Дни диапазонов дат** (3 поля «Сроки») получили id (`f-date-sign-from/to` и
   т.д.) — в исходной разметке их не было.
3. **Счётчик применённых фильтров — на двух уровнях.** На вкладках модалки
   слева (`.tab__badge`) — живой, считает активные поля текущего раздела по
   черновику формы, обновляется на каждую правку до отправки. В шапке тулбара
   (`.tfilter__applied`) — замораживается на «Применить»/«Очистить»: число
   полей, реально влияющих на отбор сейчас, не число найденных строк (план §2
   п.5). Чипа нет вовсе, пока фильтр не применён, — стартовый статичный
   «Применено: 5» был черновиком показа состояния.
4. **Реальный отбор** — `DealsStore.list()` фильтруется по ~26 контролам формы
   (текст — подстрока без учёта регистра; мультивыбор — вхождение в множество
   или пересечение с массивным полем записи; диапазон дат — `parseRuDate` в
   границах; чекбокс — точное совпадение). Пагинация, «Сделок: N» и окно
   видимых строк пересчитываются по результату; страница сбрасывается на
   первую. Нулевой результат — EmptyState (`#deals-empty`, `.es--m`,
   иллюстрация `empty-folder`) вместо таблицы и колонтитула, с действием
   «Очистить фильтр» — не промежуточное состояние (только после «Применить»).
5. **Найден и исправлен пре-существующий дефект ДС при подключении
   InputDateRange к реальным датам таблицы**: `ds-datepicker.js`
   (`formatDate`/`parseDate`) читал/писал дату месяц-первым (ММ.ДД.ГГГГ), а
   сортировка дат в TableCell (`data-sort-type="date"`) и все реальные даты
   сделок на этом экране — день-первым (ДД.ММ.ГГГГ). Календарь и типизированный
   ввод в фильтре «Сроки» давали бы значение, не совпадающее с тем, что
   показывает и сортирует таблица. Исправлено на уровне ДС (день первым
   везде) — правки DatePicker 1.006, InputDate 1.010, InputDateRange 1.007,
   TableCell 2.016 (плейсхолдер демо-поля), см. `DS-IBP/CHANGELOG.md`.
6. **Заодно найден и исправлен третий случай того же дефекта каскада**, что
   чинили в Modal 1.005 и TableCell 2.015: `.tab__badge[hidden]` не прятал
   счётчик вкладки (та же специфичность 0,1,0, что у авторского
   `display:inline-flex`) — правка Tab 1.010.
7. Разделы 9, 10, 11, 12 обновлены; открытый вопрос №7 закрыт полностью (план
   `mock-deals.plan.md` выполнен целиком — этапы 1–4); добавлен новый вопрос
   №10 (создание сделки при активном фильтре может выпасть из отбора — решение
   отложено до страницы сделки).
8. Версия экрана `1.017` → `1.018`.

## 22. Правки 05.09.2026 (четыре дефекта ДС, найденные на этом экране)

Пользователь перечислил четыре поломки. Все четыре оказались дефектами самой
дизайн-системы, а не разметки экрана: экран был собран верно. Причины найдены
воспроизведением в браузере, исправления — в ДС (см. `DS-IBP/CHANGELOG.md`,
запись за 05.09.2026).

1. **Пустое состояние «Сделки не найдены» висело под таблицей всегда.**
   `.es { display: flex }` (специфичность 0,1,0) перебивало браузерное
   `[hidden] { display: none }` (0,0,0) — атрибут `hidden`, которым скрипт
   экрана управляет пустым состоянием, не работал вовсе. Починено в
   **EmptyState 1.002** (`.es[hidden] { display: none }`). Четвёртый случай
   того же класса дефекта после Modal 1.005, TableCell 2.015, Tab 1.010.

2. **Шапка таблицы уезжала при вертикальном скролле.** `position: sticky`
   стоял на ячейках `.th`, а `.th` — grid-элемент строки `.tbl__row`:
   блок-контейнер, ограничивающий sticky-смещение, — сама строка, по вертикали
   двигаться некуда. По горизонтали правило работало только потому, что строка
   шире скроллпорта (`min-width: max-content`), — поэтому закрепление колонок
   было исправным и дефект вертикали не всплывал. Починено в **Table 1.013**:
   липкость перенесена на строку, `.dtable__body .tbl > .tbl__row--head`.
   На экране строке шапки добавлен класс `tbl__row--head` — внутри
   `.dtable__body` он теперь обязателен.

3. **В модалке фильтра не работало ни одно поле выбора, кроме дат.**
   `DSDropdownList.open()` выносил список в общий слой `DSFloat` — тот лежит в
   корне `body` с `z-index: 40`, а `.modal-scrim` несёт `z-index: 1000`: список
   рисовался под подложкой модалки и не ловил клики. Вторым слоем — `inert`,
   который `DSModal.lockPage()` вешает на всех детей `body`, кроме скримов.
   Даты работали, потому что DatePicker `DSFloat` не использует. Починено в
   **DropdownList 1.009**: поле внутри модалки монтирует список в свой скрим.

4. **Крестик чипа «Применено: N» убирал чип, но фильтр оставался применённым.**
   `ds-chip.js` в `ds.js` стоит раньше `ds-table-filter.js`; оба слушают клик
   делегированием на `document`. Первый снимал чип, второй получал уже
   оторванный от DOM узел — `chip.closest('.tfilter')` давал `null`, и событие
   `tfilter:reset`, которое слушает скрипт экрана, наружу не уходило. Починено
   в **Chip 1.014**: `.tfilter__applied` — зона ответственности TableFilter,
   общий рантайм чипа его не трогает. Код экрана не менялся: обработчик
   `tfilter:reset` был написан правильно с самого начала.

**Проверено в браузере** (headless Edge, локально): пустое состояние прячется и
показывается по атрибуту; шапка стоит при вертикальном скролле, закреплённые
колонки держатся при горизонтальном, шапка рисуется поверх строк; в модалке
настоящими кликами мыши отработали мультивыбор, одиночный выбор и повторное
открытие модалки; крестик чипа сбрасывает поля формы, счётчики вкладок и
возвращает все 50 сделок. Ошибок в консоли нет.

Версия экрана `1.018` → `1.019`. Разметка экрана изменена ровно в одном месте —
класс `tbl__row--head` на строке шапки.

## 23. Правки 05.09.2026 (поля фильтра: крестики, «+N», маска даты, плейсхолдеры)

Пять замечаний по полям. Три из них — снова недостающее поведение ДС, а не
ошибка экрана: правила были записаны в спеках, но кода за ними не стояло.

1. **Крестик очистки живёт по значению.** В ДС не было рантайма поля ввода
   вовсе — `input-kit.js` это построитель демо для страниц документации, а не
   поведение. Видимостью крестика управляла разметка, поэтому на экране было
   и то и другое сразу: у пяти полей крестик стоял всегда (в том числе на
   пустом), у остальных двенадцати его не было даже при выбранном значении.
   Заведён **`DS-IBP/scripts/ds-input.js`** (InputText 1.010) — крестик виден
   ровно при наличии значения, а где его нет в разметке, рантайм добавляет его
   сам. Экран больше не создаёт крестики руками: функция `ensureChipsAndClear`
   свелась к `ensureChips`, а сброс состояния фильтра переехал с собственного
   обработчика клика на событие `ds-input:clear` из ДС.

2. **Крестик есть всегда, когда есть значение.** Тем же рантаймом закрыты поля,
   у которых своего крестика не было: одиночный выбор («Команда сделки»,
   «Прочее»), текстовые поля («Номер сделки», «Наименование», «КНР») и обе
   половины каждого диапазона дат. Очистка одиночного выбора и мультивыбора
   снимает и галочки в списке, и счётчик на вкладке слева.

3. **Переполнение чипов сворачивается в «+N».** Правило стояло в спеке
   InputAutocomplete с первой версии, но `.inp__chips { overflow: hidden }`
   просто обрезал лишнее, а демо на странице ДС показывало **нарисованный** чип
   `'+3'` — документация иллюстрировала несуществующее поведение. Реализовано в
   `ds-input.js` (InputAutocomplete 1.013), демо на странице ДС сделано живым.
   Проверено на «Статусе» со всеми выбранными значениями: видно 2 чипа и «+8»,
   за границу поля ничего не выходит.

4. **Маска даты.** Дело было не в подписи: маска существовала **только** в
   `input-date.page.js` — демо-скрипте страницы документации — и на экраны не
   попадала. Поле обещало плейсхолдером `ДД.ММ.ГГГГ`, а принимало любой текст
   без точек, и набранная руками дата не разбиралась ни календарём, ни
   фильтром. Маска перенесена в рантайм `ds-datepicker.js` (DatePicker 1.007),
   слушатель стоит в фазе перехвата — иначе разбор даты в фильтре читал бы
   значение раньше простановки точек. Плейсхолдеры полей «Сроки» исправлены с
   `ММ.ДД.ГГГГ` на `ДД.ММ.ГГГГ` (правка редполитики от 05.09.2026 до экрана не
   дошла). Проверено: набор `21122024` даёт `21.12.2024`.

5. **Плейсхолдеры убраны везде, кроме дат.** Снято 24 плейсхолдера — «Выберите…»
   у всех полей выбора, «Да / Нет», подсказки текстовых полей и обоих полей
   модалки «Новая сделка». Остались шесть полей дат, где плейсхолдер несёт
   формат ввода, а не приглашение.

**Проверено в браузере** (headless Edge, локально): на пустой форме крестиков
нет ни у одного поля; набор текста, выбор из списка и одиночный выбор дают
крестик; клик по нему чистит поле, счётчик вкладки и состояние фильтра;
«+8» считается по фактической ширине; маска проставляет точки; плейсхолдеры
остались только у дат. Ошибок в консоли нет. Регрессия по правкам раздела 22
перепроверена — липкая шапка, списки в модалке, EmptyState и сброс по крестику
чипа работают по-прежнему.

Версия экрана `1.019` → `1.020`.

## 24. Правки 05.09.2026 (фокус в выпадающем списке)

Выпадающий список открывался с уже подсвеченной строкой: `open()` в
`ds-dropdownlist.js` вызывал `setActive` на выбранной опции, а когда выбранной
нет — на первой. На экране это читалось как два фокуса сразу: рамка на поле и
наведённая строка в списке. Фокус должен быть один и он на поле-триггере —
список это popup, реальный фокус из поля не уходит.

Починено в **DropdownList 1.010**: на открытии активной опции нет, `.is-focus` и
`aria-activedescendant` появляются только после первого шага клавишами.
Выбранная опция при открытии по-прежнему подкручивается в зону видимости — в
длинном справочнике список обязан открыться там, где стоит текущее значение, —
но подсвечивает её собственное правило `[aria-selected="true"]`, а не `.is-focus`.
Заодно исправлена клавиатура с чистого листа: `↑` давала предпоследнюю опцию
вместо последней (индекс `-1` уезжал в арифметике по модулю).

Кода экрана правка не касается — поведение целиком в рантайме ДС.

**Проверено в браузере:** после открытия подсветки нет, `aria-activedescendant`
пуст, реальный фокус на `.inp__control`; `↓` даёт первую опцию, второе `↓` —
вторую; после закрытия и повторного открытия снова чисто; `↑` с чистого листа
даёт последнюю опцию; у поля с уже выбранным значением подсветки нет, но
выбранная опция в зоне видимости. Ошибок в консоли нет.

Версия экрана `1.020` → `1.021`.

## 25. Правки 05.09.2026 (сортировки четырёх колонок + свёртка чипов «+N»)

Задача человека: добавить сортировку колонкам «Номер сделки», «Наименование
сделки», «Ограниченный доступ», «Private Equity»; в колонке валют сворачивать
чипы в «+N», когда они не влезают, и закрепить это правило в рантайме ДС.

### Что сделано на экране

1. **Четыре колонки получили сортировку.** В `th__tools` добавлена кнопка
   `.th__sort` (IconButton neutral S, глиф `arrow-up-down`), на `.th` —
   `aria-sort="none"` и `data-sort-type`. Ключи: `number`, `name`,
   `restricted`, `pe`. Порядок инструментов в шапке прежний: закрепление,
   затем сортировка. Своего кода сортировки на экране нет — цикл
   none → asc → desc → none, глиф, `aria-sort` и перестановку строк по всем
   50 записям делает рантайм (`data-sort-rows` на `.tbl`).

2. **«Private Equity» сортируется по `data-sort-value`.** Ячейка без PE
   показывает прочерк, а прочерк рантайм считает пустым значением и всегда
   уводит вниз — при обоих направлениях сверху оказывались бы сделки PE, то
   есть направление ничего не меняло бы. Поэтому ячейке проставлен явный
   признак: `data-sort-value="1"` у чипа PE и `"0"` у прочерка,
   `data-sort-type="number"` на шапке. По возрастанию сверху сделки без PE,
   по убыванию — сделки PE.

3. **Колонка «Валюты» расширена 130px → 150px.** Два кода по три символа в
   Body S — это ~104px (2 × ~48px чипа + 8px зазора ряда), плюс 32px паддингов
   ячейки: в 130px они не помещались, и после включения свёртки почти каждая
   строка показывала бы «[RUB] [+1]» вместо двух видимых значений. 44 строки
   из 50 в реестре — ровно две валюты, это основной случай, а не край. При
   150px две валюты видны целиком, сворачиваются только ячейки с тремя-четырьмя.
   Возврат к 130px — правка одного трека в `grid-template-columns` шапки и в
   константе `GRID` скрипта экрана.

### Что сделано в ДС

Свёртка чипов — правило TableCell, а не экрана: перенесено в рантайм
`DS-IBP/scripts/ds-table.js` (TableCell 2.017). Работает от разметки — два и
более чипа в одном контейнере ячейки; экран ничего не размечает и не считает.
Не поместившиеся чипы получают `hidden`, последним встаёт чип-счётчик
`[data-tc-count]` с тултипом со списком скрытых значений. Подпись «+N»
исключена из ключа сортировки, иначе колонка сортировалась бы по числу
свёрнутых чипов. Подробности — `DS-IBP/CHANGELOG.md` за 05.09.2026.

### Проверено без браузера

Стенд на `linkedom` с реальной разметкой экрана и реальными рантаймами
(`ds-table.js` + `ds-tooltip.js`; ширины — моделью, ~7px на символ Body S):

- все четыре колонки: asc → desc → возврат к исходному порядку, `aria-sort`,
  смена глифа, подсветка `.th--sorted`, активна ровно одна колонка;
- регрессия: сортировка «Дата подписания» не сломалась;
- «Валюты»: ячейки с 4 кодами свёрнуты, с 1–2 — нетронуты, тултип счётчика
  перечисляет ровно скрытые значения, сужение колонки до 96px оставляет один
  «+4», расширение возвращает все чипы, повторный пересчёт не плодит счётчики;
- одиночные чипы (PE, статусы) не сворачиваются.

**Осталось на человека (браузер):** фактическая ширина чипов в SB Sans Text —
модель стенда считает ~7px на символ; если в 150px две валюты всё же не
помещаются, колонку надо расширить ещё (170px). Также визуальная проверка
тултипа счётчика и поведения при перетаскивании ручки `.th__resize`.

Версия экрана `1.021` → `1.022`.

## 26. Правки 05.09.2026 (тултипы у чипов — правка ДС, кода экрана не касается)

Найдено человеком на этом экране: в модалке фильтра чипы в полях автокомплита
обрезаются многоточием без тултипа, у счётчика «+N» тултипа тоже нет — видно
число, но не какие значения свёрнуты.

Дефект не экрана, а ДС: оба поведения записаны в спеках, а кода за ними не было.

1. **Chip 1.017** — тултип на усечённой подписи стал правилом самого чипа и
   переехал в `DS-IBP/scripts/ds-chip.js`: ленивая привязка делегированием, по
   первому наведению или фокусу. Работает всюду, где стоит чип, — в поле, в баре
   фильтра, в тайле, в ячейке таблицы. До этого правило было реализовано только
   в `ds-table.js` и только внутри ячейки.
2. **InputAutocomplete 1.015** — счётчик «+N» в поле получил обязательный
   тултип со списком скрытых значений через запятую (то же в `aria-label`).
3. **TableCell 2.018** — из `TRUNC_SEL` таблицы убраны подписи чипов: дубль
   правила снят, владелец один.

Разметка экрана не менялась. Чипы фильтра по-прежнему строит
`DSInputKit.makeChip` в скрипте экрана, атрибутов тултипа он не пишет —
их ставит рантайм.

**Проверено на стенде** (linkedom, реальная разметка экрана, рантаймы
`ds-float.js`, `ds-tooltip.js`, `ds-chip.js`, `ds-input.js`, `ds-table.js`,
чипы кладутся тем же `DSInputKit.makeChip`, что и на экране): наведение
навешивает тултип с полным текстом и только при реальном усечении; короткая
подпись тултипа не получает; повторное наведение не плодит привязку; фокус на
чипе показывает и по уходу скрывает; у «+N» тултип перечисляет ровно скрытые
значения; структура `.inp__chips` не изменилась (обёртки `.tip-anchor` вокруг
счётчика нет, счётчик — последний прямой ребёнок); повторный `sync()` не
плодит счётчики; расширение поля возвращает значения; подписи счётчиков
исключены из правила усечения. Регрессия таблицы (сортировка и свёртка чипов)
прогнана заново — цела.

**Осталось человеку (браузер):** реальное наведение и задержка 400 мс, показ по
Tab-фокусу, отсутствие второго (нативного) тултипа.

Версия экрана `1.022` → `1.023`.

## 27. Правки 05.09.2026 (тултипы в модалке не рисовались — правка ДС)

После правки 26 тултипы в модалке фильтра так и не появились. Причина оказалась
не в чипах, а в общем слое плавающих элементов.

`ds-tooltip.js` на показе уносит тултип в `.ds-float-layer` — прямой ребёнок
`body`. Слой намеренно не создаёт stacking context, поэтому z-index его детей
разрешается в корневом контексте: у тултипа 30, а у `.modal-scrim` — 1000.
Тултип рисовался ПОД подложкой модалки, а над самой модалкой — ещё и под её
непрозрачным фоном. Вторым слоем `DSModal.lockPage()` вешает `inert` на всех
детей `body`, кроме скримов.

Ровно этот дефект в тот же день чинили на DropdownList (списки в этой же
модалке фильтра), но починили точечно, внутри компонента, — и он остался у
тултипа, меню, поповера и тултипа графика.

**Исправлено в ДС правилом слоя (Elevation 1.002):** `DSFloat.mount` получил
опцию `anchor`; плавающий элемент, открытый из модалки, монтируется в её скрим.
Якорь передают пять рантаймов: Tooltip 2.007, ContextMenu 1.009, Popover 1.007,
Chart 1.002, DropdownList 1.012 (его локальная проверка снята).

Разметка экрана снова не менялась.

**Проверено на стенде:** тултип чипа и тултип «+N» из модалки монтируются
в `#filter-scrim`, а не в общий слой; тултип ячейки таблицы (вне модалки) —
по-прежнему в `.ds-float-layer`; после скрытия тултип возвращается в свой чип
и в скриме не остаётся осиротевших узлов; во вложенном диалоге
(`.modal-scrim--nested`) выбирается ближайший скрим. Прогнаны заново наборы
по тултипам, сортировке и свёртке чипов.

**Осталось человеку (браузер):** открыть модалку фильтра и навести на
обрезанный чип и на «+N» — тултип должен появиться поверх модалки. Страница
открывается по `file://`, скрипты браузер кэширует: если ничего не изменилось,
обновить с Ctrl+F5.

Версия экрана `1.023` → `1.024`.

