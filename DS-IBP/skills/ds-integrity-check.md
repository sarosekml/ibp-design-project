# Скилл: Ревизор целостности страниц ДС (линтер)

**Роль:** ревизор. Ищешь тихие ошибки — те, что не видны на скриншоте: забытый парный CSS, ссылку на несуществующий токен, нарушенный контракт разделов, рассинхрон `index.html` / `ds-nav.js` / спек.
**Когда:** в конце захода — на каждом новом компоненте, каждой правке компонента, каждом новом экране. Сдавать работу можно только с чистым отчётом.
**Вход:** пути страниц, которые ты создал или правил. **Выход:** отчёт линтера + починенные BLOCKER'ы.

Единственный источник истины о целостности — отчёт линтера. **Страницу для перепроверки не читать** (ни целиком, ни «по-быстрому глазами»): всё, что можно посчитать кодом, считает код.

---

## Шаг 1 · Статический аудит (одна команда в терминале)

```
node scripts/ds-lint-cli.mjs pages/atoms/Badge.html     # страницы, которых касалась правка
node scripts/ds-lint-cli.mjs pages/atoms/*.html         # несколько страниц / маска
node scripts/ds-lint-cli.mjs                            # без аргументов — только глобальные правила
node scripts/ds-lint-cli.mjs --parity                   # гейт парности (группа P, ниже)
```

Запускать из корня ДС, пути — относительные от него. Код выхода: 1 при наличии BLOCKER, иначе 0.
Переданные страницы автоматически считаются изменёнными: включается C3 (сверка даты «Обновлено» с сегодняшней), а B3 («вне шкалы 4») печатается только для них — на старых страницах это чистый шум.

Обычно достаточно `node scripts/ds-check.mjs <страница>` — единый гейт, который сам вызывает линтер и, для страниц на docs-split, структурную проверку и проверку живости (`MAINTAINING.md`, «Проверка: чем гонять»).

## Шаг 2 · Живые проверки (необязательные: нужна страница, открытая в браузере)

Выполняются в консоли браузера на открытой странице. В рабочем контуре этот шаг пропускается (см. абзац про E7/E8 ниже и `.opencode/rules/ds-rules.md` §9) — обязательная часть флоу целиком статическая.

**Поведение рантайма браузера не требует.** Пропуск шага 2 касается геометрии (что где отрисовалось), а не логики. Правка `scripts/ds-*.js` проверяется прогоном в чистом JS-DOM (`linkedom`, ставится в скратчпад вне репозитория — это библиотека, а не браузер, §9 не нарушается): `parseHTML` реальной страницы → загрузить рантаймы через `new Function('window','document','CustomEvent','Event', src)` → `dispatchEvent` → сверить DOM. **Грузить все рантаймы страницы, без заглушек:** они дописывают разметку внутрь чужих слотов, и дефект живёт именно в связке — сортировка `ds-table.js` считала значением ячейки текст вместе с копией, которую туда кладёт `ds-tooltip.js` (уроки Л33, Л34 в `.opencode/skills/screen-review/references/lessons.md`). Прогон с заглушенным соседом проходит и ничего не значит.

```js
(() => {
  const r = getComputedStyle(document.documentElement);
  const used = [...new Set([...document.documentElement.outerHTML.matchAll(/var\(\s*(--[\w-]+)\s*[),]/g)].map(m => m[1]))];
  const nav = document.querySelector('.ds-nav'), toc = document.querySelector('.ds-toc'), main = document.querySelector('main');
  return {
    emptyTokens: used.filter(t => !r.getPropertyValue(t).trim()),                                    // E1
    nav: nav ? getComputedStyle(nav).position : 'НЕТ', toc: toc ? getComputedStyle(toc).position : 'НЕТ', // E2
    blueLinks: [...document.querySelectorAll('a')].filter(a => /rgb\(0,\s*0,\s*238\)/.test(getComputedStyle(a).color)).length, // E3
    overflow: main ? main.scrollWidth - main.clientWidth : null,                                     // E4
    emptyGen: [...document.querySelectorAll('main [id]')].filter(n => !n.children.length && !n.textContent.trim() && !n.matches('input,textarea,canvas,img,svg,hr,br')).map(n => n.id).slice(0, 8), // E6
    // E9 — держатся ли закреплённые колонки при горизонтальном скролле. Замер идёт в том же
    // синхронном такте, что и сдвиг scrollLeft: компоновщик скролл уже применил, а JS ещё не
    // выполнялся — нативный sticky даёт 0, любое JS-удержание отстаёт на весь сдвиг (это и
    // есть кадр, который на трекпаде виден как дрожание). scrollLeft возвращается на место.
    pinHold: (() => {
      const room = (b) => b.scrollWidth - b.clientWidth;
      // .tbl--scroll и .dtable__body бывают вложены друг в друга: берём ту, что реально
      // прокручивается (при равном запасе — внешнюю), иначе одна таблица даст два отчёта
      const boxes = [...document.querySelectorAll('.tbl--scroll, .dtable__body')].filter((b, _, all) =>
        !all.some(o => o !== b && (o.contains(b) || b.contains(o))
          && (room(o) > room(b) || (room(o) === room(b) && o.contains(b)))));
      return boxes.flatMap(box => {
        const pins = [...box.querySelectorAll('.tbl__row')].slice(0, 2)
          .flatMap(r => [...r.children].filter(c => c.matches('.tc--pinned, .th--pinned, [data-pin-side]')));
        if (!pins.length || room(box) < 8) return [];   // держать нечего: закреплённых нет или таблица влезла
        const tag = '.' + (box.className.split(/\s+/)[0] || box.tagName), out = [];
        const loose = pins.filter(p => getComputedStyle(p).position !== 'sticky');
        if (loose.length) out.push(tag + ' · position: ' + getComputedStyle(loose[0]).position
          + ' у ' + loose.length + '/' + pins.length + ' закреплённых ячеек — держит не sticky');
        const D = 400, was = box.scrollLeft, step = was + D <= room(box) ? D : -Math.min(D, was);
        const base = pins.map(p => p.getBoundingClientRect().left);
        box.scrollLeft = was + step;
        const moved = box.scrollLeft - was;
        const drift = pins.map((p, i) => Math.round(p.getBoundingClientRect().left - base[i])).filter(d => Math.abs(d) > 1);
        // перекрытие: в центре закреплённой ячейки нарисована ЧУЖАЯ ячейка ТОЙ ЖЕ строки;
        // внешний хром (липкий футер пагинации, шапка) не считается — он перекрывает законно
        const covered = pins.filter(p => {
          const r = p.getBoundingClientRect();
          if (r.top < 0 || r.bottom > innerHeight || r.width < 4) return false;
          const hit = document.elementFromPoint(r.left + r.width / 2, r.top + r.height / 2);
          return hit && hit !== p && !p.contains(hit) && p.parentElement.contains(hit);
        });
        box.scrollLeft = was;
        if (drift.length) out.push(tag + ' · ' + drift.length + '/' + pins.length
          + ' закреплённых уехали на ' + drift[0] + 'px при сдвиге ' + moved + 'px');
        if (covered.length) out.push(tag + ' · закреплённую перекрывает «' + covered[0].className + '» — z-index');
        return out;
      });
    })(),
    // E7/E8 — усечение проверяется по факту, а не по классам: линтер видит правильную
    // разметку, а на экране контент всё равно вылезает. clipper — ближайший предок,
    // который режет по горизонтали, и умеет ли он прокручиваться.
    ...(() => {
      const clipper = (el) => {
        for (let p = el.parentElement; p && p !== document.body; p = p.parentElement) {
          const ox = getComputedStyle(p).overflowX;
          if (ox !== 'visible') return { el: p, scrollable: ox === 'auto' || ox === 'scroll' };
        }
        return null;
      };
      return {
        cellBleed: [...document.querySelectorAll('.tc, .th')].map(c => {              // E7
          const box = c.getBoundingClientRect();
          const over = [...c.querySelectorAll('*')].reduce((m, n) => {
            const ns = getComputedStyle(n), r = n.getBoundingClientRect();
            if (!r.width || ns.position === 'absolute' || ns.position === 'fixed') return m;
            return Math.max(m, r.right - box.right, box.left - r.left);
          }, 0);
          return over > 1 ? Math.round(over) + 'px · ' + c.className + ' · «' + c.textContent.trim().slice(0, 30) + '»' : null;
        }).filter(Boolean).slice(0, 6),
        rowBleed: [...document.querySelectorAll('.tbl__row')].map(row => {            // E8
          const cl = clipper(row);
          if (!cl || cl.scrollable) return null;
          const over = row.getBoundingClientRect().width - cl.el.clientWidth;
          return over > 1 ? Math.round(over) + 'px за ' + (cl.el.className || cl.el.tagName) : null;
        }).filter(Boolean).slice(0, 4)
      };
    })()
  };
})()
```

Норма: `emptyTokens: []`, `nav`/`toc` — `fixed` или `sticky` (не `static`, не `НЕТ`), `blueLinks: 0`, `overflow ≤ 4`, `emptyGen: []`, `cellBleed: []`, `rowBleed: []`, `pinHold: []`. E5 (ошибки консоли) — смотреть вкладку Console браузера.

Три первых пункта — прямое следствие инцидента с `ds-toc.css`: скрипт отработал, стиля нет, ссылки браузерно-синие, панель в потоке. Любой из них поймал бы его.

**E7/E8/E9 — локальный инструмент, не часть ревью-флоу.** Все три проверки требуют открытой страницы, а запуск браузера по скрипту в контуре компании не согласован (`.opencode/rules/ds-rules.md` §9) — как и `docs-split.mjs verify`, они применяются только там, где браузер есть. Обязательный эквивалент без рендера — блокеры **Б24/Б25** сенсора `layout-check.mjs`: помещается ли значение в свою колонку (эвристика ~7px на символ body-s) и не шире ли строка контентной области при отсутствии горизонтального скролла. Ревью-флоу опирается на них, E7/E8 — точнее, но необязательны. У E9 обязательный эквивалент без рендера — статическое правило **B10**: оно ловит ту же поломку по коду рантайма ДС, но не видит экранные `<script>` и инлайн-обработчики.

**E7/E8 — почему замер, а не разбор причин (04.09.2026).** За один день усечение в таблицах сломалось тремя разными способами: обёртка рантайма тултипа (`.tip-anchor` без `min-width: 0`), сам Chip (то же), и `min-width: max-content` на всех `.tbl__row`, из-за которого доля-трек (`fr`) разворачивался по содержимому. Разметка во всех трёх случаях была правильной — статический линтер её пропускал, находились дефекты только скриншотом и замером `getBoundingClientRect`. E7 и E8 меряют результат и потому не зависят от причины: **E7** — содержимое ячейки вылезло за её границы (соседняя ячейка непрозрачна и обрежет его без многоточия); **E8** — строка шире контейнера, который её режет и НЕ прокручивается (хвост колонок физически недостижим). Абсолютно позиционированные потомки в E7 не считаются: ручка `.th__resize` и инструменты шапки выходят за границу ячейки намеренно. Обе проверки прогнаны откатом каждого из трёх фиксов — ловят все три, на починенной ДС молчат.

**E9 — пара к статическому правилу B10 (04.09.2026).** B10 ловит причину в рантайме ДС (геометрия, посчитанная на событии `scroll`), E9 меряет результат на живой странице и потому не зависит ни от причины, ни от того, чей это код: экранные `<script>` и инлайн-обработчики линтер не обходит вовсе. Приём: сдвинуть `scrollLeft` и снять `getBoundingClientRect()` **в том же синхронном такте**, не отдавая кадр. Это ровно то состояние, в котором браузер рисует кадр при инерционном скролле трекпада: компоновщик новый offset уже применил, JS-обработчик ещё не выполнялся. Нативный `position: sticky` в этом такте даёт 0, любое JS-удержание — весь сдвиг целиком. Три ветки: держит не `sticky` (разбор причины, дёшево), уехали на N px при сдвиге M px (замер результата), закреплённую перекрывает соседняя ячейка той же строки (`elementFromPoint` в центре ячейки — сбитый `z-index`). Внешний хром (липкий футер пагинации, шапка) в перекрытие не засчитывается: он закрывает прокрученную строку законно, а первая редакция проверки на этом давала ложняк на коротком вьюпорте. Таблица, которая целиком влезла в порт, молчит — держать нечего; случай «строку сжали вместо прокрутки» остаётся за E7/E8, дублировать его здесь незачем. Проверено тремя откатами на «Текущем портфеле ДИД»: возврат JS-удержания через `transform` на `scroll` — ловится обеими ветками (`position: static у 6/6`, `6/6 уехали на -400px при сдвиге 400px`), снятый приоритет отрисовки — третьей, починенная ДС молчит на четырёх размерах окна (1280×700, 1600×400, 1600×500, 1900×900).

## Правила проверок

| Группа | О чём | Правила |
|---|---|---|
| A | зависимости и подключения | A1 скрипт без парного CSS (`JS_CSS_PAIRS`; третий элемент пары — уровень: WARN у скриптов, которые CSS догружают сами, BLOCKER у `ds-tooltip.js` → `tooltip.css`, который не догружает, а разметку строит) · A2 классы компонента без его `<link>` · A3 `styles/*.css` без `@import` в `ds.css` · A4 `<i data-icon>` без `icons-data.js` + `ds-icons.js` · A5 битая относительная ссылка · A6 нет `<main class="page">` (ds-nav/ds-toc не смонтируются) |
| B | токены и запрещённые паттерны | B1 несуществующий токен · B2 хардкод цвета в `<style>` · B3 отступ/радиус вне шкалы 4 · B4 инлайн-SVG вместо `<i data-icon>` · B5 нативная таблица вместо `.tbl` · B6 React-файлы в ванильной ДС · B7 незаскоупленный `[aria-selected/current/checked/pressed/expanded="true"]`/`:is(…)` в `styles/*.css` — топ-ветка селектора без класса-компонента перед ней матчит любой чужой элемент с тем же атрибутом · B8 компонент с усечением подписи внутри (`.x__label { text-overflow: ellipsis }`), объявленный `inline-flex` без `min-width: 0` — в чужой flex-строке он не сожмётся и подпись обрежется без многоточия; компонент обязан высказаться: `min-width: 0` (сжимается), `flex: none`/`flex-shrink: 0` (не сжимается намеренно) или строка в `SHRINK_OK` с причиной · B9 обёртка, которую рантайм вставляет ВОКРУГ чужого элемента (`X.parentNode.insertBefore(W, X)`), не раскладочно прозрачна — нет `max-width: 100%` или, для flex, `min-width: 0` · B10 рантайм пишет geometry-свойство (`transform`, `left`/`right`/`top`/`bottom`) из обработчика `scroll` — компоновщик применяет скролл раньше JS, коррекция всегда на кадр позади; удержание при скролле делается `position: sticky`, а рантайм считает только инсеты по изменению состава. Легальные исключения (плавающие слои `position: fixed`) — в `SCROLL_GEOMETRY_OK` ключом «файл:функция». Заведено 04.09.2026, первый цикл живёт как WARN (см. «Как расширять», п.4) · B11 корень компонента задаёт `display`, но не объявляет парного `.x[hidden] { display: none }` — браузерное `[hidden]` имеет специфичность (0,0,0) и приходит из UA-стиля, правило класса (0,1,0) его перебивает, и атрибут `hidden` на компоненте молча не работает. Проверяются только корни (без `__` и `--`) в CSS компонентов; хром документации (`CSS_NOT_IN_BUNDLE`) не в счёт, исключения с причиной — в `HIDDEN_PAIR_OK`. Заведено 05.09.2026 как INFO (80 находок в 44 файлах — WARN-ами они утопили бы отчёт с базовым уровнем в 2 предупреждения); в тот же день список разобран целиком и правило поднято до **WARN** — оно охраняет достигнутый ноль · B15 паддинг на элементе, который несёт рейл (inset-линию), по оси этой линии: inset-тень рисуется по PADDING-BOX, поэтому паддинг не сдвигает линию вместе с содержимым, а удлиняет её — она выходит до первого элемента ряда и тянется после последнего. Ось берётся из смещений тени: `inset 0 ±N` — линия горизонтальная, запрещён горизонтальный паддинг; `inset ±N 0` — вертикальная, запрещён вертикальный. Кольцо (`inset 0 0 0 N`) не рейл; ветка селектора с комбинатором (`.btn-group--accent > .btn + .btn`) — разделитель НА элементе группы, он обязан идти во всю его сторону, и не регистрируется. Подавления — в `RAIL_PAD_OK` ключом «файл ось блок», подавляется одна ось, вторая продолжает стеречь. Заведено 11.09.2026, первый цикл — WARN |
| C | контракт страницы | C1 первая строка `@dsCard` · C2 masthead с «Версия»/«Обновлено» · C3 дата = дате правки · C4 обязательные h2 и их порядок (+ «Бэклог» — BLOCKER; устаревшее имя «Что предлагаем добавить» — WARN C5) · C5 неканонические h2 · C6 «Конструктор» первым |
| D | реестры | D1 карточка в `index.html` · D2 пункт в `ds-nav.js` · D3 счётчик группы · D4 алфавитный порядок · D5 спека + строка в `specs/_index.md` · D6 блок в `_cheatsheet.md` · D7 версия/дата спеки = странице · D8 блок чит-шита без «Инварианты» или без отсылки к полной спеке |
| E | живые (шаг 2) | E1 пустые токены · E2 nav/toc смонтированы и позиционированы · E3 неоформленные ссылки · E4 горизонтальный оверфлоу · E5 консоль · E6 пустые контейнеры-генераторы · E7 содержимое ячейки вылезло за её границы (усечение не сработало — соседняя ячейка обрежет текст без многоточия) · E8 строка таблицы шире режущего контейнера, который не прокручивается (хвост колонок недостижим) · E9 закреплённые колонки не держатся при горизонтальном скролле (замер в такте сдвига `scrollLeft`), держит не `sticky`, либо закреплённую перекрывает соседняя ячейка той же строки |
| F | разобранные инциденты (RulesAudit W5) | F1 `.chip__info` без `.pop`/DSPopover — композиция собрана наполовину · F2 `<i data-icon>` в `.sw__knob` (слот не размерен под иконку — не путать с `.cb__mark`/`.rb__mark`, где `[data-icon]` — штатная разметка) · F3 `.tabs--horiz` без `data-tabs` — переполнение не подключится · F4 одноколоночный grid-track без `minmax(0,1fr)` · F5 реестр `ANATOMY_CONTRACTS` — компонент использован не целиком: отсутствует узел, обязанный существовать в DOM независимо от текущего режима/состояния (заведён для NavPanel — `.nav__pin`/`.nav__user-text`/`.nav-layout`; любой следующий такой компонент добавляется в реестр ОДНОЙ строкой, не новым кодом) · F6 (только `pages/screens/*`) таблица на экране с `.th__label`, но без единой `.th__resize` — изменение ширины колонки базовое и не отключаемое, работает из коробки (`tbl-resize.js` в `ds.js`), но ручку в каждой `.th` надо разметить; redline-таблицы документации на тех же `.th` не трогаются |
| L | правила раскладки (решения 21.08.2026) | L1 сумма колонок в ряду `.grid12` больше 12 (BLOCKER) · L2 блок в `.grid12` без `.col-N` или с фиксированной шириной, не помеченный `data-off-grid="причина"` (BLOCKER) · L3 самодельная 12-колоночная сетка на экране вместо `.grid12` (WARN) · L4 экран переопределяет `--layout-pad-*`, `--grid-gutter/-margin` или `padding` у `.screen__content` (BLOCKER) · L5 порог раскладки на `@media` вместо `@container screen` (WARN) · L6 экран задаёт `margin` блоку ДС в своём `<style>` (WARN — первый цикл) · L1/L2 — на всех страницах, L3–L6 — только экраны |

Группа G (гейт фиделити макету, правило G1) снята 13.09.2026 — на рабочем контуре невыполнима: модель не читает изображения, браузер по скрипту запрещён.

Канонический список h2 линтер читает из `MAINTAINING.md` — в самом линтере он не дублируется. Экраны (`pages/screens/*`) проверяются группами A, B, E, F6, L3–L6; `index.html` и `templates/*` — вне проверки.

## Что делать с отчётом

- **BLOCKER** — починить до сдачи работы. Механическое (счётчик, забытый `@import`, битая ссылка, пропущенная строка в реестре) — молча. Смысловое (нет обязательного раздела, нативная таблица вместо `.tbl`) — починить и назвать одной строкой в ответе.
- **WARN** — починить, если правка очевидна и локальна; иначе одна строка пользователю.
- **INFO** — не чинить, положить пунктом в раздел «Бэклог» страницы (имя раздела ровно такое, синонимы ловит C5).
- Чистый прогон — одна строка `DS-LINT: чисто`. Ничего не пересказывать, повторно не гонять «на всякий случай».
- Правку делать точечно (`grep` → замена найденного фрагмента), по номеру правила и его тексту — без чтения страницы целиком.

## Как расширять

Разобрал инцидент — тем же заходом дописывай класс ошибки в линтер, иначе он повторится.

1. Новая проверка = одна ветка в `pageChecks` (или в `globalChecks` для проектных правил) в `scripts/ds-lint.js` + строка в таблицу выше.
2. **Исключение — «анатомия компонента урезана вместо взятой целиком» (класс F5).** Для этого класса код не пишется заново на каждый инцидент: следующий компонент с обязательными «всегда есть в DOM» узлами (кнопка/блок, которые режим только показывает/прячет CSS-ом, но не порождает) добавляется ОДНОЙ строкой в реестр `ANATOMY_CONTRACTS` в начале `scripts/ds-lint.js` — `{ name, when, require: [[regex, имя узла], …] }`. Общий проверяющий код один для всех записей реестра. Обязательные узлы ищутся в двух входах: в статической разметке и в атрибутах `class="…"` JS-шаблонов inline-`<script>` (экран, строящий компонент шаблоном, анатомию несёт в строке шаблона); условие `when` — только по статике. Фикстуры: `fixtures/F5.bad.html` (статика) и `fixtures/F5@js.bad.html` (шаблон), 13.09.2026.
3. Формулировка — машинный признак, а не «посмотреть, нормально ли выглядит».
4. Первый цикл новая проверка живёт как **WARN**; повышается до BLOCKER после подтверждения на реальных страницах.
5. Потолок — ≤ 25 активных правил: новая вытесняет неактуальную.
6. Подавление ложных срабатываний — в белые списки в начале файла (`SVG_OK`, `HEX_OK`, `HEX_ALLOW`, `CSS_NOT_IN_BUNDLE`, `CLASS_IGNORE`, `SKIP_ALL`, `CONTRACT_DIRS`, `REGISTRY_DIRS`), **каждое с комментарием почему**.

`scripts/ds-lint.js` — не браузерный скрипт и не исполняемый сам по себе: на страницах не подключается, ждёт хелперы `readFile`/`ls`. Запускать только через обёртку `scripts/ds-lint-cli.mjs` (она даёт хелперы через `node:fs`) или через гейт `scripts/ds-check.mjs`.

## Группа P — гейт парности «документация = код»

Отдельный прогон, страницы не нужны:

```
node scripts/ds-lint-cli.mjs --parity
```

| Код | Что ловит | Уровень |
|---|---|---|
| P1 | Класс из code-fence сниппета в `specs/_cheatsheet.md` или `specs/<Имя>.md`, которого нет ни в одном `styles/*.css` | BLOCKER |
| P2 | Класс в `class="…"` экрана `pages/screens/*.html`, не объявленный ни в ДС, ни в локальном `<style>` экрана | WARN |
| P4 | Класс, который рантайм вешает через `className = '…'` (`scripts/*.js`, inline-`<script>` страниц, `index.html`), отсутствует в CSS. Исключены JS-хуки и локальные классы витрин из инлайн-`<style>` | BLOCKER |
| P3 | Класс из код-панели (`<code>` внутри `scripts/*.page.js`, inline-`<script>` страниц и `index.html`) отсутствует в CSS — страница предлагает скопировать несуществующий класс | BLOCKER |

Игнорируются: утилитарные классы документации (`CLASS_IGNORE`) иклассы состояний `is-` / `js-` / `has-`, которые вешает JS. P4 дополнительно не считает находкой: рантаймы, чьи стили лежат в shadow DOM самого скрипта (`SHADOW_RUNTIMES`, сейчас `image-slot.js`), и корневой BEM-блок, у которого в CSS описаны элементы (`.tfm` при наличии `.tfm__sec`) — имя блока задаёт пространство имён, собственного правила ему не нужно.

Три типовых корня находки P1: (1) композиционный CSS лежит в инлайн-`<style>` страницы-витрины, а не в `styles/` — лечится экстракцией; (2) сниппет обещает модификатор там, где вариант является базой (`chip--readonly`, `inp--m`) — класс убирается из сниппета, в блок чит-шита добавляется строка «база, модификатора нет»; (3) в сниппете чужое имя (`.avatar` вместо `.av`) — сверить с CSS покомпонентно.

D7 (версия и дата страницы = поля спеки) проверяется и вне реестров — `pages/rnd/*` в том числе.

- **Пересчёт K1–K3/K5 скриптом (W5, 13.08.2026)** — 26 компонентов пересчитаны программно из реального CSS (не переоценены руками): K2 по наличию `:is(svg,[data-icon])`, K1 по `:checked`/`:disabled`/`[aria-*="true"]`, K3 по ≥2 признакам `text-overflow:ellipsis`/`min-width:0`/`line-clamp`/`overflow-wrap`, K5 по ссылке на реально существующий `scripts/ds-*.js`. Скрипт только повышает `part`/`no` до `ok` при прямом совпадении с определением критерия, никогда не понижает и не трогает `na` — ложных «ok» из совпадения паттерна без семантики (напр. неверно классифицированный K1) следует перепроверять глазами при следующем разборе компонента.
- **D8: чит-шит без инвариантов/отсылки к спеке** (13.08.2026, RulesAudit W4) — 27 блоков не содержали «**Инварианты:**» (выведены из CSS/страницы каждого компонента, не скопированы), 29 блоков не содержали отсылку к полной спеке. Оба случая закрыты сплошным проходом; правило добавлено в линтер, чтобы новый/правленый блок не регрессировал молча.
- **P4 и локальная раскладка экрана** (12.08.2026): классы, объявленные в `<style>` страницы-экрана и навешиваемые её же скриптом `<имя>.screen.js`, сиротами не считаются. Линтер собирает локальные стили экранов в `localDoc` наравне со страницами компонентов; до правки `.pf-grid` и `.pf-th-tools` давали ложные BLOCKER P4.
- **Классы из ревью 02-Deal-v3** (12.08.2026) — разобраны в аудите правил (страница удалена 20.08.2026), правила линтера — волна W5:
  - `.chip__info` с `aria-haspopup="dialog"` без слоя `.pop` в документе — композиция RiskMetric собрана наполовину: выглядит нормально, не работает.
  - `<i data-icon>` внутри внутреннего слота компонента (`.cb__mark`, `.rb__mark`, `.sw__knob`) — анатомия перевёрстана, глиф получает дефолтные 24px и распирает слот.
  - ряд из двух и более `.tile` без `align-items:stretch` — высоты разъедутся (инвариант `Tile.md`, «Размеры»).
  - `.tabs` горизонтальный без обёртки переполнения (`.tabs-scroll` / `.tabs-overflow`) — ряд вылезает за контейнер.
  - экран, где разметка требует рантайма, а `<script>` на него нет — проверка по таблице хуков (на 12.08.2026 недоподключено на 12 экранах из 12; закрыто волной W0 — единая точка входа `ds.js` делает этот класс бага структурно невозможным на страницах, где `ds.js` подключён).
  - одноколоночный `display:grid` без `grid-template-columns` у блока, который должен тянуться — трек сжимается по max-content (было: `.fi__sub` 824px → 318px). Линтер: **F4**.
- **Правила F1–F4 на шесть разобранных классов ошибок** (13.08.2026, RulesAudit W5): четыре из шести стали проверками линтера (F1 `.chip__info` без `.pop`, F2 `<i data-icon>` во внутреннем слоте, F3 `.tabs--horiz` без `data-tabs`, F4 grid-track без `minmax`); два (ряд тайлов без stretch, экран без рантайма) закрыты архитектурно волной W0/W2 и не нуждаются в отдельной проверке. Первый прогон F1–F3 на всех экранах нашёл реальные незакрытые дефекты (не гипотетические): `02-Deal.html` (починен тем же заходом), плюс `02-Deal-v2/v3`, `03-DealFixModal`, `04-Metrics`, `05-Instrument`, `07-CashFlow`, `08-Payments`, `09-CorpRequest` — ждут решения пользователя, чинить или это архивные версии.
- **Оценка «из коробки» снимается скриптом, не руками** (12.08.2026): ручная отметка `oob: ok` у Tab в `SectionsAudit` разошлась с фактом: клавиатура в рантайме есть, а переполнение осталось в `tab.page.js`. Вердикт по критерию ставится только по факту в CSS/рантайме.
- **Догадка вместо знания** (12.08.2026): назначение иконки, поля или связи, выведенное из внешнего вида, попадало в документацию как факт. Линтером не ловится — правило процессное (`MAINTAINING.md`, «Жёсткие запреты»): неизвестное назначение — вопрос, не реконструкция. При разборе чужого макета или экрана — не объяснять предметную область, если её никто не объяснял.
- **F5 — NavPanel урезан вместо взят целиком (14.08.2026)**: на `pages/screens/DealCard.html` собрана rail-only разметка `.nav` без `.nav__pin`/`.nav__user-text` и без обёртки `.nav-layout` — при развороте в Drawer/Fixed пропадали пин и профиль, а сам разворот раздвигал контент вместо оверлея. Причина процессная (готовый DOM-сниппет из чит-шита не скопирован целиком), но проверяется и механически — правило F5. Тем же заходом компонент доработан: `.nav-layout` в `styles/nav-panel.css` делает fixed/overlay-поведение встроенным (CSS `:has()`), без ручной раскладки на экране (NavPanel v1.9).
- **F6 — таблица на экране без ручек ширины колонки (27.08.2026)**: спека TableCell давно объявляла изменение ширины «из коробки», но `scripts/tbl-resize.js` был подключён только на страницах документации — в `scripts/ds.js` его не было, и собранные экраны его не грузили; вдобавок канонические сниппеты шапки (чит-шит, `screen-assembly/patterns.md`) шли без `.th__resize`. Сборщик экрана не получал базовое поведение таблицы без отдельного запроса. Починка: `tbl-resize.js` → в `ds.js` и в `DS_JS_BUNDLES` (теперь `A7` запретит ручное подключение), `.th__resize` добавлен во все эталонные сниппеты, правило `F6` ловит `pages/screens/*` с `.th__label` без `.th__resize`.
- **B8/B9 — усечение, отключённое чужим слоем (04.09.2026)**: в реестре «Текущий портфель ДИД» наименование сделки шло в одну строку без многоточия и уезжало под соседнюю колонку, хотя разметка ячейки была правильной (`.tc__text--truncate`). Ломал усечение сам рантайм тултипа: `ds-tooltip.js` оборачивает цель в `.tip-anchor`, а тот был `inline-flex` без `min-width: 0` — автоминимум flex-элемента равен min-content содержимого, обёртка не сжималась, и `text-overflow: ellipsis` не срабатывал (замер: ячейка 280px → `.tc__row` 248px → текст 377.6px). Тем же механизмом обрывались статусные чипы: `.chip` — тоже `inline-flex` без `min-width: 0`, вылезал за колонку, и его подпись обрезал непрозрачный фон соседней ячейки — обрыв посреди слова, без многоточия. Починка: `.tip-anchor` → `min-width: 0; max-width: 100%` (Tooltip 2.005), `.chip` → `min-width: 0` (Chip 1.010). Два класса ошибок стали правилами: **B8** (компонент с усечением обязан высказаться о сжимаемости) и **B9** (обёртка рантайма обязана быть раскладочно прозрачной). Оба проверены откатом фикса: ловят ровно исходные дефекты, ложных срабатываний по ДС нет — Avatar отсеивается своим `flex: none`, Tab и SegmentControl стоят в `SHRINK_OK` с причиной (переполнение решается скроллом ряда и растяжением трека, а не сжатием элемента).
- **B15 — паддинг, который удлиняет рейл (11.09.2026)**: на всех страницах документации ряд «Конструктор / Документация / Код» выглядел так: полоска подчёркивания 24px, потом табы, потом снова полоска. Причина не в компоненте: `styles/docs-split.css` ставил `padding: 10px 24px 0` на сам `.tabs`, а рейл ряда — `box-shadow: inset 0 -1px 0 0 var(--tab-track)`. Inset-тень рисуется по PADDING-BOX, то есть по всей ширине элемента вместе с паддингом: содержимое отодвинулось, линия — нет, она удлинилась. Правило ДС на этот счёт есть, но живёт комментарием в `tab.css`, а паддинг ставил хром документации, который этот комментарий не читает. Проверено откатом: возвращённый `padding: 10px 24px 0` ловится B15 с указанием файла-источника рейла, на исправленном CSS правило молчит. Побочно выяснилось, что по селектору `.tabs` статически не видно ориентацию ряда (блок объявляет обе), поэтому вертикальная ось для `docs-split.css` подавлена в `RAIL_PAD_OK` с причиной, а горизонтальная — та, что ловила инцидент, — оставлена.
- **A1 / Tooltip 2.009 — подпись, которая дублируется по наведению (11.09.2026)**: на странице Tab при наведении на любой таб рядом с подписью появлялась её копия и оставалась навсегда. Сложились две вещи. Первая: `DSTooltip.truncated()` в ветке `pointerover` строил разметку тултипа (`.tip-anchor` вокруг подписи + `.tip` рядом) для ЛЮБОЙ подписи под селектором, а проверка усечения стояла позже, в `allowed()` внутри `show()` — показ не происходил, но узлы уже были в DOM. Ветки `focusin` и `refresh()` проверяли усечение до построения; расходились три ветки одного механизма. Вторая: четыре страницы (Tab, Chip, Entity, Table) подключали `scripts/ds-tooltip.js` без `styles/tooltip.css` — рантайм объявляет зависимость в шапке файла, но не догружает её, и без стилей `.tip` рисуется обычным блоком с текстом. Первую закрыл `isTruncated` перед `attachTrunc` (проверено откатом на linkedom-стенде: 1 `.tip` до, 0 после, усечённая подпись тултип сохранила), вторую — пара `ds-tooltip.js` → `tooltip.css` в `JS_CSS_PAIRS` уровнем BLOCKER (проверено снятием `<link>`). Класс дефекта общий с уроком «спека объявляет — кода нет»: спека Tooltip прямо писала «тултип показывается только при реальном усечении», и это было правдой про показ, но не про построение.
- **B11 — `[hidden]`, который молча не работает (05.09.2026)**: один и тот же дефект чинили поштучно шесть раз, каждый раз по факту поломки на живом экране — Modal 1.005 (`.modal-scrim`), TableCell 2.015 (`.tbl__row`), Tab 1.010 (`.tab__badge`), EmptyState 1.002 (`.es`), InputText 1.010 (`.inp__act`), Chip 1.015 (`.chip`). Механика одна: браузерное `[hidden] { display: none }` живёт в UA-стиле со специфичностью (0,0,0), а правило компонента `.x { display: flex }` — (0,1,0); класс побеждает, и атрибут перестаёт что-либо значить. Дефект дорогой именно молчаливостью: разметка выглядит правильной, скрипт исправно ставит атрибут, элемент виден. На «Текущем портфеле ДИД» так неделю висело пустое состояние «Сделки не найдены» под полной таблицей.
  **Почему правило структурное, а не адресное.** Первая редакция искала виновных — классы, которые где-то в репозитории действительно переключают атрибутом. Разметочный признак (тег с `class` и голым `hidden`) поймал 1 случай из 6: все реальные дефекты рантаймовые. Признак «файл присваивает `.hidden` и упоминает класс» дал 49 находок с очевидными ложняками (`.modal__head`, `.inp__field`) — элемент почти всегда получают через переменную или чужую функцию, а не селектором в точке присваивания, и без межфункционального анализа связь класса с `.hidden` не восстановить. Поэтому правило не ищет виновных, а закрывает возможность (доктрина `ds-rules.md` §9 «что нельзя измерить — делаем структурно невозможным»): корень компонента, объявивший `display`, обязан рядом объявить и `[hidden]`. Цена соблюдения — одна строка на компонент.
  Проверено откатом фикса: с убранным `.chip[hidden]` правило называет `.chip`, с возвращённым — молчит про него и продолжает показывать непочиненный `.chiplist` в том же файле. Три корня внесены в `HIDDEN_PAIR_OK` с причиной (`.screen`, `.nav-layout` — каркасы страницы, существуют в единственном экземпляре; `.chart-host` — слот, скрывают контейнер-потребитель). Долг закрыт в тот же день: пары проставлены всем 80 корням в 44 файлах одним заходом, правило поднято с INFO до WARN. Проверено дымовым прогоном по всем 57 страницам ДС — базовый `display` компонентов на месте, `hidden` скрывает и снимается без остатка.
- **B10 — геометрия, посчитанная на событии `scroll` (04.09.2026)**: в реестре «Текущий портфель ДИД» закреплённые колонки дрожали при горизонтальном скролле на MacBook — уезжали вместе с контентом и возвращались на место, циклично, пока идёт скролл. На Windows дефект не воспроизводился вовсе. Причина: `tbl-pin.js` удерживал колонки через `transform: translateX`, пересчитывая его в обработчике `scroll`. Скролл применяет компоновщик браузера, и новый offset попадает в кадр РАНЬШЕ, чем выполнится JS-обработчик, — коррекция физически не может успеть в тот же кадр. Дискретное колесо мыши это скрывает (правка успевает в паузу между щелчками), непрерывные дельты трекпада с инерционным докрутом показывают ошибку на каждом кадре подряд. Усугубляли два места: `requestAnimationFrame` внутри хендлера (ещё кадр задержки) и `getBoundingClientRect()` по всем ячейкам шапки на каждый кадр (forced reflow). Починка: удержание переведено на нативный `position: sticky`, рантайм считает только инсеты `left`/`right` и пишет их по `columnpin`/`columnreorder`/resize/ResizeObserver шапки (TableCell 2.013). Попутно опровергнута предпосылка, на которой держалась JS-реализация: `position: sticky` работает и для колонок grid-раскладки — проверено на стенде и на экране, держатся и многоколоночный левый блок, и хвостовой правый. Правило проверено откатом фикса: возвращённый scroll-хендлер ловится, на починенной ДС молчит; шесть легальных записей (плавающие слои Tooltip, Popover, DropdownList, Menu, DatePicker, rail-подписи NavPanel) внесены в `SCROLL_GEOMETRY_OK` с причиной — им пересчёт при скролле обязателен, CSS-эквивалента нет.
- **B7 — межкомпонентная протечка по ARIA-атрибуту (14.08.2026)**: `styles/entity.css` красил выбранную строку правилом `:is(.entity--selected,[aria-selected="true"])` без класса `.entity` перед `:is()` — селектор матчил ЛЮБОЙ элемент с `aria-selected="true"`, включая `.tab--selected` на `pages/screens/DealCard.html` (выбранный таб рисовался с фоном Entity вместо честного «фон только на hover»). При аудите нашлись ещё два случая того же паттерна — `styles/tab.css` (свой `:is(.tab--selected,[aria-selected="true"],[aria-current="true"])`) и `styles/icon-button.css` (`:is(.ibtn--selected,[aria-pressed="true"])`) — все три пофикшены добавлением класса-скоупа перед `:is()`. Entity → v1.6.
