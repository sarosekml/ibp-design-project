# Журнал правок ДС

Что менялось в дизайн-системе, когда и почему. Одна запись — один заход работы. Текущие версии и даты компонентов — в `specs/_index.md` и в шапках страниц; здесь только содержание правок.

Журнал начат 21.08.2026. Историю до этой даты не восстанавливали: версии и даты правок видны в шапках страниц и в YAML-шапках спек.

Формат записи: дата · компонент (версия) · что изменилось · почему.

## 20.09.2026

| Компонент | Версия | Что изменилось | Почему |
|---|---|---|---|
| Colors | 1.004 | Семантика переведена на базовые токены. `styles/palette.css` переписан: слой примитивов `--c-*` (64 штуки) удалён, каждый семантический токен стал `var(--<базовый>)` или `color-mix()` поверх него; hex в файле не осталось, кроме палитры графиков `--ch-*` — у неё базовых токенов нет по спеке. Группировка приведена к спеке: Static · Active · Situative · Status · Chart (вместо Static/Active/Situative/Local), всего 119 токенов. Переименованы `--bgtable*` → `--bg-table-*`, `--chart-*` → `--ch-*`, `--st-lblue-*` → `--st-blue-*`, `--bg-mainmenu` → `--bg-main-menu`, `*-semitransparent` → `*-semy-transparent` — 505 вхождений в 89 файлах DS-IBP/Projects/Concepts. Ссылки на `--c-*` из CSS двенадцати компонентов (chip, switch, avatar, badge, link, entity, table-cell, segment-control, context-menu, dropdown-list, allocation-bar, ds-docs) переведены на базовые токены. Удалены `--bgtable-row-focus-hover` и группа `--rate-*` (8 токенов), добавлены `--tertiary-bg` и `--tertiary-bg-light`. Изменился цвет 8 токенов и прозрачность 5 — таблица в `specs/Colors.md`, раздел «История значений». На странице первая вкладка перестроена под спеку, колонка «Палитра» стала «Базовый токен» и показывает реальный токен; блок данных генерируется из `palette.css` и `colors.css`, руками не правится | Ради этого слой и разводили: до правки семантика стояла на `--c-*`, прописанных отдельными hex, и один цвет, использованный тремя токенами, приходилось менять в трёх местах. Теперь правка базового токена меняет всё, что на него ссылается. Имена и связь взяты из `Uploads/DS _ Swap _ Palette` — актуальной спеки семантики с колонкой «Стиль и токен Colors». Расхождение в `emerald-700`/`emerald-900` между спекой и рампой `Emerald.png` решено в пользу рампы (решение пользователя 20.09.2026), отсюда сдвиг цвета у `--primary-dark`, `--link-dark` и `--st-primary-dark` |

## 19.09.2026

| Компонент | Версия | Что изменилось | Почему |
|---|---|---|---|
| Примеры экранов | — | Удалён мокап `pages/screens/DealCard.html` вместе с секцией «Примеры экранов» в `index.html` и одноимённой группой в `scripts/ds-nav.js`. Папка `pages/screens/` осталась пустой. Упоминания страницы в уроках F5 и B7 (`skills/ds-integrity-check.md`) и в записях журнала от 21.08 и ранее не трогали: это записи о прошлом, а не ссылки для перехода | Настоящая страница сделки живёт в `Projects/post/deal_post/` и собирается из локальных компонентов направления. Мокап в ДС описывал другую раскладку (КНР 4 + Сроки 3 + Команда 5 против 3 + 3 + 6 по ТЗ) и попадался агенту как «эталон» — служил ложным референсом. Решение человека 19.09.2026. Секция витрины и группа меню убраны целиком, а не одной карточкой: карточка в них была единственной, а правило D3 сверяет счётчик секции с числом карточек |
| Colors | 1.003 | Цветовые токены разведены по двум файлам. `styles/colors.css` переписан под **базовые токены** — 20 полных рамп из образцов `Uploads/Colors` (265 токенов, ступени 50…900 + A100/A200/A400/A700, имена как в образцах: `--amber-500`, `--deep-orange-700`, `--swamp-A100`). Прежнее содержимое (примитивы `--c-*` + семантика Static/Active/Situative/Local) дословно переехало в новый `styles/palette.css`; `<link>` на него прописан рядом с `colors.css` во всех 93 файлах, которые его подключали, и `@import` добавлен в `ds.css`. Страница получила ряд Tab (`data-tabs`) и вторую вкладку «Базовые токены»: 20 таблиц TableCell в две колонки, сетка `.pgrid`. Оглавление `ds-toc.js` обновляться не умеет и на второй вкладке прячется — иначе скроллспай подсвечивает скрытые секции. В спеке — раздел «Переход на базовые токены» со сверкой: из 64 примитивов `--c-*` 57 совпадают с базовым токеном, 7 расходятся | В проекте эти цвета — базовые токены, и семантические ссылаются на них, а не на hex: правка базового меняет все семантические разом. В ДС такой связи не было — семантика стояла на усечённом наборе `--c-*`, прописанном hex-ами, и полной палитры не существовало. Этот заход заводит базовый слой и показывает его на странице, **ничего к нему не привязывая**; перевод семантики на `var(--<базовый>)` — следующая правка, и сверка расхождений сделана заранее, чтобы она не начиналась с нуля |
| Kanban | 1.004 | В свёрнутой колонке (`.kbcol--collapsed`, 48px) блок действий поднят над именем: `order: -1` у `.kbcol__acts`, сверху вниз теперь действия · маркер · вертикальное имя · счётчик. Разметка не менялась — она одна на оба состояния, и в развёрнутой шапке действия обязаны идти последними (`margin-left: auto` прижимает их вправо) | Свёрнутой колонкой управляют только эти три кнопки, а вертикальное имя длиной до 320px уводило их к нижней кромке — до единственного управления было дальше всего тянуться. Порядок фокуса правка не трогает: кнопки — единственные фокусируемые узлы внутри шапки, их взаимный порядок сохранён |
| TableCell | 2.020 | Ручка изменения ширины колонки (`.th__resize`) проявляется по наведению на ЛЮБОЕ место шапки: линия приглушённого тона `--secondary`, при прямом захвате (наведение на саму ручку, `:focus-visible`, перетаскивание) — `--secondary-dark`. Тон вынесен в локальную переменную `--th-resize-ink`: подсказку объявляет `.th`, захват — сама ручка, наследование разрешает конфликт без гонки специфичностей. Пока идёт перетаскивание ширины или перенос колонки (`.tbl--resizing` / `.tbl--reordering`), подсказка гаснет | Раньше линия появлялась, только когда курсор попадал ровно в 9px зоны захвата: о том, что ширину колонки вообще можно менять, пользователь не знал, а границу искал курсором вслепую. Тон подсказки взят на ступень светлее захвата — по той же шкале, что у иконок шапки (покой `--secondary` → hover `--secondary-dark`) |
| Kanban | 1.005 | Имя колонки (`.kbcol__name`) переносится на вторую строку и только потом уходит в эллипс: клэмп 2 строки по идиоме `.tile__title`, `white-space: nowrap` снят. Место под две строки резервируется всегда (`min-height: 32px`, следом `2lh` для браузеров с этой единицей), поэтому шапка стала 52px и одинакова у всех колонок. Шапка переведена на `align-items: flex-start`: маркер, имя и кнопки доводятся до центра полосы первой строки (24px, высота счётчика) собственным `margin-top`, в свёрнутой колонке эти сдвиги гасятся. В разделе «Контент» — подраздел «Имя колонки» с образцами | Названия этапов длиннее одного слова («Согласование и подписание договора») резались почти сразу, хотя ширины колонки 288px хватает на две строки. Высота шапки обязана быть общей для всех колонок: иначе карточки соседних колонок начинаются с разной высоты и доска перестаёт читаться рядами — поэтому две строки резервируются даже под однословным именем |

## 16.09.2026

| Компонент | Версия | Что изменилось | Почему |
|---|---|---|---|
| TableCell | 2.019 | scripts/tbl-pin.js: пересчёт инсетов закрепления запускается ещё и на изменение состава строк — MutationObserver по .tbl (childList, без subtree), обработчик тот же, коалесцированный в кадр. Пересчёт на таблице вне потока (hidden, свёрнутая вкладка) пропускается: нулевые ширины склеили бы лесенку инсетов в стопку у левого края, а возврат в поток ловит ResizeObserver шапки. scripts/ds-table.js: новое API DSTable.adoptRow(tbl, row) и автовызов на вставку строки — строка принимает текущую форму шапки (порядок колонок, треки, ячейка скрытой колонки уходит на склад строки); хук — ключ колонки data-col на .th и .tc. В примере кода страницы исправлен комментарий, с 2.013 говоривший про удержание через transform | Дефект увидел пользователь: в реестре сделок у строки, дописанной экраном, закреплённые колонки уезжали при горизонтальном скролле, пока остальные держались. Ячейка новой строки получала position: sticky из CSS, но без инсета left sticky стоит на своём месте и едет с контентом, а ни одно событие пересчёта (закрепление, перенос, ресайз, ResizeObserver шапки) вставкой строки не вызывается. Разбор показал класс шире симптома: дописанная строка не знала о состоянии колонок вообще — ни о порядке после переноса, ни о ширинах после ресайза, ни о скрытых колонках. Правило принадлежит рантайму: экран, обязанный звать хук руками, рано или поздно его не позовёт, и дефект молчаливый — строка выглядит правильной, пока колонку не тронут |
| Table | 1.016 | scripts/ds-table-settings.js: модель колонок догоняет состав строк перед открытием модалки (syncRows) — строки, дописанные после снимка, входят в неё: видимая ячейка берётся по позиции, ячейка скрытой колонки — со склада строки; commit кладёт ячейки скрываемых колонок на их строки, чтобы склад был один. В спеке — хук data-col и adoptRow рядом с wire() | Снимок колонок делался один раз и держал список строк на момент первого открытия: строка, созданная позже, в модель не входила, и скрытие-показ колонки её не перестраивал — она осталась бы на ячейку короче шапки, а все колонки правее съехали бы на трек |

## 15.09.2026

| Компонент | Версия | Что изменилось | Почему |
|---|---|---|---|
| Entity | 1.008 | Новый контейнер `.entity-list` (flex-колонка, зазор `--space-16`, парное `[hidden]`) и сброс подчёркивания у корня-ссылки `a.entity`. В спеке и чит-шите — инвариант: `.entity--interactive` выносит подложку hover на 8/10px отрицательным margin, соседние интерактивные строки ставятся только в `.entity-list`, у родителя поля ≥ 8/10px; диагностика «подложка накрывает соседнюю строку». На странице — подраздел «Список интерактивных строк» с живым примером, вкладка «Код» перевставлена `docs-split inject`. Сенсор экранов получил правило Б33 | На хабе проектов строки стояли с зазором 4px, и подложка hover накрывала соседнюю строку — дефект увидел пользователь. Причина в компоненте: вынос подложки нигде не был описан, и любой экран, собравший список «на глаз», повторил бы наложение. Контейнер делает правильный зазор штатным, сторож — ошибку видимой |
| Навигация документации | — | `scripts/ds-nav.js` + `styles/ds-nav.css`: закреплённый низ панели `.ds-nav__footer` со ссылкой «Хаб проектов» на корневой `index.html` рабочего пространства (`ROOT + '../index.html'`, работает с любой глубины страницы). Пункт строится вне массива `NAV`: хаб не страница ДС и не входит в реестры | Из документации ДС не было пути обратно к проектам и концептам (просьба пользователя) |

## 13.09.2026

| Компонент | Версия | Что изменилось | Почему |
|---|---|---|---|
| spec-audit | — | Проход 8: манифест `specs/_index.md` против каталога компонентов в `.opencode/rules/ds-rules.md` §6, в обе стороны; расхождение или пустой разбор дают код выхода 1; нет файла правил — `ПРОПУЩЕН:`. Правка `ds-rules.md` запускает аудит в `lessons-cli gate`. Доказано откатом: без Drawer в каталоге — находка и код 1, с ним — 58/58 и код 0 | Drawer и Kanban были в манифесте, но не в каталоге, который читает каждый агент: при запрете изобретать компонент агент не узнал бы, что компонент есть. Сторож реестров линтера (D1/D5) каталог правил не читает |
| Точка входа | — | Удалён `styles.css` — зеркало `ds.css`. `ds-lint` больше не требует `@import` в двух файлах (A3) и не принимает `styles.css` как подключение ДС; `MAINTAINING.md`, `skills/ds-integrity-check.md` обновлены | Файл был входом «компилятора» внешней витрины; ни одна страница его не линковала, а ритуал нового компонента заставлял писать `@import` дважды. Ревизия устаревшего, решение пользователя |
| RND · Atoms | — | Удалены `pages/atoms/ButtonTest.html` + `specs/ButtonTest.md` (тестовая копия Button с разделом «Токены») и концепт `pages/rnd/TabsLevel2.html`. Убраны карточки из `index.html` (Атомы 15 → 14, RND 2 → 1 раздел), пункты из `scripts/ds-nav.js`, строка из `specs/_index.md`; карта `pages-index.md` пересобрана командой `map` (была устаревшей с 01.09 — без Drawer, Kanban, SubTab). Уроки Л18/Л19 (принятые отклонения ради ButtonTest) выведены из выжимки | Тестовая копия выглядела для агента как отдельный компонент в манифесте; из концепта TabsLevel2 уже вырос SubTab. Ревизия устаревшего, решение пользователя |
| ds-lint | — | Снято правило G1 (маркер `FIDELITY-GATE` на экранах); удалены скиллы `skills/screen-from-mockup.md` и `skills/mockup-fidelity-review.md`, фикстура `Projects/test/fixtures/G1.bad.html`; маркер убран из остальных фикстур экранов одинаково во всех (пары по-прежнему «эталон + один дефект») и из `Portfolio.html`, `mainPage/index.html`, `pages/screens/DealCard.html`. Реестр якорей перезаписан (`anchors --write`) | Гейт требовал скриншотов и просмотра картинок — на рабочем контуре модель изображений не читает, а браузер по скрипту запрещён. Сборщик на opencode про маркер не знал, и G1 краснил каждый его экран (видно в журнале прогонов). Правило, ссылающееся на недоступный инструмент, — класс Л37 |
| ds-check · docs-split | — | Удалены браузерная подкоманда `docs-split.mjs verify`, харнесс `diag-harness.js` и флаг `ds-check.mjs --with-verify`/`DS_CHROME`; `rollout` больше не вызывает `verify` | Headless Chrome в репозитории противоречит ds-rules §9; `rollout` безусловно звал `verify` и на рабочем контуре всегда давал `ВЕРДИКТ: FAIL` |
| templates | — | Удалены `templates/admin/` и генератор реестра `scripts/ds-registry.js` + `ds-registry-cli.mjs` | Инструмент под раскладку `_ds/` эпохи внешней витрины, ни разу не применялся; панель грузила `registry.json` через `fetch`, что по `file://` не работает (ds-rules §10) |
| Layout | 1.010 | Спека и страница, правило «Контент»: к «заголовок страницы — PageHeader, первый блок» добавлено исключение для стартовой страницы (один блок — сетка групп, без PageHeader). Сенсор `layout-check` (Б6) на стартовой не требует h1 — признак структурный: единственная крошка текущая, есть `.grid12`, нет `.phead` | Спека противоречила сама себе: раздел о стартовой странице описывает контент как одну сетку групп, а общее правило требовало PageHeader. Сенсор держал главную Post в FAIL Б6 при разметке, соответствующей стартовой. Решение пользователя 13.09.2026 |
| ds-lint | — | F5 (анатомия компонента) ищет обязательные узлы и в атрибутах `class="…"` JS-шаблонов внутри inline-`<script>`; условие контракта по-прежнему — статический хост. Разметка для правил очищается в порядке «комментарии → стили → скрипты» (было «стили → скрипты → комментарии»). Новая фикстура `fixtures/F5@js.bad.html`; доказано откатом, отчёты по 109 файлам до/после совпали, кроме новой фикстуры | Экраны Post строят панель навигации шаблоном по роли: F5 видел пустой `<nav class="nav nav--rail">` и выносил ложный BLOCKER на полной анатомии. Старый порядок очистки съедал разметку, если в тексте комментария стояло слово «&lt;script&gt;»: регулярка скриптов начинала совпадение в комментарии (класс Л82) |
| ds-lint-cli | — | Путь экрана (`../Projects/…`) пишется в журнал прогонов от корня репозитория. Фрагменты модульного экрана (файлы без `<html>`, вшиваемые через `<ds-include>`) не линтуются как экраны — печатается `ПРОПУЩЕН:` | С 12.09.2026 ни один прогон линтера по экрану не попадал в журнал: путь читался как «вне репозитория» (урок Л107). Фрагменты пилота `Projects/test/post/` давали 21 ложный блокер — «нет ds.css», «нет каркаса» у куска разметки, который экраном становится после сборки |
| Kanban | 1.003 (дата) | Страница `pages/organisms/Kanban.html`: коробки образцов с живой колонкой в блоках «Тон маркера», «Свёрнутая колонка» (образец «Развёрнутая») и «Состояния колонки» расширены с 200/264px до 320px — одна константа `COL_SAMPLE_W` в `scripts/kanban.page.js` с расчётом ширины. Раскладка при области документации ~1220px: «Тон маркера» 3 + 3, «Состояния колонки» 3 + 3. Компонент не менялся | Колонка ДС не сжимается (`flex: 0 0 --kb-col-w` = 288px), а внутри коробки 200/264px оставалось 174/238px: заливка hover выходила за коробку, в «Состояниях» скроллпорт `.kanban__viewport` рисовал горизонтальный скролл. Коробке нужно 288 + паддинг 12+12 + рамка 1+1 = 314, 320 — с запасом на округление. Четыре образца по 320px в ряд не встают, поэтому «Тон маркера» — 3 + 3, а не 4 + 2 (решение пользователя) |
| RND | — | Удалён концепт `pages/rnd/Kanban.html`: убраны карточка в секции RND `index.html` (счётчик 3 → 2 раздела) и пункт «Канбан-доска» в `scripts/ds-nav.js`; в `scripts/ds-lint.js` комментарий к правилу D7 переформулирован без ссылки на удалённый файл, логика не менялась | Из концепта вырос организм Kanban, отдельная страница больше не нужна (просьба пользователя) |
| InputDateRange | 1.010 (дата) | Страница: блоки «Использование» получили на странице `grid-template-columns: repeat(auto-fill, minmax(440px, 1fr))` вместо базовых трёх колонок из `input-pages.css` — при 1920px два блока в ряд | Поле диапазона дат не уже 186 × 2 + Range_Line 14 = 386px, со стадией (паддинг 24 × 2, рамка) — 436px. В трети ширины его не хватало на 46px, а `overflow: hidden` у `.use-block` обрезал правое поле. Найдено обходом всех страниц документации после дефекта Kanban (урок Л106) |
| NavPanel | 1.019 (дата) | Страница, «Анатомия»: первая колонка сетки `.anat` — `max-content` вместо `minmax(280px, 340px)` | Панель в режиме Drawer (320px) с паддингами стадии 28 × 2 в 340px не помещалась: у стадии появлялся горизонтальный скролл на 19px. Колонка по содержимому не требует пересчёта при смене ширины панели |
| Modal | 1.008 (дата) | Страница, «Размеры»: полоски «Наглядно» переведены с масштаба 1:10 на 1:20 (максимум 182 → 91px), колонка «Наглядно» 0.81fr → 1.2fr за счёт «Рекомендации» 2.88fr → 2.49fr | Полоски от 8 колонок сетки и шире (до 182px) выходили за колонку шириной ~116px и наезжали на соседнюю. Пропорции между размерами сохранены |
| ContextMenu | 1.009 (дата) | Страница, «Доступность», таблица клавиш (`scripts/context-menu.page.js`): колонки 0.6fr / 2.2fr → 1.1fr / 1.7fr. Дата страницы выровнена со спекой | Клавиша «Enter / Space» (107px) не помещалась в колонку (~75px текста) и наезжала на описание. Проверено при окне 1920 и 1600px |
| PageHeader | 1.010 (дата) | Страница, «Типографика»: колонка «Образец» 0.77fr → 1.5fr, «Размер / интерлиньяж» 1.89fr → 1.16fr | Образец в `--type-h3-strong` (119px) не помещался в колонку и выходил за правый край таблицы |

## 11.09.2026

| Компонент | Версия | Что изменилось | Почему |
|---|---|---|---|
| SubTab | 1.000 | **Новая молекула** — табы второго уровня из концепта 1 страницы `pages/rnd/TabsLevel2.html`: `styles/sub-tab.css`, функция `subtabs()` в общем рантайме `scripts/ds-tabs.js` (хук `data-subtabs`, экспорт `DSTabs.subtabs`), страница `pages/molecules/SubTab.html` на docs-split, демо `scripts/sub-tab.page.js`, спека и блок в чит-шите, карточка в `index.html` с превью и пункт в `ds-nav.js`, парный `@import` в `ds.css` и `styles.css`. Счётчик «Молекулы» 20 → 21 | Второй уровень навигации продукту нужен, а компонента не было: каждый экран решал бы по-своему. Отдельный компонент, а не вариант Tab — оси не комбинируются (ни ориентация, ни размеры, ни иконки, ни переполнение Tab к нему не применимы), и правило применения у него противоположное: Tab самодостаточен, SubTab без первого уровня не существует |
| SubTab | 1.001 | Размерная шкала вместо единственного размера: **M 40 · S 32 · XS 24** на CSS-переменных (`--sub-h/--sub-px/--sub-gap/--sub-font/--sub-radius`), класс размера на ТРЕКЕ (`.subtabs--m|--s|--xs`), базовый без класса — S. M и S совпадают с рядом Tab, XS берёт паддинг и кегль у `.segctrl--xs` и радиус `--radius-control-s`. Размер плашки счётчика идёт за размером ряда (`.badge--xs` / `.badge--xxs`) и задаётся разметкой, а не потомковым правилом поверх чужого компонента. На странице — новый подраздел «Правило пары» с живыми демо обоих рядов, редлайн снимается по всем трём размерам, в конструкторе селект пары | Инвариант «второй уровень всегда ниже первого» заменён на «не крупнее первого»: шаг вниз рекомендован, равный размер допустим, крупнее — инверсия иерархии. Одна шкала с Tab делает шаг вниз возможным, а XS закрывает плотные экраны, где первый уровень собран на `.tab--s` |
| ds-tabs | — | Движок `roving()` принял `cfg.disClass` — класс форс-состояния витрины стал параметром вместо зашитого `tab--disabled`; добавлена `subtabs()`, `wireAll()` обходит ещё и `[data-subtabs]` | Третий потребитель общего движка. Дублировать roving tabindex и клавиатуру ради одного селектора — заводить вторую реализацию механизма, который уже написан и проверен |
| ds-lint | — | Новое правило `B14` (постраничное, BLOCKER): на странице есть `.subtabs`, но нет ряда первого уровня (`.tabs--horiz` с `.tab`). Доказано откатом на паре `fixtures/B14.bad.html` / `_base.ok.html` | Одиночный ряд второго уровня — дефект, невидимый всеми остальными проверками: разметка валидна, токены на месте, сенсор раскладки молчит, компонент выглядит нормально. Врёт только иерархия — экран читается как один уровень навигации с необычным видом. Что нельзя увидеть глазами на раскладке, делаем структурно невозможным |
| ds-rules.md | — | SubTab внесён в каталог молекул и в раздел «Созависимые части»: применяется только вместе с рядом табов первого уровня и размещается как содержимое одного из них | Правило применения нужно агенту в момент сборки экрана, а не в документации компонента, которую он на этом шаге не читает |
| Tab | 1.012 | Убран боковой паддинг 24px у ряда табов в панели документации (`styles/docs-split.css`, `main.ds-split .app-pane-docs .tabs`): было `padding:10px 24px 0`, стало `padding:10px 0 0`. Версия компонента не менялась — правился хром страницы, а не Tab | Рейл ряда — `box-shadow: inset 0 -1px`, а inset-тень рисуется по PADDING-BOX: паддинг не сдвигал линию вместе с табами, а удлинял её. Пользователь видел «полоска 24px, потом табы, потом снова полоска». Горизонтальный отступ ряда задаётся обёрткой без рейла |
| Tooltip | 2.009 | `truncated()` проверяет `isTruncated` в ветке `pointerover` ДО `attachTrunc`, а не только внутри `show()`. Раньше разметка тултипа (`.tip-anchor` + `.tip`) строилась на любой подписи, попавшей под селектор, и оставалась в DOM навсегда — показ гасился позже, в `allowed()`. Проверено откатом на linkedom-стенде: на откаченном коде неусечённая подпись даёт 1 `.tip` и 1 `.tip-anchor`, на исправленном — 0, усечённая по-прежнему даёт тултип | Дефект молчаливый ровно до момента, когда страница не подключила `styles/tooltip.css`: тогда построенный `.tip` рисуется обычным текстом, и подпись таба/чипа дублируется прямо в компоненте. Спека при этом утверждала обратное («тултип показывается только при реальном усечении») — правило было записано, но проверка стояла не в том месте |
| Tab · Chip · Entity · Table | — | Подключён `styles/tooltip.css` на четырёх страницах, которые грузили `scripts/ds-tooltip.js` без него | Рантайм объявляет зависимость от своего CSS в шапке файла, но не догружает его. Без стилей `.tip` — обычный блок с текстом внутри компонента |
| ds-lint | — | Пара `ds-tooltip.js` → `tooltip.css` в `JS_CSS_PAIRS` (правило `A1`), уровнем **BLOCKER**: список пар принял третий элемент — уровень. Новое правило `B15` (глобальное, WARN первого цикла): горизонтальный паддинг на элементе с горизонтальным inset-рейлом (и вертикальный — с вертикальным). Ось рейла берётся из смещений тени, кольцо (`inset 0 0 0 N`) не считается рейлом, разделитель на элементе группы (ветка с комбинатором, `.btn-group--accent > .btn + .btn`) не регистрируется. Оба доказаны откатом: A1 — снятием `<link>` с Tab.html, B15 — возвратом `padding:10px 24px 0` в docs-split.css | A1 у пар, которые CSS догружают сами, стоял WARN; `ds-tooltip.js` не догружает, и цена пропуска — видимый мусор в интерфейсе, а не «слегка не так выглядит». B15 закрывает класс: правило ДС про рейл живёт в комментарии `tab.css`, потребитель хрома его не читает и ставит паддинг там, где он ломает линию |
| spec-audit | — | Новый **проход 6** (read-only, `DS-IBP/scripts/spec-audit.mjs`): класс, названный в спеке (в обратных кавычках или в примере `class="…"`), обязан иметь правила в `styles/*.css` ЛИБО роль в рантайме/на странице. Проход молчит на классах из `scripts/*.js` (хуки `.nav__burger`, `.dpk__*`) и из разметки/`<style>` страниц (зона линтера `P5`); находка — то, чего нет нигде. Не гейтит, идёт отдельной строкой, в реестре — `аудит:6`. Доказан ручным откатом: зонд `.zzz-pass6-not-in-css` даёт находку, после удаления — нет | Ветка «объявлен компонент-родственник, CSS нет» (Л86/Л42) не покрывалась ни одним проходом: `Card (`.tile--card`)` в спеке Tile простоял без правил до 10.09.2026. Первый прогон — 23 находки; после уточнения входа остались 3 мёртвые ссылки в спеках (`dtable__edge` удалён 27.08.2026, `chip--readonly` — не модификатор, `x--has-tooltip` — плейсхолдер), приведены к конвенции ёлочек; на дереве 0 |

## 10.09.2026

| Компонент | Версия | Что изменилось | Почему |
|---|---|---|---|
| Kanban | 1.000 | Новый организм: страница `pages/organisms/Kanban.html` на docs-split, `styles/kanban.css`, рантайм `scripts/ds-kanban.js` (зарегистрирован в `ds.js`), демо `scripts/kanban.page.js`, спека и блок в чит-шите, карточки в `index.html` и `ds-nav.js`. Концепт `pages/rnd/Kanban.html` остаётся на месте как исходный разбор | Доска нужна продукту, и без общего компонента каждый экран собрал бы свою. Статусная модель объявляется разметкой (`data-kb-to`), перетаскивание имеет клавиатурный эквивалент (WCAG 2.1.1 и 2.5.7) |
| Drawer | 1.000 | **Новый организм**: панель деталей у правого края — `styles/drawer.css`, рантайм `scripts/ds-drawer.js` (зарегистрирован в `ds.js`), страница `pages/organisms/Drawer.html` на docs-split, демо `scripts/drawer.page.js`, спека, блок в чит-шите, карточка в `index.html` и пункт в `ds-nav.js`, парный `@import` в `ds.css` и `styles.css`. Счётчик «Организмы» 14 → 15 | Боковой панели в ДС не было вообще, а канбану без неё некуда открыть карточку: на лице карточки 4 поля из сорока. Слой у панели общий с Modal — второй экземпляр focus trap, `inert` и стека слоёв разошёлся бы с первым на первой же правке |
| Modal | 1.008 | Слой отделён от геометрии: корень слоя ищется по `[data-layer-root], .modal, .drawer`, тело/шапка/подвал — по парам селекторов, закрывашка — плюс `.drawer__close button`. Семантика Modal не менялась | Drawer встаёт на тот же слой. Альтернатива — свой скрим и свой focus trap у панели, то есть вторая реализация механизма, который уже написан и проверен: ровно тот класс дефекта, что зафиксирован в Elevation 1.002 («локальная починка вместо правила») |
| Kanban | 1.001 | Починены пять дефектов живой доски и снята лишняя политика. (1) Шапки колонок отлипали при прокрутке — у трека стоял `align-items: flex-start`, и колонка была высотой по своему содержимому: sticky держит шапку только внутри её колонки. Тот же дефект отнимал цель дропа ниже дна короткой колонки. (2) Колонка не подсвечивалась целью дропа: `.kbcol:hover` (0,2,0) перебивал `.kbcol--drop` (0,1,0), а клон летит с `pointer-events: none`, поэтому колонка под курсором всегда в hover. (3) Меню доски, колонок и карточек не открывались: демо пересобирает разметку через `innerHTML` и не звало `DSMenu.bindAll` заново. (4) Индикатор сегмент-контрола не появлялся: вызывался несуществующий `DSTabs.bindAll` вместо `wireAll`, и guard делал промах бесшумным. (5) На время жеста выделялся текст — добавлен `body.kb-dragging`. **Статусная модель удалена из рантайма** (`data-kb-to`, `data-kb-locked`, `allowed()`, `denyReason()`, `.kbcol--nodrop`); вместо неё — отменяемое событие `kanban:beforemove`. **Добавлен перенос колонок** за шапку: указателем и с клавиатуры, с возвратом на исходный индекс по Esc, событие `kanban:colmove`. Клик по карточке открывает Drawer (`data-kb-drawer`), событие `kanban:open` | Компонент общий: на доске живут не только сделки, но и задачи и заметки, и матрица переходов сделки не должна навязываться всем потребителям — предметная логика остаётся у решения, доска даёт ей точку входа. Порядок этапов у команд разный, и переставлять колонки через настройки — лишний слой |
| Kanban | 1.002 | Зазор между карточками по вертикали 16 → 8 (`--space-8`); компенсация линии вставки пересчитана от нового зазора (`--space-4` вместо `--space-8`, иначе соседи сходились бы на 6px в момент показа линии). Тон колонки под указателем — `--primary-bg` вместо `--bgtable-row-hover`. Заодно починено гашение hover на время переноса: правило `.kanban.is-dragging .kbcol:hover` (0,4,0) перебивало `.kbcol.kbcol--drop` (0,2,0) и гасило ровно ту подсветку, ради которой заведено — добавлен `:not(.kbcol--drop)` | Плотность по вертикали: на доске важнее, сколько карточек видно разом, а границу между ними держит рамка Card, не воздух. Тон наведения и тон цели дропа теперь один, различает их кольцо `--primary` — состояний на колонке два, а тонов достаточно одного |
| ds-lint | — | Новое правило `B13` (репозиторного уровня, WARN): горизонтальный `display: flex` без `flex-direction: column`, одновременно являющийся скроллпортом (`overflow: auto|scroll`), подозрителен в файле, где объявлен `position: sticky`. 0 срабатываний на дереве после правки Kanban 1.003 | Тот же механизм, что уронил Kanban дважды подряд (сперва `flex-start`, потом `stretch` на самом скроллпорте), встретится в следующем компоненте со sticky-шапкой и горизонтальной прокруткой. Доказано ручным откатом |
| MAINTAINING.md | — | Пункт 14: скроллпорт и ряд одинаковой высоты — разные элементы | Дефект нашли дважды на одном месте за один день; правило записано явно, со ссылкой на `B13` |
| Kanban | 1.003 | Скроллпорт и ряд колонок разведены на два элемента: новый `.kanban__viewport` (`flex: 1; min-height: 0; overflow: auto` — единственный скролл, обе оси) и `.kanban__track` внутри него (`width: max-content; min-width: 100%; min-height: 100%`, высота auto). Линия вставки колонок упростилась: `top/bottom` из CSS, рантайм считает только `left` — трек больше не скроллится сам. `autoScroll` крутит вьюпорт | На доске с 6+ карточками в колонке карточки торчали за коробкой колонки (заливка hover и цель дропа кончались раньше карточек), а липкая шапка отваливалась на первом же экране прокрутки. Причина: `.kanban__track` был одновременно скроллпортом и flex-рядом, а `align-items: stretch` растягивает элементы по высоте КОНТЕЙНЕРА, не по высоте самого длинного из них — и раз высота контейнера определённая, колонка получалась ровно с видимую область. Вторая итерация того же места: до этого здесь стоял `flex-start`, и шапка отваливалась у коротких колонок (урок Л91 исправлен) |
| Tile | 1.012 | Реализован родственник **Card** — модификатор `.tile--card`: хэдер типа Card, Title `--type-h6-strong`, отступы контента 8/16/20/16 и собственные состояния Default · Hover · Active · Move · Disabled. На странице — вариант с живыми образцами состояний | Card был объявлен в документации Tile и в спеке с самого начала (включая состояние Move), но кода за ним не было ни одной строки — класс дефекта «правило объявлено, реализации нет», урок Л86. Канбан-доска строится на нём |
| ds-lint | — | Новое правило `A9` (репозиторного уровня, BLOCKER): обращение `DSX.method(` сверяется со списком ключей `window.DSX = {…}` соответствующего рантайма. Комментарии вырезаются, неизвестный namespace пропускается. Правило `D7` сужено: версия страницы вне реестров сверяется со спекой, только если спека объявляет эту страницу полем `page:` — иначе концепт `pages/rnd/Kanban.html` вечно расходился бы с версией организма, совпадая с ним лишь именем файла | Идиома `window.DSX && DSX.method` при опечатке в имени метода не падает и ничего не пишет в консоль: guard ложный, функциональности нет, гейты зелёные. В одном заходе нашлось три таких промаха (`DSTabs.bindAll`, `DSModal.close`, `DSIllustrations.apply`) — все три чинились по факту обнаружения глазами. Правило доказано ручным откатом: возврат `DSTabs.bindAll` даёт BLOCKER, откат — 0 (урок Л90) |
| MAINTAINING.md | — | Пункты 12 и 13: имя метода рантайма берётся из экспорта; разметка, пересобранная через `innerHTML`, связывается заново всеми рантаймами, чья разметка внутрь попала | Оба класса дефекта бесшумны, оба всплыли на одной странице в один день (уроки Л90, Л93) |
| ds-lint | — | Новое правило `L6` (группа раскладки, только экраны): экран задаёт `margin` блоку ДС в своём `<style>`. Срабатывает, только если селектор ссылается на класс из CSS дизайн-системы — собственные классы экрана не трогает. Первый цикл живёт как WARN. Доказано откатом: пара `Projects/test/fixtures/L6.bad.html` / `_base.ok.html`, `lessons-cli verify` 61/61 | Расстояния между блоками принадлежат каркасу (зазор контентной области и её поля), но у экрана «Текущий портфель ДИД» жило `.dtable { margin-bottom: 8px }` — низ 32px против 24px у всех остальных экранов, и ни один гейт этого не видел: токены и шкала проверяются, `margin` у чужого класса — нет. Существующее правило L4 стерегло тот же класс слишком узко (только `padding` у `.screen__content` и токены `--layout-*`), поэтому заведена соседняя ветка той же группы, а не правило поверх (урок Л85) |
| `Projects/test/fixtures/` | — | Добавлена пара `L6.bad.html`; README и шапка эталона перечисляют шесть правил корпуса вместо пяти | Корпус экранов — единственный вход, на котором правила группы L вообще срабатывают: внутри `DS-IBP/fixtures/` они молчат всегда |

## 07.09.2026

| Компонент | Версия | Что изменилось | Почему |
|---|---|---|---|
| IconButton | 1.011 | Квадратный (скруглённый `--radius-control`) стейт-слой стал дефолтом для всех размеров (L/M — `--radius-control`, S — `--radius-control-s`); круг — только с явным `.ibtn--circle`. Селектор `.ibtn--square` оставлен как алиас-нооп (дефолт и так квадратный) | Единообразие ховера всех иконочных кнопок: раньше круг был дефолтом, квадрат — отдельной опцией; квадрат нужен повсеместно |
| NavPanel | 1.018 | Ширина развёрнутой панели (drawer/fixed) 272 → 320px; из блока пользователя убрана строка организации (`.nav__user-org`); высота футера 76 → 60px; на странице документации подключён `tooltip.css` | Развёрнутая панель стала шире; строка организации удалена по ТЗ. Без `tooltip.css` тултип усечённой подписи пункта/футера рендерился как инлайн-текст внутри `<a class="nav__user">` — «имя ещё раз в виде ссылки» |
| NavPanel | 1.019 | Футер вынесен в ДС: `IBPHome.user` + `IBPHome.footerHTML(role)` — экраны (Portfolio, главная) и демо перестали хардкодить футер, строка организации тянется из каталога. Каретка аккордеона и бейдж прижаты вправо (`margin-left:auto`) — на одной вертикали независимо от длины подписи и от обёртки `.tip-anchor` при усечении. Тултип усечённой подписи рисуется ВЫШЕ панели (`zIndex:2000` — иначе в развёрнутом виде остаётся под `z-index:1000` панели); на странице документации подключён `ds-float.js` | «Изменения не тянутся из ДС»: разметка футера дублировалась на экранах, поэтому правки (удаление организации) не доходили. Каретка прыгала вплотную к тексту после обёртки усечённой подписи. Тултип на экранах был под панелью |

## 06.09.2026

| Компонент | Версия | Что изменилось | Почему |
|---|---|---|---|
| Modal | 1.007 (дата) | В `<head>` добавлен `<link>` на `styles/spinner.css` | Демо страницы рисует кнопку в состоянии загрузки (`modal.page.js`: `<span class="spin spin--current">`), а `spinner.css` не подключался — спиннер рендерился без стилей. Поймано правилом линтера A2 после того, как оно научилось дочитывать парный `*.page.js`: раньше `ds-lint` вырезал `<script>` из разметки и классы, которые страница рисует своим скриптом, ему были не видны (урок Л41, Л48) |
| NavPanel | 1.017 (дата) | В `<head>` добавлен `<link>` на `styles/button.css` | На странице есть `.btn--outline`/`.btn--m`/`.btn__label` (кнопки выбора роли в демо-модалке, строка 833), а `button.css` не подключался. Находка A2, существовавшая до правки правила |
| Elevation | 1.002 (без изменений) | Из образца кода вкладки «Код» убран несуществующий класс `.demo-panel` — остался `<div style="box-shadow: var(--elevation-2)">` | Образец предлагал скопировать класс, которого нет ни в `styles/*.css`, ни в разметке: документация иллюстрировала несуществующее. Заодно вскрылось, что правило `P4` читало `<script type="text/plain">` как рантайм и сообщало «рантайм вешает» про строку из документации — теперь такие находки идут под `P3` «код-панель предлагает» |
| ds-include | — | Убрано присвоение `stub.className = 'lc-include-error'` в обработчике ошибки загрузки | Селектора `.lc-include-error` нет ни в одном `styles/*.css`, ни в разметке; оформление заглушки целиком задано инлайн. Класс, который рантайм ставит, а никто не читает, — мёртвый код, а не задел на будущее (урок Л32, правило P4). Гейт `--parity` впервые стал зелёным: BLOCKER 2 → 0 |
| Screen (шаблон) | — | Из шапки `templates/screen/Screen.html` убрана ссылка на несуществующий инструмент `copy_files`; копирование описано обычными средствами оболочки | Шаблон всех экранов предписывал шаг через инструмент, которого в контуре нет, — класс урока Л37: правило, ссылающееся на недоступный инструмент, порождает фиктивное «выполнено» |
| ds-lint | — | Новый ряд правил `R` — каскад раскатки docs-split: R1 (`.splitpane--app` поверх собственной flex-раскладки панелей), R2 (`segctrl` внутри `#pg-controls` без `align-self:flex-start`), R4 (остаток flex-раскладки конструктора в `<style>`) | Пункты К1/К2/К10 чек-листа приёмки не имели инструмента на своём входе: сенсор экранов страницу документации отвергает на каркасных проверках. К6 признан суждением — его условие («скрипт читает состояние рантайма в момент события») статике недоступно |
| `fixtures/` | — | Новая папка: корпус фикстур линтера (`_base.ok.html` + `R*.bad.html`). В индекс и реестры не входит, линтер читает только переданные аргументом файлы | Правило, не срабатывающее ни разу, неотличимо от мёртвого. Каждое правило ряда R доказано откатом: падает на дефекте, молчит на эталоне |
| Input (и всё семейство полей) | InputText 1.012 · InputDate 1.013 · InputAutocomplete 1.016 · InputAmountRange 1.007 · InputDateRange 1.010 | Размер M стал настоящим модификатором: переменные `--inp-h/-px/-gap/-icon/-font` перенесены из `.inp` в `.inp--m`. `input-kit.js` выдаёт модификатор всегда, включая M. Класс проставлен во всей разметке — 45 мест в 8 файлах | Селектора `.inp--m` в CSS не существовало: M был молчаливым дефолтом `.inp`. При этом спека объявляла пару M/S симметричной, чит-шит противоречил сам себе (строка 515 против 539), разметка вешала класс руками, а рантайм намеренно не ставил. Класс читался как «включает размер» и не делал ничего; линтер справедливо ругался P5 |
| ds-lint | — | Новое правило B12: `.inp` без `.inp--m` / `.inp--s`. Вход — разметка страницы И парный `*.page.js` | Цена пропуска после правки обратная: поле без размерного класса теряет высоту, паддинги, gap и шрифт МОЛЧА (`height: var(--inp-h)` без значения = `auto`). Правило поймало место, пропущенное ручной инвентаризацией (`class="inp tfm__span-2"` в `table-filter.page.js`); доказано откатом (`fixtures/B12.bad.html`) |
| ds-lint | — | Ряд `R` дополнен: R5 (`DS_SPLIT_SWITCH_LABELS` с парой `{on, off}` — подпись свитча меняется при переключении), R6 (бинарный селект в `#pg-controls` без непустой `.lbl`), R7 (`.pg__grouphead` без своих контролов следом), R8 (самописная `button.toggle[aria-pressed]` вместо штатного `pg-toggle`) | Пункты К8, К9(а), К9(б), К11 чек-листа приёмки. Части (а) и (б) пункта К9 разведены на два идентификатора: пара фикстур у идентификатора одна, и один id на две ветки доказывал бы только одну (урок Л76) |
| ds-lint | — | Комментарии вырезаются из разметки наравне со `<style>` и `<script>`. `html` остаётся сырым — C1 читает карточку `@dsCard` первой строкой, G1 ищет маркер гейта | Текст комментария неотличим от разметки для строкового правила, и страница, ОБЪЯСНЯЮЩАЯ в комментарии «здесь нужен `data-tabs`», этим объяснением правило и глушила. Поймано на новых фикстурах: `F3.bad` и `A6.bad` не срабатывали из-за собственного пояснения. Сенсор экранов вырезает комментарии с 06.09.2026 по той же причине — здесь асимметрия дожила до первого корпуса фикстур. На 61 странице ДС отчёт не изменился: BLOCKER 0 · WARN 72 · INFO 48 |
| `fixtures/` | — | Корпус вырос с 8 пар до 26: A1, A2, A4, A5, A6, B1, B2, B5, C1, C2, C7, F1, F2, F3, F4, F5, L1, L2 плюс прежние B12 и ряд R. Пары изготавливаются генератором из эталона одной заменой, обвязка (ссылка на CSS, чтобы молчало постороннее правило) названа в шапке файла отдельно от дефекта | Правило, не срабатывающее ни разу, неотличимо от мёртвого. Заодно в эталоне нашёлся собственный дефект: `.inp--m` без `<link>` на `input.css` — эталон обязан молчать, иначе доказательство «правило молчит на эталоне» ничего не значит |
| ds-lint-cli, spec-audit | — | В конце прогона дописывается строка в журнал прогонов (номер, время, инструмент, файл, вердикт, сработавшие коды). Импорт модуля журнала — через `try/catch`: без агентской оснастки инструменты работают как работали, запись не имеет права уронить проверку. Фикстуры не логируются | Правило, которое ни разу не сработало на настоящем файле, неотличимо от мёртвого, а дефект, вернувшийся после закрытия урока, до сих пор не был виден вообще ничем. Отчёт по журналу различает четыре состояния: регресс, живой, исчез, не встречался. У аудита «код» — номер прохода с ненулевым числом находок: счёт берётся из переменных прохода, а не из разбора собственного вывода, где маркер «·» несёт и находки, и инвентарь |

## 05.09.2026

| Компонент | Версия | Что изменилось | Почему |
|---|---|---|---|
| Elevation | 1.002 | `DSFloat.mount(el, opts)` получил опцию `anchor` — элемент, от которого открыт плавающий слой. Если явный `parent` не задан, а якорь лежит внутри модалки, элемент монтируется в её `.modal-scrim`, иначе в общий слой `.ds-float-layer` (новая функция `resolveParent` в `scripts/ds-float.js`). Ближайший скрим берётся через `closest`, поэтому вложенный диалог (`.modal-scrim--nested`) обслуживается тем же правилом. Спека получила подраздел «Модалка — исключение из общего слоя», в шапку добавлен `runtime: scripts/ds-float.js` | Общий слой — прямой ребёнок `body` и намеренно не создаёт stacking context, поэтому z-index его детей разрешается в корневом контексте: тултип 30, меню/поповер/список 40 против `.modal-scrim` 1000. Плавающий элемент, открытый ИЗ модалки, рисовался ПОД подложкой, а над самой модалкой — ещё и под её непрозрачным фоном; вторым слоем `DSModal.lockPage()` вешает `inert` на всех детей `body`, кроме скримов. 05.09.2026 этот дефект уже находили на DropdownList и починили точечно, внутри самого компонента, — правилом слоя это не стало, и тот же дефект остался у тултипа, меню, поповера и тултипа графика. Всплыл тем же вечером: тултипы чипов в модалке фильтра «Текущего портфеля» создавались, помечались видимыми и не рисовались |
| Tooltip | 2.007 | `show()` передаёт в `DSFloat.mount` якорь — цель тултипа: `DSFloat.mount(tip, { anchor: target })`. Своей проверки «а не в модалке ли я» рантайм не держит, решает слой. Спека (инвариант + диагностика), чит-шит, страница обновлены | Тултип внутри модалки был не виден вовсе — ни на усечённом тексте, ни на счётчике «+N». Проверено на стенде: до правки элемент получал `is-visible`, но монтировался в общий слой под скримом |
| ContextMenu | 1.009 | `open()` передаёт якорь (триггер) в `DSFloat.mount`. Спека, чит-шит, страница обновлены | Меню, открытое из модалки, рисовалось под подложкой и попадало под `inert` — то есть не только не видно, но и не кликается |
| Popover | 1.007 | `open()` передаёт якорь (триггер) в `DSFloat.mount`. Спека, чит-шит, страница обновлены | То же, что у ContextMenu: поповер из модалки уходил в общий слой под скрим |
| Chart | 1.002 | Тултип графика передаёт якорь — корень графика (`inst.root`). Спека, чит-шит, страница обновлены | График внутри модалки показывал тултип под подложкой |
| DropdownList | 1.012 | Точечная починка 1.009 заменена общим правилом: вместо `{ parent: field.closest(".modal-scrim") }` — `{ anchor: field }`, выбор родителя делает слой. Поведение не изменилось. Спека, чит-шит, страница обновлены | Знание «из модалки монтируйся в скрим» жило в одном компоненте и потому не защищало остальные четыре. Один способ вместо двух: расхождение при следующей правке исключено |
| Chip | 1.017 | Тултип на усечённой подписи чипа реализован в `scripts/ds-chip.js` и стал правилом самого компонента — работает везде, где стоит чип. Привязка ленивая, делегированием: по первому `pointerover` на `.chip__label` (или `focusin` на чипе) подпись получает `data-tooltip` с полным текстом, `data-tooltip-truncated="only"`, `data-tooltip-multiline="yes"` и привязку `ds-tooltip.js`; первый показ рантайм зовёт сам, потому что `mouseenter`, на который подписывается тултип, к этому моменту уже отработал. Клавиатура: фокус получает чип, а не подпись, поэтому показ и парное скрытие по `blur` рантайм ведёт сам — иначе тултип завис бы на экране. `title` цели забирается в текст тултипа и снимается, чтобы поверх не всплыл нативный. Счётчики `[data-tc-count]`/`[data-inp-count]` исключены — у них собственный тултип со списком скрытых значений от владельца стека. Disabled-чип событий не даёт (`pointer-events: none`), для него — проход при загрузке и экспорт `DSChip.refresh(root)`: усечённой подписи ставится `.chip--has-tooltip` (хук в CSS был заведён давно, но его никто не ставил). Спека/чит-шит/страница обновлены | Правило записано в спеке Chip с первой версии («текст уходит в многоточие, а при наведении показываем тултип с полной версией»), а кода за ним не было — тот же класс дефекта, что InputText 1.010 и InputAutocomplete 1.013. Единственная реализация жила в `ds-table.js` и только для чипов внутри ячейки, из-за чего казалось, что правило работает: в таблице чип объяснял себя, а в поле фильтра, в баре фильтра и в тайле обрезался молча. Вскрылось на «Текущем портфеле ДИД»: в модалке фильтра значения статусов не читались целиком. Владелец правила теперь один — рантайм Chip |
| InputAutocomplete | 1.015 | Счётчик «+N» в поле получил обязательный тултип: `scripts/ds-input.js` перечисляет в нём скрытые значения через запятую, то же значение уходит в `aria-label` («Ещё N: …», раньше было «Ещё N — показать все значения»). Разметка тултипа строится заранее (`DSTooltip.make`) и передаётся в `DSTooltip.bind` параметром `tip`. Спека/чит-шит/страница обновлены | Требование тултипа у «+N» стоит в спеке ReadOnlyField как общее для счётчика («по hover и focus перечисляет через запятую названия всех скрытых чипов»), но поле показывало только число: какие значения свёрнуты, узнать было нельзя. Параметр `tip` обязателен вместо привязки по умолчанию: та оборачивает цель в `.tip-anchor`, а на прямых детях `.inp__chips` завязаны и рантайм (перебор `children` в `valueChips`, наблюдатель состава чипов), и CSS `.inp__chips .chip--fit:last-child` — обёртка сломала бы счёт чипов и раскладку счётчика. `overflow: hidden` стека тултип не режет: на показе `ds-tooltip.js` уносит его в общий слой `DSFloat` |
| TableCell | 2.018 | Из `TRUNC_SEL` в `scripts/ds-table.js` убраны подписи чипов — таблица навешивает тултипы усечения только на значение ячейки (`.tc__text--truncate`) и подпись колонки (`.th__label`). Собственный тултип счётчика `[data-tc-count]` со списком скрытых значений не изменился. Спека/чит-шит/страница обновлены | Дубль правила: с Chip 1.017 усечённую подпись чипа обслуживает рантайм Chip по всей странице. Две реализации одного правила разошлись бы при первой же правке, а до этого вводили в заблуждение — по таблице казалось, что чип объясняет себя везде |
| TableCell | 2.017 | Свёртка стека чипов в ячейке в чип-счётчик «+N» — правило перенесено в рантайм `scripts/ds-table.js` (`chipOverflow`, экспортирован в `DSTable`). Работает от разметки: два и более чипа в одном контейнере ячейки (`.tc__row`, `.tc__controls`) считаются по ширине колонки, не поместившиеся получают `hidden`, последним встаёт чип `[data-tc-count]` того же размера и формы с `.chip--fit`, тултипом со списком скрытых значений и `aria-label` «Ещё N: …». Один чип не сворачивается (усекает подпись внутри плашки), не помещается даже один — остаётся только счётчик. Пересчёт на `wire()`/`refresh()`/`rowsChanged()` и по общему `ResizeObserver` на ячейках: ширину колонки меняют ручка `.th__resize` и перенос колонок, событий об этом нет. Прячется элемент ряда, а не сам чип: `ds-tooltip.js` оборачивает цель тултипа в `.tip-anchor`, и пустая обёртка иначе продолжала бы занимать ширину. Подпись «+N» исключена из ключа сортировки (`cellText`) и из тултипов усечения (`TRUNC_SEL`). Спека/чит-шит/страница обновлены, на странице заведено живое демо «Свёртка чипов «+N»» | Чип в ячейке несжимаем по правилу самого Chip (`.chip--fit`: код валюты и тег категории обязаны читаться целиком), поэтому стек из нескольких чипов в узкой колонке выезжал под соседнюю ячейку — а фон ячеек непрозрачный, и лишние значения просто пропадали под соседом, без всякого признака, что они есть. В полях ввода это же правило уже реализовано (`ds-input.js`, InputAutocomplete 1.013) и записано требованием у ReadOnlyField; ячейка оставалась единственным местом, где стек чипов обрывался молча. Вскрылось на «Текущем портфеле ДИД»: колонка «Валюты» с 2–4 кодами. Проверено на реальной разметке экрана без браузера (linkedom + рантаймы `ds-table.js` и `ds-tooltip.js`, модель ширин): свёртка, тултип, возврат чипов при расширении колонки, отсутствие дублей счётчика при повторном пересчёте, чистый ключ сортировки |
| Все компоненты | см. список ниже | Проставлены парные `[hidden] { display: none; }` всем 80 корням компонентов в 44 файлах `styles/*.css` — одним блоком после ведущего комментария файла, с разбором механики. Правило линтера **B11** поднято с INFO до **WARN**: долг закрыт, теперь оно охраняет ноль, и новый компонент без пары попадёт в отчёт сразу. Соглашение записано в `MAINTAINING.md` (новый раздел «Контракт CSS компонента») и в преамбулу `specs/_cheatsheet.md` — один общий абзац вместо строки в каждом из 47 блоков. В каждую спеку добавлен инвариант, у `specs/Illustrations.md` и `specs/Spacing.md` (нет раздела «Инварианты») — заметкой перед первым разделом. Версии подняты у 47 компонентов, `src-code-css` пересобран на 46 раскатанных страницах через `docs-split.mjs inject`. Файл `styles/table-settings.css` правлен вместе с остальными; собственной строки в манифесте у него нет — он часть Table (модалка «Настройка таблицы»). Заодно сведён накопленный рассинхрон: у части компонентов спека и шапка страницы стояли на версии, отличной от манифеста; теперь все три источника совпадают, проверено сверкой 47 из 47 | Один и тот же дефект чинили поштучно шесть раз, всегда по факту поломки на живом экране — и каждый раз это стоило пользователю сессии на поиск. Отличить статикой «этот класс реально переключают атрибутом» от «не переключают» нельзя: элемент почти всегда получают через переменную или чужую функцию, а не селектором в точке присваивания `.hidden` (разметочный признак ловил 1 случай из 6, признак «файл трогает .hidden и упоминает класс» давал 49 находок с очевидными ложняками). Поэтому вместо поиска виновных класс дефекта закрыт целиком, по доктрине `ds-rules.md` §9 «что нельзя измерить — делаем структурно невозможным». Цена соблюдения — одна строка на компонент; цена несоблюдения — молчаливая поломка, которую видно только на живом экране. Проверено дымовым прогоном по всем 57 страницам ДС и экрану портфеля: базовый `display` компонентов на месте, `hidden` скрывает и снимается без остатка, ошибок в консоли нет; правило проверено откатом фикса на `.chip` (уровень INFO) и на `.badge` (уровень WARN) |
| DropdownList | 1.010 | `open()` в `scripts/ds-dropdownlist.js` больше не назначает активную опцию: раньше подсвечивалась выбранная, а при её отсутствии — первая строка. Выбранная опция при открытии по-прежнему подкручивается в зону видимости (`scrollIntoView`), но без `.is-focus` и без `aria-activedescendant`. Клавиатура с чистого листа: ↓ даёт первую опцию, ↑ — последнюю, а если значение выбрано — отсчёт от него (раньше ↑ с пустой активной опцией давала предпоследнюю). Спека/чит-шит/страница обновлены | Пользователь: список открывается с уже наведённой первой строкой. Фокус на экране должен быть один и он на поле-триггере — список это popup, реальный фокус из поля не уходит. Подсветка строки до первого шага клавишами читается как второй фокус и как «уже выбрано» |
| Линтер | — | Заведено правило **B11** в `scripts/ds-lint.js`: корень компонента (класс без `__` и `--`), объявляющий `display`, обязан объявить и парное `.x[hidden] { display: none }`. Проверяются только CSS компонентов, хром документации (`CSS_NOT_IN_BUNDLE`) не в счёт; исключения с причиной — в новом белом списке `HIDDEN_PAIR_OK` (`.screen`, `.nav-layout`, `.chart-host`). Уровень INFO: на вердикт и код выхода не влияет. Первый прогон: **80 корней в 44 файлах**. Правило и разбор инцидента внесены в `skills/ds-integrity-check.md` | Один и тот же дефект чинили поштучно шесть раз (Modal 1.005, TableCell 2.015, Tab 1.010, EmptyState 1.002, InputText 1.010, Chip 1.015) — всегда по факту поломки на живом экране, потому что он молчаливый: разметка правильная, скрипт ставит атрибут, элемент виден. Адресный поиск «кто реально переключает `hidden`» отброшен: разметочный признак ловит 1 случай из 6 (все реальные дефекты рантаймовые), а признак «файл трогает `.hidden` и упоминает класс» даёт 49 находок с очевидными ложняками — без межфункционального анализа связь не восстановить. Поэтому правило структурное, по доктрине `ds-rules.md` §9. Проверено откатом фикса на `.chip`. Уровень INFO, а не WARN первого цикла: 80 предупреждений утопили бы отчёт с базовым уровнем в 2 WARN |
| InputText | 1.010 | Заведён общий рантайм семейства полей — `scripts/ds-input.js` (новый файл, вписан в `scripts/ds.js` после `input-kit.js`; страницы документации подключают поштучно). Даёт из коробки: крестик очистки, видимый ровно при наличии значения (текст в контроле ИЛИ чипы в `.inp__chips`), автосоздание крестика там, где его нет в разметке (порядок действий из спеки: информер → крестик → календарь → шеврон), очистку с событием `ds-input:clear` на `.inp` плюс `input`/`change` на контроле. В `styles/input.css` добавлено парное `.inp__act[hidden] { display: none }`. Демо-поля документации помечаются `data-input-static` (`scripts/input-kit.js`, флаг снимается `live: true`) и рантаймом не обслуживаются. Спека/чит-шит/страница обновлены, `src-code-css` пересобран через `docs-split.mjs inject` | Правило «крестик очистки появляется при наличии значения» было записано в спеке, но реализовано нигде: видимостью крестика управляла только разметка. На живом экране это давало обе ошибки сразу — крестик на пустом поле и отсутствие крестика у поля с выбранным значением (портфель ДИД, модалка фильтра: у пяти полей крестик стоял в разметке всегда, у остальных двенадцати его не было вовсе). Рантайма поля в ДС не существовало — `input-kit.js` это построитель демо для страниц документации, а не поведение |
| InputAutocomplete | 1.013 | Реализовано сворачивание переполнения чипов в счётчик «+N» (`ds-input.js`): не поместившиеся чипы скрываются атрибутом `hidden`, последним встаёт `.chip--fit[data-inp-count]` без крестика удаления; пересчёт по составу чипов (MutationObserver) и по ширине поля (ResizeObserver), в несколько проходов — ширину стека задаёт flex и она меняется после сворачивания. Последнее видимое значение помечается `data-inp-last`, для него в `styles/input.css` заведено `flex: 0 1 auto; min-width: 48px`. Демо «Переполнение → «+N»» на странице сделано живым (`scripts/input-autocomplete.page.js`) вместо нарисованного чипа `'+3'`. Спека/чит-шит/страница обновлены | Правило «переполнение поля → чип-счётчик «+N»» стояло в спеке с первой версии, но кода за ним не было: `.inp__chips { overflow: hidden }` просто обрезал лишние чипы, и сколько значений выбрано — не читалось. Демо на странице показывало нарисованный чип `'+3'`, то есть документация иллюстрировала поведение, которого не существовало. Через `:nth-last-child` правило не выражается — в счёт попадают скрытые чипы, поэтому последний видимый помечается атрибутом |
| DatePicker | 1.007 | Маска ручного ввода ДД.ММ.ГГГГ переехала в рантайм `scripts/ds-datepicker.js` — делегированный слушатель `input` на `document` в фазе ПЕРЕХВАТА. Поле даты узнаётся структурно: кнопка `aria-label="Открыть календарь"` или обёртка `.inp-range--date`; отказ на конкретном поле — `data-no-datemask` на `.inp`. `maskValue(raw)` добавлена в `window.DSDatePicker`. Из `scripts/input-date.page.js` удалена собственная копия маски. Спеки DatePicker/InputDate/InputDateRange, чит-шит и страницы обновлены | Маска существовала ТОЛЬКО в `input-date.page.js` — демо-скрипте страницы документации — и на экраны не попадала вовсе. Поле показывало плейсхолдер «ДД.ММ.ГГГГ», но принимало любой текст без точек: набранное руками значение не разбиралось ни календарём, ни фильтром потребителя (портфель ДИД, секция «Сроки»). Фаза перехвата обязательна: обработчики потребителя висят на самом контроле, и в фазе всплытия маска опаздывала бы на символ — фильтр читал бы ещё не нормализованное значение |
| Chip | 1.015 | Добавлено `.chip[hidden] { display: none; }` в `styles/chip.css`. Спека/чит-шит/страница обновлены, `src-code-css` пересобран | Шестой случай того же класса дефекта (Modal 1.005, TableCell 2.015, Tab 1.010, EmptyState 1.002, InputText 1.010): `.chip { display: inline-flex }` той же специфичности (0,1,0), что и браузерное `[hidden]`, побеждает по порядку подключения. Свёрнутые в «+N» чипы прячутся именно этим атрибутом — без правила сворачивание не работало бы вовсе |
| Elevation | 1.001 | Правило `.ds-float-layer { pointer-events: none; }` перенесено из инлайна в `scripts/ds-float.js` в `styles/shadow.css`, с описанием, почему слой намеренно без `position` и `z-index`. Спека получила раздел «Общий слой плавающих элементов», версия страницы поднята, `src-code-css` пересобран | Класс, который вешает рантайм, обязан иметь правила в `styles/*.css` — иначе гейт парности линтера (P4) справедливо считает его сиротой. Слой относится к механике наложения, поэтому живёт в основе Elevation, а не отдельным файлом |
| Инфраструктура | — | В `styles.css` добавлен пропущенный `@import url("styles/table-settings.css")` (в `ds.css` он был). Блокер линтера A3 закрыт | Точка подключения ДС раздвоена: `ds.css` для экранов, `styles.css` для витрины. Файл компонента, попавший только в одну из них, на второй точке отрендерился бы без стилей — правило A3 ловит ровно это |
| EmptyState | 1.002 | Добавлено `.es[hidden] { display: none; }` в `styles/empty-state.css`. Спека/чит-шит/страница обновлены (инвариант + строка диагностики), `src-code-css` пересобран через `docs-split.mjs inject`. Заодно устранён рассинхрон версии: спека стояла на 1.001, `specs/_index.md` — на 1.000 | Четвёртый случай того же класса дефекта (после Modal 1.005, TableCell 2.015, Tab 1.010): базовое `.es { display: flex }` той же специфичности (0,1,0), что и браузерное `[hidden] { display: none }`, побеждает по порядку подключения — атрибут `hidden` на компоненте не работал вовсе. Вскрылось на экране «Текущий портфель ДИД»: пустое состояние «Сделки не найдены» висело под таблицей всегда, хотя разметка и скрипт экрана были корректны |
| Table | 1.013 | Липкая шапка переведена с ячеек на строку: в `styles/table.css` добавлено `.dtable__body .tbl > .tbl__row--head { position: sticky; top: 0; z-index: 3 }`. Класс `.tbl__row--head`, до сих пор бывший необязательным маркером, стал **обязательным на строке шапки внутри `.dtable__body`**; правило `.dtable__body .th` оставлено — оно держит слой над проезжающими строками и горизонтальное закрепление `.th--pinned`. Класс проставлен во всех потребителях: `scripts/table.page.js` (генератор шапки демо), `Projects/test/FundList.html`, `Projects/test/DealPipelineCard.html`, `Projects/post/operations-did/Portfolio-did/Portfolio.html`. Спека/чит-шит/страница обновлены (инварианты, диагностика, справочник классов, текст раздела «Поведение», эталон HTML), `src-code-css` пересобран через `docs-split.mjs inject`; в `specs/TableCell.md` и `pages/organisms/TableCell.html` добавлена кросс-ссылка | `position: sticky` на `.th` по вертикали не работал никогда: `.th` — grid-элемент строки (`.tbl__row { display: grid }`), и блок-контейнер, ограничивающий его sticky-смещение, — сама строка; по вертикали её высота равна высоте шапки, двигаться некуда. По горизонтали то же правило работает, потому что строка шире скроллпорта (`min-width: max-content`) — из-за этого закрепление колонок было исправным, и дефект вертикали не всплывал. Заявленный инвариант «липкая шапка не отключаема» на деле не выполнялся ни на одном экране с прокруткой тела; вскрылось на «Текущем портфеле ДИД» — при скролле реестра шапка уезжала вверх. Проверено в браузере: после правки шапка стоит на месте при вертикальном скролле, закреплённые колонки продолжают держаться при горизонтальном, шапка рисуется поверх строк |
| DropdownList | 1.009 | В `scripts/ds-dropdownlist.js` `open()` монтирует список не в общий слой `DSFloat`, а в скрим своей модалки, если поле-триггер лежит внутри неё: `DSFloat.mount(list, { parent: field.closest('.modal-scrim') || null })`. Спека/чит-шит/страница обновлены (инвариант + строка диагностики) | Слой `DSFloat` лежит в корне `body` и намеренно не создаёт stacking context, чтобы плавающие элементы поднимались над липкой шапкой таблицы. Но `.ddl--floating` несёт `z-index: 40`, а `.modal-scrim` — `1000`: список, открытый из поля внутри модалки, рисовался под её подложкой. Вторым слоем добавлялся `inert`, который `DSModal.lockPage()` вешает на всех детей `body`, кроме скримов, — уже созданный общий слой становился недоступен указателю. Вскрылось на «Текущем портфеле ДИД»: в модалке фильтра не работало ни одно поле выбора, кроме дат (DatePicker `DSFloat` не использует). Проверено в браузере настоящими кликами мыши: мультивыбор, одиночный выбор, повторное открытие модалки |
| Chip | 1.014 | `scripts/ds-chip.js` больше не трогает чип `.tfilter__applied` — ни по клику на крестик, ни по Backspace/Delete. Спека/чит-шит/страница обновлены (инвариант + строка диагностики); строка диагностики добавлена и в `specs/TableFilter.md` | Оба рантайма слушают клик делегированием на `document`, и `ds-chip.js` в `ds.js` стоит раньше `ds-table-filter.js`. Он снимал чип «Применено: N» первым, после чего `ds-table-filter.js` получал уже оторванный от DOM узел: `chip.closest('.tfilter')` давал `null`, и событие `tfilter:reset` наружу не уходило. Визуально чип исчезал, а фильтр оставался применённым — ровно то, что видел пользователь на «Текущем портфеле ДИД». Владелец чипа один — TableFilter, он же держит анимацию снятия |
| TableCell | 2.015 | Добавлено `.tbl__row[hidden] { display: none; }` в `styles/table-cell.css`. Спека/чит-шит/страница обновлены (инвариант + строка диагностики), `src-code-css` пересобран через `docs-split.mjs inject` | Строка, скрытая атрибутом `hidden`, не пряталась: базовое `.tbl__row { display: grid }` той же специфичности (0,1,0) перебивало браузерное `[hidden] { display: none }` — тот же класс дефекта, что чинили в Modal 1.005 (`.modal-scrim[hidden]`). Дефект был скрытым багом и в самой ДС: `treeToggle` в `ds-table.js` сворачивает поддерево дерева именно через `r.hidden`, то есть сворачивание дерева в демо не работало. Вскрылось при подготовке живого реестра сделок (портфель ДИД, `Projects/post/operations-did`): пагинация должна прятать строки вне текущей страницы через `hidden`, а фильтр — строки, не прошедшие отбор |
| Table | 1.012 | В `scripts/ds-table.js` у api, которое возвращает `DSTable.wire()`, добавлен метод `rowsChanged()` — переустанавливает `tbl.__dsRowOrder` (исходный порядок для `data-sort-rows`) и довешивает тултипы усечения на новые ячейки; существующий `refresh()` состав строк не трогает намеренно. Спека/чит-шит/страница обновлены (JS-выжимка `src-code-js`); заодно устранён рассинхрон версии, накопившийся 04.09 (`specs/Table.md` уже стоял на 1.011, `specs/_index.md` и страница — на 1.010; расхождение закрыто установкой всех трёх на 1.012) | У живого реестра (создание сделки без перезагрузки страницы) новая строка добавляется в DOM мимо рантайма. Без публичного способа сообщить об этом `data-sort-rows` рантайм сортировал бы только состав, зафиксированный при первом `wire()`, — добавленная строка не участвовала бы в сортировке и не получала бы тултип усечения |
| DatePicker | 1.006 | `formatDate`/`parseDate` в `scripts/ds-datepicker.js` переведены с ММ.ДД.ГГГГ (месяц первым) на ДД.ММ.ГГГГ (день первым) — маска, автоподключение календаря к полю и ручной ввод теперь читают/пишут дату в одном порядке. Спека обновлена (инвариант + диагностика), заодно устранён рассинхрон версии в `specs/_index.md` (1.004 при спеке 1.005) | Обнаружено при подключении `InputDateRange` к реальной фильтрации реестра сделок (Портфель ДИД, Этап 4): `ds-datepicker.js` писал/читал дату месяц-первым, а сортировка дат в TableCell (`data-sort-type="date"`) и все реальные даты сделок на этом же экране — день-первым. Календарь и типизированный ввод давали значение, не совпадающее с тем, что показывает и сортирует таблица, — фильтр по датам был бы либо неверным, либо неработающим для дат с числом дня >12 |
| InputDate | 1.010 | Маска и placeholder переведены с ММ.ДД.ГГГГ на ДД.ММ.ГГГГ везде: спека, чит-шит, `index.html`, страница компонента (текст + `src-code-js`), `scripts/input-kit.js` (плейсхолдер фабрики), `scripts/input-date.page.js` (демо-значения `12.21.2022`→`21.12.2022`, `10.15.2025`→`15.10.2025`, пример маски `«12212022»→«12.21.2022»` на `«21122022»→«21.12.2022»`) | Следствие правки DatePicker 1.006 — маска поля обязана совпадать с порядком, который реально пишет/читает `ds-datepicker.js`, иначе placeholder и введённое значение расходятся |
| InputDateRange | 1.007 | Маска переведена на ДД.ММ.ГГГГ: спека, чит-шит, страница компонента, `scripts/input-date-range.page.js` (`MASK` + пример маски). Демо-значения (`01.01.2021`, `31.12.2021`) уже были корректны в обоих порядках — не менялись | Тот же дефект, что и в InputDate — компонент оборачивает два InputDate, маска должна остаться единой |
| TableCell | 2.016 | Плейсхолдер демо-поля «Table Edit → Дата» переведён с ММ.ДД.ГГГГ на ДД.ММ.ГГГГ (`pages/organisms/TableCell.html`, `scripts/table-cell.page.js`) | Плейсхолдер живой ячейки-даты в документации TableCell расходился с собственным правилом компонента (`data-sort-type="date"` сортирует день-первым) и с новым единым порядком InputDate/DatePicker |
| Tab | 1.010 | Добавлено `.tab__badge[hidden] { display: none; }` в `styles/tab.css`. Спека/чит-шит/страница обновлены | Тот же класс дефекта, что чинили в Modal 1.005 и TableCell 2.015: `.tab__badge { display: inline-flex }` той же специфичности (0,1,0), что и браузерное `[hidden]`, побеждает по порядку подключения — счётчик применённых фильтров секции (Портфель ДИД, Этап 4) со значением 0 не прятался бы атрибутом `hidden` |
| Tooltip | 2.008 | Добавлен `DSTooltip.truncated(selector, opts)` — делегированный механизм «усечено → тултип»: lazy-привязка по pointerover/focusin, показ только при реальном усечении, `opts.host` (маппинг фокуса с хоста-предка на подпись), `opts.disabled`/`opts.disabledClass` (скан `pointer-events:none` элементов), `opts.skip`, `opts.text`. Поведение извлечено из локальных копий (Chip, Table) в общий слой. Спека/чит-шит/страница обновлены | Правило «короткая строка усеклась → тултип с полным текстом» повторялось в четырёх владельцах своим кодом, а в двух (ds-chip.js, ds-table.js) — двумя почти одинаковыми реализациями. Оно чинилось в одной копии и молча не работало в прочих (тот же класс, что Chip 1.017, где кода за правилом не было вовсе — только внутри ячейки таблицы). Один владелец — слой тултипа |
| Chip | 1.018 | Тултип усечённой подписи переведён на общий `DSTooltip.truncated('.chip__label', …)` — локальная логика (attach / pointerover / focusin) удалена, остались только специфичные для чипа: исключение счётчиков «+N» (opts.skip) и скан disabled-чипов (`refresh()` + `.chip--has-tooltip`). Спека/чит-шит/страница обновлены | Две почти одинаковые реализации правила усечения (этот файл и ds-table.js) разошлись бы с первой правкой. Один владелец вместо двух копий |
| Tab | 1.012 | Вертикальный таб получил тултип усечённой подписи: `DSTooltip.truncated('.tab__label', { host: '.tab', disabled: …, disabledClass: 'tab--has-tooltip' })`. Горизонтальные табы подпись не усекают (ширина по контенту) — isTruncated отсекает, тултип не появляется. Хук `.tab--has-tooltip` (styles/tab.css:347) теперь ставится рантаймом. Спека/чит-шит/страница обновлены | Правило стояло в спеке («У вертикального таба слишком длинный текст обрезается многоточием с тултипом»), кода за ним не было; хук для disabled-таба был заведён в CSS, но никем не ставился — точная копия истории `.chip--has-tooltip` |
| NavPanel | 1.017 | Подписи пункта и футера (ФИО/должность/организация) получили тултип усечения: `DSTooltip.truncated('.nav__label, .nav__user-name, .nav__user-role, .nav__user-org', { host: '.nav__item, .nav__user' })`. В Rail собственный тултип (placeRailLabels) не конфликтует — подпись в Rail не усекается, isTruncated отсекает. Спека/чит-шит/страница обновлены | Спека обещает «усечение многоточием + тултип» для подписи пункта и «ФИО (усечение)» для футера, но в развёрнутом виде текст обрезался молча (rail — свой тултип, развёрнутый — нет). Видно на экране: «Александров Петр Константинович» в футере обрезается без подсказки |
| ReadOnlyField | 1.007 | Заведён общий рантайм `scripts/ds-readonlyfield.js` — регистрирует «усечено → тултип» для значения (.rof__value, одна строка / N строк) и аффиксов (.rof__affix) через `DSTooltip.truncated`. Вписан в `scripts/ds.js`. Копирование по иконке остаётся за `ds-copy.js`. Спека/чит-шит/страница обновлены | Спека обещает «обе версии с тултипом на переполнении», а рантайма у компонента не было вовсе (только ds-copy.js на копирование): длинное значение в карточке обрезалось без подсказки |
| Table | 1.015 | Локальная `truncationTooltips` удалена — усечение ячейки (.tc__text--truncate) и подписи колонки (.th__label) теперь обслуживает общий `DSTooltip.truncated` делегированием; новые строки/колонки подхватываются сами, без перескана в refresh/rowsChanged. Спека/чит-шит/страница обновлены | Собственная копия правила усечения (вторая после ds-chip.js) — та же проблема «двух реализаций». Делегирование снимает и необходимость перепрохода при добавлении строк |
| Инфраструктура | — | В YAML-шапки 6 спек добавлено поле `runtime:`, которого недоставало: ContextMenu (`js:` → `runtime:`), DatePicker, InputDate, InputDateRange, InputAutocomplete, Illustrations. Поле описано в `specs/_TEMPLATE.md`. Версии этих компонентов не подняты — правка метаданных, поведение не менялось | Шапка спеки — единственное машиночитаемое место, где сказано, чем компонент оживает; без поля сборщик, читающий шапку, делал вывод «рантайма нет» и писал свой код — против главного правила проекта. У ContextMenu поле называлось `js:` вместо `runtime:` — рассинхрон с остальными 26 спеками |

**Компоненты, у которых поднята версия (47):** Alert 1.010 · AllocationBar 1.002 · Avatar 1.005 · Badge 1.003 · Breadcrumbs 1.006 · ButtonGroup 1.004 · Buttons 1.011 · ButtonTest 1.012 · Chart 1.001 · Checkbox 1.007 · Chip 1.016 · ContextMenu 1.008 · DatePicker 1.008 · Divider 2.001 · DropdownList 1.011 · Entity 1.007 · IconButton 1.010 · Illustrations 1.004 · InputAmountRange 1.006 · InputAutocomplete 1.014 · InputDate 1.012 · InputDateRange 1.009 · InputText 1.011 · LabelHelper 1.005 · Modal 1.007 · NavPanel 1.016 · NavTile 1.011 · PageHeader 1.010 · Pagination 1.010 · Popover 1.006 · ProgressBar 1.003 · Radiobutton 2.008 · ReadOnlyField 1.006 · RiskMetric 1.007 · SegmentControl 1.006 · Skeleton 1.002 · SnackBar 1.005 · Spacing 1.005 · Spinner 1.001 · Splitter 1.004 · Switch 1.008 · Tab 1.011 · Table 1.014 · TableFilter 1.010 · Tile 1.011 · Toast 1.007 · Tooltip 2.006

## 04.09.2026

| Компонент | Версия | Что изменилось | Почему |
|---|---|---|---|
| Modal | 1.006 | Убрано `overflow-y:auto` со `.modal-scrim`: скрим поверх вьюпорта больше не скроллит модалку целиком. Центрирование переведено с `align-items/justify-content:center` на `margin:auto` у `.modal` (безопасное: при переполнении прижимается к началу, не клипает верх). Высота ≤ 80vh и скролл только `.modal__body` — как и было по правилу. Спека/чит-шит/страница обновлены | Скрим считал `scrollHeight` по контентной высоте модалки до ужатия `max-height` (Chrome берёт flex-basis, а не итоговый бокс), и `overflow-y:auto` превращал ложное переполнение в реальную прокрутку всего блока (шапка+тело+подвал ехали вместе). Дефект вскрылся на экране «Текущий портфель ДИД» (модалка фильтра), но источник — в компоненте Modal; экран был собран верно |
| Table | 1.010 | Починена сортировка в `ds-table.js`. (1) `setSort` красил шапку через `b.querySelector('.th__sort-icon')` — класса `.th__sort-icon` нет нигде в репозитории (ни в CSS, ни в разметке, ни в чит-шите), условие `if (g !== b)` всегда ложно, глиф не перерисовывался. Теперь глиф меняется через `data-icon` + `dsIcons.apply` (инлайн-SVG рантайм не пишет), кнопка получает `aria-label` по направлению, а колонка — класс `.th--sorted`: именно он её красит (`table-cell.css`), тогда как рантайм ставил только `aria-sort` и `is-sorted`, на которые в `styles/*.css` нет ни одного селектора. (2) Стартовое направление читается из `aria-sort` разметки при `wire()` — раньше `dataset.sortDir` был пуст и первый клик по заранее отсортированной колонке начинал цикл с `asc`. (3) Шапка определяется структурно (`isHeadRow` — строка, содержащая `.th`); класс `tbl__row--head` остаётся допустимым маркером. (4) Новая опция `data-sort-rows` на `.tbl`: рантайм сам переставляет строки — тип колонки из `data-sort-type` (`date`/`number`/`text`) или по данным, пустые всегда внизу, `dir = none` возвращает исходный порядок, поддерево дерева едет за своим корнем. Мёртвая после правки функция `icon()` удалена (других вызовов не было). (5) Значение ячейки читается не из голого `textContent`: приоритет — `data-sort-value` на `.tc`, затем `.tc__text` и подписи чипов, затем текст ячейки без узлов `[role="tooltip"]`. Спека/чит-шит/страница обновлены; проверено 19 тестами на математику сортировки (типы, даты, числа с NBSP и запятой, `localeCompare` по-русски, пустые и битые значения) и 17 сквозными — реальный `Portfolio.html` с реальными рантаймами иконок, тултипа и таблицы | Пользователь: на экране «Текущий портфель ДИД» клик по стрелкам сортировки не давал никакого отклика. Обработчик срабатывал и цикл крутился, но все три места, где меняется видимое состояние, промахивались мимо разметки ДС — клик менял только `aria-sort` в DOM. Дефект не всплывал, потому что демо-страница `TableCell.html` рисует шапку сама из своего стейта (`table-cell.page.js`) и рантайм не использует, а слушателя события `sort` не было ни на одном экране: работающей сортировки в репозитории не было ни разу. Порядок строк отдан рантайму, а не экрану, — по правилу §5 ds-rules «свой JavaScript вместо рантайма ДС не пишется»; опция явно помечена как механизм макета, потому что реестр с пагинацией обязан сортировать на сервере. Пункт (5) вскрылся при приёмке: с подключённым `ds-tooltip.js` строки не переставлялись — тултип кладёт в ячейку копию значения, `textContent` удваивался («20.04.202420.04.2024»), под маску даты не подходил, ключ выходил `NaN` у всех строк и компаратор возвращал ноль на каждой паре. Рантаймы ДС дописывают разметку внутрь чужих слотов, поэтому значение для сортировки берётся из семантического узла, а не из всего текста ячейки |
| TableCell | 2.014 | Уточнены строки «Сортировка» и «Фокус строки» в таблице взаимодействий: ключ поля в `data-sort`, подсветка `.th--sorted`, чтение стартового направления из `aria-sort`, опция `data-sort-rows`, необязательность класса `tbl__row--head`. Обновлены чит-шит, шапка версии и комментарий во вкладке «Код» страницы. Поведение самого TableCell не менялось | Поведение сортировки и фокуса строки документировано в TableCell, а чинилось в рантайме Table — без синхронной правки текста спека расходилась бы с кодом |
| Чек-листы под реальный инструментарий | — | Шаги, ссылавшиеся на инструменты внешней среды (`ready_for_verification`, `check_design_system`, `copy_files`, `run_script`, `str_replace_edit`, `write_file`, `list_files`, `eval_js`, `multi_screenshot`), переписаны на локальные команды в `MAINTAINING.md`, `skills/ds-integrity-check.md`, `skills/mockup-fidelity-review.md`, `skills/screen-from-mockup.md`. В `MAINTAINING.md` добавлен раздел «Проверка: чем гонять» — таблица всех реальных команд с назначением и кодами выхода. Заведены две обёртки по образцу `ds-lint-cli.mjs`, дающие неисполняемым скриптам ДС хелперы `readFile`/`ls` через `node:fs`: `scripts/ds-icon.mjs` (`<имя>` / `--list [подстрока]`) достаёт один глиф из `icons-data.js`, не читая файл целиком — прежний рецепт был однострочником с regex через оболочку и не работал ни в bash, ни в PowerShell (проверено, обе падают на экранировании); `scripts/ds-registry-cli.mjs` генерирует `registry.json` для проекта продукта одной командой (`--rules`, `--root`, `--out`, `--components`/`--screens`/`--requirements`) — соответственно переписан шаг 2 в `templates/admin/README.md`. Обе прогнаны: `ds-icon.mjs` во всех трёх режимах и в обеих оболочках (247 глифов), `ds-registry-cli.mjs` — на `Projects/post`. Из корня ДС удалены остальные артефакты витрины: `thumbnail.html`, `.thumbnail`, `_adherence.oxlintrc.json`; мёртвый `thumbnail.html` убран из `SKIP_ALL` линтера. Проверено: вердикт `ds-lint-cli` (глобальный и `--parity`) до и после совпадает, гейт парности отдельно сверен восстановлением удалённого файла | Чек-листы описывали шаги, которые невозможно выполнить: инструментов внешней среды в этом контуре нет, и агент упирался в них каждый заход, а затем дописывал в отчёт «остаток». Формально требования выполнялись «на словах», фактически проверка не запускалась. Теперь каждый шаг — команда, которую видно, что она отработала, по коду выхода |
| Артефакты витрины | — | Из корня ДС удалены `_ds_bundle.js` (2 МБ, сборка 68 файлов `scripts/*.js` в один IIFE) и `_ds_manifest.json` (индекс карточек `@dsCard`). Требование их пересобирать снято из `.opencode/rules/ds-rules.md` §8, урок Л25 (`lessons.md`, `lessons-raw.md`) закрыт и переписан, в `MAINTAINING.md` добавлена строка «скомпилированных артефактов витрины в репозитории нет», ссылки на бандл убраны из комментариев `scripts/ds-nav.js` и `scripts/ds-toc.js`. Маркеры `@dsCard` на страницах сохранены — их проверяет линтер (C1). Вердикт линтера до и после правок совпадает | Файлы были производными внешнего инструмента, а не исходниками: их собирала сама витрина при загрузке источников, локального компилятора не существует. В репозитории они не использовались ничем — ни одна страница их не подключала (`grep` по всем `.html` пуст), линтер и сенсоры их не читают. При этом расхождение накопилось капитальное (14 файлов `scripts/` новее бандла, 4 в него никогда не входили, 3 страницы без карточки в манифесте), и правило «пересобрать компилятором» порождало невыполнимый «остаток» в каждом отчёте. Мёртвый артефакт удалён вместе с порождавшим его правилом |
| Правила ведения ДС | — | Файл правил ведения ДС в корне переименован в `MAINTAINING.md`. Имя `DS-RULES.md` рассматривалось и отвергнуто: путалось бы с `.opencode/rules/ds-rules.md` — это разные файлы, правила *ведения* ДС против правил *работы* с ДС при сборке экранов. Обновлены ссылки: `scripts/ds-lint.js` (чтение файла + переименована переменная, куда он читается — теперь `dsRules`), `_ds_bundle.js`, `readme.md`, `SKILL.md`, `skills/ds-integrity-check.md`, `skills/screen-from-mockup.md`, `specs/_TEMPLATE.md`. Канонический список h2 читается как прежде — 12 пунктов, сверено с копией до правки | Имя файла привязывало репозиторий к конкретному агентному инструменту. Целевой инструмент — opencode на локальной модели, репозиторий уезжает на рабочий контур; имён сторонних вендоров в нём быть не должно. Содержимое файла не изменилось |
| Tooltip | 2.005 | (1) Якорь `.tip-anchor` стал раскладочно прозрачным: добавлены `min-width: 0; max-width: 100%`. Рантайм оборачивает якорем уже свёрстанную цель, а inline-flex без `min-width:0` получает автоминимум = min-content содержимого — якорь переставал сжиматься, и `text-overflow: ellipsis` у цели не срабатывал: текст выезжал за границы контейнера. (2) Рантайм читает `data-tooltip-multiline="yes"` — длинный текст переносится по `--tip-max` вместо усечения самого тултипа. Спека/чит-шит/страница обновлены | Тултип, подключённый к усечённому значению ячейки, ЛОМАЛ это усечение: наименование сделки в реестре ДИД шло в одну строку без многоточия и уезжало под соседнюю колонку (непрозрачный фон соседней ячейки обрезал его без многоточия). Второй дефект: сам тултип с полным значением тоже усекался многоточием — подсказка не выполняла свою работу |
| TableCell | 2.010 | В инварианты добавлено: контент никогда не выходит за границу ячейки; идентификатор (номер сделки, ИНН, № договора) выравнивается по левому краю — `.tc--numbers` только для величин, которые сравнивают по столбцу. Спека/чит-шит/страница обновлены | Правило про идентификаторы фиксируется, потому что номер сделки в портфеле ДИД был свёрстан как число (правый край); сжимаемость чипа закрыта в самом Chip (1.010), а не частным правилом таблицы |
| Table | 1.009 | `truncationTooltips` в `ds-table.js` распространён на всю таблицу: тултип с полным текстом получают не только значения ячеек (`.tc__text--truncate`), но и подписи шапки (`.th__label`) и подписи чипов внутри ячейки (`.tc .chip__label`); всем ставится `data-tooltip-multiline="yes"`. В инварианты добавлено: тултип на усечённом тексте — общее поведение таблицы, а не разметка экрана | Пользователь: «тултип — это общее поведение для всех ячеек». Раньше усечённая подпись колонки («Статус Operati…») и усечённый статус в чипе не объясняли себя ничем |
| Chip | 1.010 | `.chip` получил `min-width: 0`. Чип несёт данные переменной длины и рассчитан на усечение подписи (`.chip__label` с ellipsis), поэтому обязан сжиматься в чужой flex-строке. Инвариант добавлен в спеку и чит-шит | Статусные чипы в узких колонках реестра ДИД обрывались посреди слова без многоточия: `inline-flex` без `min-width:0` имеет автоминимум = min-content, чип не сжимался, вылезал за колонку, и его подпись обрезал непрозрачный фон соседней ячейки. Правило вынесено в сам компонент, а не в частное правило таблицы — дефект воспроизводился бы в любом тесном flex-контейнере |
| TableCell | 2.011 | `min-width: max-content` снято с общего `.tbl__row` и ограничено прокручиваемой таблицей (`.tbl--scroll .tbl__row`, `.dtable__body .tbl__row`). Спека/чит-шит/страница обновлены; закрепление проверено замером позиций колонок при `scrollLeft` 0 → 600 → 2000 → до упора → назад: закреплённые держатся на left=8 и left=158, зазор между ними 0, `scrollWidth` 3856 — цифры совпадают с состоянием до правки до пикселя | Правило добавлено 03.09 для закрепления колонок, но стояло на ВСЕХ строках без разбора. У таблицы без горизонтального скролла под `max-content` доля-трек (`fr`) разворачивается по содержимому: колонка растягивается под самый длинный текст, усечение (`.tc__text--truncate`) не срабатывает вовсе, а строка вылезает за контейнер. Доли использованы в 73 файлах документации и 21 месте в экранах — усечение в них не работало нигде; видно на демо «Реестр сделок» страницы TableCell |
| TableCell | 2.012 | `.th__label` (подпись колонки шапки) переведена с однострочного эллипсиса (`white-space: nowrap`) на line-clamp 2: имя переносится до двух строк и только затем усекается многоточием; полное значение остаётся в тултипе. Спека/чит-шит/страница обновлены, `src-code-css` через `docs-split.mjs inject` | Имя колонки уходило в многоточие первой же строкой, что обрезало названия до неузнаваемости («Статус Operati…»). По решению пользователя подпись должна переноситься на вторую строку и лишь потом усекаться |
| ds-integrity-check (живые проверки) | — | Две новые проверки группы E, меряют результат вместо разбора причин. **E7 `cellBleed`** — содержимое ячейки вылезло за её границы (соседняя ячейка непрозрачна и обрежет текст без многоточия); абсолютно позиционированные потомки не считаются — `.th__resize` и инструменты шапки выходят за границу намеренно. **E8 `rowBleed`** — строка шире режущего её контейнера, который НЕ прокручивается (хвост колонок физически недостижим). Сниппет шага 2 и таблица групп в `skills/ds-integrity-check.md` обновлены | За один день усечение в таблицах сломалось тремя разными способами (якорь тултипа, Chip, `max-content` на `fr`-треках), и во всех трёх разметка была правильной — статический линтер их не видел, дефекты находились только скриншотом и замером `getBoundingClientRect`. Проверки прогнаны откатом каждого из трёх фиксов: ловят все три, на починенной ДС молчат |
| ds-lint | — | Два новых правила группы B. **B8** — компонент, чья подпись усекается многоточием, объявлен `inline-flex` без `min-width: 0`: в чужой flex-строке он не сожмётся и подпись обрежется без многоточия; компонент обязан высказаться — `min-width: 0`, либо `flex: none`/`flex-shrink: 0`, либо строка в новом белом списке `SHRINK_OK` с причиной (заведены Tab и SegmentControl: переполнение решается скроллом ряда и растяжением трека, не сжатием элемента). **B9** — обёртка, которую рантайм вставляет ВОКРУГ чужого элемента (`X.parentNode.insertBefore(W, X)`, находится по исходникам `scripts/ds-*`, `tbl-*`, `input-kit`), обязана быть раскладочно прозрачной: `max-width: 100%` и, для flex, `min-width: 0`. Разбор инцидента и обе проверки описаны в `skills/ds-integrity-check.md` | Оба дефекта 04.09.2026 были невидимы для линтера и диагностировались только скриншотом: разметка экрана правильная, ломает её слой ниже. Правила проверены откатом фиксов — ловят ровно исходные дефекты, по остальной ДС ложных срабатываний нет |
| Экран «Текущий портфель ДИД» | — | Колонка «Номер сделки» переведена с `tc--numbers`/`th--right` на левое выравнивание; всем текстовым значениям строк проставлен `tc__text--truncate` (80 ячеек) — одна строка + многоточие + тултип по наведению | Запрос пользователя по реестру портфеля |
| Редполитика (правила) | 1.000 | Создана страница `pages/patterns/Redpolicy.html`: правила валюты (3-буквенный код ISO 4217 — RUB/USD/EUR/JPY), чисел и единиц, названий окон и действий. Перенесён черновик «Редполитика названий модалок» из `Modal.html` (в Бэклоге заменён ссылкой; `specs/Modal.md` обновлена). Страница подключена в `ds-nav.js` и `index.html` | Пользователь: валюта в интерфейсе — кодом ISO 4217 (RUB, USD, EUR, JPY) во всей системе; правило зафиксировано в редакционной политике ДС |
| Валюта по всей ДС | — | Символы валют `₽`/`$`/`€`/`¥` и «руб.» заменены на коды в спецификациях (`specs/*.md`, `_cheatsheet.md`), конструкторах (`scripts/*.page.js`) и страницах компонентов; `src-code-html` пересобран через `docs-split.mjs inject --html`. Компилятор витрины (`_ds_bundle.js`/`_ds_manifest.json`) недоступен — остаток зафиксирован (урок Л25) | Валюта по правилу редакционной политики — 3-буквенным кодом, без символов и «руб.»; конвенция приведена к уже существующему `RUB` |
| Экран «Текущий портфель ДИД» | 1.009 | Колонка «Валюты» и фильтр «Валюта сделки» переведены с символов `₽`/`$`/`€`/`¥` на коды RUB/USD/EUR/JPY; `Portfolio.screen.md` обновлён (формат значений, версия 1.009, правки) | Валюты по правилу редакционной политики ДС — 3-буквенным кодом |
| Chip | 1.013 | Добавлен несжимаемый вариант `.chip--fit` (`flex: 0 0 auto; min-width: auto`): чип с коротким фиксированным значением (код валюты, PE, «Применено: N») не сжимается в чужой flex-строке и не усекает подпись — значение показывается целиком. Спека/чит-шит/страница обновлены, `src-code-css` через `docs-split.mjs inject` | Пользователь: чипы валют не должны сжиматься — код обрезался до «R…» вместо полного `RUB` |
| Экран «Текущий портфель ДИД» | 1.010 | Чипы валют и PE переведены на `.chip--fit` (Chip 1.013): коды показываются целиком, не «R…»; `Portfolio.screen.md` обновлён (формат, раздел «Private Equity», правки, версия 1.010) | Чипы валют сжимались — код обрезался до «R…» |
| TableCell | 2.013 | Удержание закреплённых колонок при горизонтальном скролле переведено с `transform: translateX` на нативный `position: sticky`. В `styles/table-cell.css` добавлено `position: sticky` для `.tc--pinned`/`.th--pinned` и прилипших сепараторов; `tbl-pin.js` больше НЕ подписан на `scroll` — он считает инсеты `left`/`right` (сумма ширин закреплённых слева/справа) и пишет их инлайном на `columnpin`, `columnreorder`, resize окна и ResizeObserver по ячейкам шапки (ресайз колонки своего события не шлёт). Пропал пересчёт `getBoundingClientRect` по всей шапке на каждый кадр скролла. Проверено замером позиций при `scrollLeft` 0 / 700 / до упора и `elementFromPoint` внутри закреплённых ячеек: левый блок держится на 0..8 (сепаратор), 8..158, 158..438; хвостовой правый блок — 1322..1462 + сепаратор 1462..1470 = `clientWidth`; поверх закреплённых колонок ничего не рисуется. Спека/чит-шит/страница/`src-code-css`/`runtime-hooks.md` обновлены | Закреплённые колонки дрожали при горизонтальном скролле на MacBook: удержание считалось в обработчике `scroll`, а скролл применяет компоновщик браузера ДО выполнения JS, поэтому каждый кадр колонка уезжала вместе с контентом и возвращалась следующим кадром. На Windows дискретное колесо это скрывало — коррекция успевала в паузу между щелчками; на трекпаде macOS (непрерывные дельты + инерционный докрут) ошибка показывалась на каждом кадре подряд. Исходное обоснование JS-реализации («CSS `position: sticky` не работает для колонок grid-раскладки — sticky-элемент ограничен своей grid-областью») оказалось неверным: проверено на изолированном стенде и на самом экране — sticky на grid-элементах держит и многоколоночный левый блок, и хвостовой правый. Похоже, при первой попытке на строке ещё не стоял `min-width: max-content`, строка не была шире скроллпорта, и sticky выглядел как «не работает» |
| ds-lint | — | Новое правило группы B. **B10** — рантайм ДС пишет geometry-свойство (`transform`, `left`/`right`/`top`/`bottom`) из обработчика `scroll`. Линтер идёт от регистрации хендлера (`addEventListener('scroll', …)`, `.onscroll =`) по локальным функциям файла на два вызова вглубь — так ловится и типовая обвязка `scroll → requestAnimationFrame(…) → apply()`. Первый цикл правило живёт как WARN («Как расширять», п.4). Белый список `SCROLL_GEOMETRY_OK` с ключом «файл:функция» — шесть записей с причиной: плавающие слои Tooltip, Popover, DropdownList, Menu, DatePicker и rail-подписи NavPanel позиционируются в координатах вьюпорта и обязаны пересчитываться при скролле, CSS-эквивалента нет. Таблица групп и абзац-разбор инцидента — в `skills/ds-integrity-check.md`. Проверено откатом фикса TableCell 2.013: возвращённый scroll-хендлер ловится, на починенной ДС правило молчит, базовый отчёт не изменился | Дефект дрожания закреплённых колонок (TableCell 2.013) на Windows невидим в принципе: дискретное колесо мыши прячет отставание на кадр. Он уехал к пользователю и нашёлся только на MacBook. Класс «JS против компоновщика» глазами на приёмке не ловится — нужен статический сторож |
| ds-lint | — | Новая живая проверка группы E (шаг 2 скилла, `eval_js`). **E9** — держатся ли закреплённые колонки при горизонтальном скролле: сдвиг `scrollLeft` и замер `getBoundingClientRect()` в одном синхронном такте, без отдачи кадра. Три ветки: держит не `sticky`, закреплённые уехали на N px при сдвиге M px, закреплённую перекрывает соседняя ячейка той же строки (`elementFromPoint`). Вложенные `.tbl--scroll` / `.dtable__body` схлопываются в одну — отчёт не двоится; внешний хром (липкий футер, шапка) в перекрытие не засчитывается; таблица, влезшая в порт, молчит. Норма — `pinHold: []`. Проверено тремя откатами на «Текущем портфеле ДИД»: JS-удержание через `transform` на `scroll` — `position: static у 6/6` и `6/6 уехали на -400px при сдвиге 400px`; снятый приоритет отрисовки — ветка перекрытия; починенная ДС молчит на четырёх размерах окна | Пара к статическому правилу B10. B10 разбирает причину и только в рантаймах ДС — экранные `<script>` и инлайн-обработчики линтер не обходит. E9 меряет результат на любой странице и ловит поломку закрепления независимо от того, чьим кодом она внесена |

## 03.09.2026

| Компонент | Версия | Что изменилось | Почему |
|---|---|---|---|
| Layout (Каркас экрана) | 1.008 | Добавлен верхний паддинг контентной области: новый токен `--layout-pad-top: var(--space-8)` и `padding: var(--layout-pad-top) var(--layout-pad-x) var(--layout-pad-bottom)` у `.screen__content` (было сверху 0). Обновлены спека, чит-шит, страница `Layout.html` (псевдокод, сводка конструктора, `layout.page.js`, `src-code-css` через inject), правила сборки (`screen-assembly`) и приёмки (`screen-review`) | Пользователь попросил увеличить отступ от крошек до первого блока контента: верхний паддинг 8px становится стандартом каркаса для всех экранов, а не правкой отдельной страницы |
| Layout (Каркас экрана) | 1.009 | Добавлен вариант app-shell `.screen--app`: `height:100vh; overflow:hidden` на рабочей области + `min-height:0` на `.screen__content`. Закреплено правило каркаса: единственная/основная таблица страницы занимает всю свободную высоту (минус крошки/шапка/тулбар), тело скроллится внутри; применяется вместе с `.dtable--fill` (Table 1.007). Спека/чит-шит обновлены | Таблица по умолчанию имела высоту по контенту (числу строк). Для реестров нужна «прикладная» модель: таблица заполняет свободную высоту экрана, тело скроллится, шапка/тулбар/пагинация закреплены. Модификатор опционален: обычные страницы (тайлы/карточки) остаются с прокруткой страницы |
| Table | 1.007 | Добавлен вариант «на всю высоту» `.dtable--fill`: `flex:1; min-height:0` у `.dtable` и `.dtable__body` — таблица заполняет свободную высоту родителя, тело скроллится внутри (`.th` липкие уже были). Требует `.screen--app` (Layout 1.009) для фиксированной высоты вьюпорта. Спека/чит-шит/страница обновлены | Компонент не давал способа занять свободную высоту экрана: таблица всегда была высотой по контенту. Вариант даёт реестрам «на весь экран» с внутренним скроллом тела |
| NavPanel | 1.014 | Реализовано сворачивание развёрнутой (drawer) панели в rail кликом вне `.nav` и по Esc: document-обработчики в `ds-nav-panel.js`, только для `.nav--drawer` (fixed не трогается), исключение — клики внутри открытых модалок (например, смена роли из футера: `.modal-scrim`), панели с `data-nav-modes="no"` не затрагиваются; Esc не сворачивает, пока открыта модалка. Спека/чит-шит/страница обновлены | Поведение было задокументировано в спеке («Drawer сворачивается кликом вне области»), но не реализовано в рантайме: развёрнутая панель оставалась открытой при клике по контенту. По решению пользователя поведение реализовано, с исключением для модалки смены роли |
| PageHeader | 1.009 | `.phead__title-row` min-height 32→40px (выравнивание с кнопками Actions, Button M = 40px; растёт под многострочный Title), `.phead__title-group` добавлен `padding-top:4px` (группа 36px центрируется в 40px-строке, line-box Title сдвинут от верха на 6px). Спека и страница обновлены; чит-шит высоту title-row не упоминает — правки не требовалось. Заодно выправлен манифест `_index.md` (PageHeader был 1.007 при спеке 1.008) | Заголовок в одну строку (32px) визуально не совпадал по высоте с кнопками действий (40px); title-group оставлен 32px с отступом сверху 4px — выравнивание с кнопками |
| Table | 1.008 | Добавлен вариант «тулбар над таблицей»: зона фильтра и настроек вынесена из таблицы — standalone-блок `.dtable-toolbar` (без подложки: слева TableFilter, справа кнопка настройки колонок) стоит НАД `.dtable`; таблица при этом headless (без `.dtable__toolbar` — сразу шапка, ни заголовка, ни счётчика). Вертикальный ритм варианта — 12px (заголовок → зона → таблица, задаёт экран `gap: var(--space-12)`). Спека/чит-шит/страница/конструктор обновлены; применено к экрану «Текущий портфель ДИД» (Portfolio-did) | Для реестра, где заголовок и счётчик не нужны, а фильтр/настройки висят над таблицей без подложки: раньше фильтр жил только внутри `.dtable__toolbar` на фоне таблицы. Вариант разводит зону и таблицу (headless) с ритмом 12px |
| TableCell | 2.007 | Закрепление колонок стало интерактивным и «из коробки» (Этап 1): новый рантайм `scripts/tbl-pin.js` (в составе `ds.js`, добавлен в `DS_JS_BUNDLES` линтера) — клик по `.th__pin` переключает `.th--pinned`/`.tc--pinned` у всей колонки (глиф pin↔pin-filled, `aria-pressed`, событие `columnpin`), не переставляет колонку. Убрано неработающее CSS `position:sticky; left:0` для колонок (в grid-раскладке sticky ограничен grid-областью колонки — закреплённые колонки ехали со скроллом): удержание у левого края теперь делает JS (`transform: translateX = max(0, scrollLeft − ширина незакреплённых слева)`) на фактическом горизонтальном скроллпорте (`.dtable__body` или `.tbl--scroll`). Убрано hover-затемнение фона у закреплённой шапки (`.th--pinned:hover`); фон закреплённой колонки стабилен. Спека/чит-шит/страница/runtime-hooks обновлены; применено к «Текущему портфелю ДИД» (убрано кастомное смещение второй pinned-колонки — заменено накоплением отступов). Right-edge pin и перетаскивание закреплённых — Этап 2 | Пользователь: закрепление в портфеле не интерактивно; при горизонтальном скролле закреплённые колонки едут (sticky в grid не работает); затемнение хедера закреплённой колонки по hover не нужно; закреплять нужно любую колонку без переноса в начало |
| TableCell | 2.008 | Этап 2 закрепления: (1) **right-pin** — закреплённая колонка из хвостового блока (все колонки правее закреплены) удерживается у правого края контейнера (`translateX = min(0, clientWidth − отступ_справа − правая_кромка + scrollLeft)`), тень-разделитель слева (`data-pin-side="right"` на ячейках); если правее появилась незакреплённая колонка (перенос) — колонка снова left-pinned. (2) **Сепаратор прилипает** с примыкающей закреплённой: левый крайний — если закреплена первая колонка (остаётся у левого края, колонка за ним), правый — если последняя (у правого края). (3) **Перетаскивание закреплённых**: `tbl-reorder.js` больше не исключает `.th--pinned` — класс pin едет с колонкой, `tbl-pin.js` пересчитывает удержание по `columnreorder`. Спека/чит-шит/страница/Table.md обновлены | Пользователь: закреплять нужно любую колонку, в т.ч. посреди таблицы (без переноса в начало); закреплённая у правого края прилипает к правому краю, при появлении незакреплённой правее — ведёт себя как левая; сепаратор, примыкающий к закреплённой колонке, тоже должен оставаться на месте |
| TableCell | 2.009 | Заливка ячеек-сепараторов и три фикса закрепления: (1) сепаратор (`.tc--separator`/`.th--separator`) получил явную заливку `--bgtable` и при прилипании к закреплённой колонке красится `--bgtable-pinned` (рантайм ставит `data-pin-side` на прилипший сепаратор **по факту прилипания**, а не по активному transform — заливка нужна и в покое) — сливается с колонкой в сплошную полосу, без собственной тени. (2) Фикс перекрытия: `.tbl__row { min-width: max-content }` — строка резервирует полную ширину контента, поэтому `transform: translateX` закреплённых колонок не сужает `scrollWidth`; иначе браузер считал `scrollWidth` по визуальным (трансформированным) боксам, полоса прокрутки «не доезжала» до естественной позиции закреплённой колонки, и она перекрывала соседние слева ровно на свою ширину. (3) Прилипший сепаратор получил z-index закреплённой колонки (`.tc`→1, `.th`→3, `.dtable__body .th`→4) — иначе ведущий сепаратор (первый ребёнок строки, z-index auto) рисуется ПОД проезжающими незакреплёнными ячейками и «теряет заливку». (4) Прилипший сепаратор наследует состояния закреплённой колонки (`--bgtable-pinned-hover`/`-focus`/`--primary-bg`) вместо общего `--bgtable-row-hover` (иначе ховер «не срабатывает»). (5) Тень-разделитель блока — только на граничной колонке: базовое правило тени сужено до `data-pin-side="left"` (было у всех `.tc--pinned` — у внутренней колонки правого блока оставалась правая тень, «полоска» между ней и замыкающим сепаратором). Тот же принцип доведён до левого блока: `tbl-pin.js` ставит `data-pin-side="left"` только на правую границу левого блока (новая `leftBoundary` в `analyze`), внутренние left-pinned тени не несут — блок стал симметричным сплошным блоком. Откат неверного эпсилона 0.5px + `scrollend`/трейлинг-пересчёта. Спека/чит-шит/страница обновлены, `src-code-css` через `docs-split.mjs inject` | Пользователь: сепараторы были «пустыми» (заливка совпадала с фоном таблицы `--bg-tile` → белая полоса), прилипший сепаратор оставался mgrey-50 рядом с зелёной закреплённой колонкой; после скролла вправо и назад крайняя правая закреплённая колонка перекрывала соседние незакреплённые слева ровно на ширину закреплённой колонки (эпсилон/scrollend не помогли — корень в сужении ширины прокрутки transform-ом) |

## 02.09.2026

| Компонент | Версия | Что изменилось | Почему |
|---|---|---|---|
| NavPanel | 1.012 | Утверждён вариант «Вложенные разделы» — аккордеон: пункт-родитель `.nav__item--acc` с кареткой `.nav__caret` и вложенным под-списком `.nav__sub` (раскрывается в потоке, grid 0fr→1fr). Классы перенесены из экспериментального `<style>` страницы в `styles/nav-panel.css`; раскрытие/сворачивание добавлено в рантайм `ds-nav-panel.js` (в rail клик по родителю разворачивает панель). Вариант «Контекстное меню» (флайаут `ContextMenu` справа от панели) удалён: секция RND и её код (`bindFlyout`, `buildFlyoutMenu`, `initRnd`) вычищены из страницы и `nav-panel.page.js`; пункт «Вложенные разделы» убран из бэклога. Спека, чит-шит, документация, `src-code-*` и редлайн обновлены | Из двух экспериментальных подходов к под-списку утверждён аккордеон — раскрытие в потоке списка; выпадающее контекстное меню отброшено |
| NavPanel | 1.012 (без изменения — правка документации) | Закреплено правило «тайл-со-ссылками ↔ аккордеон»: пункт с `item.tile.links` рендерится аккордеоном-родителем, под-пункты — ссылки тайла, без иконок, по умолчанию свёрнуты (`aria-expanded="false"`). Пример в спеке/чит-шите/`src-code-html` заменён с синтетического «Обязательные сделки → Подстраницы» на реальный «Отчёты для Рисков» (Коды RWA / Расчет RWA / Расчет ОВП). Демо `nav-panel.page.js` рендерит аккордеон из `tile.links`; строка «Элементы панели» обновлена. Экран `Projects/post/operations-did/mainPage` рендерит аккордеон по тому же правилу | Пункты меню, чьи тайлы на главной содержат ссылки, должны раскрывать их под-списком в панели; пример должен быть реальным, а не заглушкой |
| NavTile | 1.008 | `.ntile` `min-height` 188→190px; `.ntile-grid` добавлен `align-items:start` — плитки больше не выравниваются по высоте ряда, каждая = `max(190px, высота своего контента)`. Обновлены спека/чит-шит/страница (`Размеры`, футер, `src-code-css`) | Плитки в ряду вытягивались до высоты самой высокой — простые тайлы становились непропорционально высокими; теперь высота определяется только собственным контентом с минимумом 190px |
| NavTile | 1.009 | В `.ntile` добавлен `box-sizing: border-box` (и для потомков) — `min-height: 190px` теперь полная высота плитки, а не контент-бокс. Спека/чит-шит/`src-code-css` обновлены | У `.ntile` не было `box-sizing: border-box` (в отличие от остальных компонентов ДС): `min-height: 190px` считался как контент-бокс, и с паддингами 32px плитка рендерилась ~256px высотой вместо 190px |
| NavPanel | 1.013 | Панель по умолчанию свёрнута (Rail) на всех экранах: рантайм `ds-nav-panel.js` — `modeOf` без класса режима теперь возвращает `rail` (было `drawer`); конструктор `nav-panel.page.js` по умолчанию «Rail»; эталонная разметка в спеке/чит-шите/`src-code-html` переведена на `nav--rail`; в «Инварианты» добавлено «по умолчанию Rail». Экран `Projects/post/operations-did/mainPage` переведён с `nav--drawer` на `nav--rail` | Левая панель должна открываться свёрнутой на всех страницах; ранее экраны и эталон несли `nav--drawer` и панель разворачивалась при загрузке |
| NavTile | 1.010 | Иллюстрация `.ntile__illu` переведена в фон (`z-index: 0`), текст (заголовок/описание/ссылки) — поверх (`position: relative; z-index: 1`): при сужении тайла текст больше не уходит под картинку. Зафиксировано центрирование иллюстрации по вертикали (`top: 50%; translateY(-50%)`) — исправлены расхождения в комментарии CSS и «Инвариантах» спеки («закреплена от верха, не по центру»). Спека/чит-шит/`src-code-css` обновлены | Абсолютно позиционированная иллюстрация рисовалась поверх статичного текста, и при сужении тайла описание пряталось под картинкой (заметно на «CF СБИ»); текст должен быть читаемым, а картинка — фоном |
| TableCell | 2.006 | Перетаскивание колонок стало базовым поведением таблицы (как изменение ширины): новый общий рантайм `scripts/tbl-reorder.js` (в составе `ds.js`) — перенос за подпись шапки `.th__label`, разделители и закреплённые исключаются, маркер `.th--movable` больше не нужен. CSS курсоров переведён с `.th--movable` на `:not(.th--separator):not(.th--pinned)`. Спека/чит-шит/`src-code-css` обновлены | Порядок колонок был задокументирован и стилизован, но не реализован как общий рантайм — работал только в демо страниц; теперь как и ресайз доступен везде из коробки |
| Table | 1.006 | В рантайм-список добавлен `scripts/tbl-reorder.js`; «Варианты» — перетаскивание колонок вынесено из модалки-шестерёнки (там остались видимость/закрепление), порядок колонок теперь базовое поведение из коробки; добавлена строка псевдокода | Перетаскивание колонок — неотъемлемая функция таблицы (как изменение ширины), а не настройка через модалку |

## 01.09.2026

| Компонент | Версия | Что изменилось | Почему |
|---|---|---|---|
| RND-документы и аудит | — | Удалены: `AUDIT.md` (аудит 25.07.2026, все волны выполнены), `skills/HANDOFF.md` (устарел — ссылался на несуществующие экраны 01..09), `skills/_plan-ds-ripple-check.md` (план без реализации), `pages/rnd/Plan3-comments.md` (журнал к удалённому Plan3), `pages/rnd/Plan4.html` (план перекрыт реализацией). Из витрины `index.html`, навигации `scripts/ds-nav.js`, манифеста `_ds_manifest.json` и бандла `_ds_bundle.js` убраны ссылки на Plan4; комментарии `ds-include.js`/`ds-registry.js` с отсылкой «Plan4, этап 2» переформулированы; в `Backlog.html` убрана ссылка на удалённый файл плана | Файлы аудита и устаревшие планы неактуальны; RND-секция витрины и левой панели оставлена с одним бэклогом |
| NavPanel | 1.010 | Строка пользователя в футере стала ссылкой на личный кабинет (`<a class="nav__user" aria-label="Открыть личный кабинет">`), добавлен hover фона по всей полосе футера (`.nav__footer:hover` — `--bgtable-row-hover`) и focus-кольцо на строке; выход — только кнопкой `.nav__logout`. В спеке `NavPanel.md` зафиксирован канонический состав групп меню (Origination/Pipeline/Текущий портфель ДИД/Отчеты/Клиенты и задачи/Администрирование с пунктами и иконками), модель ролей (full/partial) и пометка о будущем соответствии «пункт меню ↔ тайл главной страницы». Эталон в `src-code-html`, чит-шит и экран `DealCard.html` обновлены | Требование пользователя: вся строка пользователя кликабельна и ведёт в личный кабинет, ховер — как у пункта меню, но фоном по всей области футера; выход из аккаунта только кнопкой. Эталонный состав групп нужно сохранить в компоненте (источник истины — спека); соответствие тайлам главной страницы отложено до создания стартовой страницы |
| NavPanel | 1.011 | Меню собирается по роли из единого каталога `scripts/ibp-home.js` (`window.IBPHome`): состав групп/пунктов — по `itemsFor(role)`, частичная доступность группы (например, Администрирование без «Администратор ЦКП»). Переименования в составе: «Отчеты»→«Отчёты», «RWA»→«Отчёты для Рисков» (иконка `rwa` осталась). Должность в футере — заглушка по текущей роли. Кнопка выхода открывает модалку смены ролей (`data-modal="nav-role-modal"`, список ролей; выбор перерисовывает меню). Конструктор переведён с кастомных тумблеров на бинарные селекты (docs-split.js конвертирует их в свитчи pg-toggle; словарь `DS_SPLIT_SWITCH_LABELS`; урок Л4/К9). Спека/чит-шит/`src-code-html` обновлены | Единый источник истины (каталог тайлов главной + роли) должен быть один на панель и экраны; добавлен механизм ролей с переключением; исправлены названия по эталону; кастомные тумблеры конструктора — блокер по уроку Л4 |
| NavTile | 1.007 | На странице добавлена секция «Каталог тайлов» (h3 в «Вариантах»): витрина всех тайлов главной страницы по группам из единого каталога `scripts/ibp-home.js`; пункты меню без тайла не выводятся. Спека и чит-шит дополнены («Каталог тайлов»), реестр `_index.md` выправлен 1.006→1.007 (расхождение спека/манифест) | Пользователь хочет полный список тайлов в компоненте NavTile, чтобы сборщик экранов брал их из одного места; изменение каталога подхватывается страницей и экранами |

## 27.08.2026

| Компонент | Версия | Что изменилось | Почему |
|---|---|---|---|
| ds-lint-cli.mjs | — | Новый файл `scripts/ds-lint-cli.mjs` — обёртка для запуска `ds-lint.js` из терминала под Node (даёт хелперы `readFile`/`ls` через `node:fs`, грузит линтер тем же `new Function`, что и `run_script`). `node scripts/ds-lint-cli.mjs <страница>` / `--parity`. Сам линтер не тронут | В сессиях без инструмента `run_script` (обычный агентный CLI на машине) линтер прогнать было нечем; `ds-lint.js` не исполняется напрямую — ждёт хелперы от харнеса |
| SegmentControl | 1.005 | Высота внутреннего сегмента уменьшена на удвоенный паддинг трека во всех трёх размерах: M `--seg-h` 40→34, S 32→28, XS 24→20. Паддинги/радиусы трека не тронуты. В `segment-control.page.js` строка редлайна «Высота трека (расчётная)» переведена с `getComputedStyle(track).height` (возвращает content-box, совпал с высотой сегмента) на явный расчёт `itemH + 2*trackPad` | Полная высота контрола считается по border-box: сегмент + обводка трека сверху и снизу. При `--seg-h: 40` трек в размере M рендерился 46px и торчал на 6px из ряда с Button M (40px). Обводка трека — часть компонента, а не внешний отступ, поэтому ужимать надо сегмент, а высоту контрола держать равной Button того же размера. Ошибка была одинаковой во всех трёх размерах |
| Modal | 1.005 | Добавлено `.modal-scrim[hidden] { display: none; }` | `.modal-scrim { display: flex }` и браузерный `[hidden] { display: none }` — одинаковая специфичность (0,1,0); класс подключается позже дефолтного UA-стиля и побеждал в каскаде, поэтому скрытая атрибутом `hidden` модалка всё равно рендерилась открытой на любой странице, не только на месте её вызова. Баг вскрылся при сборке экрана `FundList` (пользователь не мог посмотреть страницу — обе модалки были открыты поверх контента), но источник — в самом компоненте |
| TableCell | 2.005 | `tbl-resize.js` добавлен в `scripts/ds.js` (после `ds-table.js`) и в `DS_JS_BUNDLES` линтера (`A7`). Чит-шит, эталонные сниппеты (TableCell.md, Table.md) и сниппет `screen-assembly/patterns.md` получили `.th__resize` в каждой `.th`; замыкающий разделитель в сниппете чит-шита переведён на `minmax(8px,1fr)`. Прозе Table.md/TableCell.md/skills добавлено: изменение ширины — не отключаемая опция, работает из коробки, отдельно скрипт не подключать | Спека давно объявляла ресайз колонки «из коробки», но `tbl-resize.js` был только на страницах документации — в `ds.js` его не было, и собранные экраны его не грузили; вдобавок канонические сниппеты шапки шли без `.th__resize`. Сборщик экрана не получал базовое поведение таблицы без отдельного запроса |
| Table | 1.005 | Удалены тени-градиенты по левому/правому краю тела при горизонтальном скролле: класс `.dtable__edge` / `.dtable__edge--l` / `--r` и модификаторы `.dtable--edge-l` / `--edge-r` вычищены из `styles/table.css`; из `scripts/ds-table.js` убраны их переключатели (остался только `.dtable--scrolled` для тени липкой шапки). Разметка `.dtable__edge` убрана из `Table.html`, `table.page.js`, обоих gen-экранов, эталонных сниппетов (Table.md, `_cheatsheet.md`, `patterns.md`), редлайн-строка «Ширина тени края» и подраздел «Тени горизонтального скролла» удалены | Элемент-градиент позиционировался `position:absolute` внутри скролл-контейнера `.dtable__body` и уезжал вместе с колонками: правая тень появлялась у правого края при `scrollLeft≈0` и дрейфовала внутрь таблицы по мере прокрутки. По решению пользователя таких теней у края таблицы быть не должно вовсе — скрытый контент по бокам обозначает скроллбар |

## 26.08.2026

| Компонент | Версия | Что изменилось | Почему |
|---|---|---|---|
| NavPanel | 1.009 (без изменения — правка только документации) | Исправлено расхождение чит-шита и спеки: в HTML-сниппете чит-шита бургер/пин/выход были `ibtn--s`, тогда как спека и расчёт оси в `styles/nav-panel.css` (`top 18 + 10 = 28`) требуют `ibtn--m`. Правило продублировано текстом в обоих файлах | Сборщик экрана взял `ibtn--s` из чит-шита (он читает именно чит-шит, а не спеку) — бургер визуально не совпал с компонентом и сбилась вертикальная ось панели. Источник ошибки был в самой ДС, а не в агенте |
| DatePicker | — | В HTML-сниппете чит-шита футерные кнопки исправлены `btn--s` → `btn--xs` + возвращены классы-хуки `dpk__today/cancel/ok`; проза того же блока и спека всегда говорили XS | Тот же класс дефекта, что и у NavPanel: сниппет чит-шита противоречил и спеке, и собственной прозе строкой выше. Найдено сплошным аудитом, не по жалобе |
| Tile | 1.010 | Введён `.tile-group` (flex-колонка, gap 16) — обёртка нескольких рядов. Заменил ошибочное правило `.tile-row + .tile-row { margin-top: 16px }` из первой редакции: `.screen__content` — flex с `gap: 24px`, margin складывался с ним и давал 40px вместо 16. Группа работает, потому что она — один ребёнок контентной области | Пользователь на скриншоте показал, что зазор между рядами остался больше 16px после «починки». Причина — не в значении, а в том, что зазор задавался механизмом, который конфликтует с `gap` родителя |
| Tile | 1.009 | (1) вертикальный зазор рядов одной группы теперь держит компонент, экран его не пишет. (2) Новый `.tile__grid-full` (`grid-column:1/-1`) — элемент на всю ширину сетки тайла остаётся В сетке. (3) В чит-шит добавлены явные правила выбора числа колонок (`span N` под объём контента, а не «по умолчанию 6») и запрет своих классов ширины | На DealPipelineCard экран выставил себе зазор 24px между рядами (взял `--layout-block-gap`, предназначенный для крупных зон), «Описание» вынес из сетки отдельным блоком с разъехавшимися отступами, а ширины задал своими классами `.dpc-half/.dpc-full` вместо `span N` — при том, что паттерн `span 4` в чит-шите есть |
| ProgressBar | 1.002 (без изменения — правка только документации) | В спеку и чит-шит добавлена таблица выбора варианта: обычный (`.pbar__head`) — доля/процент выполнения; `--floating` — оценка/позиция на шкале (вероятность, скор). Явно указано, что `--floating` не «нарядная версия» обычного | Оба варианта лежали в доке рядом как примеры, без правила выбора — сборщик поставил `--floating` на «Прогресс», а «Вероятность сделки» отрисовал голым текстом без бара, ровно наоборот от задуманного |

## 25.08.2026

| Компонент | Версия | Что изменилось | Почему |
|---|---|---|---|
| Рантайм подключения (ds-include.js) | — | Создан custom element `<ds-include src="…">`: подключает CSS компонента (путь без расширения + `.css`, переопределяется `css=`), вставляет html-фрагмент компонента на своё место, переносит `id`/`class`, атрибуты `state`/`mode` перекрывают `data-state`/`data-mode` фрагмента, бросает событие `ds-include:loaded` для заполнения данными со стороны экрана. Добавлен в `scripts/ds.js`, `templates/screen/Screen.html` переведён на единый `ds.js` вместо ручного списка скриптов | Пункт этапа 2 плана: компонент подключается строкой, а не копируется в каждый экран — правка меняет все вхождения сразу |
| Plan4 | 1.005 | Строка «Рантайм подключения» переведена в «Сделано» | Пункт этапа 2 закрыт |
| Генератор панели и реестра (ds-registry.js) | — | Создан `scripts/ds-registry.js` (сборщик, не браузерный — запуск через run_script, как ds-lint.js): сканирует `kit/components/*/*.md` и `screens/*.md`, строит `registry.json` — usedOn компонентов считается от экранов (не берётся из паспорта), обратный индекс требований, дрейф `rulesVersion`, битые ссылки на компонент/требование, незаполненные строки таблиц в паспорте. `templates/admin/admin.html` — витрина: читает `registry.json`, рисует таблицы на `.tbl`. Проверено на синтетических данных | Пункт этапа 2 плана: реестр не должен вестись руками, компонент без паспорта или устаревшая версия правил должны быть видны как список, а не держаться в памяти |
| Plan4 | 1.006 | Строка «Генератор панели и реестра» переведена в «Сделано» | Пункт этапа 2 закрыт |

## 25.08.2026

| Компонент | Версия | Что изменилось | Почему |
|---|---|---|---|
| Layout (Каркас экрана) | 1.007 | Явное жёсткое правило: у экрана горизонтального скролла не бывает никогда — адаптивная вёрстка закладывается при проектировании, объект за границей экрана — баг. Исключение — контейнер со скроллом в собственной раскладке (тело таблицы, отдельный тайл), не сама рабочая область. Заодно поправлена устаревшая строчка чит-шита «min-width 1280px → горизонтальный скролл», противоречившая новому правилу | Готовим правило для будущих шаблонов каркасов (Plan4, этап 2) — до старта нужна одна явная формулировка, а не разрозненные упоминания скролла по странице |
| Рантайм подключения (ds-include.js) | — | Создан custom element `<ds-include src="…">`: подключает CSS компонента (путь без расширения + `.css`, переопределяется `css=`), вставляет html-фрагмент компонента на своё место, переносит `id`/`class`, атрибуты `state`/`mode` перекрывают `data-state`/`data-mode` фрагмента, бросает событие `ds-include:loaded` для заполнения данными со стороны экрана. Добавлен в `scripts/ds.js`, `templates/screen/Screen.html` переведён на единый `ds.js` вместо ручного списка скриптов | Пункт этапа 2 плана: компонент подключается строкой, а не копируется в каждый экран — правка меняет все вхождения сразу |
| Plan4 | 1.005 | Строка «Рантайм подключения» переведена в «Сделано» | Пункт этапа 2 закрыт |

## 21.08.2026

| Компонент | Версия | Что изменилось | Почему |
|---|---|---|---|
| Spacing (Сетка и отступы) | 1.003 | Раздел «Колоночная сетка» дополнен: ширина блока — любое число колонок 1–12; раскладки трёх экранов продукта схемами; перестроение при сужении парой span'ов — `.colw-1`…`.colw-12` + активатор `.grid12--narrow` (или `@container`-порог экрана); зафиксировано исключение — кастомная фиксированная ширина или ширина по контенту | Пользователь показал на трёх макетах, как 12 колонок ложатся на экраны и как плитки перестраиваются при сужении рабочей области |
| Layout (Каркас экрана) | 1.006 | Создан компонент: страница с конструктором, `styles/layout.css`, спека. Зоны экрана, поля 24px, залипающие крошки, фиксированная панель навигации, перестроение от ширины рабочей области через `@container screen` | Все экраны продукта строятся по одному каркасу; до этого правила существовали только в разметке отдельных экранов |
| Breadcrumbs | 1.005 | Зона крошек (44px, поля 16/24/12) перенесена в сам компонент, старые padding-паттерны на экранах убраны | Крошки и контент должны стоять на одной вертикали без ручных полей на каждом экране |
| Линтер ДС (ds-lint.js) | — | Группа правил раскладки L1–L5: переполнение ряда `.grid12` (>12 колонок), блок вне колонок без пометки `data-off-grid`, самодельная 12-колоночная сетка на экране, переопределение полей контентной области и зазора сетки, порог раскладки на `@media` вместо `@container screen` | Правила раскладки существовали только текстом; решения по уровням ошибок приняты пользователем 21.08.2026 |
| Spacing (Сетка и отступы) | 1.004 | Исключение «плитка вне колонок» помечается в разметке `data-off-grid="причина"` | Линтер должен отличать осознанное исключение от небрежности |
| Локальные компоненты (правила) | 1.003 | Добавлено обязательное поле паспорта `category` и раздел «Категории и витрина кита»: одна витрина на кит с левой навигацией по категориям, та же панель в витрине компонента, список строится из паспортов по `category`, алфавит внутри группы, категория не влияет на вид. Поле добавлено в шаблон паспорта и README | Локальных компонентов будет много и они делятся по категориям (переиспользуемые, инструменты, сделка, отчёты, прочее) — нужна быстрая навигация между ними |
| Локальные компоненты (правила) | 1.002 | Добавлены поле паспорта `rulesVersion` (версия правил, по которой компонент заполнен) и раздел «Шаблон папки»: состав `templates/local-component/`, таблица «как правила попадают в проект и как обновляются». Создан сам шаблон: README, схема паспорта `Component.md`, разметка, стили, витрина состояний, fixtures, журнал | Правила должны переезжать в проект продукта копией ДС и обновляться вместе с ней, а расхождение заполненных паспортов с новыми правилами — быть видимым, а не держаться в памяти |
| Локальные компоненты (правила) | 1.001 | Описание компонента разделено на два поля паспорта: `purpose` — краткое назначение одной строкой (подпись в реестрах), `description` — развёрнутый текст без ограничения длины | Одной строки для полного описания компонента не хватает |
| Локальные компоненты (правила) | 1.000 | Создана страница `pages/patterns/LocalComponents.html`: границы ДС / компонента / экрана, обязательные поля паспорта, обязательные состояния данных (загрузка, есть, частично, нет, ошибка, обновление) и прав (правка доступна, только просмотр, правка временно запрещена, нет права видеть), обязательность заполнения, связанные артефакты со связью в обе стороны, зависимости, вёрстка на токенах, версионирование, чек-лист. Карточка в index.html, пункт в ds-nav.js | Пункт плана 4 «Правила локальных компонентов»: кроме владельца ДС с ней работают другие дизайнеры и агенты, проекты должны читаться одинаково |
| Plan4 | 1.004 | Строка «Правила локальных компонентов» переведена в «Сделано»: страница, схема паспорта и шаблон папки созданы | Пункт этапа 2 закрыт |
| Plan4 | 1.003 | В столбце «Объём» таблиц «Что получится на выходе» и «Что добавить в эту ДС» перечислены конкретные файлы вместо «3 файла»; строка правил локальных компонентов переведена в «Частично» со ссылкой на созданную страницу | Из «3 файла» не было понятно, какие именно файлы имеются в виду |
| Plan4 | 1.002 | В таблицы «Что добавить в эту ДС» и «Порядок работ» добавлен столбец «Статус» | Нужно видеть, что из плана сделано, а что предстоит |
