# Дизайн-система — правила ведения

## Язык
Отвечать и рассуждать в проекте только на русском.

## Формат ДС (ВАЖНО — отличие от исходного проекта)
Эта ДС — **чистый ванильный HTML/CSS**, без React и без шага сборки. Источник истины — токены (`styles/*.css`), классы компонентов и HTML-страницы документации; страница открывается двойным кликом (`file://`) и работает как есть. **Не создавать** React-компоненты (`<Имя>.jsx` / `<Имя>.d.ts`): с файлами напрямую работает любой агент или харнес, промежуточный слой ничего не добавляет. Раздел «Для разработчиков» на страницах — framework-agnostic хендофф (redline + псевдокод), а не React-код.

## Жёсткие запреты (НИКОГДА)
- Не читать `icons-data.js` (671 KB, одна строка). Имена глифов — `specs/Icons.md`. Один SVG и поиск по именам — утилитой `node scripts/ds-icon.mjs <имя>` / `--list [подстрока]` (отдаёт ровно нужный глиф, файл в контекст не идёт).
- Не читать страницу `<Имя>.html` целиком. Сначала спека `specs/<Имя>.md`; мало — `grep` или точечное чтение с offset/limit.
- Не перечитывать файл после своей правки «для самопроверки». Проверка одна — в конце, гейтом `node scripts/ds-check.mjs <страница>` (см. «Проверка: чем гонять»).
- Не инлайнить SVG в разметку экранов — только `<i data-icon="имя"></i>` (см. «Сборка экранов»).
- Не выдумывать цвета/размеры/радиусы/шрифты — только токены `var(--*)` и классы ДС. Нет токена → спроси.
- Не перезаписывать файл целиком при точечной правке — только замена конкретного фрагмента. Однотипные правки во многих файлах — скриптом на Node, чтобы содержимое не шло через контекст.
- Не трогать `uploads/` — это референсы пользователя.
- Не писать React-компоненты (`.jsx`/`.d.ts`) и не подключать их — ДС ванильная (см. «Формат ДС»).
- **Не переверстывать внутреннюю анатомию компонента.** Разметка слотов берётся из спеки/чит-шита дословно: где показан `<svg>` — там `<svg>`, размер и тег уже решены в CSS компонента. Правило «иконки только `<i data-icon>`» относится к иконкам в раскладке экрана, не к слотам компонентов (подмена в `.cb__mark` сломала галочки чекбокса, 12.08.2026).
- **Не выбирать иконку по имени, не посмотрев глиф.** Имя из 247 позиций о рисунке не говорит. Сначала отрендерить сетку `pages/foundations/Icons.html`, выбрать глазами.
- **Не достраивать неизвестное догадкой.** Назначение иконки, поля, связи или термина предметной области, которого я не знаю, — вопрос пользователю, а не реконструкция по внешнему виду. Выведенная догадка неотличима от факта и оседает в документации ДС (12.08.2026: назначение глифа и связей в тайлах было выведено из вида и записано как факт). В документации не остаётся утверждений о предметной области, источник которых не назван.

## Назначение ДС
Точка истины по визуальному языку продукта — для нас (дизайнеров) и для разработчиков, которые собирают по ней компоненты (см. раздел «Для разработчиков»).

**Кто потребитель.** Целевой потребитель ДС — не экран напрямую, а промежуточный слой: локальные организмы под конкретный продукт (тайлы «КНР», «Финансовые метрики сделки», «Дерево продуктов ДИД»), собранные из общих компонентов и несущие собственные правила. Экраны собираются уже из них. Компонент общей ДС предметной области не знает; он обязан дать организму состояния, варианты, поведение при переполнении и точки расширения. Сборка экранов прямо из компонентов (`pages/screens/`) — способ проверки ДС, а не целевой сценарий: так видно, что компонент отдаёт наружу, а что молча оставляет на сборщике. В будущем контент страниц соберут в md для автоматического разбора агентом, поэтому текст должен быть точным и однозначным: чёткие заголовки, таблицы вместо прозы, явные значения токенов.

## Версионирование
В шапке страницы (masthead `.meta`) — поля «Версия» и «Обновлено».
- **Формат версии — `М.ммм`, три знака после точки, без префикса `v`** (`1.000`, `1.009`, `2.004`). Единый стандарт для всей ДС и для локальных компонентов проектов (решение 20.08.2026; до этого было `v1.6` с шагом +0.1).
- Менялся сам компонент (стили/поведение/токены) → «Версия» **+0.001** И «Обновлено» = текущая дата.
- Кардинальная переделка (решили поменять подход, сломали совместимость) → **+1 в мажорной части**, минорная обнуляется (`1.009` → `2.000`).
- Менялась только страница → только «Обновлено» = текущая дата.
- Формат даты ДД.ММ.ГГГГ. Новые компоненты — с `1.000`.
- Формат проверяет линтер (правило `C7`): `v1.6` или `1.6` в шапке — ошибка.
- **Журнал правок — `CHANGELOG.md` в корне.** Менял компонент (стили/поведение/токены) или создал новый → тем же ходом добавь строку в таблицу текущей даты (компонент · версия · что изменилось · почему); нет секции с сегодняшней датой — создай. Правка только текста страницы в журнал не пишется. Журнал начат 21.08.2026, историю до этой даты не восстанавливали.

## Контракт CSS компонента

Корень компонента (класс без `__` и `--`), объявляющий `display`, **обязан рядом объявить** `.<корень>[hidden] { display: none; }`. Браузерное `[hidden] { display: none }` живёт в UA-стиле со специфичностью (0,0,0), правило класса — (0,1,0): класс побеждает, и атрибут `hidden` на компоненте молча перестаёт работать. Дефект чинили поштучно шесть раз (Modal, TableCell, Tab, EmptyState, InputText, Chip), после чего закрыли соглашением и правилом **B11** линтера — новый компонент без пары попадёт в отчёт сразу. Исключения заводятся в `HIDDEN_PAIR_OK` (`scripts/ds-lint.js`) с причиной.

Ряд, который несёт рейл (общую линию под табами, слева от вертикальных табов), **не принимает паддинг по оси этой линии** — ни в CSS компонента, ни в хроме страницы, ни инлайном на экране. Inset-тень рисуется по padding-box: паддинг не сдвигает линию вместе с содержимым, а удлиняет её до первого элемента ряда и после последнего. Отступ задаётся обёрткой без рейла. Охраняется правилом **B15** линтера; подавления — в `RAIL_PAD_OK` (`scripts/ds-lint.js`) ключом «файл ось блок», с причиной.

Страница, подключившая рантайм, подключает и его CSS: рантаймы ДС стилей не догружают, а разметку строят. Пары с уровнем — в `JS_CSS_PAIRS` (правило **A1**); у `ds-tooltip.js` → `tooltip.css` уровень BLOCKER: без стилей построенный `.tip` рисуется обычным текстом и дублирует подпись компонента.

## Контракт страницы компонента
Обязательные разделы в этом порядке; имена h2 — контракт для агентов, только эти слова (варианты «Когда использовать», «Текстовка» и т.п. запрещены):
1. Шапка (masthead) с «Версия» и «Обновлено»
2. Конструктор — первым после шапки
3. Использование (h3: когда использовать/не использовать, сравнения с соседями, примеры в интерфейсе)
4. Анатомия
5. Варианты — контейнер всех осей конфигурации (типы, тоны, формы, ориентация, режимы, «С бейджем»…), каждая ось — h3
6. Размеры
7. Контент (текст, переполнение, иконографика)
8. Поведение — контейнер: позиционирование, авто-разворот, анимации, тайминги, адаптивность; каждая тема — h3
9. Состояния
10. Доступность
11. Типографика
12. Цвета
13. Для разработчиков

Правила:
- Отсутствующий раздел — добавить. Неприменимый (например «Типографика» без текста) — оставить с одной строкой «Не применимо: …».
- Уникальные темы — h3 внутри канонических h2 («Варианты», «Поведение», «Использование», «Контент»); новые h2 не вводить.
- Бейджи («Новое», «Расширение») — отдельным элементом внутри h2, не в тексте заголовка.
- Интерактивные блоки (Конструктор/playground) — наверху, сразу после шапки.
- Сегмент-контролы не переносить в 2 ряда: не помещаются — `<select>`.
- Блок **«Бэклог»** — в самом конце, после «Для разработчиков». Заголовок ровно такой: `<h2>Бэклог</h2>`. Синонимы («Что предлагаем добавить», «Предложения», «Идеи») запрещены — имя раздела контракт для агентов, как и остальные h2. **Раздел обязателен и не удаляется никогда**, даже если предложений не осталось: тогда оставить его с одной строкой вида «Открытых предложений нет: … реализовано и описано в разделах …». Удалять можно только устаревшие пункты внутри, не сам раздел.

### Раздел «Для разработчиков»
Хендофф-пакет (не копипаст-код под стек), только два блока с copy-to-clipboard:
- **Точные размеры (redline)** — таблица высот/паддингов/радиусов/зазоров/типографики по размерам. Значения — через `getComputedStyle(...)` с реально отрендеренного экземпляра (НЕ `getBoundingClientRect()` — на скрытых off-screen вернёт 0), чтобы не расходились с CSS.
- **Поведение · псевдокод (framework-agnostic)** — сложная логика (позиционирование, измерения, клавнавигация) как алгоритм, не vanilla-JS сниппет.
- «Анатомию DOM», «Рекомендуемый API (React)», «Справочник классов» НЕ добавлять.

### Конструктор (playground)
Панель настроек — на pg-kit (`styles/pg-kit.css` + `pg-kit.js` подключены):
- Между настройками и превью — Splitter из ДС (`.spl`), перетаскивание меняет ширину, двойной клик — сброс. Не убирать.
- Дефолт для всех страниц: панель настроек 360px (`--pg-controls-w` в `pg-kit.css`), превью 1fr, настройки в одну колонку. На странице ничего не прописывать — только подключить pg-kit. Две колонки — лишь если пользователь сам расширил панель сплиттером (≥640px).
- Селекты из 2–4 коротких опций → SegmentControl; 5+ опций или длинные подписи → выпадающий список (сегменты не переносить в 2 ряда). Принудительно оставить селект — `data-pg-keep-select` на `.ctl`.
- Выбор иконки — сетка глифов, не список названий.
- Неактуальные контролы скрывать полностью (`.is-off`), не блокировать/затемнять.

## Таблицы в документации
Любая табличная разметка на странице компонента (редлайн, справочник токенов/классов, размеры, **справочник цветов**) собирается на компоненте ДС Table/TableCell (`.tbl`/`.tbl__row`/`.th`/`.tc`, см. `specs/TableCell.md` и блок TableCell в `specs/_cheatsheet.md`), не на нативном `<table>`/`.ref-table`/`.dev-table` и не на самодельной сетке `.color-ref`/`.cref-group`/`.cref-row` (устаревший паттерн, отовсюду убран). Образец блока «Цвета» — `pages/molecules/DropdownList.html`: `.panel` → `.tbl` со столбцами Раздел/Роль/Токен/Образец (`<span>` 22×22, `border-radius:var(--radius-xs)`, `background:var(--токен)`)/Hex. Подключить `styles/table-cell.css`.

## Порядок компонентов в списках
В каждой группе (Основы / Атомы / Молекулы / Организмы) карточки в `index.html` и пункты в `scripts/ds-nav.js` — строго по алфавиту видимого названия (`card__title` / `label`): «Основы» по кириллице (Иконки, Иллюстрации, Скругления, Тени, Типографика, Цвета), остальные по латинице (имя = имя файла). Заглушки `soon: true` — в конце группы, вне алфавита. Новый компонент вставлять на алфавитно верную позицию, не в конец.

## Экономия токенов (приоритет над привычками)
1. **Спеки вместо страниц.** У каждого компонента `specs/<Имя>.md` (6–9 KB вместо 40–70 KB страницы): YAML-шапка, правила, DOM-анатомия, классы. Манифест — `specs/_index.md`. Для молекул/организмов читай спеки вложенных компонентов, не страницы.
2. **Спеки держать актуальными.** Изменил/создал компонент → тем же ходом обнови/создай спеку (образец `specs/_TEMPLATE.md`). Устаревшая спека хуже отсутствующей.
3. **Точечно.** `grep` вместо чтения целиком; замена фрагмента вместо перезаписи файла; однотипные правки во многих файлах — скриптом на Node.
4. **Новые страницы** — копией ближайшей (обычное копирование файла) + точечные правки, не boilerplate. В правильную папку: `pages/{foundations,atoms,molecules,organisms}/`.
5. **Скриншоты не нужны и не заменяют проверку.** Раскладка проверяется статическим сенсором `layout-check.mjs` (см. «Проверка: чем гонять»), целостность — линтером. Папка `screenshots/` — временная свалка для человека: не плодить, чистить за собой.
6. **Полнота index.html и ds-nav.js — при КАЖДОМ новом компоненте.** Оба обязаны перечислять ВСЕ файлы из `pages/*/`. Сверка без чтения страниц: перечислить `pages/<категория>/` → сравнить с `ds-nav.js` (читается целиком) → `grep` по `index.html` на `<Имя>.html`. Расхождение — добавить в оба места. Механически это же ловит линтер (`node scripts/ds-lint-cli.mjs`, правила D1–D4).

## Сборка экранов и организмов
1. **Контекст = один файл, но точечно.** `specs/_cheatsheet.md` — все компоненты: css, deps, назначение, DOM-сниппет. Файл большой, **целиком не читать**: оглавление `grep -n "^## "`, блок компонента `sed -n '/^## <Имя>$/,/^## /p'`. Полную спеку — только для 1–2 компонентов со сложным поведением. CSS компонентов не читать.
2. **Один линк стилей** — только `ds.css` (импортирует всю ДС), не styles/* поштучно. Новый компонент → добавить его @import в `ds.css`.
3. **Шаблон** — копия `templates/screen/Screen.html`, не с нуля.
4. **Иконки** — только `<i data-icon="имя"></i>` + подключённые `scripts/icons-data.js` и `scripts/ds-icons.js` (сам вставляет SVG в currentColor; размер — width/height, дефолт 24px). Инлайн-SVG запрещён.
5. **Стили экрана** — токены и классы ДС; свой CSS только раскладка (grid/flex/gap).
6. **Нет нужного компонента в ДС** (Input, Table, Modal, Card…) — не выдумывать на ходу. Остановись, назови недостающее, сначала создаём его как компонент ДС, потом экран.
7. **Экран статичен** (мокап): состояния показывать вёрсткой (открытый дропдаун — отрисован открытым). JS — только если явно попросили кликабельный прототип.
8. **Чит-шит актуален**: новый/изменённый компонент → обнови его блок в `specs/_cheatsheet.md` тем же ходом.
9. **Тайлы в одном ряду равны по высоте** — высота строки определяется тайлом с максимальным контентом (`Tile.md`, «Размеры»). Пока правило не в CSS компонента — ставить `align-items:stretch` на ряду руками.
10. **Рантаймы — через единую точку входа `scripts/ds.js`.** Один тег `<script src="ds.js"></script>` в конце body догружает все рантаймы ДС (icons-data, ds-icons, ds-tabs, ds-tile, ds-menu, ds-popover, ds-tooltip, ds-modal, ds-table, ds-pagination, ds-notify, ds-datepicker, input-kit, ds-nav-panel, ds-splitter, ds-illustrations) в правильном порядке; каждый молча ничего не делает без своей разметки на странице, поэтому список одинаков для любого экрана. Экранный скрипт с данными конкретного экрана — отдельным тегом сразу после `ds.js`. Подключать перечисленные файлы напрямую не нужно и ловится линтером (`A7`). Закрыто 12.08.2026 — до этого недоподключено на всех 12 экранах.
11. **Компонент-композиция собирается целиком.** RiskMetric = Chip + Popover: без слоя `.pop` это не компонент. Так же Split-кнопка (ButtonGroup + меню), Select (+ DropdownList), переполнение табов (обёртка + меню «Ещё»). Половина композиции не диагностируется — выглядит нормально, не работает.
12. **Метод рантайма зовут по имени из экспорта, а не по памяти.** Идиома `window.DSX && DSX.method(...)` при опечатке в имени метода не падает и ничего не пишет в консоль: guard просто ложный, и функциональность тихо отсутствует. Так на Kanban не появлялся индикатор сегмент-контрола (`DSTabs.bindAll` вместо `wireAll`), не закрывалась модалка подтверждения (`DSModal.close` вместо `closeTop`), не рисовались иллюстрации пустого состояния (`DSIllustrations.apply` вместо `render`). Ловится линтером (`A9`) — правило репозиторного уровня: сверяет обращения `DSX.method(` во всех скриптах и страницах с ключами `window.DSX = {…}` соответствующего рантайма. Заведено 10.09.2026 после трёх таких промахов в одном заходе.
13. **Разметка, перерисованная скриптом, обязана быть связана заново.** Рантаймы ДС связывают свою разметку один раз, на `DOMContentLoaded`. Экран или витрина, пересобравшая блок через `innerHTML`, зовёт `DSMenu.bindAll(scope)`, `DSModal.bindAll(scope)`, `DSDrawer.bindAll(scope)`, `DSTabs.wireAll(scope)`, `dsIcons.apply(scope)` — иначе кнопки и меню остаются мёртвыми, причём молча.
14. **Скроллпорт и ряд одинаковой высоты — разные элементы.** Горизонтальный flex-контейнер, который сам является скроллпортом (`overflow: auto`), растягивает своих детей по высоте ВИДИМОЙ ОБЛАСТИ (`align-items: stretch` — дефолт), а не по высоте самого длинного из них. Если внутри ребёнка есть `position: sticky`, он держится только до конца этой коробки — то есть до конца одного экрана прокрутки, а контент, не влезший в коробку, торчит за её пределами и теряет hover/dropzone. Решение — развести роли: контейнер с `overflow` (скроллпорт) и вложенный в него ряд с высотой `auto` (растягивание по контенту). Пример — `.kanban__viewport` / `.kanban__track`. Ловится линтером (`B13`, WARN): срабатывает только если в том же CSS-файле объявлен `position: sticky`.

### Сверка с макетом
Соответствие картинке-референсу автоматика не проверяет: ни линтер, ни `layout-check.mjs` с макетом не сверяют. Скиллы сборки по скриншоту и гейт `FIDELITY-GATE` (правило линтера G1) сняты 13.09.2026: на рабочем контуре модель не читает изображения, а браузер по скрипту запрещён, так что гейт был невыполним и краснил каждый экран. Если макет есть — сверка остаётся за человеком на приёмке; метрики макета (ширина, плотность, типошкала) переносятся в ТЗ текстом.

### Чек-лист: новый экран
1. Прочитай `specs/_cheatsheet.md`, составь список нужных компонентов.
2. Чего-то нет в ДС — остановись, сообщи, дождись решения.
3. Для 1–2 сложных компонентов прочитай полные спеки. Больше ничего.
4. Скопируй `templates/screen/Screen.html` под именем экрана.
5. Собери разметку из DOM-сниппетов; иконки — `<i data-icon>`; свой CSS — только раскладка.
6. Прогони линтер целостности: `node scripts/ds-lint-cli.mjs <путь к экрану>` — почини все BLOCKER.
7. Прогони сенсор раскладки: `node ../.opencode/skills/screen-review/tooling/layout-check.mjs <путь к экрану> --width 1920` — почини FAIL.
8. Оба чисты — экран готов к показу. Отчёт: что собрано, что осталось открытым, что сверить с макетом на приёмке.

## Новый компонент
Промпт-роль (действует автоматически при референсе + названии компонента): ты топовый дизайнер интерфейсов и дизайн-систем. Собери страницу компонента и добавь в ДС на токенах и шрифтах ДС (ванильный HTML/CSS, без React). Не знаешь, что брать — спрашивай. Предложи, чего не хватает (состояния, корнер-кейсы). Основной референс — Material Design, но бери лучшие практики и из других ДС. Делай по аналогии с соседними компонентами.

### Чек-лист (строго по порядку)
1. Прочитай `specs/_cheatsheet.md` и спеки 1–2 похожих. Страницы целиком не читай.
2. Создай `styles/<имя>.css` — на токенах ДС.
3. Добавь `@import url("styles/<имя>.css");` в `ds.css` (в конец группы).
4. Страница `<Имя>.html`: копия ближайшего похожего в нужную папку + точечные правки. Шапка: «v1.0», текущая дата. Разделы — по контракту выше. Первой строкой — карточка `<!-- @dsCard group="..." ... -->` (требует линтер, правило C1).
5. Создай спеку `specs/<Имя>.md` (образец `specs/_TEMPLATE.md`).
6. Добавь блок компонента в `specs/_cheatsheet.md`.
7. Добавь карточку в `index.html`: `grep` соседних карточек группы → точечная вставка на алфавитную позицию, обнови счётчик в `.sec-head`.
8. Добавь пункт в `scripts/ds-nav.js` — та же группа, алфавитная позиция.
9. Сверься: число файлов в `pages/<категория>/` = число карточек в index.html = пунктов в ds-nav.js (это же проверяют правила D1–D4 линтера).
10. Прогони гейт: `node scripts/ds-check.mjs pages/<категория>/<Имя>.html` — почини все BLOCKER/FAIL до вердикта OK.
11. Прогони глобальные правила: `node scripts/ds-lint-cli.mjs` (без аргументов) — реестры index/ds-nav/спек должны сойтись.
12. **Если у компонента есть правило применения, которое стережёт линтер** — порядок строго такой: сперва правило в `scripts/ds-lint.js` и пара фикстур, потом `lessons-cli verify` (падает на дефекте, молчит на эталоне), потом `lessons-cli anchors --write`, и только после этого ссылка на номер правила в CSS, спеке, чит-шите и `ds-rules.md`. Номер берётся из реестра, а не назначается по памяти: занятый номер в ссылке выглядит совершенно нормально — идентификатор живой, `anchors` молчит, гейт зелёный, — и врёт молча (урок Л94; при заводе SubTab ссылка «правило B13» указывала на чужое правило про sticky-скроллпорт).

## Правка существующего компонента
1. Прочитай `specs/<Имя>.md`; страницу — точечно (grep/offset).
2. Правь точечной заменой фрагмента, не перезаписью файла.
3. Версия/дата по правилу «Версионирование»; обнови те же поля в YAML-шапке спеки. Допиши строку в `CHANGELOG.md`.
4. Обнови изменённое в `specs/<Имя>.md` и в блоке `specs/_cheatsheet.md`.
5. Прогони гейт: `node scripts/ds-check.mjs pages/<категория>/<Имя>.html` — почини всё до вердикта OK.

## Проверка: чем гонять
Всё проверяется локально, из терминала, обычным Node — внешних сервисов, шага сборки и браузера в этом флоу нет. Браузерная проверка живости `docs-split verify` удалена 13.09.2026: в контуре компании её нельзя выполнить, а гейт с недоступным шагом либо всегда красный, либо молча пропускает (класс урока Л37).

| Команда (из корня ДС) | Что делает | Когда |
|---|---|---|
| `node scripts/ds-check.mjs <страница>` | **Единый гейт.** Прогоняет линтер по странице, а для страниц на docs-split (`class="page ds-split"`) ещё структурную проверку. Вердикт один: OK / FAIL | Финал любой правки компонента или новой страницы |
| `node scripts/ds-lint-cli.mjs` | Только глобальные правила: реестры `index.html` / `ds-nav.js` / спек, парные `@import`, счётчики групп | После добавления/переименования компонента |
| `node scripts/ds-lint-cli.mjs <страница…>` | Линтер по конкретным страницам (принимает несколько путей и маски) | Точечно, когда гейт избыточен |
| `node scripts/ds-lint-cli.mjs --parity` | Гейт парности «доки = код» | Когда правился CSS компонента |
| `node ../.opencode/skills/screen-review/tooling/layout-check.mjs <экран> --width 1920` | Статический сенсор раскладки экрана, без браузера. Состав проверок — `layout-check.mjs --rules` (прозой не перечисляется: копия разъезжается с кодом, урок Л43) | Финал сборки экрана |
| `node ../.opencode/skills/screen-review/tooling/lessons-cli.mjs add --from <черновик.md>` | Дозапись урока в архив: номер `max+1` по обоим файлам, дата, EOL файла, `updated:`. Сам прогоняет `check` | Когда записываешь урок |
| `node ../.opencode/skills/screen-review/tooling/lessons-cli.mjs check` | **Форма записей журнала.** БЛОКЕР — запись врёт (якорь на несуществующего сторожа, подмена алфавита `B9`/`Б9`, задвоенный номер, недоказанное закрепление, ритуал описан в двух местах); ЗАМЕЧАНИЕ — запись неполна. Код 1 только на блокерах | После записи урока и после переименования правил |
| `node ../.opencode/skills/screen-review/tooling/lessons-cli.mjs anchors [--write]` | Реестр живых идентификаторов сторожей против кода. Код 1 при расхождении: «есть в реестре, нет в коде» = уроки с этим якорем повисли | После того как правило добавлено, удалено или переименовано |
| `node ../.opencode/skills/screen-review/tooling/lessons-cli.mjs verify` | **Оракул закрепления.** Прогоняет пары фикстур «дефект/эталон» по ТРЁМ корпусам: сенсор — `.opencode/skills/screen-review/tooling/fixtures/`, линтер-страницы — `DS-IBP/fixtures/`, линтер-экраны — `.opencode/skills/screen-review/tooling/fixtures/lint-screens/`. Правило засчитывается закрытым, только если реально падает на воспроизведённом дефекте и молчит на эталоне. Код 1 — сторож не доказан | После того как урок закреплён сторожем; и как регресс-корпус обоих инструментов |
| `node ../.opencode/skills/screen-review/tooling/lessons-cli.mjs coverage` | Чем закрыт каждый пункт чек-листов: сторожем · суждением (с причиной) · «посильно, не написано». Код 1 — есть пункт без решения | Когда решаешь, какое правило закрывать следующим |
| `node ../.opencode/skills/screen-review/tooling/lessons-cli.mjs state` | Состояние журнала уроков: объём выжимки против предела, долг курации, сколько записей объявили закрепление. Код 1 — долг курации ≥ 3 | В конце захода, где что-то чинилось |
| `node scripts/ds-icon.mjs <имя>` / `--list [подстрока]` | Один глиф или поиск по именам, без чтения `icons-data.js` | Когда нужна иконка |
| `node scripts/spec-audit.mjs` | **Read-only аудит «правило объявлено — кода нет»** (Л42): проходы по репозиторию: обещания спек, чит-шита и манифеста против кода, CSS и каталога компонентов в правилах агента (состав проходов — в шапке скрипта, прозой не перечисляется). Ничего не пишет. Код выхода 1 — если есть не закрытое обещание «усечено → тултип» (корневой дефект CM) или манифест `specs/_index.md` расходится с каталогом §6 правил агента (проход 8) | После правки спеки/рантайма, манифеста или каталога в правилах агента |

Коды выхода: 0 — чисто, 1 — есть BLOCKER/FAIL. Все пути — относительные от корня ДС.

## Целостность страниц (линтер ДС)
Тихие ошибки — забытый парный CSS, несуществующий токен, нарушенный контракт разделов, рассинхрон index / ds-nav / спек — ловит линтер `scripts/ds-lint.js`. Сам по себе он не исполняемый (ждёт хелперы `readFile`/`ls`) и на страницах не подключается: запускать только через обёртку `scripts/ds-lint-cli.mjs` или гейт `scripts/ds-check.mjs`. Разбор правил и групп — `skills/ds-integrity-check.md`. Прогон обязателен на любом новом или правленом компоненте и экране; страницу «глазами для перепроверки» не читать — источник истины один, отчёт линтера. Разобрал инцидент — тем же заходом допиши класс ошибки в линтер, иначе он повторится.

Класс дефекта «правило объявлено в спеке — реализации нет» (Л42) статический линтер не ловит — он проверяет связность/токены, а не «обещано поведение → есть код». Для него — read-only `scripts/spec-audit.mjs` (см. «Проверка: чем гонять»): сверяет спек/манифест против рантаймов и выносит не закрытые обещания (например, «усечено → тултип»). Прогонять после правки спеки/рантайма, когда заявлено поведение.

## Общие принципы работы
Очевидные баги правь сам. По неоднозначному (новый раздел, смена визуала, изменение API) — сначала спроси. Не переписывай разделы без замечаний.

## Тона состояний Button и Link
У Button и Link есть тоновые классы `.btn--<tone>` и `.link--<tone>` (info/warning/error/success). Кнопки и ссылки внутри Alert и SnackBar красятся именно этими классами (совпадают с тоном плашки); локальная подмена `--primary`/`--link-fg` убрана (17.07.2026) — вместе с ней ушло и предупреждение о 37 «своих» токенах Snack/Alert.

## Структура проекта
- `index.html` — обзор ДС (карточки), в корне.
- `CHANGELOG.md` — журнал правок ДС (см. «Версионирование»), в корне. Большой: **целиком не читать**, дописывать в секцию текущей даты (найти её `grep -n "^## "`).
- `ds.css` — единственная точка подключения ДС (@import всех styles/*), в корне. Зеркало `styles.css` удалено 13.09.2026: его не линковала ни одна страница, а ритуал нового компонента требовал писать `@import` дважды.
- `templates/screen/Screen.html` — стартовый шаблон экрана.
- **Артефактов витрины в репозитории нет.** `_ds_bundle.js` (сборка всех `scripts/*.js` в один файл), `_ds_manifest.json` (индекс карточек `@dsCard`), `thumbnail.html` и `.thumbnail` (иконка ДС), `_adherence.oxlintrc.json` (конфиг стороннего линтера) были производными внешнего инструмента и удалены 04.09.2026: ни одна страница их не подключала, локальный линтер и сенсоры их не читают. Не воссоздавать. Маркер `@dsCard` первой строкой страницы остаётся — его требует линтер (C1).
- `pages/` — документация компонентов по категориям (первой строкой каждой — карточка `@dsCard`):
  - `foundations/`, `atoms/`, `molecules/`, `organisms/`, `patterns/`, `screens/` — актуальный состав не перечисляется здесь (копия разъезжается с диском); источник — `specs/_index.md`, полноту сверяет линтер (D1–D4).
  - `rnd/` — рабочие материалы, не часть поставляемой ДС: Backlog (общий бэклог ДС).
  - Внутри `pages/<категория>/`: стили `../../styles/*.css`, скрипты `../../scripts/*.js`, ссылки на страницы `../<категория>/<Имя>.html`, на index `../../index.html`. Компоненты подкачки (Alert, Label + Helper) переехали в molecules/atoms — отдельной папки `loading/` больше нет.
- `scripts/` — общие (`ds-icons.js`, `ds-nav.js`, `ds-toc.js`, `pg-kit.js`, `icons-data.js`, `image-slot.js`), инструменты проверки (`ds-lint.js` + обёртка `ds-lint-cli.mjs`, гейт `ds-check.mjs`, `ds-icon.mjs`; не браузерные, запускаются из терминала) и per-компонентные `<имя>.page.js`.
  - `ds-icons.js` — `<i data-icon="имя"></i>` → SVG из icons-data.js в currentColor.
  - `ds-nav.js` — левая навигация; полный список страниц путями от корня, префикс через `window.__DS_ROOT` (страницы в `pages/*/` объявляют `'../../'`; index.html — по умолчанию `''`). В начале `mount()` — no-op guard: монтируется только при наличии `<main class="page">`, иначе молча выходит и ничего не ломает.
  - `ds-toc.js` — правый оглавитель; тот же no-op guard.
  - `pg-kit.js` — слой панелей «Конструктор» (см. раздел «Конструктор»).
  - **Стиль подтягивается сам.** `ds-nav.js`, `ds-toc.js`, `pg-kit.js` при монтировании сами вставляют `<link>` на свой CSS (`ds-nav.css` / `ds-toc.css` / `pg-kit.css`), если страница его не подключила. Явный `<link>` в `<head>` предпочтителен (нет вспышки нестилизованного блока), но забыть его больше не ломает страницу. Правило остаётся: эти скрипты подключаются парой js + css.
- `styles/` — CSS компонентов и фундамента (плоско): `typography.css` (SB Sans Display/Text/Screen, токены `--type-*`, классы `.ds-*`), `colors.css` (базовые токены цветовых рамп: `--amber-500`, `--swamp-A100` — единственное место с hex), `palette.css` (семантика Static/Active/Situative/Status/Chart, значения — только `var(--базовый)`), по файлу на компонент.
- `specs/` — md-спеки с YAML-шапкой: `_index.md` (манифест), `<Имя>.md` (поле `page:` = путь `pages/<категория>/<Имя>.html`), `Icons.md` (все глифы).
