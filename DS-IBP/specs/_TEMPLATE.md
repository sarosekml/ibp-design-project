---
component: ИмяКомпонента          # = имя файла страницы без .html
title: "Название по-русски"
version: "1.000"                   # держать равным версии на странице; формат М.ммм, шаг +0.001
updated: "ДД.ММ.ГГГГ"              # держать равным дате на странице
page: pages/<atoms|molecules|organisms>/ИмяКомпонента.html
page_js: scripts/имя-компонента.page.js    # если есть; иначе удалить строку
runtime: scripts/ds-имя-компонента.js      # рантайм(ы), оживляющие компонент; если есть; иначе удалить строку (механика «усечено → тултип» — через scripts/ds-tooltip.js, см. его регистрацию)
css: styles/имя-компонента.css
deps: [button, tooltip]            # CSS-зависимости (имена файлов без .css); нет — удалить строку
status: curated                    # curated = проверено человеком/агентом; auto = автогенерация
---

> Спека для быстрого контекста. Источник истины — CSS-файл и страница компонента. При изменении компонента обновляй эту спеку и блок в specs/_cheatsheet.md.

## Назначение
1–3 предложения: что это и когда используется.

## Инварианты
2–5 пунктов: правила, которые нельзя нарушить при сборке экрана (не пересказ разделов ниже — только то, что ломается молча, если пропустить). Пример: «только в составе группы, не как единственная опция», «справа — либо шеврон, либо иконка, не одновременно». Строка попадает в specs/_cheatsheet.md как есть — не переформулировать.

## Ключевые правила (из разделов страницы)
Пункты = канонические h2 страницы (единый контракт, см. MAINTAINING.md, Шаг 2). Порядок и имена не менять; неприменимый раздел — строка «Не применимо: …», а не удаление.
- **Использование** — краткая выжимка: когда использовать/не использовать, сравнения с соседями.
- **Анатомия** — из чего состоит.
- **Варианты** — оси конфигурации: типы, тоны, формы, ориентация, режимы.
- **Размеры** — размерный ряд и высоты.
- **Контент** — правила текста/наполнения, переполнение.
- **Поведение** — позиционирование, анимация, тайминги, адаптивность.
- **Состояния** — Default/Hover/Active/Disabled и особые.
- **Доступность** — роли, ARIA, фокус.
- **Типографика** — токены `--type-*`.
- **Цвета** — какие токены в каких частях.

## Диагностика
Опционально, только если есть реальные симптомы из разбора экранов (не выдумывать). Формат — одна строка на симптом, видимый на экране → класс/приём компонента, который это решает:
- «длинный текст растягивает строку» → `.имя--truncate`

## Для разработчиков (выжимка)

### Точные размеры (redline)
Таблица рендерится на странице через getComputedStyle. Точные значения — в CSS-файле компонента (см. `css:` в шапке).

### Разметка · HTML (эталонная реализация ДС)

```
<div class="имя">…</div>
```

### Поведение · псевдокод (framework-agnostic)
Только если у компонента есть JS-логика; иначе раздел удалить.

### Справочник классов и атрибутов

| Класс/атрибут | Назначение |
|---|---|
| `.имя` | корень компонента |
