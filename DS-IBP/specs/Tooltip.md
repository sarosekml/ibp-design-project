---
component: Tooltip
title: "Tooltip"
version: "2.009"
updated: "11.09.2026"
page: pages/molecules/Tooltip.html
page_js: scripts/tooltip.page.js
runtime: scripts/ds-tooltip.js
css: styles/tooltip.css
deps: [button]
status: auto
---

> Автоспека для быстрого контекста. Источник истины — CSS-файл и страница компонента. При изменении компонента обновляй эту спеку.

## Назначение
Всплывающая подсказка при наведении или фокусе на объект — иконку, IconButton, обрезанный текст или поле с ошибкой. Контент — строка, тултип не может быть пустым. Хинт может быть со стрелкой или без неё, в любом из 12 краевых положений.

## Из коробки
Рантайм `scripts/ds-tooltip.js` (`window.DSTooltip`) — поведение доступно потребителю без кода на экране.

| Что | Как |
|---|---|
| Автоподключение | `<button data-tooltip="Удалить">` — рантайм оборачивает цель в `.tip-anchor` и строит `.tip`; либо своя разметка: цель с `aria-describedby` на `.tip` внутри `.tip-anchor` |
| Настройки на цели | `data-tooltip-placement` (top\|bottom\|left\|right, деф. top) · `data-tooltip-align` (start\|center\|end, center) · `data-tooltip-type` (main\|error) · `data-tooltip-gap` (8) · `data-tooltip-delay` (400) · `data-tooltip-flip="no"` · `data-tooltip-boundary="<селектор>"` · `data-tooltip-truncated="only"` · `data-tooltip-multiline="yes"` |
| API | `bind(target, opts)` · `bindAll(root)` · `make(text, opts)` · `place(tip, target, opts)` · `hideAll()` · `current()` · `truncated(selector, opts)` |
| Что даёт | 12 позиций, авто-flip стороны и выравнивания, clamp по границе, стрелка доводится до центра цели, 400 мс по hover / мгновенно по focus, мгновенное скрытие, Esc, один показанный тултип одновременно, rich остаётся открытым под курсором (300 мс), детект усечения по `scrollWidth > clientWidth` |
| «Усечено → тултип» | `DSTooltip.truncated('.подпись', { host, disabled, disabledClass, skip, text })` — одна регистрация на владельца: lazy-делегирование по pointerover/focusin, тултип показывается только при реальном усечении. `host` — хост, на который приходит фокус (chip→`.chip`, tab→`.tab`, nav→`.nav__item`/`.nav__user`); `disabled`/`disabledClass` — скан `pointer-events:none` элементов (усечённая подпись disabled-чипа/таба, хука вида «корень--has-tooltip»); `skip`/`text` — исключения и свой извлекатель. Регистрируют Chip, Tab, NavPanel, ReadOnlyField, Table |

`tooltip.page.js` после экстракции только собирает демо-разметку и вызывает этот рантайм.

## Инварианты
- Плавающий слой, открытый ИЗ модалки, монтируется в её скрим, а не в общий слой `.ds-float-layer`: правило слоя, `DSFloat.mount(el, { anchor })` (см. Elevation). Своей проверки «а не в модалке ли я» компонент не держит.
- Корень компонента объявляет парное `[hidden] { display: none }`: браузерное правило имеет специфичность (0,0,0) и приходит из UA-стиля, а `display` компонента — (0,1,0) и перебивает его, из-за чего атрибут `hidden` молча перестаёт работать. Соглашение ДС от 05.09.2026, охраняется правилом B11 линтера.
- По умолчанию одна строка с эллипсисом; перенос — только через явный `.tip--multiline` (или `data-tooltip-multiline="yes"` на цели), иначе длинный текст обрезается тихо.
- Проверка усечения стоит ПЕРЕД построением разметки, а не только перед показом. `attachTrunc` оборачивает подпись в `.tip-anchor` и вставляет рядом `.tip`; `allowed()` внутри `show()` гасит только ПОКАЗ, а узлы к этому моменту уже в DOM — на странице без `tooltip.css` такой `.tip` рисуется обычным текстом и подпись дублируется. Ветка `pointerover` в `truncated()` проверяет `isTruncated` до `attachTrunc` (2.009); `focusin` и `refresh()` проверяли изначально.
- Страница, подключившая `scripts/ds-tooltip.js`, обязана подключить и `styles/tooltip.css`: рантайм свой CSS не догружает, а разметку строит. Охраняется правилом A1 линтера (BLOCKER).
- Якорь `.tip-anchor` раскладочно прозрачен (`min-width:0; max-width:100%`): рантайм оборачивает им уже свёрстанную цель, и якорь не должен мешать ей сжиматься.
- `.tip--rich` — единственный интерактивный тултип (курсор может зайти внутрь); обычный тултип неинтерактивен и закрывается при уходе курсора.
- Тип (main/error) красит тултип и его стрелку одновременно — не два независимых цвета.
- Стрелка показана по умолчанию (в отличие от Menu/DropdownList/Popover, где она выключена) — отключается явно `.tip--no-arrow`.

## Диагностика
- «Тултип не виден внутри модалки» → рантайм старше 2.007: тултип монтировался в общий слой `DSFloat` (`z-index: 30`) под скрим модалки (`z-index: 1000`) и попадал под её `inert` — элемент создавался и помечался видимым, но рисовался за подложкой. С 2.007 `show()` передаёт в `mount` якорь (цель тултипа), и слой сам выбирает скрим
- «По наведению подпись дублируется: рядом с текстом таба/чипа появляется его копия» → страница не подключила `styles/tooltip.css` (рантайм построил `.tip`, стилей нет — он рисуется обычным текстом), и/или рантайм старше 2.009, который строил `.tip` даже для неусечённой подписи. Чинится подключением CSS; правило A1 линтера ловит это статически
- «Длинный текст в тултипе обрезается многоточием» → добавить `.tip--multiline`, иначе текст ограничен одной строкой по спеке
- «Тултип закрывается при наведении на его содержимое» → ожидаемо для обычного тултипа; для интерактивного контента использовать `.tip--rich`
- «После подключения тултипа текст цели перестал усекаться и вылез за контейнер» → якорь без `min-width:0` не сжимался (inline-flex, автоминимум = min-content); исправлено в 2.005, отдельных правок на экране не требуется

## Ключевые правила (из разделов страницы)
- **Использование** — Три типичных сценария — пояснение иконки, полное значение обрезанного текста, сообщение об ошибке рядом с полем. Наведите курсор на каждый пример.
- **Использование** — Используйте тултип, когда нужно:
- **Анатомия** — Тултип состоит из контейнера с фоном, текстового контента и опциональной стрелки, направленной на объект подсказки.
- **Размеры** — В общем случае размер тултипа определяется размером контента. Задаётся максимальная ширина контейнера (--tip-max = 280px). Если ширина контента с паддингами меньше максимальной — тултип сжимается до обтекания контента.
- **Размеры · Радиус скругления** — Радиус не зависит от размера контента и места стрелки. плашка — 8px (--tip-radius = --radius-control) · кнопка-действие — 6px (--radius-control-s) · стрелка — 0.
- **Контент** — Текст тултипа — единственное содержимое компонента; пустым он не бывает. Правила:
- **Варианты · Типы** — Виды тултипа отличаются между собой цветом фона. Main — нейтральные подсказки, Error — сообщения об ошибке рядом с полем или контролом.
- **Варианты · Богатый контент** — `.tip--rich`: заголовок + описание (1–3 строки) + опциональное одно действие, max-width 320px, паддинги 12/16, текст всегда переносится. Единственный интерактивный вариант — курсор может зайти внутрь, скрытие с задержкой 300 мс. Два действия и больше, форма, список или крестик — это Popover.
- **Поведение · Размещение и стрелка** — Тултип размещается с одной из 4 сторон объекта (top · bottom · left · right), а стрелка выравнивается вдоль края по одному из 3 положений (start · center · end) — всего 12 комбинаций. Стрелка всегда направлена на объект подсказки.
- **Авто-разворот и смещениеНовое** — Если у предпочтительной стороны не хватает места, тултип разворачивается на противоположную сторону, а вдоль края смещается, чтобы не выходить за границы экрана — при этом стрелка остаётся направленной на объект. Перетащите цель к краям области, чтобы увидеть поведение.
- **Триггеры и таймингиНовое** — Тултип появляется при наведении курсора и при фокусе с клавиатуры, скрывается при уходе курсора, потере фокуса или нажатии Esc. Чтобы случайные пересечения курсора не вызывали мелькания, появление задерживается на ~400 мс, скрытие — мгновенное. На тач-устройствах тултип вызывается долгим нажатием.
- **ДоступностьНовое** — Контейнеру присваивается role="tooltip" и уникальный id, а объект-триггер ссылается на него через aria-describedby. Триггер должен быть фокусируемым (кнопка, ссылка или элемент с tabindex="0"). Тултип не перехватывает фокус и закрывается по Esc.
- **Использование · Рекомендации** — Держите текст коротким и по делу. Тултип поясняет объект, а не дублирует видимую подпись и не содержит длинных инструкций.
- **Типографика** — Текст тултипа набирается гарнитурой SB Sans Text токеном --type-body-s.
- **Цвета** — Компонент использует только семантические токены. Стрелка наследует фон контейнера через переменную --tip-bg.


## Для разработчиков (выжимка)

### Точные размеры (redline)

Таблица рендерится на странице через getComputedStyle. Точные значения — в CSS-файле компонента (см. `css:` в шапке).

### Разметка · HTML (эталонная реализация ДС)

```
<!-- триггер ссылается на тултип через aria-describedby -->
<button type="button" class="ibtn ibtn--neutral ibtn--m" aria-label="Удалить" aria-describedby="tip-1">…</button>

<!-- сам тултип: позиционируется JS относительно триггера, скрыт до показа -->
<span class="tip tip--main tip--top tip--center tip--floating" role="tooltip" id="tip-1">
  Удалить
  <span class="tip__arrow"></span>
</span>

<!-- без стрелки / с переносом -->
<span class="tip tip--error tip--bottom tip--start tip--no-arrow tip--multiline" role="tooltip">…</span>
```

### Поведение · псевдокод (framework-agnostic)

```
// 1. Триггеры: hover (задержка ≈400мс перед показом, чтобы случайное пересечение курсора
//    не вызывало мелькание), focus (мгновенно, без задержки); скрытие — мгновенно по mouseleave/blur/Esc

// 2. Позиционирование (вызывается при показе и при resize):
//    взять getBoundingClientRect() цели и тултипа относительно позиционирующего родителя;
//    top/bottom — y = край цели ± gap(8px); left/right — аналогично по X;
//    align center/start/end сдвигает вторую ось относительно центра цели (start/end — отступ 13px для стрелки)

// 3. Авто-разворот: проверить свободное место по предпочтительной стороне относительно viewport/контейнера;
//    не хватает — пробовать противоположную, затем перпендикулярные стороны; выбрать первую подходящую;
//    затем зажать позицию внутри границ контейнера и пересчитать позицию стрелки, чтобы она всё ещё указывала на цель

// 4. Перенос: если ширина одной строки > --tip-max — включить white-space:normal (.tip--multiline)

// 5. Доступность: триггер должен быть фокусируем элементом; тултип не перехватывает фокус и закрывается по Esc без сброса фокуса с триггера
```

### Tooltip.types.ts

```
type TooltipType = 'main' | 'error';
type TooltipPlacement = 'top' | 'bottom' | 'left' | 'right';
type TooltipAlign = 'start' | 'center' | 'end';

interface TooltipProps {
  content: React.ReactNode;         // обязательно; тултип не рендерится без содержимого
  children: React.ReactElement;     // триггер — должен принимать ref и быть фокусируемым
  type?: TooltipType;               // по умолчанию 'main'
  placement?: TooltipPlacement;     // по умолчанию 'top'; авто-разворачивается при нехватке места
  align?: TooltipAlign;             // по умолчанию 'center'
  arrow?: boolean;                // по умолчанию true
  rich?: boolean;                 // заголовок + описание + действие (интерактивный тултип)
  title?: string;                 // заголовок rich-варианта
  action?: { label: string; onClick: () => void }; // одно действие в rich-варианте
  delay?: number;                 // задержка появления при hover в мс, по умолчанию 400
  disabled?: boolean;             // полностью отключить тултип
}
```

### Справочник классов и атрибутов

| Класс / атрибут | На чём | Назначение |
|---|---|---|
| .tip | span | Контейнер: фон, паддинги 8/12, тень |
| .tip--main / --error | span | Тип (цвет фона) |
| .tip--top / --bottom / --left / --right | span | Размещение относительно объекта |
| .tip--start / --center / --end | span | Выравнивание стрелки вдоль края |
| .tip--no-arrow | span | Скрывает .tip__arrow, отступает от объекта на 8px |
| .tip--multiline | span | Перенос текста (white-space: normal) в границах --tip-max |
| .tip--rich | span | Богатый контент: заголовок + описание + действие; --tip-max 320px, паддинги 12/16 |
| .tip__title / .tip__text | span | Заголовок (Body S Strong) и описание (Body S, opacity .82) rich-тултипа |
| .tip__actions / .tip__action | span / button | Ряд действий и текстовая кнопка на тёмном фоне (hover — подложка из --tip-fg 16%) |
| .tip--floating | span | JS-позиционирование относительно триггера; показ/скрытие через .is-visible |
| .tip__arrow | span | Треугольная стрелка (clip-path), наследует фон через --tip-bg |
| .tip-anchor | span | position:relative обёртка для floating-тултипа |
| role="tooltip" / aria-describedby | span / триггер | Доступность: связь тултипа с объектом-триггером |
