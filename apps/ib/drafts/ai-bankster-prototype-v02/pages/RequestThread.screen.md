---
screen: RequestThread
title: Новый отчёт (нить запроса)
file: apps/ib/drafts/ai-bankster-prototype-v02/pages/RequestThread.html
source: ТЗ (БТ-2, БТ-3, БТ-4, БТ-6, БТ-7, БТ-8); черновой прототип модуля; макет экрана — сообщение заказчика от 12.09.2026
version: "1.020"
created: "12.09.2026"
design_system: IBP DS
components: [Layout, NavPanel, Breadcrumbs, PageHeader, Tile, Entity, Avatar, Alert, ProgressBar, Chip, Divider, LabelHelper, Link, Buttons, IconButton, ContextMenu, Modal, EmptyState, ReadOnlyField, SnackBar, Toast, Illustrations]
---

# Новый отчёт (нить запроса)

## 1. Назначение

Точка входа в раздел «AI Pitcher» и основная рабочая поверхность
запроса. В одной нити находятся постановка задачи, ход подготовки материала,
готовый материал и все уточнения (БТ-3.1). Отдельного диалогового окна у готового
материала не создаётся: уточнение задаётся здесь же.

Экран открывается **пустой нитью**: по БТ-4.2 (шаг 1) инструмент показывает нить
работы и поле ввода, и только потом пользователь выбирает, спросить текстом или
собрать материал в конструкторе.

Слева — панель истории запросов: последние 10 нитей с состоянием и действиями
(БТ-4.1, БТ-4.8).

**Роль пользователя:** сотрудник инвестиционного департамента (доступ по перечню,
кроме ролей администратор / ИТ / аудит / финансы).
**Права:** по умолчанию видны только собственные запросы и материалы (БТ-9.2).
Исключение на экране — документы коллег по выбранному объекту в конструкторе:
сами их запросы и переписка не видны (раздел 6; вопрос — раздел 16, №23).

## 2. Навигация

| Уровень | Текст | Ссылка |
|---|---|---|
| 1 | Главная | `index.html` |
| 2 | AI Pitcher | `RequestThread.html` |
| 3 (текущий) | Новый отчёт — или название открытого запроса | — |

Пункт бокового меню в состоянии `nav__item--selected`: «AI Pitcher»
(группа «Origination») — пункт ведёт прямо сюда.

## 3. Состояние экрана задаётся адресом

| Адрес | Состояние |
|---|---|
| `RequestThread.html` | новая пустая нить, заголовок «Новый отчёт», чипов и действий нет; с отправкой первого сообщения появляется запрос (раздел 4) |
| `RequestThread.html?id=N` | открыт запрос N из истории: заголовок — его название, чип — его состояние, нить восстановлена (БТ-4.3) |
| `RequestThread.html?run=1` | переход из конструктора: нить открывается сразу с записью о запуске и ходом работы (БТ-4.2 шаг 5) |

Заголовок и текущая крошка меняются вместе: пока нить пуста — «Новый отчёт»;
после первого сообщения — суть запроса и чип «Ответ в диалоге»; у открытого
запроса — его название и чип состояния.

## 4. Шапка страницы (PageHeader)

Шапка минимальная: у нового запроса ещё нет ни имени, ни версий, ни файлов,
поэтому показывать нечего. Как только у нити появляется запрос, справа встаёт
кебаб с меню этого запроса. «Новый отчёт» — кнопка над списком в панели слева
(раздел 5), конструктор запроса открывается из поля ввода (раздел 6).

| Элемент | Значение | Компонент / класс | Когда виден |
|---|---|---|---|
| Заголовок | «Новый отчёт» → название запроса | `h1.phead__title` | всегда |
| Статус | «Черновик» / «Ответ в диалоге» / «Подготовка идёт» / «Материал готов» | `.phead__chips` → `chip--rounded chip--s` | когда у нити есть запрос |
| Параметры запроса | сценарий и объём, дата обновления, число версий, число файлов, доступ | `.phead__subtitle` → `.phead__meta` | у открытого запроса и при подготовке; у запроса из первого сообщения параметров ещё нет |
| Кебаб действий | то же меню, что у строки списка | `btn--outline btn--m btn--icon-only`, глиф `more-dots` | когда у нити есть запрос |
| Получатели доступа | первые четыре пользователя и `+N` для остальных; список имён в тултипе | `.av-group.av-group--m.av-group--link` + `.av.av--circular.av--m` | после сохранения непустого выбора в «Поделиться», слева от кебаба |

Аватарки показывают **право просмотра**, а не присутствие онлайн. Владелец в
этот счётчик не входит. Люди, выбранные напрямую и через деск, не дублируются.
Подпись доступа в метаданных показывает число получателей.

Клик по аватаркам (и Enter или пробел с клавиатуры) открывает тот же диалог
«Поделиться чатом», что и пункт меню: получатели сразу видны в чипах полей.

У новой пустой нити в шапке только заголовок: ни имени, ни версий, ни файлов у
запроса ещё нет, и показывать нечего.

**Новый запрос из первого сообщения.** Пока пользователь печатает, экран остаётся
пустой нитью: список и шапка не меняются. С отправкой первого сообщения запрос
встаёт первой строкой в «Недавних запросах» — с чипом «Ответ в диалоге» и
отмеченный текущим; десятая строка уходит из списка. Заголовок и крошка
показывают суть: начало сообщения до 64 знаков по границе слова, с заглавной
буквы, без конечной пунктуации. Справа в шапке встаёт кебаб.
Запрос появляется после отправки, а не с началом печати, — решение заказчика
(13.09.2026).

## 5. Панель «Недавние запросы»

**Локальный компонент направления.** Компонента «список нитей» в ДС нет; панель
собрана из Button, LabelHelper, Entity, Chip, IconButton и ContextMenu, а
раскладку, поверхность и состояния строки держит сама панель на токенах ДС. Над
списком — кнопка «Новый отчёт», подпись и поиск по чатам. Подвала у панели нет.

**Место в каркасе.** Панель — колонка на всю высоту окна между меню и рабочей
областью, а не блок внутри контента:

```
.nav-layout
├── nav.nav.nav--rail            меню, 56px
└── .app-shell                   flex-строка, 100vh
    ├── aside.hist               панель истории, 320px
    └── .screen.screen--app      крошки + шапка + нить
```

Крошки и шапка страницы лежат внутри `.screen` и стоят **только над контентом**.
Это даёт два свойства: на узком экране панель прячется целиком (`@media
max-width: 1200px`), не задевая остальную раскладку, и в мобильной версии её
можно будет убрать под кнопку. Отступ под меню (`margin-left`) `.nav-layout`
по-прежнему даёт единственному не-`nav` ребёнку — `.app-shell`.

| Часть | Компонент | Значение |
|---|---|---|
| Поверхность | фон рабочей области `--bg-page` + правая граница `--border-light` | колонка на всю высоту |
| Кнопка | `btn--accent btn--m btn--fullwidth`, глиф `add` | Новый отчёт → пустая нить |
| Подпись списка | `.ds-label` (LabelHelper) | Недавние запросы |
| Поиск | локальное поле `.hsearch`, глиф `search`, крестик очистки | Поиск по чатам; пустой результат — локальный блок `.hist__empty`: «Ничего не нашлось» и Button Transparent XS «Сбросить поиск» |
| Строка списка | своя обёртка `.hist__item` + Entity `--s` внутри | одна нить |

**Что прокручивается.** Кнопка, подпись и поиск закреплены над списком и не
прокручиваются (решение заказчика, 14.09.2026); прокручивается только список.
Пока он прокручен, под поиском появляется линия `--border-light` — как у шапки
модалки ДС.

**Почему отступы совпадают.** У скролл-контейнера списка место под полосу
прокрутки зарезервировано всегда (`scrollbar-gutter: stable`), а правый паддинг
уменьшен на её ширину: 8px полосы + 8px паддинга дают те же 16px, что у кнопок.
Без этого строки списка были бы на ширину полосы уже кнопок.

**Почему состояния на обёртке, а не на Entity.** У Entity выбор строки —
`--primary-bg`, 6 % от `--primary`: на любой из подложек панели он практически
не читается. Переопределять состояние компонента снаружи нельзя (состояние
задаёт только сам компонент, ds-rules §4), поэтому наведение и «текущая нить»
живут на собственной обёртке строки и берут те же токены, которыми ДС красит
выбранную строку в NavPanel и в таблице: `--bg-table-default-hover` и
`--bg-table-default-focus`. Сам Entity внутри остаётся нетронутым.

Ширина панели — 320px, как на макете.

**Строка списка (Entity):**

| Слот | Что в нём | Пример |
|---|---|---|
| `entity__header` | сценарий · тип объекта · объём | По клиенту · Группа · Детальный |
| `entity__label` | название запроса, усечение многоточием | ГК «Северный агрохолдинг» — выход миноритария |
| `entity__subs` | число версий и дата обновления | Версий: 2 · обновлён 12 минут назад |
| `entity__chips` | состояние запроса, Chip `--s` | Материал готов |
| `entity__actions` | кебаб, IconButton `--s` с `data-menu` | меню действий над запросом |

Ведущего элемента (`entity__lead`) у строки нет: колонка узкая, состояние несёт
чип, и каждый лишний слот съедает ширину названия.

**Состояния запроса** — те же, что в реестре «История и материалы»: «Материал
готов» (success), «Подготовка идёт» (info), «Черновик» (нейтральный), «Ответ в
диалоге» (info), «Ошибка подготовки» (error).

**Кебаб-меню строки (ContextMenu, одно на список):** Поделиться · Переименовать ·
Дублировать · Настроить уведомление · разделитель · Удалить (danger).
Первое действие — «Поделиться». Триггеры всех строк и кебаб в шапке ссылаются на одно меню
`hist-menu` — штатная схема `data-menu` рантайма ДС.

Показываются последние 10 запросов. Перехода к полному перечню («История и
материалы», `RequestHistory.html`) с этого экрана нет: ссылка «Все запросы и
материалы» из панели убрана решением заказчика (21.09.2026), см. раздел 16, №20.
Запрос, созданный первым сообщением, встаёт первым (раздел 4).

## 6. Структура контента

Рабочая область — `.screen--app` (фикс. вьюпорт): нить скроллится внутри себя,
поле ввода закреплено снизу. Если открытый конструктор целиком не помещается
в окно, скроллить начинает сама рабочая область. Панель истории в рабочую
область не входит — она стоит рядом, см. раздел 5.

| № | Блок | Компонент | Поведение |
|---|---|---|---|
| 1 | Стартовое состояние | EmptyState `--l` + иллюстрация `dcm-potentials`, действие — Button Outline M «Показать пример запроса» | показывается, когда сообщений нет; кнопка примера видна, только пока поле ввода пустое; прячется через `visibility`, место резервируется — центрированный блок не прыгает |
| 2 | Лента сообщений | Entity (автор) + утилиты типографики + Tile для структурных блоков | скролл-колонка, ширина текста ограничена 880px |
| 3 | Композер с конструктором запроса | локальный компонент `.cmp`: поле, ряд действий, выезжающий конструктор | закреплён, не скроллится |

У поля ввода нет видимой подписи — она избыточна рядом с плейсхолдером и
хелпером; название поля осталось в `aria-label` для скринридера.

**Пример запроса.** Кнопка «Показать пример запроса» ставит в поле готовый вопрос.
Она видна только над пустым полем: пример заменил бы начатый текст, поэтому
кнопка уходит с первым символом — набранным руками, вставленным конструктором
или примером — и возвращается, когда поле снова пустое (стёрто или очищено
крестиком). Правило то же, что у крестика «Очистить поле», только наоборот:
один из двух виден всегда.

**Композер.** Локальный компонент `.cmp`: поле и ряд действий в одной карточке,
под ней — хелпер. Поле — `textarea` с «зеркалом» `.cmp__mirror`: зеркало
повторяет текст и подсвечивает фрагменты, вставленные конструктором.
Ряд действий слева направо:

| Элемент | Компонент | Когда виден |
|---|---|---|
| Приложить файлы | IconButton `--neutral --m`, глиф `attach`, ограничения — в тултипе | всегда |
| Конструктор / Закрыть конструктор | Button Outline S, переключатель с `aria-expanded`; глиф `ai-stars` меняется на `close` | всегда |
| Сбросить выбор | Button Transparent S, глиф `filter-reset` | конструктор открыт и в нём что-то выбрано |
| Очистить поле | IconButton `--neutral --m`, глиф `close` | в поле есть текст |
| Отправить | Button Accent S `--icon-only`, глиф `arrow-up` | всегда |

«Сбросить выбор» стоит в ряду действий, рядом с переключателем конструктора:
ряд держит распорка, поэтому появление кнопки ничего не сдвигает (раздел 16,
№22). Приложенные файлы и фрагменты материала — чипы над полем.

**Конструктор запроса.** Выезжает вниз внутри карточки композера; закрытый —
`inert`, чтобы Tab не заходил в невидимые контролы. Своего заголовка у панели
нет: она открывается кнопкой «Конструктор», название дублировало бы её. Имя
группы для скринридера — `aria-label="Конструктор запроса"`. Секции — номер шага
и подпись слева (колонка 184px), контролы справа; у первой секции нет верхней
пунктирной линии — сверху её отделяет граница самой панели.

**Высота.** Панель растягивается под контент без внутреннего скролла —
вместе с блоком отчётов коллег. Нить уступает место панели, но не ниже
`--space-96`; если панель вместе с нитью не влезает в окно целиком,
скроллит рабочая область (`.screen__content`), а не сама панель:
скролл-контейнер включается только при открытом конструкторе, бегунок —
в общей схеме «виден только во время прокрутки». Замер:
на 1440×900 всё помещается без страничного скролла (панель 470px, нить
196px); на 1440×680 нить стоит на минимуме и рабочая область прокручивает
панель целиком.

| № | Секция | Контролы | Что встаёт в текст запроса |
|---|---|---|---|
| 1 | Объект анализа | поиск по реестру объектов; варианты — пилюли с типом (Группа, Компания, Бенефициар, Отрасль), один выбор; нет точного совпадения — пилюля «Использовать «…»». Под вариантами — блок отчётов коллег, если они есть | «Собери отчёт по группе компаний …», у отрасли — «Собери отчёт по отрасли «…»» — в начало запроса |
| 2 | Дополнительный фокус | шесть переключаемых чипов, можно несколько: долговая нагрузка и ковенанты, оценка по мультипликаторам, структура владения, рефинансирование, отраслевые бенчмарки, продуктовые идеи для встречи | по предложению на чип, вслед за объектом |

Выбор сразу встаёт фрагментом в текст и вспыхивает подсветкой; текст остаётся
редактируемым: фрагмент, стёртый руками, снимает отметку с контрола. Секция с
выбором получает галочку вместо номера. Внизу панели — срок подготовки.

**Отчёты коллег по объекту.** Когда выбран объект, по которому коллеги уже
готовили отчёты, под вариантами объекта появляется блок на всю ширину секции
(`.bprior`, поверхность как у поиска объекта: `--bg-page`, `--border-light`,
`--radius-m`). Объект сняли или отчётов нет — блока нет.

| Часть | Компонент | Значение |
|---|---|---|
| Заголовок | глиф `history` + текст body-s-strong | «ГК «Северный агрохолдинг»: коллеги уже готовили отчёты — 3» |
| Подсказка | body-xs, `--text-secondary` | Документ откроется в чате, конструктор останется на месте. Переписка запросов видна только их авторам. |
| Строка отчёта | Entity `--s` в `.entity-list`: ведущий — Avatar `--circular --m` с инициалами автора; Label — название документа (усечение + тултип); Subs — автор · дата и время создания · объём | ГК «Северный агрохолдинг» — долговой профиль и рефинансирование · Петров А. В. · 12.09.2026, 14:20 · Детальный |
| Действие строки | Button Outline XS «Показать в чате», глиф `message-text` → после нажатия Button Transparent XS «В чате», глиф `check` | — |

Автор отчёта — владелец чата, в котором документ собран. Сам чат другим
пользователям не показывается, поэтому в блоке и в нити есть только документ:
ссылки на чужой запрос нигде нет.

«Показать в чате» ставит в нить сообщение AI Pitcher «Отчёт коллеги из другого
запроса.» с карточкой документа — той же, что у своего материала, но в подписи
автор и дата создания: «Автор — Петров А. В. · создан 12.09.2026, 14:20 ·
Детальный»; действия — «Просмотр» и «Скачать». Под карточкой — Button
Transparent S «Добавить в контекст» (глиф `add`), размером и стилем — как
действия ответа («Копировать»): документ уходит в композер как приложенный
файл — чип в ряду файлов над полем ввода, по нему можно спросить как по
прикреплённому документу. Добавление подтверждается SnackBar «Документ добавлен
в контекст запроса», кнопка переходит в состояние «В контексте»
(глиф `check`, `aria-pressed`); повторное нажатие или крестик чипа убирают
документ из контекста и возвращают кнопке исходный вид. Конструктор не
закрывается: можно прокликать все документы, и в нити встанет по карточке на
каждый. Документ
встаёт в нить один раз: повторное нажатие («В чате») прокручивает нить к его
карточке. Если панель просмотра уже открыта, оба нажатия сразу переключают её на
этот документ. Карточки не создают запрос: заголовок остаётся «Новый отчёт», в
«Недавних запросах» новой строки нет — запрос создаёт только первое сообщение
пользователя (раздел 4).
Направление анализа в конструкторе не задаётся (раздел 16, №21).

**Как устроено сообщение.** Строка автора — компонент Entity: ведущий элемент
(Avatar с инициалами у пользователя, `.entity__icon--accent` с глифом `ai-stars`
у агентной системы), `entity__label` — имя, `entity__subs` — время. Тело
сообщения — под строкой автора, выровнено по текстовой колонке Entity.

**Плашек-«пузырей» у сообщений нет — это осознанное решение.** Компонента
«сообщение чата» в ДС не существует, а собственный `<style>` экрана по правилам
проекта содержит только раскладку и не красит фон. Пользователя и агентную
систему различает ведущий элемент Entity, а структурные ответы (ход подготовки,
готовый материал) подаются компонентом Tile — он даёт плашку средствами ДС.
Это соответствует БТ-3.2 («минималистичное оформление, по образцу
распространённых AI-продуктов»).

## 7. Ход подготовки материала (БТ-7)

В текущем прототипе подготовка представлена строкой индикатора с меняющимся
статусом и таймером. В этой же строке справа — прозрачная кнопка S
«Настроить уведомление» с иконкой `settings`. На узкой ширине кнопка переносится
на следующую строку и остаётся выровненной вправо. Нажатие на сам индикатор
завершает демонстрационную подготовку, на кнопку — открывает `notify-scrim`.
Текст ожидания: «Это займёт до 30 минут. Как будет готово, сообщу в чате и
уведомлю по выбранным каналам». Фраза про кофе не используется.

Ниже сохранены требования к подробному отображению шагов для реализации продукта.

Тайл в ленте, на всю ширину колонки (`grid-column:span 12` в `.tile-row`).

| Часть | Компонент | Требование |
|---|---|---|
| Заголовок | `tile__title` | БТ-7.9 — ход работы в нити, отдельного экрана нет |
| Подзаголовок | `tile__subtitle` + глиф 16px: `clock-timer` в работе, `check-circle-filled` по завершении, `blocked` после остановки | БТ-7.4 — ожидаемое время до завершения |
| Кнопка «Остановить» | `tile__actions` → `btn--outline btn--s btn--error` | БТ-6.4 — остановка в любой момент |
| Общий прогресс | ProgressBar, обычный вариант `.pbar__head` | «Выполнено шагов: N из 7» |
| Перечень шагов | Entity `--s` на шаг, иконка шага — по верхнему краю строки | БТ-7.1, БТ-7.2 |
| Сбой шага | `tile__alert` → Alert `--warning` + кнопки | БТ-7.5, поведение при отказах |

**Ритм внутри тайла — 24px.** У `.tile__body` своего ритма нет (`display: block`),
поэтому прогресс, разделитель и перечень шагов лежат в обёртке `.thr__stack` с
зазором `--space-24`; между шагами — тот же зазор. Так же собрана карточка
готового материала: файл, разделитель и ряд кнопок.

**Шаги (БТ-7.2)** и их содержательный результат (БТ-7.3):

| Шаг | Глиф | Результат при успехе |
|---|---|---|
| Разбор запроса и определение объекта | `client-search` | определён объект: 11 юрлиц группы |
| Обработка приложенных файлов | `file-check` | обработано 3 файла |
| Поиск во внешних источниках | `global` | найдено 23 источника |
| Обращение к внутренним данным Банка | `server-05` | сведения по 4 системам |
| Работа агентов-аналитиков | `bar-line-chart` | рассчитан Net Debt / EBITDA, 6 peers |
| Проверка подготовленного результата | `check-verified` | проверено 42 утверждения |
| Сборка материала и файлов выгрузки | `file-basic` | 17 страниц, PDF и DOCX |

**Состояния шага (БТ-7.1)** — чипом, по правилу «статус всегда Chip»:

| Состояние | Chip | Глиф маркера |
|---|---|---|
| Предстоит | `chip--s` нейтральный | `circle` |
| Выполняется | `chip--s chip--info` | `clock-timer` |
| Выполнен | `chip--s chip--success` | `check-circle` |
| Выполнен частично | `chip--s chip--warning` | `alert-triangle` |
| Не выполнен | `chip--s chip--error` | `alert-circle` |

## 8. Поведение

| Триггер | Результат |
|---|---|
| Клик по строке истории | открывается запрос: `RequestThread.html?id=N`, нить восстанавливается целиком (БТ-4.3) |
| Клик по кебабу строки | меню действий над запросом; клик не открывает сам запрос |
| «Новый отчёт» в панели | пустая нить — `RequestThread.html` |
| «Конструктор» в ряду действий композера | конструктор выезжает внутри карточки композера, фокус — в поиск объекта; повторный клик или Esc закрывают его, текст в поле остаётся |
| Выбор в конструкторе | фрагмент встаёт в текст запроса и вспыхивает подсветкой; повторный клик по тому же варианту снимает фрагмент |
| «Сбросить выбор» | фрагменты конструктора уходят из текста, свой текст пользователя остаётся; кнопка прячется, фокус — в поиск объекта |
| «Показать пример запроса» | в поле встаёт пример вопроса, фокус — в поле; сама кнопка уходит, потому что поле больше не пустое |
| Ввод в пустое поле / поле стёрто | «Показать пример запроса» уходит с первым символом и возвращается, когда поле снова пустое |
| Выбран объект с отчётами коллег | под вариантами объекта появляется блок отчётов коллег; объект сняли — блок уходит |
| «Показать в чате» у отчёта коллеги | в нить встаёт карточка документа с автором и датой создания, конструктор остаётся открытым; кнопка становится «В чате»; открытая панель просмотра переключается на этот документ |
| «Добавить в контекст» под карточкой отчёта коллеги | документ встаёт чипом в ряд файлов композера, SnackBar подтверждает, кнопка становится «В контексте»; крестик чипа или повторное нажатие убирают документ из контекста |
| «В чате» у отчёта коллеги | нить прокручивается к карточке документа, повторной карточки нет; открытая панель просмотра переключается на этот документ |
| «Просмотр» или название на карточке отчёта коллеги | панель просмотра справа; в её шапке — название документа и «Автор — … · создан … · объём» |
| Отправка сообщения (кнопка или Enter) | сообщение пользователя в нити, ответ появляется постепенно, посимвольно (БТ-3.8) |
| Вопрос без запроса на отчёт | ответ в нити + чипы источников + действия «Копировать», «Собрать материал» (БТ-3.3, БТ-3.7, БТ-3.9) |
| В сообщении есть слова «собери», «материал», «отчёт», «подготовь» | ответ предлагает открыть конструктор с контекстом нити (БТ-3.5) |
| Уточнение при готовом материале | развилка «Ответить в диалоге» / «Собрать новую версию» (БТ-6.3) |
| «Собрать новую версию» | запуск подготовки без повторного ввода параметров и файлов, номер версии +1 (БТ-6.6, БТ-6.7) |
| «Остановить» | подготовка прекращается, шаги «выполняется» возвращаются в «предстоит», появляется Alert с предложением уточнить и запустить заново (БТ-6.4, БТ-6.5) |
| «Запустить заново» после остановки | подготовка стартует с нуля на сохранённых вводных (БТ-5 «Введённые данные сохраняются при сбое») |
| «Повторить шаг» на отказавшем шаге | шаг выполняется повторно и завершается успешно |
| «Продолжить без него» | шаг помечается «выполнен частично», материал готовится на доступных данных (СТ-2.1) |
| Завершение подготовки | карточка материала в нити + SnackBar «Материал готов» с переходом к материалу (БТ-8.1, БТ-8.4) |
| «Приложить файлы» | модалка с ограничениями, затем чипы файлов над полем ввода (БТ-2.8, БТ-3.4) |
| Крестик на чипе файла | файл убирается из сообщения до отправки (БТ-2.7) |


**Чего рантаймы ДС не дают и что описано словами:** реальная подготовка
материала, реальная обработка файлов, отправка уведомления на почту, переходы
между экранами по правам. На макете это показано вёрсткой в нужном состоянии и
скриптом экрана.

## 9. Модальные окна

| id | Заголовок | Ширина | Содержимое | Кнопки подвала |
|---|---|---|---|---|
| `rename-scrim` | Переименовать запрос | `--w4` | одно поле с текущим названием | «Отмена», «Сохранить» |
| `delete-scrim` | Удалить запрос? | `--w3` | предупреждение про корзину и 30 дней | «Отмена», «Удалить» (`btn--danger`) |
| `notify-scrim` | Настроить уведомление | `--w4` | чекбоксы «Почта», «Сберчат», «В интерфейсе»; одна настройка на все чаты, без области действия и названия чата | «Отмена», «Сохранить» |
| `share-scrim` | Поделиться чатом | `--w4` | поля «Пользователи» и «Дески» на всю ширину модалки, выбранные пользователи чипами с аватаром, выбранные дески квадратными чипами, список десков с чекбоксами и «Выбрать всё», число получателей и владелец | «Отмена», «Сохранить» |
| `files-scrim` | Приложить файлы | `--w4` | Alert с ограничениями загрузки (БТ-2.8) | «Отмена», «Выбрать файлы» |
| `preview-scrim` | ООО «Друза» — бриф перед встречей | `--w9` | превью материала, см. ниже | справа «Закрыть» |

### Уведомления и доступ к чату

Общий модуль `chat-settings.js` создаёт диалоги и дополняет меню чатов.
Меню сохраняет идентификатор строки при открытии: действие относится именно
к этой строке, даже если в центре открыт другой чат. Кнопка рядом с таймером
и кебаб шапки относятся к текущей нити.

Уведомления — одна настройка на все чаты: области действия и названия
чата в диалоге нет. Начальный выбор каналов
— «Почта» и «В интерфейсе» (допущение агента). Можно отключить все каналы.
Сохраняется только по «Сохранить», в общем для всех чатов ключе хранилища;
отмена, крестик
и Esc отбрасывают черновик. Сообщение о готовности остаётся в чате при любом
выборе; SnackBar показывается только при включённом канале «В интерфейсе».
Почта и Сберчат — параметры прототипа, реальной отправки нет.

Шаринг: имя фильтрует локальный демонстрационный справочник; пустой результат
показан строкой «Пользователи не найдены». Можно выбирать несколько людей и
десков одновременно. Оба поля — на всю ширину модалки. Выбранные пользователи
показываются под полем скруглёнными чипами с аватаром-инициалами, выбранные
дески — квадратными чипами с названием; у тех и других крестик справа убирает
значение и синхронизирует чекбоксы списка. Дески: Недвижимость, Сельское
хозяйство, Промышленность, Транспорт и логистика. Все их участники получают
право просмотра; одинаковый пользователь считается один раз. «Сохранить» применяет
выбор и обновляет аватары; пустой выбор оставляет доступ только владельцу.
«Отмена», крестик и Esc ничего не меняют. Второй триггер диалога — аватарки
получателей в шапке: клик, Enter и пробел открывают его для текущей нити.

Настройки хранятся по идентификатору чата в `localStorage`; при недоступном
хранилище — в памяти страницы. Новые чаты получают отдельный ключ настроек.
Справочник и членство в десках — демо-данные. Появление чата и его владельца
в истории другого пользователя — контракт продуктовой реализации, без
подключённой базы пользователей и авторизации в этом HTML-прототипе.
Открытие, закрытие, фокус и клавиатура — штатные рантаймы ДС.

### Превью материала

Отчёт формируется файлом — PDF или Word, — а в инструменте читается превью.
Сценарий, шаг 8: инструмент «показывает документ, перечень версий и выгрузку;
уточнения при этом остаются в нити». Функций у документа две — выгрузка и
версии, поэтому превью — модальное окно поверх нити, а не отдельная страница:
закрытие возвращает туда, где шла работа. Прежняя страница `MaterialDocument.html`
больше ни откуда не открывается.

**Входы:** «Открыть материал» в карточке готового материала и в SnackBar
«Материал готов». Тот же блок стоит в реестре «История и материалы» —
`RequestHistory.screen.md`, раздел 8.

| Зона | Содержимое | Компоненты и классы |
|---|---|---|
| Шапка | название материала, крестик | Modal `--w9` |
| Alert окна | у текущей версии — «Полнота ограничена»: консолидированной отчётности по группе нет, выгрузки из платных систем не было — это отмечено в самом файле, в разделе «Ключевые риски» (СТ-2); у прежней — «Версия 1 — не текущая» | Alert `--warning --m --flush` в `.modal__alert` |
| Тело, слева | файл-пример `../refs/report-example-for-test.pdf` — краткий материал, 2 страницы A4 — во встроенном просмотрщике PDF браузера и подпись «Краткий материал · 2 страницы. Детальный — в выгрузке» | локальная рамка `.prv__pdf` — `<object type="application/pdf">`, внутри — Alert `--info` со ссылкой «Открыть PDF» для браузера без просмотрщика; подпись `ds-body-s` |
| Тело, справа, 320px | «Выгрузка — версия N»: краткий PDF, детальный PDF, детальный Word и хелпер — краткий только в PDF, маркировка, журнал (БТ-1.4, БТ-1.9, БТ-1.10). «Версии материала»: строки с датой, отличием от предыдущей и чипом «Текущая» (БТ-4.6, БТ-4.7) | Buttons `--outline --m --fullwidth`, LabelHelper, Entity `--s`, Chip; локальная `.prv__ver` |
| Подвал | справа «Закрыть» | Button `--accent` |

| Триггер | Результат |
|---|---|
| Открытие окна любым входом | текущая версия, первая страница |
| Клик по строке версии; ↑ ↓, Enter, пробел на строке | Alert окна, подпись «Выгрузка — версия N» и сообщения о выгрузке переключаются на выбранную версию (БТ-1.7). Страницы у обеих версий одни — файл-пример в прототипе один |
| «Краткий материал — PDF» | скачивается файл-пример `../refs/report-example-for-test.pdf`, Toast «…файл сформирован и промаркирован» |
| «Детальный материал — PDF», «Детальный материал — Word» | Toast; файлов в прототипе нет |
| Прокрутка | окно всегда на полную высоту (`--modal-max-h`, 80% экрана): документ прокручивается в своём просмотрщике, колонка выгрузки и версий — отдельно, если не помещается по высоте |
| Ширина окна браузера до 1100px | одна колонка: выгрузка и версии — над документом, прокручивается тело окна, у просмотрщика высота 60% экрана |

**Локальный компонент.** Компонента «документ» в ДС нет. Документ — сам PDF-файл
во встроенном просмотрщике браузера: `<object class="prv__pdf" type="application/pdf">`
на всю высоту тела окна (фон `--bg-page` до загрузки файла, граница
`--border-light`, радиус `--radius-xs`). Файл открывается по ширине, без панели
инструментов и боковой панели (`#toolbar=0&navpanes=0&view=FitH`); выгрузка —
в колонке справа. Другой файл показывается без подготовки — достаточно поменять
адрес. Вид документа задаёт браузер: в Chrome вокруг листов тёмный фон, прокрутка
и масштаб — его собственные. Если браузер не показывает PDF внутри страницы,
виден вложенный в `<object>` Alert `--info` со ссылкой «Открыть PDF». Просмотрщик
помнит прокрутку файла, поэтому при повторном открытии окна скрипт пересоздаёт
элемент — документ снова с первой страницы. `<object>` не входит в список
фокусируемых элементов Modal (раздел 17, №6). Строка версии `.prv__ver`
повторяет строку истории (раздел 5): те же отступы, `--radius-m` и состояния
`--bg-table-default-hover` / `--bg-table-default-focus`.

**Разметка.** Блок одинаков в `RequestThread.html` и `RequestHistory.html`,
различается только подвал: в реестре добавлена «Перейти в нить». Стили — в конце
`<style>`, окно и его скрипт — перед `ds.js`.

## 10. Состояния экрана

| Состояние | Что показывается |
|---|---|
| Новая нить (адрес без параметров) | EmptyState «С чего начнём?», заголовок «Новый отчёт», чипов нет |
| Открыт запрос из истории | заголовок и чип — из строки истории, сама строка помечена как текущая |
| Идёт подготовка | тайл хода работы, кнопка «Остановить», чип шапки «Подготовка» |
| Сбой отдельного шага | Alert `--warning` внутри тайла, шаг помечен отдельно от общего состояния (БТ-7.5) |
| Выполнение остановлено | Alert `--info` с предложением уточнить и запустить заново |
| Материал готов | карточка материала с выгрузкой, чип шапки «Материал готов» |
| Полнота ограничена | Alert `--warning` в карточке материала: что не учтено и как это влияет (СТ-2.2), с указанием, что ограничение попадёт в файл (СТ-2.3) |
| Открыт материал | превью в модальном окне поверх нити: текущая версия с первой страницы, выгрузка и перечень версий (раздел 9) |

## 11. Тексты интерфейса

| Место | Текст |
|---|---|
| Пустая нить — заголовок | С чего начнём? |
| Пустая нить — действие | Показать пример запроса |
| Плейсхолдер поля ввода | Задайте вопрос или опишите задачу… |
| Хелпер поля ввода | Enter — отправить · Shift + Enter — новая строка · К запросу можно приложить PDF, Word, PowerPoint и Excel |
| Переключатель конструктора | Конструктор / Закрыть конструктор |
| Секции конструктора | 1 Объект анализа · 2 Дополнительный фокус |
| Поиск объекта | Компания, группа, бенефициар или отрасль |
| Пустая нить — текст | Спросите про компанию, группу или отрасль — ответ будет в этом чате с указанием источников. … |
| Отчёты коллег — заголовок | <Объект>: коллеги уже готовили отчёты — N |
| Отчёты коллег — подсказка | Документ откроется в чате, конструктор останется на месте. Переписка запросов видна только их авторам. |
| Отчёты коллег — кнопка | Показать в чате → В чате |
| Сообщение с отчётом коллеги | Отчёт коллеги из другого запроса. |
| Подпись карточки отчёта коллеги | Автор — <Фамилия И. О.> · создан ДД.ММ.ГГГГ, ЧЧ:ММ · <объём> |
| Сброс конструктора | Сбросить выбор |
| Низ конструктора | Подготовка отчета обычно занимает до 30 минут. Работа продолжится, даже если закрыть инструмент. |
| Кнопка остановки | Остановить |
| Развилка уточнения | Ответить в диалоге / Собрать новую версию |
| Уведомление о готовности | Материал готов. …версия N. Уведомление отправлено на корпоративную почту. |
| Подпись панели | Недавние запросы |

## 12. Данные

| Поле | Тип | Обязательное | Примечание |
|---|---|---|---|
| `requestId` | строка | да | идентификатор запроса |
| `messages[]` | список | да | роль, текст, время, приложенные файлы |
| `steps[]` | список | да | код шага, состояние, содержательный результат |
| `eta` | число | да | ожидаемое время до завершения, минуты (БТ-7.4) |
| `versions[]` | список | да | номер, дата, отличие от предыдущей (БТ-4.7) |
| `versions[].pages` | число | да | страниц в документе версии — подпись «Показаны первые 3 страницы из N» |
| `versions[].limited` | флаг | да | ограничение полноты версии — Alert окна превью (СТ-2) |
| `versions[].files[]` | список | да | детальный PDF и Word, краткий PDF (БТ-1.4); выгружается любая версия (БТ-1.7) |
| `limited` | флаг | да | признак ограниченной полноты (СТ-2.2) |
| `embeddedFrom` | объект | нет | вызвавшее рабочее место (БТ-10.1) |
| `priorReports[]` | список | нет | документы коллег по объекту анализа; ключ — объект. Своих запросов пользователя в списке нет — они в панели истории |
| `priorReports[].title` | строка | да | название документа |
| `priorReports[].author` | строка | да | владелец чата, в котором документ собран: «Фамилия И. О.» |
| `priorReports[].created` | дата и время | да | создание документа, `ДД.ММ.ГГГГ, ЧЧ:ММ` |
| `priorReports[].volume` | строка | да | объём: «Детальный» / «Краткий» |
| `priorReports[].file` | ссылка | да | сам документ для просмотра и выгрузки; ссылки на чат автора нет намеренно |

## 13. Компоненты ДС

| Компонент | Где на экране | Варианты |
|---|---|---|
| Layout | каркас | `nav--rail`, `.screen--app` |
| NavPanel | боковое меню | rail, пункт «AI Pitcher» в группе Origination |
| Entity | автор сообщения, шаг подготовки, строка истории | базовый и `--s --interactive` |
| ContextMenu | кебаб строки истории | одно меню на весь список |
| PageHeader | шапка | иконка + edit + чипы + return + мета + 3 действия |
| Entity | автор сообщения, шаг подготовки, карточка материала, идеи и версии в превью | базовый и `--s` |
| Tile | ход подготовки, готовый материал | обычный, с `tile__alert` |
| ProgressBar | общий прогресс | обычный `.pbar__head`, тон `--accent` |
| Alert | сбой шага, остановка, ограничение полноты | `--warning`, `--info` |
| Buttons | панель: «Новый отчёт»; пустая нить: «Показать пример запроса»; композер: «Конструктор», «Сбросить выбор», «Отправить» | Accent M `--fullwidth`; Outline M; Outline S, Transparent S, Accent S `--icon-only` |
| IconButton | композер: приложить файлы, очистить поле | `--neutral --m`, подпись — тултип `data-tooltip` |
| EmptyState | пустая нить | `--l` |
| Entity | строки отчётов коллег в конструкторе | `--s` в `.entity-list`, Label `--truncate` с тултипом усечения |
| Avatar | инициалы автора отчёта; получатели доступа в шапке — триггер «Поделиться» | `--circular --m`, `.av-group--m`, `.av-group__more`, локальный `.av-group--link` |
| InputAutocomplete / DropdownList | пользователи и дески в диалоге доступа | поле M `--fullwidth`, список с чекбоксами, выбранные пользователи — чипы `chip--edit chip--s chip--rounded` с `.chip__avatar`, дески — `chip--edit chip--s` |
| Checkbox | каналы уведомлений | независимые нативные чекбоксы `.cb` |
| Modal | окна действий с запросом, уведомлений и доступа | `--w3`, `--w4` |
| SnackBar / Toast | уведомления | `DSSnack.show`, `DSToast.show` |

## 14. Доступность

- `<main>` для контентной области, `<nav aria-label>` у меню и крошек;
- единственный `h1` — заголовок страницы;
- у всех кнопок-иконок есть `aria-label`;
- прогресс объявлен через `role="progressbar"` + `aria-valuenow`;
- Alert со сбоем — `role="alert" aria-live="assertive"`, информационные — `role="status" aria-live="polite"`;
- контраст обеспечивается токенами ДС;
- версии в превью — `role="radiogroup"` / `role="radio"` с `aria-checked`, стрелки ↑ ↓ переключают версию; у просмотрщика документа — `aria-label` с названием материала, объёмом и форматом;
- в листе документа нет фокусируемых элементов, поэтому фокус при открытии превью встаёт на первую кнопку выгрузки, а окно открывается с первой страницы.

## 15. Скрипт экрана

Экран подключает `ds.js` и один собственный скрипт после него. Он не подменяет
рантаймы ДС, а закрывает модель данных и сценарий, которых в ДС нет и не может
быть: панель истории запросов и её строки, состояние экрана по параметру адреса,
лента сообщений, постепенное появление ответа, машина шагов подготовки,
остановка и повторный запуск, развилка уточнения, вызов штатного SnackBar.
После каждой перерисовки поддерева рантаймы связываются заново одной функцией
`rebind(scope)` (`dsIcons.apply`, `DSMenu.bindAll`, `DSModal.bindAll`,
`DSTile.wireAll`, `DSTabs.wireAll`, `DSChip.refresh`).

Конструктор запроса держит модель «контрол → фрагмент текста» (`B.frag`): выбор
ставит фрагмент в текст в порядке «объект → фокусы», ручная правка текста снимает
отметку с контрола, зеркало поля подсвечивает фрагменты. Видимость кнопок,
зависящих от текста, решается в одном месте — функции перерисовки зеркала
`paint()`: «Очистить поле» видна, когда в поле есть текст, «Показать пример
запроса» — наоборот, когда поле пустое. «Сбросить выбор» зависит от выбора и от
того, открыт ли конструктор (`syncControls()`).

Блок отчётов коллег рисует `drawPrior()` из `syncControls()`, но перерисовывает
только при смене объекта или когда документ встал в нить — иначе каждый ввод
сбрасывал бы фокус с кнопок блока. `showPrior(id)` ставит карточку в нить один
раз (учёт — `S.priorShown`), прокручивает к ней, переключает открытую панель
просмотра и возвращает фокус на кнопку строки. Панель просмотра получает
описание документа: `openPreview(doc)` пишет название и подпись в шапку панели;
без аргумента — описание своего материала.

У превью материала свой короткий скрипт — сразу за разметкой окна, тот же, что в
реестре: переключение версий, сброс на текущую версию и первую страницу при
каждом открытии (наблюдение за атрибутом `hidden` скрима покрывает все входы,
включая `data-modal`), прилипание колонки выгрузки. Действие окна `dl`
обрабатывает скрипт экрана.

## 16. Открытые вопросы и допущения

| № | Вопрос / допущение | Статус |
|---|---|---|
| 1 | Оформление сообщений без плашек-«пузырей», различие — ведущим элементом Entity | **допущение**, причина в разделе 4; нужно подтверждение дизайн-лида |
| 2 | Набор из семи шагов подготовки взят из БТ-7.2 дословно; их порядок в ТЗ не задан | **допущение**, порядок предложен |
| 3 | Формулировки результатов шагов — демо-данные, влияют только на длину строки | «рыба» |
| 4 | Ожидаемое время «до 30 минут» взято из БТ-7 и БТ-8 | из ТЗ |
| 5 | Каналы уведомлений: Почта, Сберчат, В интерфейсе; по умолчанию Почта и В интерфейсе | каналы — **решение пользователя**; начальный выбор — **допущение агента** |
| 6 | Срок обратимого удаления 30 дней — БТ-4.8 говорит «в течение установленного срока», конкретный срок не задан | **вопрос заказчику** |
| 7 | Панель «Недавние запросы» — новый блок направления, компонента «список нитей» в ДС нет | **допущение**: собрана из Button, LabelHelper, Entity, Chip, IconButton, ContextMenu; кандидат в компоненты ДС |
| 8 | Относительное время в панели («12 минут назад») для свежих запросов и дата `ДД.ММ.ГГГГ` для остальных | **допущение** по макету заказчика |
| 9 | «Новый отчёт» в панели и «Отправить» в композере обе `btn--accent` | **осознанное отклонение** от инварианта Buttons «один Accent на область экрана»: панель и нить — две независимые зоны, так нарисовано в макете. «Отправить» — размера S, как соседние кнопки композера |
| 11 | Панель истории вынесена из `.screen` и стоит между меню и рабочей областью; крошки — только над контентом | **по макету заказчика**: так панель прячется целиком на узком экране и уходит под кнопку в мобильной версии. Каркас ДС при этом не нарушен: у `.nav-layout` остаётся один не-`nav` ребёнок |
| 12 | Поверхность панели — фон рабочей области `--bg-page` с правой границей | **решение заказчика** (13.09.2026): белая панель (`--bg-tile`) сливалась с белым боковым меню. Состояния строки полупрозрачные (`--bg-table-default-hover` / `--bg-table-default-focus`) и ложатся на любой фон |
| 10 | Связь со встречей (БТ-10) на экране больше не показана: у нового запроса вызвавшего рабочего места нет | **допущение**, предзаполнение из встречи осталось в конструкторе |
| 13 | Превью материала — модальное окно, а не отдельная страница | **по предложению заказчика** (12.09.2026): у документа только выгрузка и версии, уточнения остаются в нити |
| 14 | Превью строится по самому PDF-файлу, картинок страниц нет | **решение заказчика** (14.09.2026). В прототипе — встроенный просмотрщик браузера. Вопрос к разработке — оставить его или рисовать страницы библиотекой: тогда лист выглядит как в ДС и одинаково во всех браузерах. Раскладка окна от способа не зависит |
| 15 | Файл-пример — бриф по ООО «Друза», а демо-данные нити и реестра — по ГК «Северный агрохолдинг» и другим компаниям; превью показывает этот файл для любого материала, у обеих версий одни страницы | демо-данные прототипа |
| 16 | Маркировки конфиденциальности на файле-примере нет — БТ-1.9 требует принятую в Банке маркировку, но не называет её | **вопрос к ИБ** |
| 17 | Название нового запроса — начало первого сообщения | **допущение** прототипа: как запрос получает название в продукте, не определено |
| 19 | Композер `.cmp` с выезжающим конструктором запроса | **допущение**: компонентов «композер» и «конструктор запроса» в ДС нет; собраны из токенов поля ввода, Button, IconButton и локальных контролов (пилюли объекта `.bopt`, чипы фокуса `.bchip`); кандидаты в компоненты ДС |
| 20 | Ссылки на полный перечень запросов («История и материалы», `RequestHistory.html`) на экране нет — ни в панели, ни в меню строки | **решение заказчика** (21.09.2026) убрать ссылку из панели. **Вопрос заказчику**: откуда пользователь попадает в «Историю и материалы» — сейчас экран не связан с ним переходом |
| 21 | Направление анализа (полный анализ, прямые инвестиции, M&A, рынки капитала) в конструкторе не задаётся: секции «Уровень проработки» нет, фраза о направлении в запрос не встаёт, в подписи карточки материала — только объём, версия и дата | **решение заказчика** (21.09.2026). **Вопрос заказчику**: нужно ли направление подготовке материала — если да, откуда оно берётся (из текста запроса, по умолчанию «полный анализ», из объекта) |
| 23 | Документы коллег по объекту видны любому пользователю модуля, выбравшему этот объект; запрос и переписка — только автору | **решение заказчика** (21.09.2026). **Вопрос заказчику и ИБ**: БТ-9.2 по умолчанию открывает только собственные материалы — кому видны чужие документы (всем пользователям модуля, только при доступе к клиенту, по отметке автора «поделиться») и видит ли автор, кто открывал его документ |
| 24 | У карточки отчёта коллеги есть «Скачать» — как у своего материала | **допущение агента**: просьба была «посмотреть»; выгрузка чужого документа требует того же решения по правам, что №23 |
| 25 | В блоке только отчёты коллег, свои прошлые запросы пользователя туда не попадают | **допущение агента**: свои запросы уже есть в панели «Недавние запросы» |
| 26 | Тип объекта «Рынок» переименован в «Отрасль» для всех рыночных объектов, включая региональные («Логистика последней мили, Юг России», «Минеральные удобрения, СЗФО»); сценарий в строке истории остаётся «По рынку» | **решение заказчика** (21.09.2026). В демо-истории у старых запросов тип «Регион» — оставлен как есть. **Вопрос**: нужен ли тип «Регион» наряду с «Отраслью» |
| 27 | Панель конструктора не выше 42% окна, дальше прокручивается внутри | **допущение агента**: без потолка блок отчётов коллег сжимал нить до 17px (раздел 6, «Высота») |
| 22 | «Сбросить выбор» — в ряду действий композера рядом с переключателем конструктора, а не в панели конструктора | **допущение агента**: у панели больше нет шапки, где стояла кнопка. В низу панели кнопка переносила текст срока подготовки на вторую строку, и при первом выборе панель подрастала на 8px (замер на 1440px); в ряду с распоркой её появление ничего не сдвигает |
| 18 | Форматы PDF, DOC, DOCX, PPT, PPTX, XLS, XLSX и лимит 30 МБ на файл — в подсказке под полем ввода и в окне «Приложить файлы»; в БТ-2.1 Excel не упомянут | **данные заказчика** (14.09.2026) |

## 17. Дефекты ДС, найденные при сборке

- В композиции DropdownList внутри Modal глобальный capture-обработчик
  `DSModal` закрывает окно по Esc раньше обработчика списка. Для диалога
  доступа добавлен локальный адаптер: capture на `window` передаёт первый
  Esc в `DSDropdownList.current().close(true)`, следующий — в DSModal.
  Рантаймы ДС не изменены. Проверено DOM-прогоном со всеми рантаймами; Л134.

Файлы ДС не изменялись. Найденное вынесено сюда для владельца дизайн-системы.

| № | Компонент | Симптом | Причина | Что сделано на экране |
|---|---|---|---|---|
| 1 | Radiobutton | При выборе радиокнопки точка внутри кольца не появляется — меняется только цвет | Глиф зашит в разметку (`radio-button-unchecked` / `-checked`), CSS красит `.rb__mark` по `:checked`, но глиф не подменяет; рантайма у компонента нет | Радиогруппы не использованы: выбор значения параметра собран на SegmentControl, у которого поведение из коробки (`data-segctrl`) |
| 2 | Illustrations | На экране вместо иллюстрации — штриховая заглушка, ошибок в консоли нет | `ds-illustrations.js` собирает путь как `(window.__DS_ROOT \|\| '') + 'assets/illustrations/<имя>.svg'`. Страницы документации задают `window.__DS_ROOT` отдельным тегом в `<head>`, но ни обязательный каркас экрана (`templates/screen/Screen.html`, ds-rules §5), ни таблица хуков `runtime-hooks.md` об этом не говорят — экран получает пустой корень и ищет файл рядом с собой | `window.__DS_ROOT = '../../design-system/'` выставляется отдельным тегом в начале `<head>` — как на страницах документации и на главной. Первый вариант, первой строкой скрипта экрана, оставлял в консоли 404: рантайм успевает запросить файл по пустому корню раньше скрипта экрана и только потом — по верному |
| 3 | Breadcrumbs | Собственный `id` на текущей крошке теряется, обращение к ней из скрипта падает | `ds-breadcrumbs.js` перестраивает трейл под свою свёртку и оборачивает подпись в `.tip-anchor` | Текущая крошка ищется по классу `.crumbs__current` с проверкой на `null` |
| 4 | Entity | Выбор строки (`--selected`, 6 % от `--primary`) практически не отличается от невыбранной строки | Токен рассчитан на белую плашку и очень светлый; спека условия не называет | Состояния вынесены на собственную обёртку строки `.hist__item` с токенами `--bg-table-default-hover` / `--bg-table-default-focus` — теми же, которыми ДС красит выбранную строку в NavPanel и в таблице. Состояние компонента не переопределялось |
| 5 | Entity | Подложка наведения в контейнере со скроллом обрезается слева и выглядит квадратной | Пара «паддинг 8/10 + margin −8/−10» у `--interactive` рассчитана на Entity в общем потоке; в контейнере с `overflow` вынос срезается | `--interactive` не используется: наведение живёт на обёртке строки |
| 6 | Modal | Превью материала открывалось прокрученным ко второй странице документа | `firstTarget()` в `ds-modal.js` ставит фокус на первый фокусируемый элемент `.modal__body`, а `focus()` прокручивает тело к нему. В листе стояла Table ДС с ручками ширины колонок (`.th__resize`, `tabindex="0"`). Спека Modal об этом не говорит, способа назначить цель фокуса у окна нет | В листе нет фокусируемых элементов: сначала таблица листа была собрана разметкой документа, потом страницы показывались изображениями, теперь документ — PDF в `<object>`, которого нет в списке фокусируемых Modal. Скрипт окна при открытии сбрасывает прокрутку тела |
| 7 | ContextMenu | Экран с фиксированным вьюпортом прокручивается на 20px | Обёртка `.menu-anchor` объявлена `display: inline-flex`; прямым ребёнком `body` она создаёт строку текста высотой в межстрочный интервал. Для меню с триггером по `data-menu` обёртка не нужна: рантайм находит меню по id и ставит его от триггера | Обёртка убрана, меню стоит прямым ребёнком `body` |
| 8 | Полоса прокрутки | В панели «Недавние запросы» и в ленте — системная полоса, в модалке — своя, тонкая | Общего класса полосы прокрутки в ДС нет: Modal, Drawer и Popover рисуют её одним рецептом (8px, бегунок `--border-light`), DropdownList и ContextMenu — другим (`--border-primary`); у скролл-контейнеров раскладки стиля нет вовсе | Рецепт тела модалки повторён для `.hist__body` и `.thr__scroll` в `<style>` экрана; Firefox, который не знает `::-webkit-scrollbar`, получает `scrollbar-width: thin` того же цвета. Бегунок виден только во время прокрутки и под курсором (решение заказчика, 14.09.2026): класс `is-scrolling` ставит скрипт экрана на событии `scroll` и снимает через 1 с тишины; место под полосу зарезервировано, контент не сдвигается |
| 9 | Tile (сетка полей) | В ответе агента колонки параметров запроса стояли вплотную | `.tile__grid` берёт зазоры из `--tile-gap-row` / `--tile-gap-col`, а их задаёт только тайл; сетка в сообщении стоит вне тайла | `.thr__body > .tile__grid` получает эти переменные = `--space-24` |
| 10 | Entity | В перечне шагов подготовки иконка шага стояла по центру трёх строк — названия, результата и чипа состояния — и отрывалась от названия | Ведущий элемент Entity выровнен по центру (`align-self: center`); варианта «по верхнему краю» для многострочного содержимого нет. Та же находка — в конструкторе (RequestBuilder.screen.md, раздел 15, №6) | `.thr__list .entity__lead { align-self: flex-start }` в `<style>` экрана (решение заказчика, 14.09.2026) |
| 11 | Tile | Иконка подзаголовка крупнее текста и выходит за свой контейнер; заметнее всего у залитой `check-circle-filled` в «Подготовка завершена» | Контейнер `.tile__subtitle-icon` — 16px, но размер глифа он не задаёт, и `<i data-icon>` остаётся 24px — размер по умолчанию из `ds-icons.js`. Сниппет чит-шита (`<span class="tile__subtitle-icon"><i data-icon="check-circle-filled"></i></span>`) даёт именно такую иконку | `.tile__subtitle-icon > [data-icon]` занимает контейнер целиком — 16px (решение заказчика, 14.09.2026) |

Класс находки №1 — «правило записано, кода нет» (урок Л42): чит-шит обещает, что
нативный `:checked` даёт тот же вид, что и класс-модификатор, а на деле
подменяется только цвет. Находка №2 — «правило есть, вход не тот» (урок Л48):
рантайм рабочий, но обязательный для него вход не описан там, где собирают экран.
