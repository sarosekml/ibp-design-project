# КНР — журнал изменений

Формат записи: что изменилось · почему · по какому требованию.

## Перенос — 24.09.2026

Компонент перенесён из прототипа Post (`Projects/post/post_local_components/`) в модуль
`apps/postrade/deals-app` и назван по правилу модуля «имя фронтенда, тип в конце». Разметка,
классы, `id` и глобалы скриптов не менялись. *Требование:* решение человека 24.09.2026.

- Тайл `TileKNR` → `widgets/tiles/KNRTile/KNRTile.*`; окно `TileKNRModal` → `widgets/modals/KNRModal/KNRModal.*`;
  подтверждение `TileKNRConfirm.html` → `widgets/modals/KNRModal/KNRConfirmModal.html`.
- Страницы документации (`*.doc.html`) и витрина пока не перенесены — после переноса харнеса.
  Записи ниже — история до переноса, имена и пути в них старые.

## Окно 0.007 — 23.09.2026

Три колонки окна — равной ширины. *Требование:* решение человека 23.09.2026.

- **Первый трек сетки больше не фиксированный**: `minmax(0, 1fr)` на каждую из трёх колонок
  вместо `calc(240px + 16px)` у колонки поиска. *Почему:* так решил человек. Поля формы
  тянутся за колонкой — они `inp--fullwidth`, своей ширины у них нет.
- Разделители, отступы колонок и полосы прокрутки не менялись.

## Окно 0.006 — 23.09.2026

Линия под заголовком колонки снята. *Требование:* решение человека 23.09.2026. Тайл
остаётся 0.008, карточка контрагента не менялась.

- **Горизонтальный Divider убран** из всех шести колонок окна (три на каждой вкладке).
  *Почему:* колонки разделяют вертикальные Divider, а линия под заголовком добавляла шума.
- **Зазор между заголовком и областью прокрутки сохранён**: `gap` колонки — 16px вместо
  прежних 8px + линия 1px + 8px, поэтому карточки и форма не сдвинулись. Отступы колонок
  справа (16px до разделителя, 8px под бегунок у последней) не менялись.

## Без бампа версий — 22.09.2026

Правка описания, компоненты не менялись. *Требование:* решение человека 22.09.2026.

- **LabelHelper убран из состава** тайла и окна — из таблиц «Из чего собрано» и из полей
  `ds` обоих паспортов. *Почему:* лейбл не самостоятельный компонент, а часть поля, и
  отдельной единицей в составе не перечисляется (LabelHelper 1.007, `ds-rules.md` §6).

## Окно 0.005 — 21.09.2026

Режим просмотра окна и запрет правки по статусу сделки. *Требование:* решение человека
21.09.2026 — в сделках со статусом «Активная», «Погашена», «Ожидает подтверждения» и
«Подтверждение изменений» правка сущностей невозможна при любой роли (основание —
«Текущий портфель.md»: «В активной сделке нельзя редактировать сущности»). Тайл остаётся
0.008: его `view` уже был, поменялись только описание и комментарий фрагмента. Карточка
контрагента — 0.005 (свой журнал).

**Окно (0.005)**
- **Режим прав** — `data-mode` на `.lc-knrm` (`edit` | `view`), ставит страница.
  `TileKNRModal.js` передаёт режим карточкам и перерисовывает колонки при его смене.
- **Вид в `view`**: два таба и три колонки на месте, поиск работает; карточки без кнопки
  переноса, двойной клик ничего не делает, отметка «КНР» не переключается; в подвале вместо
  «Сохранить» — «Закрыть» `btn--outline`. *Почему:* так решил человек — «как сейчас, но без
  перемещения и сохранения». Строка «Только просмотр» в паспорте окна больше не «не решено».
- **Состояния `locked` нет** — ни у тайла, ни у окна: запрет по статусу сделки — это тот же
  «Только просмотр» (решение человека 21.09.2026). Паспорта и комментарии фрагментов приведены.
- **Паспорта тайла и окна — по правилам ДС 1.005** (`rulesVersion`): правила убрали состояние
  «Правка временно запрещена» из обязательного набора, строка удалена из обоих паспортов,
  пункт бэклога витрины тайла снят.
- Витрина окна: раздел «Режим просмотра», свитч в конструкторе; пункт бэклога закрыт.

## 0.008 — 21.09.2026

Третья волна правок человека: перенос без перетаскивания, живой поиск, состав участников
сделки с сохранением. *Требование:* правки человека 21.09.2026 (7 пунктов). Окно — 0.004,
карточка контрагента — 0.004 (свой журнал).

**Тайл**
- **Строки — из данных сделки.** Новый `TileKNR.js` (`PostTileKNR.rowsHTML / render`): на
  странице сделки тайл показывает КНР своей сделки из `CounterpartiesStore`, пока состав
  грузится — `loading`, после «Сохранить» в окне перерисовывается сразу. Витрина строит
  строки той же функцией. *Почему:* правка состава в окне должна быть видна на странице
  сделки и в колонке «КНР» реестра.
- **Триггеры окна — `data-modal-guarded`**: в окне черновик, клик мимо окна его не сбрасывает.
- **Минимальная высота 200px** — задаёт страница сделки для всех тайлов; открытый вопрос
  «H min» закрыт.
- **Отклонение «свой JavaScript переноса» закрыто**: перетаскивания больше нет.

**Окно (0.004)**
- **Перенос — кнопкой со стрелками на карточке и двойным кликом** в соседнюю колонку; жест
  перетаскивания (порог, клон, призрак, линия вставки, клавиатурный перенос,
  `knr:beforemove`) удалён. Фокус после переноса — на той же карточке в новой колонке.
- **Поиск работает**: формы стали `<form data-knr-search>` (Enter = «Найти»), поиск — по
  базе контрагентов в сторе. Пустой результат — текст «По заданным параметрам ничего не
  найдено» в колонке, не Alert (решение человека). «Юридическое лицо» у физлиц — автокомплит
  из юрлиц-участников.
- **Форма юрлиц — Наименование · ИНН · КПП · ОГРН · КИО**: CRM ID убран, предзаполнение
  «Самолёт» снято (на витрине первый поиск делает её скрипт).
- **Черновик**: «Сохранить» применяет правки к сделке, крестик и Esc отменяют.
- **«Подтвердить» — в тоне Error** (`btn--accent btn--error`), по правилу ДС.
- **Подсказки кнопок переноса** привязываются при рендере карточки: `ds-tooltip.js` обходит
  `[data-tooltip]` только на загрузке страницы; перед перерисовкой открытая подсказка
  прячется.

**Данные (новое, `Projects/post/data/`)**
- **База контрагентов** — `mock-counterparties.js` без `place`/`knr` (это состав сделки):
  +29 юрлиц — все КНР реестра, +2 физлица у «ООО «ЮгСтрой»», поисковые ключи физлиц;
  «ЕГК ID» → «ЕПК ID», CRM ID удалён.
- **Физлица у всех юрлиц базы — по 2–3** (+81 запись, `fl-30` и дальше). *Почему:* физлица
  были только у двух юрлиц, и вкладка физлиц находила кого-то лишь на 2 сделках из 50 —
  поиск идёт по физлицам юрлиц-участников, а участники сделки для начала — её КНР.
  *Требование:* правка человека 21.09.2026. В `API.md` описан контракт поиска для
  настоящего бэкенда; в прототипе поиск остаётся на фронте, по мок-базе.
- **Состав сделки** — `CounterpartiesStore` (переписан): `use(dealId)`, поиск, перенос,
  черновик `begin/commit/rollback`; начальный состав несохранённой сделки — из её `knr` в
  реестре.
- **Хранение** — адаптер `post-api.js` и контракт `API.md`: сервер данных
  (`Projects/post/server.mjs`, база — `data/db/participants.json`) или, без него,
  localStorage. *Почему:* человек просил писать в бэкенд-базу; настоящего бэкенда у
  прототипа нет, поэтому контракт API отделён от хранилища, и смена бэкенда — правка
  одного адаптера.

## 0.007 — 21.09.2026

Вторая волна правок человека к тайлу, окну и витринам. *Требование:* правки человека
21.09.2026 (20 пунктов). Окно — 0.003, карточка контрагента — 0.003 (свой журнал).

**Тайл**
- **Лейблы колонок — обе подписи в две строки с переносом по смыслу**: «Наименование /
  контрагента», «Рейтинг и зона / проблемности» (`<br class="lc-knr__br">`). В одну строку —
  только обе сразу, от 406px ширины тайла (`@container`); порог выведен по метрикам шрифта
  Body XS. *Почему:* подписи в одну и в две строки рядом читались неровно, а свободный перенос
  рвал «Рейтинг и / зона проблемности».

**Окно (0.003)**
- **Название в ките и документации — «Модальное окно КНР»**; заголовок самого окна прежний.
- **Ширина — `modal--w8`** (было `--w6`). Паспорт называл `--w6` 1053px — это ширина `--w7`,
  исправлено.
- **Полоса прокрутки колонок — `.ds-scroll` из ДС** (Layout 1.011, заведён по этой правке):
  только бегунок цвета Secondary, появляется при прокрутке и гаснет. Бегунок стоит вплотную к
  разделителю: зазора между треками нет, полосу в 16px держат отступ разделителя и правая
  полоса колонки. Отступы и прозрачная рамка зоны переноса ушли.
- **Колонки при переносе не подсвечиваются** — только линия вставки.
- **Подтверждение — один текст в две строки** (`p.ds-body-m` с `<br>`) вместо двух абзацев
  с зазором.
- **Встроенный режим подтверждения** в `TileKNRModal.js`: у `.modal-scrim--inline`
  подтверждение показывается атрибутом `hidden`, а не слоем `DSModal`. *Почему:* на витрине
  окно стоит статично в демо-зоне, и слой поверх страницы из неё бы вышел.

**Витрины**
- Во всех трёх «Документациях» — только описание и поведение: убраны классы, атрибуты,
  события, пути, «решения человека» и сравнение с Kanban. Стандарт записан в README кита.
- «Бэклог» последним разделом на всех трёх страницах — по образцу страниц ДС.
- Витрина тайла: снят блок «Осознанные отклонения» (в паспорте они остаются).
- Витрина окна: окно стоит в демо-зоне статично, кнопки вызова нет; в конструкторе один свитч
  «Показывать окно подтверждения»; «Из чего собрано» — таблицей; в разделе подтверждения
  показано само окно (копия фрагмента).
- Конструкторы — по новым правилам ДС (docs-split): селекты на 2–3 варианта стали
  ButtonGroup, пустая колонка не рисуется.

## 0.006 — 21.09.2026

Пятнадцать правок человека к тайлу, окну выбора и витрине. *Требование:* правки человека
21.09.2026. Окно «Ключевой носитель риска» — 0.002, карточка контрагента — 0.002 (свой журнал).

**Тайл**
- **Лейблы колонок переносятся до двух строк** — модификатор ДС `ds-label--wrap`
  (LabelHelper 1.006), шапка колонок выровнена к низу.
  *Почему:* на трёх колонках из двенадцати лейблы резались до пары слов. Перенос заведён
  модификатором в ДС, а не переопределением Label в тайле — решение человека.
- **Наименование — до двух строк** вместо одной с многоточием: `link--truncate` снят, клэмп
  держит обёртка `.lc-knr__name`. *Почему:* одна строка на узком тайле оставляла от названия
  несколько символов.
- **Тултип «и ещё N КНР» больше не уходит под панель навигации** — правка в ДС (Tooltip 2.010,
  максимальный z-index у всех тултипов). В тайле ничего не менялось.

**Окно выбора (0.002)**
- **Фиксированная высота 60vh** — окно не прыгает при смене вкладки. Отклонение от «высота
  по контенту» записано в паспорте окна.
- **У каждой колонки свой скролл**, заголовок колонки и линия под ним стоят на месте
  (ряд сетки `minmax(0,1fr)`, колонка формы тоже `flex:1`).
- **Вертикальные Divider между колонками**, по 16px с каждой стороны линии (было 24 без линии).
- **Заголовки колонок — H6 Strong** (`h3.ds-h6-strong`) вместо серого лейбла.
- **Участники сделки другого вида видны на обеих вкладках, но недоступны** (Disabled у Card):
  на вкладке юрлиц — физлица, на вкладке физлиц — юрлица. Раньше на вкладке юрлиц физлиц не
  было вовсе, а юрлица на вкладке физлиц рисовались в режиме `view`.
- В карточке — грип из ДС (IconButton M 20×20), риск-метрика справа в теле, зазор под
  заголовком 4px (Card в Tile 1.013).

**Витрина тайла**
- «Из чего собрана» — таблицей на TableCell.
- Состояния — по три в ряд.
- Ширина демо — слайдер 180–940px, по умолчанию 400px (было 235px и список 3/6/12 колонок).
  Слайдер живёт в правой колонке конструктора: `kit-docpage.js` получил тип контрола `range`
  и переносит его в `.toggles` после раскладки docs-split.
- Мета-версия витрины приведена к паспорту: там стояло 0.003 при паспорте 0.005.

## 0.005 — 20.09.2026

Правки по замечаниям человека к странице компонента.

- **Вёрстка под табами «Конструктор» и «Код»** приведена к эталону: общий
  контейнер `.docs-layout > .docs-main`, как под «Документацией», и штатная
  разметка сегмент-контрола (`.segctrl--s` + `.segctrl__thumb` +
  `.segctrl__item`) вместо самодельных `.seg`.
  *Почему:* правила требуют одинаковой вёрстки под всеми тремя табами
  (`ds-rules` §11); без обёртки панели расходились по ширине и отступам, а
  переключатель языка был собран мимо компонента ДС.
- **В панель вернулся доступ к странице компонента.** Родитель аккордеона по
  контракту ДС не ссылка — `ds-nav-panel.js` вызывает на нём `preventDefault`,
  поэтому на страницу тайла из панели было не попасть. Теперь сама страница —
  первый под-пункт «Обзор», артефакт — вторым.
  *Требование:* замечание человека «не могу попасть на страницу тайла КНР».
- **Окно поднимается со страницы тайла**: демо-тайл получил `data-modal`, а
  страница — фрагменты окна и подтверждения и их скрипты. После каждой
  перерисовки демо зовётся `DSModal.bindAll` — рантайм привязывает триггеры
  поштучно на `DOMContentLoaded`, а тайл здесь рисуется скриптом.
  *Требование:* замечание человека «со страницы тайла должна быть возможность
  поднять модальное окно».
- **Страница тайла стала сборочным источником** — у неё появился
  `TileKNR.doc.preview.html` (в ней теперь есть метки). Ссылки и реестр ведут
  на собранный файл.

## 0.004 — 20.09.2026

Карточка контрагента вынесена в переиспользуемый компонент, у окна выбора появилась своя
страница. Требование — правки человека 20.09.2026 к странице компонента.

- **Карточка уехала в «Общие»** — `../CardCounterparty/`. В окне её девять раз не пишут:
  окно подключает компонент меткой, а карточки рисует из `CounterpartiesStore`.
  *Почему:* девять копий разметки расходились бы на первой же правке, а данные в них были
  прибиты к разметке. *Требование:* решение человека — «модальное окно тянет из локальных
  компонентов компонент карточки, данные тянутся из файла базы».
- **Данные окна** — `Projects/post/data/mock-counterparties.js` + `counterparties-store.js`
  по образцу `mock-deals.js` + `deals-store.js`. Перенос и отметка КНР пишутся в стор, а не
  в разметку; колонки перерисовываются по событию стора.
  *Почему:* одни и те же контрагенты видны в трёх колонках и на двух вкладках — без общего
  источника они рассинхронились бы на первом переносе.
- **Страница артефакта** `TileKNRModal.doc.html` + паспорт `TileKNRModal.md`. Длинный раздел
  «правила состава» уехал из паспорта тайла туда.
  *Почему:* правила требуют у артефакта собственное описание и связь в обе стороны.
  *Требование:* решение человека — «у модалки своя страница с конструктором и документацией,
  свой паспорт».
- **Страница компонента переведена на паттерн ДС**: сплиттер с живым демо, три таба
  (Конструктор · Документация · Код) и якорный TOC. Блоки состояний переехали в секцию
  «Состояния» вкладки «Документация», в документации появились ссылки на вложенные
  компоненты. *Требование:* решение человека — «якорные ссылки и три таба как у обычных
  компонентов; документацию дополнить ссылками на вложенные компоненты».
- **Категория** `компоненты сделки` → `Сделка`.
  *Требование:* решение человека — список категорий команды Post.

## 0.003 — 20.09.2026

Приведение к правилам локальных компонентов. Требование — страница правил
`DS-IBP/pages/patterns/LocalComponents.html`, сверка 20.09.2026: компонент собирался
без её прочтения, и часть расхождений была закрыта «отклонениями» там, где правила дают
прямой ответ.

- **Перестроение по своей ширине.** Список разложен на `.lc-knr__head` + `.lc-knr__row`
  с общей сеткой, у `.lc-knr` появился `container-type: inline-size`, ниже 240px строка
  складывается в две и правый лейбл убирается.
  *Почему:* правило «компонент реагирует на свою ширину, а не на ширину окна» не
  исполнялось — контейнерного запроса не было вовсе. Плоская сетка перестроиться не могла:
  при схлопывании в одну колонку лейбл второй колонки уезжал вперёд данных.
  *Требование:* правила локальных компонентов, раздел «Вёрстка и токены».
- **Состояние «Загрузка».** Каркас с лейблами колонок и двумя строками-заготовками на
  Skeleton; действия шапки не рисуются.
  *Почему:* «состояние, которого нет в витрине, считается непроработанным», а правила
  прямо называют компонент сборки — владелец продукта для этого не нужен.
  *Требование:* правила локальных компонентов, раздел «Состояния»; решение человека
  20.09.2026 — из непроработанных добрать только «Загрузку».
- **Поле `opens` в шапке паспорта** со ссылками на оба артефакта.
  *Почему:* правила требуют связь компонент ↔ артефакт в обе стороны именно этим полем;
  раздел «Связанные артефакты» её не заменяет. *Требование:* раздел «Связанные артефакты».
- **«Данных нет» перестало быть отклонением**, `rulesVersion` → 1.004.
  *Почему:* правило переписано в ДС — EmptyState больше не единственный способ, в узком
  компоненте пустота собирается строкой и кнопкой. *Требование:* решение человека
  20.09.2026 «делаем по макету, правило переписываем»; см. `DS-IBP/CHANGELOG.md`.
- **Ошибка без повтора запроса записана отклонением явно** (правила требуют Alert · Button).
  *Почему:* на макете кнопки нет. *Требование:* решение человека 20.09.2026.

## 0.002 — 20.09.2026

Компонент перестал быть заглушкой. Основание — макеты `Uploads/Post _ DEAL _ R&D/`.

- Разметка всех состояний в одном фрагменте, переключение — `TileKNR.css`
  по `data-state` / `data-mode`: данные есть · заполнено частично · данных нет ·
  ошибка, каждое в режимах `edit` и `view`.
- Заполненное состояние: две колонки «Наименование контрагента» ↔ «Рейтинг и зона
  проблемности», строка — Link + RiskMetric. Больше двух КНР — строка «и ещё N КНР»
  с тултипом-списком.
- Пустое состояние заменено: вместо EmptyState с иллюстрацией — кнопка «Заполнить»
  (есть право) или строка «Не заполнено» (права нет).
- Появился `TileKNR.css` — первый собственный CSS у модуля направления Post.
- Связанные артефакты: `TileKNRModal.html` (окно «Ключевой носитель риска», два таба,
  три колонки, перенос карточек) и `TileKNRConfirm.html` (вложенное подтверждение).
  Поведение — `TileKNRModal.js`.
- Паспорт заполнен: поля, права, состояния, переполнение, артефакты, открытые вопросы
  и осознанные отклонения.
- Витрина `TileKNR.doc.html` — первая в направлении Post.
- `baseline.png` **не снят**: запуск браузера по скрипту в контуре компании не
  согласован (`ds-rules.md`, §9), а рисовать снимок руками нельзя. Снимок делает
  человек из витрины, когда состояния приняты.

### Починено в оснастке приёмки

Оба дефекта — ложные блокеры на первом же наполненном локальном компоненте;
по правилу «если дефект должен был поймать сторож и не поймал — чинится сторож»
правились сторожа, а не разметка (`.opencode/skills/screen-review/tooling/layout-check.mjs`):

- **Б4** читал классы только из `DS-IBP/styles/*.css` и `<style>` экрана и не знал про
  CSS локальных компонентов, который ассемблер подключает `<link>`-ами после `@lc-css`.
  Теперь читает и их.
- **Витрина проверялась как экран** — 17 блокеров про каркас, крошки и ширину тайлов.
  Теперь `<Имя>.doc.html` с паспортом рядом печатает `ПРОПУЩЕН:`, как фрагмент.

## 0.001 — 19.09.2026

Создание компонента: оболочка тайла (заголовок, действие шапки, EmptyState
«Данные ещё не заведены») и паспорт с неизвестным составом полей.
