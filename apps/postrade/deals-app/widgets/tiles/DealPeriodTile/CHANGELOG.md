# Сроки сделки — журнал изменений

Формат записи: что изменилось · почему · по какому требованию.

## Перенос — 24.09.2026

Компонент перенесён из прототипа Post (`Projects/post/post_local_components/`) в модуль
`apps/postrade/deals-app` и назван по правилу модуля «имя фронтенда, тип в конце». Разметка,
классы, `id` и глобалы скриптов не менялись. *Требование:* решение человека 24.09.2026.

- Тайл `TileDealTerms` → `widgets/tiles/DealPeriodTile/DealPeriodTile.*`; окно `TileDealTermsModal` →
  `widgets/modals/DealPeriodModal/DealPeriodModal.*` (имена фронтенда).
- Страницы документации (`*.doc.html`) и витрина пока не перенесены — после переноса харнеса.
  Записи ниже — история до переноса, имена и пути в них старые.

## окно 0.004 — 23.09.2026

Крестик очистки отдан рантайму. Тайл не менялся. *Требование:* решение человека
23.09.2026 — «нужен рантайм».

**Окно (0.004)**
- **Крестик очистки убран из разметки** — его рисует `ds-input.js` по значению,
  между информером и календарём (рантайм сам держит порядок действий).
  *Почему:* нарисованный руками крестик висит и на пустом поле, а прятать его
  разметка не умеет — видимостью управляет рантайм. Заодно это снимает
  расхождение с окном «Команда сделки», где крестика в разметке нет.
  `DSInput.sync` после заполнения поля кодом окно звало и раньше — на нём
  всё и держится (правило InputText 1.014).

## окно 0.003 — 22.09.2026

Второй круг замечаний человека по окну. Тайл не менялся. *Требование:* замечания
человека 22.09.2026.

**Окно (0.003)**
- **Даты-границы — в один ряд, в две колонки** (`.grid12` + `.col-6`, лейблы
  `ds-label--wrap`); «Дата первой выдачи» под ними во всю ширину. *Почему:* два
  нередактируемых поля читаются как границы диапазона, а столбиком из трёх полей
  окно длиннее без нужды. Ширина осталась `modal--w3`, своего CSS по-прежнему нет.
- **Тултипы информеров — по одному предложению**: второе, про пересчёт и сдвиг
  дат, убрано из подсказок обеих дат-границ. *Почему:* процесс расписывает
  документация, подсказка называет поле.
- **LabelHelper убран из состава** — из таблицы «Из чего собрано» и из поля `ds`
  паспорта. *Почему:* лейбл не самостоятельный компонент, а часть поля; правило
  заведено в ДС (LabelHelper 1.007) и в `ds-rules.md` §6.

## 0.005 · окно 0.002 — 22.09.2026

Правки человека после первого показа. *Требование:* замечания человека 22.09.2026.

**Тайл (0.005)**
- **Состояния «Ошибка» нет** — Alert с «Повторить», его CSS, вариант конструктора, кейс витрины и
  фикстура удалены. *Почему:* «для этого тайла его нет».

**Окно (0.002)**
- **Поля друг под другом, ширина `modal--w3`** — вместо двух колонок `.grid12` на `modal--w4`. Тело
  окна ДС само колонка, своего CSS по-прежнему нет.
- **Тултип даты первой выдачи короче** — «Заполняется вручную, если в сделке есть НКЛ с баланса
  ПАО»: подробности в подсказке не нужны.
- **Окно открывалось с поднятым тултипом** — фокус при открытии попадал на информер первого поля.
  Починено в ДС (Modal 1.009: фокус — на первое поле ввода), в окне правок нет. Урок Л135.
- **Цвет информера — secondary** — у ReadOnlyField в ДС (1.009); у InputDate он уже был таким.

**Страница сделки** — заглушки соседних тайлов больше не растягивают первый ряд: EmptyState в них
заменён строкой по центру (журналы у заглушек не ведутся, см. их паспорта).

## 0.004 · окно 0.001 — 22.09.2026

Тайл наполнен и получил окно правки. *Требование:* ТЗ тайла со слов человека 22.09.2026, без
макета; предметный контекст — там же.

**Тайл (0.004)**
- **Состав полей**: «Дата заключения», «Дата окончания», «Дата первой выдачи» — ReadOnlyField в две
  колонки, пустая дата — прочерк. *Почему:* состав задал человек; до этого тайл был заглушкой.
- **Все состояния**: загрузка (каркас в сетке полей), данные, частично, нет данных («Заполнить» /
  «Не заполнено»), ошибка (Alert с «Повторить» — по правилу состояния, макета, который его бы
  отменял, нет). «Не заполнено» = пусты все три даты (решение человека 22.09.2026).
- **Действие шапки** — IconButton «Редактировать» только в `edit` и только когда даты есть; в
  `view` его нет вовсе (ТЗ).
- **Перестроение по своей ширине**: ниже 220px — одна колонка.
- **`TileDealTerms.js`** — `PostTileDealTerms.render` / `fieldsHTML` / `stateOf`: страница сделки
  и витрина строят поля одной функцией.
- **Паспорт** — по правилам 1.006; описание — предметный контекст: откуда даты, когда меняются,
  почему дату первой выдачи вводят вручную (НКЛ с баланса ПАО). Признак НКЛ не реализован —
  придёт из дерева продуктов (решение человека 22.09.2026: не забегать вперёд).
- Purpose сменился: «вход и выход ДИД» из заглушки сняты — источника у них нет.

**Окно (0.001)**
- **Состав**: две даты для справки и InputDate «Дата первой выдачи», у каждого поля информер с
  тултипом (просьба человека 22.09.2026). Ширина `modal--w4`, только «Сохранить» в подвале.
- **Проверка** по «Сохранить»: пусто можно; иначе дата не раньше заключения и не позже окончания
  (решение человека 22.09.2026). Ошибка — тултипом на фокусе, по правилу InputText.
- **Отдельная страница документации** — по правилу 1.006 «окно правки всегда получает свою
  страницу».

**Страница сделки** — тайл получает даты сделки из `DealsStore`, окно сохраняет дату туда же;
тайл перерисовывается по событию стора.
