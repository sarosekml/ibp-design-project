---
name: Сроки сделки
version: 0.005
updated: "22.09.2026"
rulesVersion: 1.006
owner: не решено (19.09.2026)
designer: Роман Эсэф
category: Сделка
purpose: Ключевые даты сделки — заключение, окончание и первая выдача — и точка входа в ввод даты первой выдачи
ds: [Tile, ReadOnlyField, Skeleton, Buttons, IconButton, Modal, InputDate, Tooltip]
usedOn: [Страница сделки — блок «Общая информация», первый ряд, 3 колонки из 12 (узкая ширина — 6)]
opens: ["Модальное окно сроков сделки → widgets/modals/DealPeriodModal/DealPeriodModal.md (паспорт артефакта)"]
variants: []
modifiers: []
dependsOn: []
requirements: [ТЗ страницы сделки 19.09.2026, ТЗ тайла «Сроки сделки» 22.09.2026 (со слов человека, без макета)]
knowledge: [apps/postrade/deals-app/refs/Текущий портфель.md — колонки «Дата подписания», «Дата первой выдачи», «Дата окончания»]
---

# Сроки сделки

## Описание

Тайл страницы сделки. Показывает три даты сделки: дату заключения, дату окончания и дату
первой выдачи. Те же даты видны в колонках реестра «Текущий портфель» (там дата заключения
называется «Дата подписания»).

**Откуда даты по смыслу** (со слов человека, 22.09.2026):

- **Дата заключения** — самая ранняя дата заключения договора среди всех инструментов сделки.
- **Дата окончания** — самая поздняя дата среди всех инструментов сделки: дата последнего
  планового платежа.
- Обе заполняются автоматически, когда в сделке появляется первый инструмент с заполненными
  данными, и могут меняться: появился инструмент с более поздней датой окончания, у инструмента
  сдвинулись платежи и последний платёж стал позже.
- **Дата первой выдачи** вводится вручную, и только если в сделке есть инструмент **НКЛ с
  баланса ПАО**: его данные в системе не хранятся — они в стороннем сервисе. Для остальных
  инструментов данные для отчётов берутся из плановых платежей сделки, где хранятся все платежи
  по всем инструментам, и поле заполнять не нужно; в тайле тогда прочерк и правки нет.

Признак «в сделке есть НКЛ с баланса ПАО» придёт из дерева продуктов, которого пока нет. До
тех пор правка зависит только от режима прав — см. «Открытые вопросы», п. 3.

Правится в тайле одна дата — первой выдачи — и не в самом тайле, а в связанном модальном окне
сроков сделки. Две другие даты стоят в окне для справки: по ним видно допустимый диапазон.

Место на странице, ширину в колонках и минимальную высоту (200px) задаёт экран
(`apps/postrade/deals-app/pages/Deal.html`), не компонент.

**Откуда данные в прототипе.** Даты во фрагменте `DealPeriodTile.html` — пример. На странице
сделки их заменяет `DealPeriodTile.js` (`PostTileDealTerms.render`) полями `signDate`,
`endDate`, `firstDisb` сделки из `DealsStore` (`apps/postrade/deals-app/data/deals-store.js`). После
«Сохранить» в окне страница пишет дату в `DealsStore.update` и перерисовывает тайл по событию
стора. Хранение — в памяти вкладки, как у всего `DealsStore`.

## Поля

| Поле | Смысл | Тип, единица | Источник | Обязательное |
|---|---|---|---|---|
| Дата заключения | Когда заключён первый договор по сделке | дата `ДД.ММ.ГГГГ` | инструменты сделки — самая ранняя дата заключения; в прототипе `signDate` сделки | нет — пусто, пока в сделке нет инструмента с данными |
| Дата окончания | Когда кончается последний договор по сделке | дата `ДД.ММ.ГГГГ` | инструменты сделки — дата последнего планового платежа; в прототипе `endDate` сделки | нет — пусто, пока в сделке нет инструмента с данными |
| Дата первой выдачи | Когда выданы первые средства | дата `ДД.ММ.ГГГГ` | ручной ввод в окне сроков сделки — при инструменте НКЛ с баланса ПАО; для остальных инструментов в тайле прочерк; в прототипе `firstDisb` сделки | нет — окно разрешает стереть дату |

Порядок полей — порядок отображения: две колонки, «Дата заключения · Дата окончания» в первой
строке, «Дата первой выдачи» во второй. Пустая дата показывается прочерком, поле с места не
уходит.

## Права

| Роль | Просмотр | Редактирование |
|---|---|---|
| Финансист ДИД | да | не решено (22.09.2026) |

Режим прав приходит на компонент атрибутом `data-mode` — его ставит страница сделки:

| `data-mode` | Что в шапке |
|---|---|
| `edit` | IconButton `edit` «Редактировать» — открывает окно сроков сделки (`data-modal-guarded`: в окне ввод, клик мимо окна его не закрывает). Только когда даты есть (`data`, `partial`) |
| `view` | действий нет ни в одном состоянии |

В режиме `view` действие правки **не рисуется**, а не выключается.

Статус сделки сильнее роли: в статусах «Активная», «Погашена», «Ожидает подтверждения» и
«Подтверждение изменений» страница ставит `view` при любой роли (`DealsStore.isEditable`).

## Состояния

| Состояние | Есть в продукте | Вид |
|---|---|---|
| Загрузка | да | Каркас в сетке полей: у каждого поля строка-заготовка подписи (16px) и значения (20px, 80px ширины), чтобы раскладка не прыгнула после ответа. Действия в шапке нет |
| Данные есть | да | три даты в две колонки |
| Заполнено частично | да | есть хотя бы одна дата, но не все: пустые — прочерком, поля на местах. Действие правки в шапке — как у заполненного |
| Данных нет | да | все три даты пусты. С правом на правку — кнопка «Заполнить» (открывает то же окно); без права — строка «Не заполнено». Действия в шапке нет ни в одном режиме. Собрано строкой и кнопкой, без EmptyState — по правилу узкого компонента |
| Ошибка | нет | в этом тайле нет (решение человека 22.09.2026) |
| Обновление | не решено (22.09.2026) |  |
| Редактирование доступно | да | IconButton `edit` в шапке (при `data` и `partial`), кнопка «Заполнить» (при `empty`) |
| Только просмотр | да | состав и порядок полей те же, действий нет; «Данных нет» — строкой «Не заполнено». Так же — когда статус сделки не допускает правку |
| Нет права видеть | не решено (22.09.2026) | решает экран |

### Свои состояния

Своих состояний сверх обязательного набора нет. Будущее правило «правка только при НКЛ с
баланса ПАО» — не состояние компонента, а условие права на правку: без НКЛ тайл выглядит как в
режиме просмотра, а в поле «Дата первой выдачи» — прочерк.

## Обязательность заполнения

- Компонент обязателен к заполнению целиком: нет — даты заключения и окончания приходят из
  инструментов, дату первой выдачи вводят не для всех сделок.
- Обязательные поля внутри: не решено (22.09.2026) — обязательна ли дата первой выдачи у
  сделки с НКЛ с баланса ПАО, человек не говорил.
- Что отдаёт наружу: ничего.

## Переполнение

| Случай | Правило |
|---|---|
| Длинный текст в поле | Значение — дата фиксированной длины, не переполняется. Лейбл переносится до двух строк (`ds-label--wrap`), дальше многоточие |
| Много строк или записей | Не встречается: полей всегда три |
| Узкая ширина | Тайл стоит на 3 колонках, на узкой странице — на 6. Колонки сжимаемые (`minmax(0,1fr)`), лейблы переносятся в две строки. Компонент реагирует на свою ширину (`@container`): ниже 220px поля встают в одну колонку в том же порядке. Порог — оценка по шрифту Body M (16px): дата «00.00.0000» ≈ 82px, 2×82 + зазор 16 + поля тела тайла 2×20 = 220px. На странице сделки тайл уже порога не бывает (ниже 1560 правая колонка уходит вниз, ниже 1240 тайл встаёт на 6 колонок) — правило для других мест и узкой витрины. Колонки заданы в CSS модуля, а не инлайном на `.tile__grid`: инлайн перебил бы перестроение. Цена — сенсор `layout-check` не читает колонки и пишет замечание «нет repeat(N)» |
| Полная ширина | Не встречается: тайл всегда 3 или 6 колонок |

## Связанные артефакты

| Триггер | Тип | Что внутри | Что меняется после | Кто ещё вызывает |
|---|---|---|---|---|
| IconButton `edit` в шапке, кнопка «Заполнить» | Модальное окно сроков сделки (`DealPeriodModal.html`, заголовок «Сроки сделки») | Дата заключения и дата окончания для справки, поле «Дата первой выдачи»; у каждого поля информер | После «Сохранить» — новая дата первой выдачи: тайл перерисовывается сразу. Крестик и Esc — без изменений. Фокус возвращается на триггер (рантайм окна) | нет |

### Модальное окно сроков сделки

Состав окна, правило проверки даты и состояния живут в паспорте самого артефакта —
[DealPeriodModal.md](../../modals/DealPeriodModal/DealPeriodModal.md) (страница документации в этот репозиторий ещё не перенесена).
Здесь они не дублируются.

## Открытые вопросы

1. **Владелец компонента** — не назначен (19.09.2026).
2. **Хранение даты первой выдачи.** В прототипе дата сохраняется в `DealsStore` — в памяти
   вкладки: после перезагрузки и на реестре портфеля её не видно. Постоянное хранение (через
   `PostApi`, как состав КНР) — не решено (22.09.2026).
3. **Правка только при НКЛ с баланса ПАО.** Правило известно (человек, 22.09.2026), признак
   придёт из дерева продуктов (тайл «Продукты сделки», пока заглушка). Когда он появится:
   без НКЛ — прочерк в поле «Дата первой выдачи», правки и «Заполнить» нет. До тех пор правка
   зависит только от режима прав, а дата первой выдачи показывается из данных сделки.
4. **Дата погашения.** База знаний: при погашении сделки дата погашения «встанет в карточку
   сроки сделки». Решение человека 22.09.2026 — в тайле только три поля; четвёртое не
   добавлено.
5. **Прежний purpose заглушки** называл ещё «вход и выход ДИД». Источника у этой пары нет,
   состав тайла — по ТЗ 22.09.2026.
6. **Непроработанные состояния** — «Обновление» и «Нет права видеть» (решает экран).

## Осознанные отклонения

1. **Три поля на трёх колонках.** Правило ширины тайла (Tile, K4 сенсора `layout-check`):
   блок на 3–4 коротких поля — от 4 колонок. Тайл стоит на 3 колонках из 12 — так задан первый
   ряд страницы сделки (3 + 3 + 6, ТЗ 19.09.2026) и так сказал человек 22.09.2026 («тайл на три
   колонки»). Поля — даты фиксированной длины, в две колонки они помещаются (см. «Переполнение»).
   Сенсор на странице сделки красный по K4 ожидаемо — это отклонение по ТЗ, а не дефект.
