---
name: Модальное окно сроков сделки
type: модальное окно
version: 0.004
updated: "23.09.2026"
rulesVersion: 1.006
owner: не решено (22.09.2026)
designer: Роман Эсэф
category: Сделка
artifactOf: Сроки сделки (DealPeriodTile.md)
opensFrom: [Тайл «Сроки сделки» — IconButton edit в шапке и кнопка «Заполнить» в пустом состоянии]
purpose: Ввести дату первой выдачи, сверяясь с датами заключения и окончания сделки
ds: [Modal, ReadOnlyField, InputDate, Tooltip, Buttons, IconButton]
requirements: [ТЗ тайла «Сроки сделки» 22.09.2026 (со слов человека, без макета)]
knowledge: [apps/postrade/deals-app/refs/Текущий портфель.md]
---

# Модальное окно сроков сделки

## Описание

Артефакт тайла «Сроки сделки». Не компонент: сам по себе не существует и в плоском списке кита
его нет — он принадлежит своему тайлу и живёт под-пунктом его навигации. В ките и документации
оно называется «Модальное окно сроков сделки», заголовок самого окна — «Сроки сделки».

Окно решает одну задачу: ввести дату первой выдачи. Её вводят вручную, когда в сделке есть
инструмент НКЛ с баланса ПАО: его выдачи хранятся в стороннем сервисе, а не в системе. Дата
заключения и дата окончания приходят из инструментов сделки и стоят в окне только для справки —
по ним видно допустимый диапазон. У каждого поля информер: что это за поле и откуда данные.

Даты окно не хранит и сделку не знает: при каждом открытии берёт их у страницы, проверенную
дату отдаёт странице (`PostDealTermsModal.bind(scrim, { get, save })`,
`DealPeriodModal.js`). Страница сделки пишет дату в `DealsStore` и перерисовывает тайл.

## Состав

| Зона | Чем собрана |
|---|---|
| Шапка | `.modal__head`, заголовок «Сроки сделки» + IconButton L «Закрыть» |
| Тело | «Дата заключения» и «Дата окончания» — ReadOnlyField в один ряд, в две колонки: штатная сетка ДС `.grid12`, ширина колонки `.col-6`, зазор 16px. Под рядом — «Дата первой выдачи», InputDate M во всю ширину (прямой ребёнок `.modal__body`, в ДС это колонка с тем же зазором). Порядок — как в тайле; лейблы в колонках переносятся до двух строк (`ds-label--wrap`) |
| Информеры | у ReadOnlyField — иконка справа `.rof__icon` `info-circle` с тултипом (`data-tooltip`, `data-tooltip-multiline="yes"`, фокус с клавиатуры — `tabindex="0"`); у InputDate — действие-информер `inp__act--static` «Подсказка» первым в ряду действий (порядок ДС: информер → крестик → календарь). Цвет обоих — `--secondary`, его задают компоненты ДС (ReadOnlyField 1.009) |
| Крестик очистки | в разметке не пишется: он живёт по значению, и рисует его рантайм `ds-input.js` — между информером и календарём, ровно когда в поле есть что стирать. Нарисованный руками, он висел бы и на пустом поле. Значение окно ставит кодом, поэтому после заполнения зовёт `DSInput.sync` (присваивание `.value` событий не шлёт — правило InputText 1.014). С 23.09.2026; до этого крестик стоял в разметке |
| Начальный фокус | поле «Дата первой выдачи»: рантайм окна ДС (Modal 1.009) ставит фокус на первое поле ввода тела, а не на информер первого поля, — окно открывается без поднятой подсказки |
| Подвал | `.modal__foot`: кнопка «Сохранить» accent (`data-terms-save`). Без `data-modal-close`: окно закрывает скрипт, когда проверка прошла |

Ширина — `modal--w3`, 3 колонки (решение человека 22.09.2026). Высота — по контенту. Своего
CSS у окна нет.

Текст информера — одно предложение: что это за дата и откуда она. Процесс
(когда заполняется, от чего сдвигается) в подсказку не выносится — для этого
есть документация (решение человека 22.09.2026).

Тексты информеров (черновик, согласовать на витрине):

| Поле | Текст |
|---|---|
| Дата заключения | Самая ранняя дата заключения договора среди инструментов сделки |
| Дата окончания | Самая поздняя дата среди инструментов сделки — по последнему плановому платежу |
| Дата первой выдачи | Заполняется вручную, если в сделке есть НКЛ с баланса ПАО (подробности в подсказке не нужны — решение человека 22.09.2026) |

## Правила состава и проверки

1. Правится только «Дата первой выдачи». Две другие даты — ReadOnlyField: их не правят и здесь,
   они приходят из инструментов.
2. Пустая дата для справки — прочерк, поле с места не уходит.
3. Дату первой выдачи можно стереть и сохранить пустой (решение человека 22.09.2026).
4. Непустая дата проверяется по «Сохранить»:
   - не дата `ДД.ММ.ГГГГ` (в том числе несуществующий день) — «Введите дату в формате ДД.ММ.ГГГГ»;
   - раньше даты заключения — «Не раньше даты заключения — <дата>»;
   - позже даты окончания — «Не позже даты окончания — <дата>».
   Границы включительно. Если даты заключения или окончания нет, эта граница не проверяется.
5. Ошибка — по правилу InputText: поле красное (`inp--error`, `aria-invalid`), текст — в тултипе
   на фокусе поля, не в хелпере. Окно с ошибкой не закрывается, фокус уходит в поле.
6. Правка поля пересчитывает уже показанную ошибку и снимает её, как только ввод стал верным.
   Новая ошибка появляется только по «Сохранить» — пока дату набирают, поле не краснеет.
7. Маска `ДД.ММ.ГГГГ` и календарь — из коробки ДС (InputDate, DatePicker).

## Состояния

| Состояние | Есть в продукте | Вид |
|---|---|---|
| Загрузка | не решено (22.09.2026) | даты берутся у страницы в момент открытия |
| Данные есть | да | две даты для справки, в поле — текущая дата первой выдачи |
| Заполнено частично | да | пустые даты для справки — прочерком; отсутствующая граница не проверяется |
| Данных нет | да | обе даты для справки прочерком, поле пустое; окно открывается кнопкой «Заполнить» |
| Ошибка проверки | да | красное поле, текст ошибки в тултипе на фокусе; «Сохранить» не применяет дату |
| Ошибка сервиса | не решено (22.09.2026) | в прототипе сохранение не уходит на сервер |
| Сохранение | не решено (22.09.2026) | в прототипе мгновенное |
| Редактирование доступно | да | поле даты активно, «Сохранить» в подвале |
| Только просмотр | да | окно не открывается: в режиме просмотра у тайла нет действий |
| Нет права видеть | не решено (22.09.2026) | решает экран |

## Права

| Роль | Просмотр | Редактирование |
|---|---|---|
| Финансист ДИД | да | не решено (22.09.2026) |

Своего режима прав у окна нет: его открывают только из тайла в режиме `edit`. Статус сделки,
не допускающий правку, ставит тайлу `view`, и окно становится недоступным.

## Переполнение

| Случай | Правило |
|---|---|
| Длинный текст в поле | Значения — даты фиксированной длины; тексты информеров переносятся в тултипе (`multiline`) |
| Много строк или записей | Не встречается: три поля |
| Узкая ширина | Не встречается: ширина окна фиксирована шагом ДС, ряд из двух колонок не перестраивается |
| Полная ширина | — |

## Открытые вопросы

1. **Владелец** — не назначен (22.09.2026).
2. **Окно только при НКЛ с баланса ПАО.** Когда признак придёт из дерева продуктов, без НКЛ
   окно из тайла не открывается (см. `DealPeriodTile.md`, «Открытые вопросы», п. 3).
3. **Тексты информеров** — черновик по словам человека 22.09.2026; согласовать.
4. **Хранение** — дата уходит в `DealsStore` (память вкладки); постоянное хранение не решено
   (22.09.2026).
5. **Контекст проверки** — человек дополнит правило диапазона позже (22.09.2026).
