{
  "_": [
    "Разметка пунктов чек-листов, которые НЕ закрыты сторожем. Читает lessons-cli coverage.",
    "Смысл: число «46 непокрытых» само по себе — счётчик вины. Разметка превращает его",
    "в две разные величины: рабочий список (mechanizable — сторож посилен, просто не написан)",
    "и честная граница статики (judgment — статикой не ловится, ds-rules §9).",
    "Пункт, которого нет ни здесь, ни в выводе сенсора, — нерешённый: lessons-cli coverage даёт код 1.",
    "Закрыл пункт сторожем — убери его отсюда; список mechanizable обязан уменьшаться.",
    "closedBy — пункт закрыт НЕ сенсором экранов: якорь с пространством имён плюс обоснование. Без этой графы отчёт занижал закрытость (урок Л67).",
    "partial — пункт закрыт ЧАСТИЧНО: названо, что именно осталось. Пункт с оговоркой, закрытый молча, читается как закрытый целиком, и оставшаяся половина не попадает ни в один список."
  ],
  "mechanizable": [],
  "judgment": {
    "Б11": "репозиторий не под git; отличить правку файла ДС от исходного состояния статикой нечем, а mtime доказательством не является (это прямо сказано в К4)",
    "Б19": "выбор иконки по смыслу действия — семантика: «переход внутри системы» против «во внешнюю» из разметки не выводится",
    "Б20": "однородность показателей внутри блока — суждение о природе величин, а не о разметке",
    "Б27": "строки таблицы на реальных экранах рисует JS (на Portfolio.html статических .tbl__row ноль). Статическая проверка дала бы PASS там, где данных нет вовсе — это хуже отсутствия проверки. Место правила — прогон рантаймов в linkedom, а не сенсор",
    "Б28": "живость по определению требует прогона рантаймов; статикой не проверяется (уроки Л32–Л34)",
    "З1": "«статус ли это» — смысл поля, а не признак разметки",
    "З4": "величина против идентификатора различается смыслом колонки (урок Л31): «Номер сделки» и «Сумма» в разметке неотличимы",
    "З8": "инфинитив и качество формулировок — редактура, не структура",
    "К3": "фактический скролл и работа якорей без рендера не проверяются (ds-rules §9, честное ограничение статики)",
    "К4": "правило поведения ревьюера («не выносить блокер по одному mtime»), а не проверка артефакта",
    "К5": "правило поведения ревьюера («не объявлять мёртвым до чтения page.js»), а не проверка артефакта",
    "К14": "правило поведения («сторож промолчал — проверь, видит ли он вход»), а не проверка артефакта",
    "K2": "класс поля (короткое/длинное/составное) определяется смыслом содержимого; эвристика ширины уже используется в K2.1 и дальше не тянется",
    "K3": "сопоставимость полей в ряду — суждение о классе поля",
    "K7": "однородность оформления величин — суждение о природе величин",
    "K8": "режим экрана (чтение или ввод) из разметки не выводится — он задаётся задачей",
    "K10": "«ключевое для роли» определяется ролью пользователя, а не разметкой",
    "K11": "правдивость обоснования в screen.md — смысловая проверка текста против ТЗ",
    "K13": "«добавленная сущность» определяется сверкой с ТЗ: есть ли в требованиях доля/процент/динамика",
    "К6": "порядок скриптов важен ТОЛЬКО когда страничный скрипт читает состояние рантайма в момент события. Порядок сам по себе дефектом не является: проверка «рантайм после page.js» дала 11 блокеров на рабочих страницах (последним подключается ds-nav.js, к конструктору отношения не имеющий). Отличить «читает состояние рантайма» от «читает свой демо-DOM» статикой нельзя — tab.page.js ставит aria-selected своим же элементам"
  },
  "_прим": {
    "К13": "частично закрыт аудитом spec-audit.mjs (проход 4). Полное закрытие — расширение аудита, не сенсора",
    "Б29": "механизируется только половина правила — «нет ручного выравнивания .pgn__pagesize в <style>». Структурная половина («пагинатор справа») даёт ложный FAIL: на Portfolio.html колонтитул строит DSPagination.footer и .pgn-row в разметке нет вовсе, а в FundList.html есть .pgn-row--bulk, которому __right не нужен"
  },
  "closedBy": {
    "К7": "линтер:A2 — класс, доступный только через @import-цепочку, считается неподключённым: A2 требует <link> на файл-владелец",
    "К1": "линтер:R1 — .splitpane--app вместе с собственной flex-раскладкой панелей; доказано откатом (DS-IBP/fixtures/R1.bad.html)",
    "К2": "линтер:R2 — segctrl внутри #pg-controls без align-self:flex-start; доказано откатом (R2.bad.html)",
    "К10": "линтер:R4 — остаток flex-раскладки конструктора в <style>; доказано откатом (R4.bad.html). Переехал из сенсора: страница документации сенсору не по входу",
    "К8": "линтер:R5 — запись { on, off } в DS_SPLIT_SWITCH_LABELS: подпись свитча меняется при переключении; доказано откатом (R5.bad.html)",
    "К9": "линтер:R6 (часть а — бинарный селект без непустой .lbl) и линтер:R8 (часть б — самописная button.toggle[aria-pressed] в #pg-controls); обе доказаны откатом",
    "К11": "линтер:R7 — .pg__grouphead без своих контролов следом; доказано откатом (R7.bad.html)",
    "К13": "аудит:1 · аудит:2 · аудит:3 · аудит:4 — spec-audit сверяет обещания спек с реализацией по четырём семействам: классы-состояния, data-хуки, API window.DSx, «усечено → тултип». Обещание вне этих четырёх форм остаётся суждением — это названо здесь, а не спрятано в графе «закрыто»"
  },
  "partial": {
    "K9": "сторож закрывает ЧИСЛОВУЮ половину пункта (полей в тайле: до 10 норма, 11–12 замечание, 13+ блокер). Вторая половина — «блоков в зоне не больше 7–8» — не закрыта: «зона» структурно не определена, и додумывать её значило бы запрещать неописанное (урок Л72)",
    "К13": "аудит сверяет обещания спек по четырём семействам (классы-состояния, data-хуки, API window.DSx, «усечено → тултип»). Обещание вне этих форм остаётся суждением",
    "Б15": "закрыты ИМЕНОВАННЫЕ случаи пункта: бургер/пин/выход NavPanel и кнопки в .tile__actions — все ibtn--m. Общее требование «размер совпадает со спекой компонента» не закрыто: это сверка каждого компонента с его спекой, а не строковая проверка",
    "Б29": "закрыта половина «нет ручного выравнивания пагинатора в <style> экрана». Структурная половина не закрыта намеренно: на реальном экране колонтитул строит рантайм DSPagination.footer, и .pgn-row в разметке нет вовсе — проверка дала бы ложный FAIL",
    "З2": "закрыты две механические части: не больше одной активной сортировки (aria-sort) и непустой data-sort у .th__sort. Часть «порядок строк в макете задан data-sort-rows» не закрыта: отличить «порядок задан рантаймом» от «порядок случаен» статикой нельзя"
  }
}
