---
belongs_to: screen-review
purpose: АРХИВ всех уроков (append-only). Сюда попадают все выявленные дефекты и их разборы — ничего не выбрасывается. Курация в lessons.md запускается по событию (разница «архив минус выжимка» >= 3), а не по календарю. Этот файл агентами при приёмке НЕ читается — только кураторская выжимка lessons.md.
updated: "16.09.2026"
---

# Журнал уроков — АРХИВ (raw)

Формат записи: `дата | страница/зона | симптом → причина → правило/блокер`.

**Механизм (пересмотрен 06.09.2026; заведён 31.08.2026):**
- Все уроки складываются сюда сразу, без фильтра — это полная история.
- **Курация — по событию, а не по календарю.** Правило «раз в неделю» не
  отработало ни разу: у агента нет часов и нет памяти между сессиями. За
  11 дней курация случалась в том же ходе, что и дозапись (mtime этого файла
  и выжимки разошлись на 99 секунд), и оставила непромоученными 11 записей —
  Л15–Л22, Л41, Л46, Л47. Триггер теперь численный: **разница «записей здесь
  минус записей в выжимке» >= 3** — курация обязательна.
- **Состояние промоута объявляется явно.** У записи, судьба которой решена,
  стоит строка `**Промоут:**` — `в выжимке` / `не промоутить — <причина>` /
  `выведен из выжимки <дата> — закреплён <якорь>`. Без этой строки отчёт
  «что не промоучено» врёт в обе стороны: Л15–Л22 не промоучены намеренно
  (частные случаи одной страницы), а Л45 значился «убран из выжимки» и при
  этом в ней лежал.
- **Выведение из выжимки — не потеря.** Урок, закреплённый исполняемым
  сторожем, остаётся здесь полностью, а из выжимки убирается: агенту его
  больше не надо помнить, за него отвечает код.

---

## 30.08.2026 — раскатка docs-split (молекулы + правки)

### Л13. Раскатка тулчейном (scaffold/rollout) — атомы
- **Симптом:** 14 атомов раскатаны подкомандой `scaffold` (авто-каркас: splitpane+табы+panes+код-вкладка+скрипты+src-code-html из спеки) + `inject --html`; вручную — словарь `DS_SPLIT_SWITCH_LABELS`, `src-code-js` (выжимка API), перевод легаси `.toggle`/`.seg` на бинарные селекты (урок Л4).
- **Правило:**
  - Каждый атом после scaffold требует ручного дозаполнения: словарь (для бинарных селектов с подписью-существительным), src-code-js (иначе code-out js=0 — блокер приёмки!), проверка отсутствия легаси `button.toggle`/`.seg` в конструкторе.
  - Мёртвые легаси-стили `.toggle`/`.toggles`/`.seg` в `<style>` удалять (К10).
  - Словарь нужен и для бинарных селектов, ставших свитчами с не-действенной подписью («Тип»→«Читаемый (ReadOnly)», «Ориентация»→«Вертикальный»).
  - src-code-js выжимка: make-фабрики + классы/структура DOM — достаточно ~8-12 строк.
- **Закрепление:** неизмеримое — процедурное правило раскатки (вынесено в шард docs-split): ручное дозаполнение после `scaffold`; статике недоступно.
- **Промоут:** выведен из выжимки 06.09.2026 — scope docs-split, переехал в `skills/docs-split/references/lessons.md`. Предмет урока — страница документации, а выжимку screen-review читают перед каждой сборкой экрана.

### Л14. Пилот не освобождает от полного цикла
- **Симптом:** Badge (первый пилот scaffold) раскатан без перевода «Опций» на ctlToggle — ревьюер поймал легаси `.toggle` во втором круге.
- **Правило:** пилотная страница проходит ТУ ЖЕ приёмку, что и батч; тулчейн ускоряет каркас, но конвенция конструктора (свитчи ДС, без легаси) проверяется одинаково.
- **Закрепление:** неизмеримое — процедурное правило раскатки (вынесено в шард docs-split): пилот проходит ту же приёмку, что и батч.
- **Промоут:** выведен из выжимки 06.09.2026 — scope docs-split, переехал в `skills/docs-split/references/lessons.md`. Предмет урока — страница документации, а выжимку screen-review читают перед каждой сборкой экрана.

### Л1. NavTile — свитч без подписи
- **Симптом:** свитч «Вариант→Со ссылками» в конструкторе без текста.
- **Причина:** `nav-tile.page.js` создаёт подпись контрола через `.ds-label`/`.ds-label__text`, а `docs-split.js` (шаг 5a) читает подпись свитча только из `.ctl > .lbl` → `labelName=''` → словарь не совпал → свитч пустой.
- **Правило:** подпись бинарного селекта в конструкторе — ОБЯЗАТЕЛЬНО `.lbl` (не `.ds-label`). Свитч с пустой подписью — блокер. Проверка: verify `toggles labels:` не пусто, если есть бинарные селекты; grep по хелперам на `ds-label` внутри `#pg-controls`-логики.
- **Закрепление:** исполняемое — `линтер:R6`, доказан парой фикстур `DS-IBP/fixtures/R6.bad.html`. Уровень поднят 06.09.2026: строка «сторож посилен, не написан» пережила написание сторожа на несколько часов — поле «Закрепление» правится вместе с кодом, а не когда-нибудь потом.
- **Промоут:** выведен из выжимки 06.09.2026 — закреплён `линтер:R6` (бинарный селект в #pg-controls без непустой .lbl). Только исполняемый уровень выводит урок из выжимки: пока правило держится на памяти, оно обязано быть перед глазами.

### Л2. Pagination — большие отступы в левой колонке конструктора
- **Симптом:** поля левой колонки разъехались с огромными промежутками.
- **Причина:** в `<style>` остались правила старой (до docs-split) flex-раскладки: `.pg__controls .ctl { flex:1 1 180px; min-width:160px }`, `.pg__controls { flex-wrap:wrap }`, `.pg__widthctl { flex:1 1 260px }`. В колонке `.ctl-col` (`flex-direction:column; gap:18px`) `flex-grow:1` растягивает каждое поле на всю высоту колонки.
- **Правило:** при раскатке из `<style>` страницы удаляются ВСЕ остатки старой раскладки конструктора: `.pg__controls { … flex-wrap / flex:1 1 … / grid-template … }`, `.pg__controls .ctl { flex:… }`. Остаются только живые page-стили контролов (сам слайдер, `.lbl b` и т.п.).
- **Закрепление:** исполняемое — `линтер:R4`, доказан парой фикстур `DS-IBP/fixtures/R4.bad.html`. Уровень поднят 06.09.2026 вместе с Л1 и Л3: три записи объявляли сторожа ненаписанным, когда он уже работал.
- **Промоут:** выведен из выжимки 06.09.2026 — закреплён `линтер:R4` (остаток flex-раскладки конструктора в <style>). Только исполняемый уровень выводит урок из выжимки: пока правило держится на памяти, оно обязано быть перед глазами.

### Л3. ReadOnlyField — заголовки групп вверху без содержимого
- **Симптом:** `.pg__grouphead` (Состояние/Значение/Обрамление/Иконка справа) стопкой вверху `#pg-controls`, под ними пусто; все контролы уехали в `.ctl-col` ниже.
- **Причина:** `docs-split.js` шаг 5b переносил в левую колонку только `:scope > .ctl`; `.pg__grouphead` (не `.ctl`) оставались прямыми детьми `#pg-controls` и складывались в grid 1fr 1fr (у каждого `grid-column:1/-1`).
- **Правило:** `docs-split.js` переносит в `.ctl-col` ВСЕ прямые дети `#pg-controls` в исходном порядке (`:scope > *`, скрытые бинарные пропускаются по `display:none`). `.pg__grouphead` следует за своими контролами. Проверка: verify `ctl-col children` = контролы + заголовки групп; заголовок предшествует контролам своей группы.
- **Закрепление:** исполняемое — `линтер:R7`, доказан парой фикстур `DS-IBP/fixtures/R7.bad.html`. Уровень поднят 06.09.2026 вместе с Л1 и Л2.
- **Промоут:** выведен из выжимки 06.09.2026 — закреплён `линтер:R7` (.pg__grouphead без своих контролов следом). Только исполняемый уровень выводит урок из выжимки: пока правило держится на памяти, оно обязано быть перед глазами.

### Л4. Кастомные кнопки-тумблеры вместо свитчей ДС
- **Симптом:** в конструкторах самописные `button.toggle` с рамкой и `aria-pressed` (Selected/Бейдж в Tab, Стрелка/Перенос в Tooltip и т.д.).
- **Причина:** легаси-хелперы page.js (`sw`/`toggle`/`ctlToggle`/`ctlToggles`), доставшиеся от старого конструктора.
- **Правило:** бинарная опция в конструкторе — это бинарный селект (`['no','Нет']/['yes','Да']`), который `docs-split.js` конвертирует в штатный свитч `pg-toggle` (правая колонка, статичная подпись из `DS_SPLIT_SWITCH_LABELS`, правило Switch). `state[key]` остаётся boolean (`change → state[key] = sel.value === 'yes'`). Кастомная кнопка-тумблер в конструкторе — блокер (К9).
- **Закрепление:** исполняемое — `линтер:R8` (самописная `button.toggle[aria-pressed]` в `#pg-controls`).
- **Промоут:** выведен из выжимки 06.09.2026 — закреплён `линтер:R8` (самописная button.toggle[aria-pressed] в #pg-controls). Только исполняемый уровень выводит урок из выжимки: пока правило держится на памяти, оно обязано быть перед глазами.

### Л5. `data-pg-keep-select` / `dataset.pgKeepSelect` — инертный маркер
- **Симптом:** атрибут/`dataset.pgKeepSelect` встречается в разметке и page.js, но нигде не читается (docs-split.js его не использует).
- **Причина:** легаси `pg-kit.js` (селекты 2–4 опций → SegmentControl); pg-kit.js в docs-split не подключается.
- **Правило:** принятое решение — оставлен как смысловая метка, нигде не обрабатывается. НЕ полагаться на него; при раскатке новых страниц не добавлять. Существующие вхождения не удалять (задокументировано).
- **Закрепление:** неизмеримое — принятое решение: инертный маркер, не обрабатывается, не переоткрывать.

### Л6. Нативные segctrl в конструкторе (ReadOnlyField)
- **Симптом:** `segctrl in constructor: 8`.
- **Причина:** page.js строит конструктор на собственных SegmentControl'ах компонента (`seg()`-хелпер); файл в `DS-IBP/scripts/` править нельзя без поручения.
- **Правило:** принятое решение — НЕ блокер. Конвенция «без segctrl в конструкторе» запрещает КОНВЕРТАЦИЮ селектов в segctrl (docs-split.js её не делает); здесь — нативные контролы, селектов нет. Отклонение задокументировано. Не переоткрывать при приёмке.
- **Закрепление:** неизмеримое — принятое решение: отклонение «нативные segctrl в конструкторе», не блокер, не переоткрывать.

### Л7. Чекбоксы `.cb` в левой колонке (NavTile) / слайдер в левой колонке (Pagination)
- **Симптом:** чекбоксы и слайдер оказались в `.ctl-col`, хотя по конвенции — правый `.toggles`.
- **Причина:** `docs-split.js` конвертирует в свитчи только бинарные селекты; не-селектные `.ctl` (чекбоксы `.cb`, слайдеры, кастомные контролы) уходят в левую колонку.
- **Правило:** принятое решение — задокументированное отклонение, не блокер. Если странице нужны чекбоксы/слайдеры в правой колонке — это отдельное улучшение общего слоя, не починка страницы.
- **Закрепление:** неизмеримое — принятое решение: отклонение «чекбоксы/слайдер в левой колонке», не блокер, не переоткрывать.

### Л8. `toggles labels` пусто, хотя свитчи созданы
- **Симптом:** verify показывал `toggles labels:` пусто при существующих свитчах (Alert, InputAmountRange и т.д.).
- **Причина:** харнесс брал первый `.toggles` внутри `#pg-controls` — вложенный `.toggles` группы «Опции» (кастомные кнопки) маскировал правую колонку.
- **Правило:** в диагностике искать `:scope > .toggles` (правую колонку), а не первый вложенный. Исправлено в diag-harness.js.
- **Закрепление:** исполняемое — `структурно:diag-harness.js` (поиск сузился до `:scope > .toggles`).
- **Промоут:** выведен из выжимки 06.09.2026 — закреплён `структурно:diag-harness.js` (поиск сузился до :scope > .toggles). Только исполняемый уровень выводит урок из выжимки: пока правило держится на памяти, оно обязано быть перед глазами.

### Л9. Ложные FAIL при параллельном `ds-check`
- **Симптом:** при параллельном запуске нескольких `ds-check` страницы падали с кодом 2 (ложный FAIL verify).
- **Причина:** headless Chrome с общим `--user-data-dir` — профиль блокируется первым процессом.
- **Правило:** `ds-check`/`verify` запускать ТОЛЬКО последовательно; не запускать страницы параллельно. Ревьюеру это предписано.
- **Закрепление:** неизмеримое — процедурное правило (вынесено в шард docs-split): `ds-check`/`verify` только последовательно; инструмент не видит порядок запусков агента.
- **Промоут:** выведен из выжимки 06.09.2026 — scope docs-split, переехал в `skills/docs-split/references/lessons.md`. Предмет урока — страница документации, а выжимку screen-review читают перед каждой сборкой экрана. Выведен и из шарда 13.09.2026 — подкоманда `verify` и headless Chrome удалены, параллельно запускать больше нечего.

### Л10. Диагностика вне папки страницы ломает относительные пути
- **Симптом:** headless-проба из `%TEMP%` показывала нестилизованную страницу (высоты «не те», `main` 4863px) — ложные выводы.
- **Причина:** копия в другую папку ломает `../../DS-IBP/styles/…`.
- **Правило:** временная копия страницы — ТОЛЬКО в ту же папку (как делает `verify`); chrome-путь задавать через `DS_CHROME`; зависшие headless-процессы чистить перед прогоном.
- **Закрепление:** неизмеримое — процедурное правило (вынесено в шард docs-split): временная копия страницы — только в ту же папку; свойство среды.
- **Промоут:** выведен из выжимки 06.09.2026 — scope docs-split, переехал в `skills/docs-split/references/lessons.md`. Предмет урока — страница документации, а выжимку screen-review читают перед каждой сборкой экрана.

### Л11. Общий слой устойчив к вариациям страниц
- **Симптом:** три правки за цикл (Л1–Л3) — это вариации, которые общий слой мог поддержать сразу.
- **Правило:** `docs-split.js`, харнесс и тулчейн проектируются под вариации (подпись из `.lbl` или `.ds-label`, групповые заголовки, вложенные контейнеры), а не под один эталон. Проверка на реальной странице с отклонением — часть разработки общего слоя.
- **Закрепление:** неизмеримое — процедурное правило (вынесено в шард docs-split): общий слой проектируется под вариации страниц.
- **Промоут:** выведен из выжимки 06.09.2026 — scope docs-split, переехал в `skills/docs-split/references/lessons.md`. Предмет урока — страница документации, а выжимку screen-review читают перед каждой сборкой экрана.

### Л12. pg-kit.css не нужен страницам docs-split
- **Симптом:** pg-kit.css подключён на всех docs-split страницах, но построен под старый `.panel.pg` (grid настройки|превью, `.toggle`, `.seg`); внутри — `@import splitter/segment-control`, которые по `file://` не работают и дублируют явные `<link>`.
- **Причина:** легаси от старого конструктора; при раскатке pg-kit.css оставляли «на всякий случай».
- **Правило:** решение 30.08.2026 — живые правила контролов (`.ctl`, `.ctl.is-off`, `.ctl > .lbl`) перенесены в `docs-split.css`; `@import` удалены; `<link pg-kit.css>` снят со всех 34 раскатанных страниц (полный регресс ds-check 34/34 OK). **Правило:** на docs-split странице pg-kit.css не подключается вовсе; `.icnpick`/`.readout`/`.seg`, если нужны, страница объявляет локально в `<style>` (как ReadOnlyField/Splitter). pg-kit.css остаётся только для НЕ раскатанных страниц (foundations/атомы/patterns в старом формате).
- **Закрепление:** исполняемое — `структурно`: живые правила контролов перенесены в `docs-split.css`, `pg-kit.css` снят со всех раскатанных страниц.

---

## 26.08.2026 — разбор реальных ошибок сборки (экран DealPipelineCard)

Все шесть — не нехватка информации, а необращение к ней или отступление от найденного. Читай как список того, что повторять нельзя:

Разбор оформлен записями **Л58–Л63** — по одной на строку прежней таблицы. Таблица убрана: строки таблицы не видны ни `lessons-cli check`, ни `state`, и потому не могли получить ни поля «Закрепление», ни якоря.

---

## 30.08.2026 — разбор ошибок конструктора документации (docs-split)

При раскатке страниц документации (паттерн docs-split) не повторять:

Разбор оформлен записями **Л1–Л4** — таблица дублировала их слово в слово и разошлась бы с ними при первой же правке (урок Л43: у правила один владелец).

---

## 31.08.2026 — правка Colors.html (карточки → таблицы, foundation-страница)
- **Промоут:** выведен из выжимки 06.09.2026 — scope docs-split, переехал в `skills/docs-split/references/lessons.md`. Предмет урока — страница документации, а выжимку screen-review читают перед каждой сборкой экрана.

### Л15. Произвольный вертикальный отступ, унаследованный от старой версии страницы
- **Симптом:** `.category { margin-top: 80px; }` — значение не из токенов спейсинга и не встречается в эталонах (Icons.html — та же структура категорий, `.cat { margin-top: 64px; }`).
- **Причина:** отступ существовал в исходной версии страницы и перенесён при правке без сверки с эталонами (правка — замечание, не блокер: отступ наследован, а не введён заново).
- **Правило:** при правке страницы документации проверять локальный `<style>` на произвольные значения (отступы, размеры, не-токеновые px) и приводить к токенам спейсинга или к значениям страницы-эталона той же структуры; унаследованные значения не переносить молча.

---
- **Закрепление:** неизмеримое — частный случай одной страницы (Colors.html), в общее правило не обобщён.
- **Промоут:** не промоутить — частный случай одной страницы (Colors.html), в общее правило не обобщается

## 31.08.2026 — дубликат страницы Button (ButtonTest.html) и реестры

### Л16. Новая страница в DS-IBP/pages/ — синхронная регистрация во всех реестрах
- **Симптом:** страница-дубликат ButtonTest.html добавлена в левую навигацию (ds-nav.js), но ds-lint дал FAIL D1 (нет карточки в DS-IBP/index.html) и D5 (нет спеки DS-IBP/specs/ButtonTest.md и строки в DS-IBP/specs/_index.md).
- **Причина:** реестры (ds-nav.js, DS-IBP/index.html, DS-IBP/specs/, DS-IBP/specs/_index.md) пополнялись независимо; «страница-дубликат» не была заведена в каталог главной и в манифест спек.
- **Правило:** создание любой новой страницы в `DS-IBP/pages/**` требует синхронного заведения в ТРЁХ реестрах: `DS-IBP/scripts/ds-nav.js` (левая панель), `DS-IBP/index.html` (карточка + счётчик секции), `DS-IBP/specs/<Имя>.md` + строка в `DS-IBP/specs/_index.md`. Иначе ds-lint блокирует (D1/D5).
- **Закрепление:** исполняемое — `линтер:D1`, `линтер:D5` (новая страница не заведена в реестры).
- **Промоут:** не промоутить — процедура заведения страницы, закрыта линтером:D1/линтер:D5

### Л17. Версия согласуется в трёх реестрах одним числом
- **Симптом:** строка манифеста `| ButtonTest | … | 1.010 |` расходилась со спекой (`version: "1.011"`) и шапкой страницы («Версия: 1.011»).
- **Причина:** строка манифеста скопирована из строки Buttons (1.010) без бампа при регистрации копии.
- **Правило:** версия в `DS-IBP/specs/_index.md` обязана совпадать с `version:` в YAML спеки и «Версия» в шапке страницы — три реестра согласуются одним числом.
- **Закрепление:** исполняемое — `линтер:D7` (версия страницы вне реестров сверяется со спекой).
- **Промоут:** не промоутить — процедура версионирования, закрыта линтером:D7

### Л18. Стаб-спека для страницы-дубликата
- **Симптом:** дубликат Button требовал спеки; полная копия спеки Button бессмысленна.
- **Правило:** принятое решение — стаб `DS-IBP/specs/ButtonTest.md` — YAML (component/version/page/css/deps) + ссылка на `DS-IBP/specs/Buttons.md` + раздел «Отличия» + инварианты «совпадают с оригиналом». WARN D6 («нет блока в DS-IBP/specs/_cheatsheet.md») осознанно принимается — блок в чит-шите дублировал бы Button. Не переоткрывать.
- **Закрепление:** неизмеримое — принятое решение: стаб-спека для страницы-дубликата (ButtonTest), не переоткрывать.
- **Промоут:** выведен 13.09.2026 — страница ButtonTest и её стаб-спека удалены, отклонению не к чему относиться

### Л19. h2 «Токены» — неканонический заголовок (WARN C5)
- **Симптом:** ds-lint WARN C5 на h2 «Токены» (не входит в канон заголовков страниц документации).
- **Причина:** это прямое требование пользователя — справочный раздел токенов в конце страницы.
- **Правило:** принятое решение — известное отклонение, не блокер; не переоткрывать при приёмке.
- **Закрепление:** неизмеримое — принятое решение: отклонение WARN C5 (h2 «Токены»), не переоткрывать.
- **Промоут:** выведен 13.09.2026 — раздел «Токены» был только на ButtonTest, страница удалена

---

## 31.08.2026 — сравнение токенов ДС ↔ сторибук (ButtonTest.html)

### Л20. Несуществующие алиасы в примерах не оборачивать в var() 
- **Симптом:** в плане миграции написали `<code class="tok">--primary-dark: var(--sb-primary-dark)</code>` — ds-lint дал BLOCKER B1 «токена --sb-primary-dark нет в DS-IBP/styles/*.css».
- **Причина:** имя будущего алиаса, которого ещё нет, было оформлено как реальное употребление CSS-переменной.
- **Правило:** в примерах/планах имена несуществующих токенов показывать как текст (`--sb-primary-dark`) или с fallback (`var(--sb-primary-dark, var(--primary-dark))`) — иначе линтер считает употреблением и блокирует.
- **Закрепление:** исполняемое — `линтер:B1` (несуществующий токен в `var()`).
- **Промоут:** не промоутить — частный случай, закрыт линтером:B1

### Л21. Числа в сводке и плане одной страницы обязаны совпадать с составом перечней
- **Симптом:** заголовок «Реальные расхождения значений (8)» после добавления link-dark/link-light, но в «Как перейти» осталось «синхронизировать 6».
- **Причина:** правка числа в одном разделе не была сверена с упоминаниями в соседних.
- **Правило:** правка числа/состава перечня в любом разделе страницы требует сверки всех упоминаний того же числа на странице (заголовки, сводки, планы) — считать по строкам данных (match:'sem'), а не «по памяти».
- **Закрепление:** неизмеримое — частный случай одной страницы (ButtonTest), в общее правило не обобщён.
- **Промоут:** не промоутить — частный случай одной страницы (ButtonTest)

### Л22. В строках .tbl обязательны ячейки-разделители по краям (сдвиг колонок)
- **Симптом:** в JS-генерации таблиц строки имели только 8 ячеек данных, шапка — 10 (8px + 8 заголовков + 8px). CSS grid расставил ячейки строк по колонкам 1–8, а данные шапки — по 2–9: «название токена» попало в 8px-желоб, «что означает» — под «название токена», значение — под «что означает», образец — под «значение», статус — под «образец сторибук». Пользователь увидел «бардак в таблицах».
- **Причина:** при копировании паттерна статичных таблиц (сеп + ячейки + сеп) в JS-рендер забыты краевые `.tc--separator`.
- **Правило:** любой `.tbl__row` в JS-рендере обязан иметь ту же сетку, что и шапка: `<div class="tc tc--separator"></div>` + N ячеек + `<div class="tc tc--separator"></div>`. Проверка: число ячеек строки = число ячеек шапки (по `<div class="th`/`<div class="tc`).

---
- **Закрепление (06.09.2026): исполняемое — `сенсор:Б8`.** Сторож написан: сверяет число треков каждой строки с шапкой и требует краевых `.tc--separator`/`.th--separator`. Доказан откатом (`fixtures/Б8.bad.html`). Считаются только строки, объявившие `grid-template-columns`, — остальные наследуют сетку от контейнера.
- **Промоут:** не промоутить — закреплён кодом.

## 01.09.2026 — RND NavPanel (вложенные разделы)

### Л23. Страничный CSS перебил состояние компонента по специфичности
- **Симптом:** у выделенного родительского пункта («Обязательные сделки», `nav__item--selected`) пропала подложка фона; ховер тоже не подсвечивал.
- **Причина:** в `<style>` страницы добавлен `button.nav__item { background:none; … }` (специфичность 0,1,1) — перекрывает состояния компонента из `nav-panel.css`: `.nav__item--selected { background: var(--bgtable-row-focus) }` и `.nav__item:hover` (0,1,0). Родитель писался `<button>` от руки, хотя компонент рендерит пункты `<a class="nav__item …">` (`itemHTML`) и состояние задаёт данными (`it.selected`).
- **Правило:** не переопределять состояния компонентов извне (ds-rules.md §4): собственный `<style>` — только раскладка; без селекторов `button.<класс>`/`a.<класс>`/`input.<класс>` и сбросов background/display/color, перебивающих `--selected`/`--disabled`/`:hover`/`[aria-current]`; разметку и состояние брать из чит-шита дословно и задавать данными. Проверка приёмки — блокер Б21. Класс бага воспроизводится при сборке экранов, не только в RND.
- **Закрепление:** исполняемое — `сенсор:Б21` (селекторы «тег+класс компонента» в `<style>` экрана).

---

## 01.09.2026 — NavPanel, футер (ссылка на личный кабинет)
- **Промоут:** выведен из выжимки 06.09.2026 — закреплён `сенсор:Б21` (селекторы «тег+класс компонента» в <style> экрана). Только исполняемый уровень выводит урок из выжимки: пока правило держится на памяти, оно обязано быть перед глазами.

### Л24. CSS-эталон вкладки «Код» расходится с styles/*.css после прямой правки файла
- **Симптом:** после правки `styles/nav-panel.css` (футер: `transition`, `.nav__footer:hover`, `.nav__user` как ссылка, `:focus-visible`) вкладка «Код» страницы (`src-code-css`) осталась со старым CSS — новых правил футера нет, хотя файл их содержит; ревьюер поймал дифф.
- **Причина:** CSS правился напрямую в `styles/nav-panel.css`, а `src-code-css` обновляется ТОЛЬКО тулчейном `docs-split.mjs inject --css`; ручная правка файла без прогона тулчейна оставила эталон со старым содержимым.
- **Правило:** после любой правки `styles/*.css` компонента раскатанной страницы перегенерировать `src-code-css` тулчейном (`docs-split.mjs inject <page> --css DS-IBP/styles/<файл>.css`) и сверить содержимое диффом (не только наличие блока, но и ключевые правила); приёмке сверять футер/изменённые правила эталона с файлом.
- **Закрепление:** неизмеримое — процедурное правило (вынесено в шард docs-split): `src-code-css` обновляется только тулчейном `docs-split.mjs inject`.

---

## 01.09.2026 — главная страница и роли (единый каталог ibp-home.js)
- **Промоут:** выведен из выжимки 06.09.2026 — scope docs-split, переехал в `skills/docs-split/references/lessons.md`. Предмет урока — страница документации, а выжимку screen-review читают перед каждой сборкой экрана.

### Л25. Бандл витрины (_ds_bundle.js) удалён — сверять не с чем (закрыт 04.09.2026)
- **Симптом:** исторически — после переименования «RWA»→«Отчёты для Рисков» в `nav-panel.page.js` карточка NavPanel в `index.html` (витрина) показывала старое имя «RWA»: `_ds_bundle.js` содержал копию page.js со старым `label: 'RWA'`, и кода нового `ibp-home.js` в бандле не было.
- **Причина:** бандл `_ds_bundle.js` и манифест `_ds_manifest.json` — производные внешнего инструмента, а не исходники. Собирала их сама витрина при загрузке источников; локального компилятора не существовало, и в репозитории эти файлы ничем не использовались (ни одна страница их не подключала, линтер и сенсоры не читали). Требование «пересобрать» было невыполнимым по определению и порождало вечный «остаток» в каждом отчёте.
- **Правило:** оба файла удалены из репозитория 04.09.2026. Ничего не пересобирать, не воссоздавать, не сверять; источник истины по странице — `scripts/*.page.js`. Маркер `<!-- @dsCard … -->` первой строкой страницы остаётся: его требует линтер (C1).
- **Закрепление:** исполняемое — `структурно`: `_ds_bundle.js`/`_ds_manifest.json` удалены из репозитория, воспроизвести нечем.
- **Промоут:** выведен из выжимки 06.09.2026 — закреплён структурно (файлы удалены из репозитория, воспроизвести нечем)

### Л26. Экран с нуля вместо шаблона — потерян сброс `body { margin: 0 }`
- **Симптом:** на главной странице (mainPage) белая рамка 8px по периметру экрана («полоска справа на панели»), панель/контент не совпадают по краю, вклад в горизонтальный скролл.
- **Причина:** экран писался с нуля, а не копией `templates/screen/Screen.html` — в шаблоне есть `body { margin: 0; font-family: var(--font-text), …; color: var(--text-primary) }`, который сбрасывает UA-дефолт `body { margin: 8px }`. Без сброса фиксированная панель (position:fixed, от вьюпорта) и контент (в потоке, со смещением на margin body) разъезжаются, и вокруг появляется белая полоса.
- **Правило:** любой новый экран собирается из `templates/screen/Screen.html` (каркас + `body`-reset), а не «с нуля»; при ручной сборке проверять сброс `body { margin: 0 }` и отсутствие UA-дефолтов. Приёмке проверять отсутствие горизонтального скролла и совпадение краёв фиксированной панели с контентом.
- **Закрепление:** исполняемое — `сенсор:Б22` (сброс `body { margin: 0 }`).
- **Промоут:** выведен из выжимки 06.09.2026 — закреплён `сенсор:Б22` (сброс body { margin: 0 }). Только исполняемый уровень выводит урок из выжимки: пока правило держится на памяти, оно обязано быть перед глазами.

### Л27. Класс документационного слоя на экране мёртв
- **Симптом:** `<p class="desc tight">` в модалке главной страницы не получает типографику — текст выглядит как дефолтный `<p>`.
- **Причина:** классы `.desc`/`.tight` есть только в документационных слоях (`ds-docs.css`, `input-pages.css`, `docs-split.css`), которые `ds.css` (точка входа экрана) не импортирует; классы взяты «по памяти» из док-страниц.
- **Правило:** при сборке экрана использовать только классы, доступные через `ds.css` (компоненты + фундамент); классы документационных страниц на экран не переносить. Приёмке проверять: каждый класс экрана либо в чит-шите компонента, либо в стилях, импортируемых `ds.css`.
- **Закрепление:** исполняемое — `сенсор:Б23` (классы документационного слоя на экране).
- **Промоут:** выведен из выжимки 06.09.2026 — закреплён `сенсор:Б23` (классы документационного слоя на экране). Только исполняемый уровень выводит урок из выжимки: пока правило держится на памяти, оно обязано быть перед глазами.

### Л28. Обёртка рантайма отключает усечение у правильной разметки
- **Дата/зона:** 04.09.2026 | `Projects/post/operations-did/Portfolio-did/Portfolio.html`, `styles/tooltip.css`
- **Симптом:** наименование сделки в реестре шло в одну строку без многоточия и уезжало под соседнюю колонку, хотя у ячейки стоял `.tc__text--truncate` и CSS усечения был написан верно.
- **Причина:** `ds-table.js` навешивает тултип на каждый усечённый текст, а `ds-tooltip.js` при этом оборачивает цель в `.tip-anchor`. Якорь был `inline-flex` без `min-width: 0` — автоминимум flex-элемента равен min-content содержимого, обёртка не сжималась, и `text-overflow: ellipsis` не срабатывал. Замер: ячейка 280px → `.tc__row` 248px → текст 377.6px.
- **Правило:** обёртка, которую рантайм вставляет ВОКРУГ уже свёрстанного элемента, обязана быть раскладочно прозрачной — `max-width: 100%` и, для flex, `min-width: 0`. Ловится статически правилом линтера **B9** (`ds-lint.js`, ищет `X.parentNode.insertBefore(W, X)` по `scripts/ds-*`, `tbl-*`, `input-kit` и сверяет CSS обёртки). Починка: Tooltip 2.005.
- **Для приёмки:** дефект НЕ виден по разметке экрана — правки на экране бесполезны, чинится только в ДС. Симптом «усечение не работает, хотя класс стоит» → сначала проверять слой ниже (рантайм/обёртка), а не разметку.
- **Закрепление:** исполняемое — `линтер:B9` (обёртка рантайма без `max-width:100%`/`min-width:0`).
- **Промоут:** выведен из выжимки 06.09.2026 — закреплён `линтер:B9` (обёртка рантайма без max-width:100% / min-width:0). Только исполняемый уровень выводит урок из выжимки: пока правило держится на памяти, оно обязано быть перед глазами.

### Л29. Компонент с усечением подписи, который не умеет сжиматься
- **Дата/зона:** 04.09.2026 | тот же экран, `styles/chip.css`
- **Симптом:** статусные чипы в узких колонках реестра обрывались посреди слова, без многоточия («На сопровождени», «Ожидает подтверж»).
- **Причина:** `.chip` — `inline-flex` без `min-width: 0`, поэтому в строке значения ячейки не сжимался и вылезал за колонку; обрывал его непрозрачный фон соседней ячейки, а не собственное усечение. `.chip__label` с `ellipsis` при этом был на месте — усечению просто не с чего было срабатывать.
- **Правило:** компонент, чья подпись усекается многоточием, обязан явно высказаться о сжимаемости — `min-width: 0` (сжимается: Chip), либо `flex: none`/`flex-shrink: 0` (не сжимается намеренно: Avatar), либо запись в белом списке `SHRINK_OK` с причиной (Tab, SegmentControl — переполнение решается скроллом ряда и растяжением трека). Ловится правилом линтера **B8**. Починка: Chip 1.010.
- **Для приёмки:** обрыв текста без многоточия — это НЕ «узкая колонка», а несжимаемый элемент. Признак: обрыв ровно по границе соседней ячейки.
- **Закрепление:** исполняемое — `линтер:B8` (компонент с усекаемой подписью без `min-width:0`).
- **Промоут:** выведен из выжимки 06.09.2026 — закреплён `линтер:B8` (компонент с усекаемой подписью без min-width:0). Только исполняемый уровень выводит урок из выжимки: пока правило держится на памяти, оно обязано быть перед глазами.

### Л30. `min-width: max-content` на всех строках убивает усечение в колонках-долях
- **Дата/зона:** 04.09.2026 | `styles/table-cell.css`, демо «Реестр сделок» на `pages/organisms/TableCell.html`
- **Симптом:** в таблице на треках `fr` колонка растягивалась под самый длинный текст, усечение не срабатывало вовсе, строка вылезала за контейнер и хвост колонок становился недостижим.
- **Причина:** правило `.tbl__row { min-width: max-content }`, добавленное 03.09 для закрепления колонок (TableCell 2.009), стояло на ВСЕХ строках. Под `max-content` доля-трек разворачивается по содержимому. Закреплению правило нужно, обычной таблице — вредит. Доли используются в 73 файлах документации и 21 месте в экранах — усечение в них не работало нигде.
- **Правило:** `min-width: max-content` ставится только строке прокручиваемой по горизонтали таблицы (`.tbl--scroll .tbl__row`, `.dtable__body .tbl__row`). Починка: TableCell 2.011; закрепление проверено замером позиций колонок при прокрутке 0 → 600 → 2000 → до упора → назад (цифры совпали с состоянием до правки до пикселя).
- **Для приёмки:** правка, сделанная ради одного варианта компонента, проверяется и на остальных вариантах. Симптом «колонка шире, чем нужно, и таблица не влезает» → смотреть правила, действующие на все строки без разбора.
- **Закрепление:** исполняемое — `структурно:TableCell 2.011` (`min-width:max-content` только у строки прокручиваемой таблицы).
- **Промоут:** выведен из выжимки 06.09.2026 — закреплён `структурно:TableCell 2.011` (min-width:max-content только у строки прокручиваемой таблицы; процессная часть — в Л44). Только исполняемый уровень выводит урок из выжимки: пока правило держится на памяти, оно обязано быть перед глазами.

### Л31. Идентификатор — не число: `tc--numbers` к нему не применяется
- **Дата/зона:** 04.09.2026 | тот же экран, инвариант TableCell 2.010
- **Симптом:** колонка «Номер сделки» была свёрстана как число — правый край и tabular-nums (`tc--numbers`).
- **Причина:** правило «числа вправо» применили к идентификатору. Номер сделки не сравнивают по столбцу и не суммируют — по поведению это текст.
- **Правило:** идентификатор (номер сделки, ИНН, № договора, код) выравнивается по ЛЕВОМУ краю. `tc--numbers` — только для величин, которые сравнивают взглядом по столбцу: суммы, доли, количества, проценты. Инвариант записан в `specs/TableCell.md`.
- **Для приёмки:** замечание З4 `screen-review` («числа в таблице — в `tc--numbers`») и таблица выравниваний в `layout-composition/references/tables.md` уточнены — иначе приёмка потребует вернуть правый край идентификатору.
- **Закрепление:** временное — `чек-лист:З4`. Сторож посилен только через словарь заголовков колонок, а он гадателен: `tc--numbers` законен у величин с любым названием. **Продлено 06.09.2026:** словарь заголовков — угадывание, а не правило (класс Л72).

### Л32. Хук рантайма стоит, а поведения нет: селектор, которого нет в ДС
- **Дата/зона:** 04.09.2026 | `scripts/ds-table.js`, экран «Текущий портфель ДИД»
- **Симптом:** клик по стрелке сортировки в шапке таблицы не давал никакого отклика. Разметка правильная: `data-table` на `.tbl`, `data-sort` на кнопке, `ds.js` подключён — приёмка по чек-листу Б13 проходила.
- **Причина:** `setSort` красил шапку через `b.querySelector('.th__sort-icon')`, а класса `.th__sort-icon` нет НИГДЕ в репозитории — ни в CSS, ни в разметке, ни в чит-шите. Условие `if (g !== b)` всегда ложно, глиф не перерисовывался. Второй промах там же: рантайм ставил `aria-sort` и `is-sorted`, тогда как колонку красит `.th--sorted` — на `[aria-sort]` и `.is-sorted` в `styles/*.css` нет ни одного селектора. Итог: клик менял один атрибут в DOM и ничего больше.
- **Причина:** почему прожило незамеченным — демо-страница `TableCell.html` рисует шапку сама из своего стейта (`table-cell.page.js`) и общий рантайм не использует — на витрине сортировка «работала». Ни один экран не слушал событие `sort`. Работающей сортировки в репозитории не было ни разу.
- **Правило:** класс или атрибут, который рантайм ищет или ставит, обязан существовать в `styles/*.css` либо в разметке компонента. Селектор, которого нет нигде, — мёртвый код, а не «задел на будущее». Проверка: `grep` имени класса по `styles/`, `pages/`, `specs/` перед тем, как на него опираться. Починка — Table 1.010.
- **Для приёмки:** наличие хука ≠ работающее поведение. Блокер Б13 проверяет только разметку; добавлена проверка живости прогоном рантайма (см. Л34).
- **Закрепление:** исполняемое — `линтер:P4` (класс, который рантайм ставит, обязан существовать в CSS ДС).
- **Промоут:** выведен из выжимки 06.09.2026 — закреплён `линтер:P4` (класс, который рантайм ставит, обязан существовать в CSS ДС). Только исполняемый уровень выводит урок из выжимки: пока правило держится на памяти, оно обязано быть перед глазами.

### Л33. Рантайм ДС дописывает разметку в чужой слот — `textContent` перестаёт быть значением
- **Дата/зона:** 04.09.2026 | `scripts/ds-table.js` + `scripts/ds-tooltip.js`, тот же экран
- **Симптом:** после починки Л32 стрелка менялась, а строки не переставлялись. На «голой» разметке сортировка работала, на реальной странице — нет.
- **Причина:** `ds-tooltip.js` кладёт рядом со значением копию текста в `.tip[role="tooltip"]` внутри той же ячейки. `cell.textContent` становится `20.04.202420.04.2024`, под маску `дд.мм.гггг` не подходит, ключ сортировки выходит `NaN` у всех строк — компаратор возвращает 0 на каждой паре и порядок не меняется. Молчаливое вырождение: ни ошибки, ни предупреждения.
- **Правило:** значение ячейки для любой логики берётся из семантического узла (`data-sort-value` на `.tc` → `.tc__text` / `.chip__label`), а не из `textContent` контейнера. Рантаймы ДС дописывают разметку внутрь чужих слотов — контейнер не является источником значения.
- **Для приёмки:** проверять поведение с ПОЛНЫМ набором рантаймов страницы. Дефект существует только в связке; на изолированной разметке его нет.
- **Закрепление:** неизмеримое — требует ПРОГОНА разметки с рантаймами (Л34): статикой видно, что хук стоит, но не то, что рантайм дописал в слот.

### Л34. Проверка живости возможна без браузера — заглушка соседнего рантайма её обесценивает
- **Дата/зона:** 04.09.2026 | приёмка Table 1.010
- **Симптом:** дефект Л33 отдали пользователю на ручной клик, объявив DOM-часть непроверяемой. Пользователь открыл страницу — сортировка не работала.
- **Причина:** «браузера нет» было прочитано как «проверить нельзя». Проверялись только чистые функции (19 тестов) — они были правы, ломалось взаимодействие рантаймов. Первый же прогон в DOM тоже прошёл, потому что тултип и иконки были ЗАГЛУШЕНЫ: проверялась страница, которой не существует.
- **Правило:** живость проверяется прогоном реальной разметки с реальными рантаймами в чистом JS-DOM (`linkedom`, ставится в скратчпад вне репозитория; это библиотека, не браузер — §9 `ds-rules.md` не нарушается). В тест грузятся ВСЕ рантаймы, которые есть на странице, без заглушек. Человеку остаётся только то, что действительно требует рендера: пиксели, тени, скролл, фактический overflow — и это называется явно.
- **Для приёмки:** «не проверяется» пишется только про то, что пробовали проверить. Заглушка соседнего рантайма в тесте — такой же дефект, как отсутствие теста.
- **Закрепление:** неизмеримое — это процедура захода, а не свойство файла; живёт в `ds-rules §9` и в чек-листе приёмки. Сторожа не будет: инструмент не видит, прогонял ли агент разметку.

### Л35. Заявленное состояние компонента не совпадает с данными
- **Дата/зона:** 04.09.2026 | экран «Текущий портфель ДИД», колонка «Дата подписания»
- **Симптом:** шапка объявлена отсортированной по убыванию (`aria-sort="descending"`, `.th--sorted`, стрелка вниз), а строки идут 15.03.2024 → 02.06.2023 → 10.09.2022 → 14.02.2021 → 22.05.2024 — порядок не монотонный. После починки сортировки это дало ложный симптом: первый клик по колонке (desc → none) возвращает исходный порядок, то есть внешне «ничего не происходит», и рабочая сортировка выглядит сломанной.
- **Причина:** стартовое состояние выставили как оформление, не сверив с данными. Пока сортировка была мертва, расхождение ничем себя не проявляло.
- **Правило:** состояние, заявленное разметкой (сортировка колонки, выбранный таб, раскрытый узел, применённый фильтр, счётчик «Применено: N»), обязано соответствовать данным на экране. Не подтверждается данными — состояние снимается, а не рисуется.
- **Для приёмки:** сверять заявленное состояние с содержимым, а не только наличие класса. Замечание З2 уточнено.
- **Закрепление:** временное — `чек-лист:Б27`. Механизируется частично, не написано. **Продлено 06.09.2026:** сверка `aria-sort` с фактическим типом колонки требует разбора значений строк, а строки строит рантайм — статике виден пустой контейнер (класс Л66).

---

## 04.09.2026 — TableCell 2.013 (закрепление колонок), чек-листы ДС

### Л36. Геометрия, посчитанная на событии `scroll`, всегда на кадр позади компоновщика
- **Дата/зона:** 04.09.2026 | `styles/table-cell.css`, `scripts/tbl-pin.js`, экран «Текущий портфель ДИД», MacBook (трекпад)
- **Симптом:** закреплённые колонки дрожали при горизонтальном скролле на MacBook — уезжали вместе с контентом и возвращались на каждый кадр подряд. На Windows колесо мыши скрывало дефект: правка успевала в паузу между щелчками, поэтому переданный экран «работал».
- **Причина:** `tbl-pin.js` удерживал колонки через `transform: translateX`, пересчитывая значение в обработчике `scroll`. Компоновщик применяет скролл раньше JS — новый offset попадает в кадр ДО выполнения хендлера, коррекция физически не может успеть в тот же кадр. Усугубляли: `requestAnimationFrame` внутри хендлера (ещё кадр задержки) и `getBoundingClientRect()` по всем ячейкам шапки на каждый кадр (forced reflow).
- **Правило:** удержание при скролле делается нативным `position: sticky`; рантайм считает только инсеты `left`/`right` и пишет их по `columnpin`/`columnreorder`/resize/ResizeObserver — на событие `scroll` не подписывается. Предпосылка «sticky не работает для колонок grid-раскладки» неверна: проверено на изолированном стенде и на самом экране. Ловится статически правилом линтера **B10** (geometry из обработчика `scroll`) и живой проверкой **E9** (замер в такте сдвига `scrollLeft`). Починка: TableCell 2.013.
- **Для приёмки:** класс «JS против компоновщика» невидим на Windows. Дефект, найденный только на конкретном железе/вводе, — повод для статического сторожа или правила, а не «не воспроизводится».
- **Закрепление:** исполняемое — `линтер:B10` (запись `.style.transform` в обработчике `scroll`).
- **Промоут:** выведен из выжимки 06.09.2026 — закреплён `линтер:B10` (запись .style.transform в обработчике scroll). Только исполняемый уровень выводит урок из выжимки: пока правило держится на памяти, оно обязано быть перед глазами.

### Л37. Правило/чек-лист, ссылающееся на недоступный инструмент, порождает фиктивное «выполнено»
- **Дата/зона:** 04.09.2026 | чек-листы `DS-IBP/skills/*`, `MAINTAINING.md`, `scripts/*.mjs`
- **Симптом:** чек-листы и правила ведения ДС ссылались на шаги через инструменты внешней среды (`ready_for_verification`, `check_design_system`, `copy_files`, `run_script`, `eval_js`, `multi_screenshot` и т.п.), которых в контуре нет. Агент каждый заход упирался в такой шаг и дописывал в отчёт «остаток»; формально требования выполнялись «на словах», фактически проверка не запускалась. Тот же класс — требование «пересобрать `_ds_bundle.js` компилятором», которого не существует.
- **Причина:** правило описывало действие, а не исполнимую команду, и не имело кода выхода, по которому видно, что оно отработало.
- **Правило:** каждый шаг чек-листа/правила — локальная исполнимая команда с видимым результатом (код выхода). Ссылка на инструмент, которого нет в контуре, — не шаг, а место для «не выполняется». Требование, которое невозможно выполнить, удаляется/переписывается, а его «остаток» не фиксируется как незакрытый пункт. Контролируется ревью (сверка «отчёт = команда + код выхода»), а не линтером.
- **Для приёмки:** пункт отчёта «выполнено» валиден, только если шаг — команда с кодом выхода. «Выполнено на словах» — тот же дефект, что и отсутствие проверки.
- **Закрепление:** неизмеримое — держится правилом `ds-rules §8` и контролем ревью (сверка «отчёт = команда + код выхода»); линтером не берётся.

---

## 04.09.2026 — Pagination (колонтитул таблицы), экран «Текущий портфель ДИД»
- **Промоут:** выведен из выжимки 06.09.2026 — закреплён `правило ds-rules §8` (шаг обязан быть исполнимой командой с кодом выхода). Только исполняемый уровень выводит урок из выжимки: пока правило держится на памяти, оно обязано быть перед глазами.

### Л38. Инвариант «пагинатор всегда справа» и вертикальное центрирование чеврона — не гарантии ДС, а память сборщика
- **Дата/зона:** 04.09.2026 | `styles/pagination.css` + эталон билдера `screen-assembly/references/patterns.md`, экран «Текущий портфель ДИД»
- **Симптом:** в реестре сделок кнопки страниц пагинатора оказались в левой части нижней строки таблицы, а чеврон выбора размера «Показывать строк: 50» — задран выше текста (не центрирован). Причина не в конкретном экране: та же ошибка воспроизводилась бы на любом экране, собранном по эталону билдера.
- **Причина:** два независимых явления.
  (1) `.pgn-row` — flex `justify-content:space-between`: правый слот `.pgn-row__right` (`flex:none`) уезжает вправо ТОЛЬКО при наличии левого слота `.pgn-row__left` (`flex:1 1 auto`). Эталон билдера опускал `.pgn-row__left`, и при одном ребёнке `space-between` прижимает его к левому краю. Инвариант «пагинатор всегда справа» был заявлен в спеке, но не был структурно гарантирован CSS — держался на том, что сборщик не забудет левый слот.
  (2) Центрирование чеврона задавал селектор `.pgn__pagesize.ddl-anchor` (`display:inline-flex; align-items:center`). Класс `ddl-anchor` рантайм (`ds-pagination.js`) вешает только на генерируемую разметку; в статичной эталонной разметке (чит-шит, спека, `patterns.md`) его нет. Без него спан остаётся inline, кнопка `pgn__pagesize-btn` (32px inline-flex) выравнивается по базовой линии и «всплывает» вверх.
- **Правило:** инвариант «пагинатор всегда справа» и центрирование чеврона — **структурные гарантии ДС**, а не разметка:
  `.pgn-row__right { margin-left:auto }` (прижимает вправо независимо от левого слота); селектор `.pgn__pagesize, .pgn__pagesize.ddl-anchor` (центрирует независимо от `ddl-anchor`, двойной селектор перебивает `flex-direction:column` из `dropdown-list.css`). Колонтитул таблицы собирается через `data-pagination` на пустом `<div>` — рантайм рисует пигмент сам и гарантирует обе оси. Блокер приёмки — Б29.
- **Для приёмки:** эталонные сниппеты `patterns.md` и статичная разметка чит-шита/спеки не должны противоречить тому, что реально рисует рантайм. Инварианты, названные «железными правилами», — в CSS, а не в памяти исполнителя.
- **Закрепление:** исполняемое — `структурно:Pagination 1.009` · `сенсор:Б29` (пагинатор справа и чеврон центрирован — CSS ДС; «а не разметка экрана» — сенсор).
- **Промоут:** выведен из выжимки 06.09.2026 — обе половины закреплены: «пагинатор всегда справа и чеврон центрирован» держит CSS ДС (`структурно:Pagination 1.009`), «а не разметка экрана» ловит `сенсор:Б29` (доказан откатом). Только исполняемый уровень выводит урок из выжимки.

### Л39. Кнопка фильтра и модалка — неотъемлемые части; модалка не подтянулась, т.к. задокументирована как «модель потребителя»
- **Дата/зона:** 04.09.2026 | `specs/TableFilter.md`, `specs/_cheatsheet.md`, экран «Текущий портфель ДИД» (`Portfolio-did/Portfolio.html`)
- **Симптом:** модалка фильтра собрана на самодельных классах `.fil-group/.fil-title/.fil-grid` вместо штатной `.tfm`-структуры: нет панели якорных табов слева, инпуты не растягиваются, зазоры не 28px, в полях нет крестиков очистки, у дропдаунов нет списков, шеврон ничего не открывал, футер с кнопками вразбивку. Собралась «кнопка + чип» (бар) — модалка нет.
- **Причина:** в ДС TableFilter бар (кнопка + чип) и модалка были задокументированы как РАЗРЫВНЫЕ: чит-шит и спека дают разметку бара, а про модалку пишут «содержимое модалки держит модель данных потребителя… из коробки не даём». Полный скелет `.tfm`-модалки жил только в `table-filter.page.js` (демо-страница), которую сборщик не читал. В отличие от RiskMetric, где чип+поповер — «неотъемлемая часть» и рантайм `data-riskmetric` собирает их вместе, TableFilter не фиксировал созависимость, и агент правомерно решил, что модалка — его выбор.
- **Правило:** **кнопка «Фильтр» и модалка фильтра созависимы и собираются вместе** (как чип+поповер у RiskMetric). Собираешь фильтр — собираешь и модалку `.tfm` (`.tfm__body` = `tfm__nav` + `tfm__panel` + `.tfm__sec` + `.tfm__grid`). Структура — ДС; только данные (какие секции/поля/пресеты) — потребитель. Собственный CSS на форму фильтра не пишется. Вторичная кнопка подвала — «Очистить фильтр» (конвенция Modal/EmptyState), не «Очистить».
- **Для приёмки:** сопутствующая находка — в ДС вторичная кнопка подвала TableFilter значилась «Очистить», тогда как по всей ДС (Modal, EmptyState) — «Очистить фильтр». Исправлено в ДС (TableFilter 1.009). Также `runtime-hooks.md` устарел про DropdownList: он инициализируется не только из кода — автопривязка через `data-ddl` (`DSDropdownList.bindAll` в `ds.js`). Сторож `layout-check.mjs` (Б13) ошибочно требовал `data-tabs` у `.tabs--vert` якорной навигации TableFilter — исправлено: исключение для вертикальных табов-якорей (у них `aria-current` scroll-spy, не `role="tablist"`).
- **Для приёмки:** при наличии `tfilter` в экране проверять полноценную `.tfm`-модалку (навигация + секции + `data-ddl` дропдауны + крестики очистки + футер «Очистить фильтр»/«Применить» справа), а не только бар.
- **Закрепление:** временное — `чек-лист:Б13`. Пара «есть `tfilter` → есть `.tfm`» механизируема, но в `hookPairs` не заведена. **Продлено 06.09.2026:** доказать пару отдельно нельзя — у всего перечня пар один идентификатор Б13 (класс Л76).

## 04.09.2026 — Modal (скролл модалки: «едет весь блок»)

### Л40. Скроллится весь блок модалки (шапка+тело+подвал) — дефект ДС, а не экрана
- **Дата/зона:** 04.09.2026 | `styles/modal.css`, экран «Текущий портфель ДИД» (`Portfolio-did/Portfolio.html`, модалка фильтра `.modal--w6.tfm`)
- **Симптом:** при открытии модалки фильтра скроллилась вся она целиком — шапка, тело и подвал ехали вместе. По правилу Modal должен скроллиться только `.modal__body` (высота ≤ 80vh), шапка и подвал остаются на месте.
- **Причина:** `overflow-y: auto` на `.modal-scrim` (фолбэк «модалка выше вьюпорта»). Chrome считает `scrollHeight` скрима не по фактическому боксу модалки (728px при `max-height:80vh`), а по контентной высоте до ужатия — по flex-basis (шапка + полный контент тела ~1436px + подвал ≈ 1560px). Ложное переполнение 1560px > clientHeight 910px, и `overflow-y: auto` превращает его в реальную прокрутку всего блока. Способ центрирования на это не влияет: проверено и `align-items:center`, и `margin:auto` — `scrollHeight` скрима не менялся (`1560`). Экран был собран верно (`.modal`/`.modal__body` без переопределений) — источник в компоненте Modal, а не в экране.
- **Правило:** скрим поверх вьюпорта НЕ скроллится — у `.modal-scrim` отсутствует `overflow-y:auto` (и `overflow` вообще): модалка всегда ≤ 80vh (`--modal-max-h`), прокручивается только `.modal__body`. Симптом «едет весь блок» — дефект ДС (Modal), не экрана: ревьюеру не выносить экрану NEEDS-WORK, а сообщить о дефекте ДС. Центрирование — `margin:auto` на `.modal` (безопасное: при переполнении прижимается к началу).
- **Для приёмки:** статически дефект не ловится (нужен рендер/вычисленный scrollHeight). Правило — нормативная гарантия Modal 1.006; на экране ничего править не нужно.
- **Закрепление:** исполняемое — `структурно:Modal 1.006` (скрим без `overflow-y:auto`, скроллится только `.modal__body`).

## 04.09.2026 — Table, модалка «Настройка таблицы» (+ Divider/Checkbox/драг-индикатор)

### Л41. Разделитель `.dvd` в модалке не виден, т.к. `divider.css` не подключён на docs-странице
- **Дата/зона:** 04.09.2026 | `pages/organisms/Table.html`, `<hr class="dvd dvd--h">` в модалке настройки колонок (`table.page.js` `columnsModalHtml`)
- **Симптом:** после добавления серого разделителя `<hr class="dvd dvd--h">` между родительским чекбоксом «Выбрать все» и списком колонок линия не отображалась вовсе.
- **Причина:** на странице Table.html в `<head>` не было `<link rel="stylesheet" href="../../styles/divider.css">`. CSS по `file://` не работает через `@import` — нужные `DS-IBP/styles/*.css` подключаются **явными `<link>`** (§10 ds-rules). Без `divider.css` классы `.dvd`/`.dvd--h` не существуют, `<hr>` рендерится дефолтным (практически невидим). Дивайдер — атом; его CSS не транзитивен через modal/table (Modal подключает только typography/colors/radius/shadow/button/icon-button).
- **Правило:** при вставке любого компонента ДС в docs-страницу проверять (пере)подключение его `styles/*.css` в `<head>` явным `<link>` — даже если компонент «внутри» другого. Добавляешь `.dvd` — значит нужен `divider.css`. Статик-сенсор `docs-split.mjs check` баланс тегов ловит, но CSS-зависимости компонента не проверяет.
- **Закрепление (06.09.2026): исполняемое — `линтер:A2`.** Правило A2 («классы есть, а `<link>` нет») существовало и раньше, но не могло сработать: `ds-lint.js` вырезает `<script>` из разметки, а `.dvd`/`.dvd--h` живут ТОЛЬКО в `table.page.js` (`columnsModalHtml`) и в HTML страницы не встречаются ни разу. Правило дополнено: классы собираются и из парного `*.page.js` (пара берётся из `<script src>`, не из имени страницы — транслитерация неверна: `DatePicker.html` → `datepicker.page.js`). Проверено на 61 странице: одна новая находка (`Modal.html` рисует `.spin--current` из `modal.page.js`, `spinner.css` не подключён), ложных срабатываний нет; токены с `${…}` из шаблонных строк отсеиваются.
- **Для приёмки:** смежное — состояние IconButton `[aria-pressed="true"]` задаёт сам компонент (`--primary`, `icon-button.css`); внешний `<style>` по равной специфичности и позже в каскаде не должен переопределять его цвет (ds-rules §4). Демо-правило `.col-item__pin[aria-pressed="true"]{color:var(--secondary-dark)}` удалено (блокер приёмки, урок Л23).
- **Промоут:** не промоутить — закреплён кодом (`линтер:A2`), агенту помнить нечего.

---

## 05.09.2026 — раскатка docs-split, «контракт CSS», рантайм-фиксы (регресс по классам дефектов)

Один заход закрыл большой пласт: раскатаны оставшиеся компоненты под docs-split, проведена кампания `[hidden] { display: none }` (44 файла, 80 корней, правило линтера B11), заведены общие рантаймы поля/тултипа/свёртки чипов, исправлены маска дат и липкая шапка. По ходу вскрылись **повторяющиеся классы дефектов**, каждый чинился по факту и каждый заслуживает отдельной записи. Группировка по классу, а не по компоненту — иначе архив задваивает одно и то же правило по числу файлов.

### Л42. Правило записано в спеке/чит-шите, а кода (рантайма) за ним нет
- **Дата/зона:** 05.09.2026 | `InputText 1.010`, `InputAutocomplete 1.013`, `Chip 1.017`, `Tab 1.012`, `NavPanel 1.017`, `ReadOnlyField 1.007`, `DatePicker 1.007`
- **Симптом:** спека и чит-шит обещают поведение «из коробки», а в рантайме ДС реализации нет — компонент живёт дефолтным, либо документация иллюстрирует то, чего не существует. Разбор по каждому: крестик очистки поля (обещан «появляется при наличии значения» — реально управлялся только разметкой); свёртка чипов «+N» (обещана с первой версии — лишние чипы просто обрезались `.inp__chips { overflow: hidden }`, демо рисовало нарисованный чип `'+3'`); тултип усечённой подписи чипа (правило в спеке с 1.000 — кода не было, «работало» только внутри ячейки таблицы из-за локальной копии в `ds-table.js`); тултип вертикального таба; тултип подписей пункта/футера NavPanel; тултип усечения значения и аффиксов ReadOnlyField; маска ручного ввода даты (жила только в `input-date.page.js` — демо-скрипте страницы, на экраны не попадала).
- **Причина:** поведение описывалось только в документации и/или реализовывалось локально в демо-скрипте страницы (`.page.js`, `input-kit.js` — построитель демо, не поведение), а не в рантайме ДС (`scripts/ds-*.js`). Разметка правильная, «выглядит собранным», но ничего не делает; на живом экране даёт обе ошибки сразу (дефект + его противоположность).
- **Правило:** любое поведение, заявленное спекой/чит-шитом («из коробки», «при наведении», «при наличии значения», «усечено → тултип»), обязано иметь реализацию в рантайме ДС (`scripts/ds-*.js`), а не только в демо-скрипте страницы и не только в вёрстке. Документация, иллюстрирующая несуществующее поведение, — дефект уровня «вводит в заблуждение». Проверка: заявленное поведение подтверждается **прогоном рантайма**, а не наличием хука (см. Л34); при ревью ДС-страницы искать реализацию (`grep` по `scripts/ds-*`) прежде чем считать правило работающим.
- **Для приёмки:** правило в спеке — НЕ гарантия. Симптом «в документации написано, а на экране нет» → сначала проверять слой ниже (рантайм/обёртка), а не разметку экрана.
- **Закрепление:** частично исполняемое — `аудит:1` · `аудит:2` · `аудит:3` · `аудит:4` · `аудит:6` сверяют обещания спек с реализацией. Ветка «объявлен компонент-родственник, CSS нет» закрыта шестым проходом 11.09.2026.

### Л43. Одно правило — несколько владельцев; копии расходятся
- **Дата/зона:** 05.09.2026 | `Tooltip 2.008`, `Chip 1.018`, `Table 1.015`, `NavPanel 1.017`
- **Симптом:** правило «короткая строка усеклась → тултип с полным текстом» было реализовано в четырёх владельцах своим кодом, в двух (`ds-chip.js`, `ds-table.js`) — двумя почти одинаковыми реализациями. Чинилось в одной копии и молча не работало в прочих (та же история, что Л42: казалось, что правило работает, потому что одна из копий жила в таблице).
- **Причина:** отсутствие единого владельца правила; каждая копия эволюционировала отдельно и расходилась бы с первой же правкой.
- **Правило:** одно правило — один владелец (слой). Повторяющаяся логика выносится в общий слой (здесь — `DSTooltip.truncated`), владельцы делегируют и сохраняют только специфику (исключения `opts.skip`, скан disabled). Наличие двух почти одинаковых реализаций одного правила в репозитории — дефект архитектуры. Также: делегирование снимает необходимость перепрохода при добавлении строк/колонок.
- **Для приёмки:** перед ревью правила, повторяющегося в нескольких рантаймах, проверить наличие общего слоя (`grep` на `DSTooltip.truncated`, `chipOverflow` и т.п.); дубль реализации — NEEDS-WORK.
- **Закрепление:** частично исполняемое — `lessons-cli check`, находка `Л-ВЛАДЕЛЕЦ` (один перечень); дубль правил между рантаймами ловит `аудит:5`.

### Л44. Точечная починка в одном компоненте ≠ правило слоя
- **Дата/зона:** 05.09.2026 | `Elevation 1.002`, `DropdownList 1.009 → 1.012`, `Tooltip 2.007`, `ContextMenu 1.009`, `Popover 1.007`, `Chart 1.002`
- **Симптом:** дефект «плавающий элемент, открытый ИЗ модалки, рисуется под её подложкой и попадает под `inert`» сначала нашли на DropdownList и починили точечно внутри компонента (`{ parent: field.closest(".modal-scrim") }`). Знание не стало правилом слоя, и тот же дефект остался у тултипа, меню, поповера и тултипа графика — всплыл тем же вечером на «Текущем портфеле ДИД».
- **Причина:** в ДС плавающий слой `DSFloat` лежит в корне `body` и намеренно не создаёт stacking context (тултип 30, меню/поповер/список 40 против `.modal-scrim` 1000); второй слой — `DSModal.lockPage()` вешает `inert` на всех детей `body`, кроме скримов. Знание «из модалки монтируйся в скрим» жило в одном компоненте и потому не защищало остальные четыре.
- **Правило:** дефект, порождённый общим механизмом наложения (плавающий слой + скрим + stacking context + `inert`), чинится в слое, а не точечно в каждом компоненте. Здесь — `DSFloat.mount(el, { anchor })` + `resolveParent`: выбор скрима делает слой, компоненты передают только якорь. Один способ вместо двух: расхождение при следующей правке исключено. Правило «что нельзя измерить — делаем структурно невозможным» (`ds-rules.md` §9) применяется на уровне слоя, а не компонента.
- **Для приёмки:** плавающий элемент из модалки — общая зона; дефект, найденный в одном компоненте, проверить на остальных (`grep` `DSFloat.mount`).
- **Закрепление:** неизмеримое — «дефект общего механизма» опознаётся суждением: одинаковый симптом у разных компонентов статикой не связывается.

### Л45. Браузерное `[hidden]` перебивается `display` той же специфичности (закрыт структурно)
- **Дата/зона:** 05.09.2026 | `styles/*.css`, 44 файла, 80 корней компонентов
- **Симптом:** атрибут `hidden` на компоненте молча не работал — базовое `display` корня компонента (0,1,0) побеждало браузерное `[hidden] { display: none }` (0,0,0) по порядку подключения. Дефект чинили поштучно шесть раз (Modal, TableCell, Tab, EmptyState, InputText, Chip), всегда по факту поломки на живом экране; он молчаливый — разметка правильная, скрипт ставит атрибут, элемент виден.
- **Причина:** отсутствие структурного соглашения; адресный поиск «кто реально переключает `hidden`» отброшен (разметочный признак ловит 1 случай из 6 — все реальные дефекты рантаймовые; признак «файл трогает `.hidden` и упоминает класс» даёт 49 находок с ложняками).
- **Правило:** корень компонента (класс без `__` и `--`), объявляющий `display`, обязан рядом объявить `.<корень>[hidden] { display: none }` — «Контракт CSS компонента» (записан в `MAINTAINING.md`). Ловится правилом линтера **B11** (WARN). Исключения с причиной — в белом списке `HIDDEN_PAIR_OK`. **Закрыто структурно** — из кураторской выжимки убирается; в архиве остаётся как полная история шести поштучных починок.
- **Для приёмки:** класс дефекта закрыт линтером B11; при ревью проверять, что B11 в отчёте чист, а не «внимательно смотреть скрытие».
- **Закрепление:** исполняемое — `линтер:B11` (контракт `[hidden] { display: none }` у корня компонента).
- **Промоут:** выведен из выжимки 06.09.2026 — закреплён линтер:B11 (исполняемое)

### Л46. `position: sticky` на grid-элементе строки по вертикали не работает
- **Дата/зона:** 05.09.2026 | `styles/table.css`, `Table 1.013`
- **Симптом:** липкая шапка таблицы уезжала вверх при вертикальном скролле тела; заявленный инвариант «липкая шапка не отключаема» не выполнялся ни на одном экране с прокруткой тела (вскрылось на «Текущем портфеле ДИД»).
- **Причина:** `position: sticky` стоял на `.th` (grid-элемент строки). Блок-контейнер, ограничивающий sticky-смещение, — сама строка; по вертикали её высота равна высоте шапки, двигаться некуда. По горизонтали то же правило работало, потому что строка шире скроллпорта (`min-width: max-content`) — из-за этого закрепление колонок было исправным, а дефект вертикали долго не всплывал.
- **Правило:** липкая шапка — `position: sticky` на **строке** (`.dtable__body .tbl > .tbl__row--head`), а не на ячейке `.th`; класс `.tbl__row--head` стал обязательным на строке шапки внутри `.dtable__body`. Правило `.dtable__body .th` оставлено — оно держит слой и горизонтальное закрепление. Проставить класс во всех потребителях (генератор демо, экраны). Аккуратно: sticky на grid-элементе — классическая ловушка; смотреть направление (вертикаль/горизонталь) отдельно.
- **Для приёмки:** «липкая шапка» проверяется на поведении с прокруткой, а не по наличию класса; sticky-инвариант, работающий по горизонтали, не гарантирует работу по вертикали.
- **Закрепление (06.09.2026): исполняемое — `сенсор:Б30`,** доказан откатом (`fixtures/Б30.bad.html`).
- **Промоут:** выведен из выжимки 06.09.2026 — закреплён кодом, агенту помнить нечего

### Л47. Даты в разном порядке ММ.ДД vs ДД.ММ — молчаливо неверный фильтр
- **Дата/зона:** 05.09.2026 | `DatePicker 1.006`, `InputDate 1.010`, `InputDateRange 1.007`, `TableCell 2.016`
- **Симптом:** `ds-datepicker.js` писал/читал дату месяц-первым (ММ.ДД.ГГГГ), а сортировка дат в TableCell (`data-sort-type="date"`) и все реальные даты сделок на экране — день-первым (ДД.ММ.ГГГГ). Календарь и типизированный ввод давали значение, не совпадающее с тем, что показывает и сортирует таблица — фильтр по датам был бы либо неверным, либо неработающим для дат с числом дня >12.
- **Причина:** маска жила только в демо-скрипте страницы (`input-date.page.js`) и в ручном порядке; рассинхрон между календарём, маской, сортировщиком и плейсхолдером. Плюс `formatDate`/`parseDate` в рантайме были месяц-первыми.
- **Правило:** даты во всей ДС — единый порядок **ДД.ММ.ГГГГ**; маска ручного ввода, календарь, сортировка дат, плейсхолдер и демо-значения обязаны совпадать в одном порядке. Использование календаря/типизации без единого порядка даёт молчаливо неверную фильтрацию (неверный результат вычислим, а не карта). Проверка: `formatDate`/`parseDate` рантайма сверяются с маской `input-kit.js` и `data-sort-type="date"`.
- **Для приёмки:** при наличии календаря/маски/сортировки дат сверять единый порядок между компонентами; рассинхрон не виден по разметке — только по смыслу поля.
- **Закрепление:** исполняемое — `сенсор:З5`, доказан откатом (`fixtures/З5.bad.html`). Ловит три расхожие подмены формата: ISO `ГГГГ-ММ-ДД`, дробь `ДД/ММ/ГГГГ`, двузначный год `ДД.ММ.ГГ`.
- **Промоут:** выведен из выжимки 06.09.2026 — закреплён `сенсор:З5`. Только исполняемый уровень выводит урок из выжимки: пока правило держится на памяти, оно обязано быть перед глазами.

---

## 06.09.2026 — ревизия механизма самообучения (журнал, гейт, сенсор)

Заход не про компонент, а про сам механизм уроков: почему записи не
превращались в улучшения. Уроки ниже — про инструменты и про способ работы,
а не про вёрстку.

### Л48. Правило существует, но его вход усечён — сработать оно не может
- **Дата/зона:** 06.09.2026 | `scripts/ds-lint.js`, правило A2; кейс урока Л41
- **Симптом:** урок Л41 («компонент вставлен в docs-страницу, его CSS не подключён») был записан как процедурное правило «проверять `<link>` руками», хотя правило линтера A2 («классы есть, а `<link>` нет») существовало с самого начала и обязано было его поймать.
- **Причина:** `pageChecks` строит `markup` как `html` без `<style>` и **без `<script>`**, а `.dvd`/`.dvd--h` живут ТОЛЬКО в `table.page.js` (`columnsModalHtml`) и в HTML страницы не встречаются ни разу (проверено: 131 класс на Table.html существует лишь в page.js). Правило было право, ему не показали данные.
- **Правило:** это отдельный класс, не Л42 («правила нет») и не Л43 («копии разошлись»): **правило есть, реализация есть, а вход в неё срезан**. Симптом «дефект прошёл мимо сторожа, который обязан был его поймать» → сначала проверять, ВИДИТ ли сторож нужные данные, и только потом писать процедурное правило поверх. Процедурное правило, написанное вместо починки входа, маскирует дефект инструмента.
- **Закрепление: исполняемое — `линтер:A2`.** Сбор классов дополнен парным `*.page.js` (пара — из `<script src>`, не из имени страницы: `DatePicker.html` → `datepicker.page.js`). Прогон по 61 странице: одна новая находка (`Modal.html` рисует `.spin--current` из `modal.page.js`, `spinner.css` не подключён), ложных нет.
- **Промоут:** не промоутить — закреплён кодом; класс-эвристика вынесена в `чек-лист:К14`.

### Л49. Гейт, который всегда красный, сигнала не несёт
- **Дата/зона:** 06.09.2026 | `scripts/ds-check.mjs`
- **Симптом:** `ВЕРДИКТ: FAIL` был штатным состоянием единого гейта на всех раскатанных страницах; на экранах он вдобавок выдавал 5 блокеров, из которых 4 ложные.
- **Причина:** два независимых дефекта. (1) Шаг `docs-split verify` (headless-браузер) добавлялся **безусловно**, хотя §9 запрещает браузер в рабочем контуре — без Chrome шаг падал кодом 2. (2) `isScreen` определялся только как `pages/screens/`, а экраны приходят путём `../Projects/test/Имя.html` — к экрану применялся контракт docs-страницы (ложные C1/C2), плюс A5 резолвил ссылки от корня ДС и объявлял битыми существующие файлы.
- **Правило:** постоянно красный вердикт эквивалентен отсутствию вердикта — новый сигнал в таком отчёте не виден. Шаг, которому нужен недоступный инструмент, делается **явным и выключенным по умолчанию** (`--with-verify` / `DS_CHROME`), а его пропуск печатается строкой `ПРОПУЩЕН:`, а не молчит. Уточняет Л37: там правило ссылалось на несуществующий инструмент, здесь — гейт молча зависел от него.
- **Закрепление: исполняемое —** шаг за флагом в `ds-check.mjs`; `isScreen` учитывает пути вне дерева ДС; A5 отслеживает выход выше корня (`escaped`) и не проверяет ссылки, которых нет в своём индексе.
- **Для приёмки:** проверено — экран 5 блокеров → 1 (оставшийся F5 настоящий: `nav--rail` без `.nav__pin`); 61 страница ДС 1 блокер → 0 при неизменных WARN 81 / INFO 48; откат-тест — битая ссылка ВНУТРИ ДС по-прежнему ловится.
- **Промоут:** не промоутить — закреплён кодом.

### Л50. Перечисление состава сторожа прозой расходится с кодом
- **Дата/зона:** 06.09.2026 | `ds-rules.md` §9, `screen-review/SKILL.md`, `MAINTAINING.md`, `layout-check.mjs`
- **Симптом:** состав проверок сенсора был перечислен в четырёх местах — «Б1–Б16» (трижды) и «Б1–Б25» (однажды). Фактический состав: `Б1 Б2 Б3 Б5 Б6 Б9 Б10 Б13 Б14 Б16 Б17 Б21–Б26` и `K1 K2.1 K4 K5 K6`. Не совпадает **ни с одним** из четырёх описаний.
- **Причина:** список — производное от кода, а хранился как проза у четырёх владельцев (Л43). Расхождение бесшумное: неверный диапазон выглядит правдоподобно.
- **Правило:** производное от кода не пересказывается прозой, а **печатается командой**. Заодно вскрылось: `Б8` и `K2` упоминались только в комментариях — проверок под ними нет, хотя описания их обещали (это Л42 внутри сенсора).
- **Закрепление: исполняемое — `layout-check.mjs --rules`.** Список извлекается из самого файла (идентификатор в начале строкового литерала метки), четыре прозаических перечисления заменены ссылкой на команду.
- **Промоут:** не промоутить — закреплён кодом.

### Л51. Шелл-слой съедает экранирование — регулярка приезжает битой и молчит
- **Дата/зона:** 06.09.2026 | правка `layout-check.mjs` через `node -e` и через heredoc
- **Симптом:** записанная в файл регулярка `/['"`]\s*(Б\d{1,2}|K\d{1,2})/gu` превратилась в `/['"`]s*(Бd{1,2}|Kd{1,2})/gu`. Она валидна, исполняется без ошибки и находит **ноль** совпадений там, где их 22.
- **Причина:** слой Bash схлопывает `\\` в `\` даже внутри закавыченного heredoc (`<<'X'`) — проверено отдельным тестом. Одного уровня экранирования не хватает: `\s` доезжает как `s`, а `\u0411` интерпретируется в саму букву Б ещё до записи.
- **Правило:** код, содержащий регулярные выражения или escape-последовательности, **не пишется через шелл** — только файловым редактором. Дефект молчаливый (класс Л33): битая регулярка не падает, а возвращает пустой результат, и «проверка» выглядит пройденной. После записи любого сторожа — прогон, подтверждающий непустой результат на заведомо подходящих данных.
- **Закрепление: временное — `правило ds-rules §8`** (работа с файлами и кодировкой). Исполняемого сторожа нет: свойство среды, а не репозитория. **Продлено 11.09.2026:** ни один инструмент контура не читает командную строку агента — закрепить нечем; правило держится `ds-rules §8`.
- **Промоут:** не промоутить — правило распространяется через `ds-rules.md`, который подключается всем агентам.

### Л52. Код выхода, померенный после подстановки, — код подстановки
- **Дата/зона:** 06.09.2026 | замер базовых уровней сенсора
- **Симптом:** сенсор печатал `ВЕРДИКТ: FAIL`, а замер рядом показывал `код 0`. Дважды подряд — и это выглядело как дефект сенсора.
- **Причина:** `echo "$(basename $f) -> код $?"` — подстановка `$(...)` выполняется первой и перезаписывает `$?`; то же с `cmd | tail`, где `$?` принадлежит `tail`. Сенсор был исправен, врал замер.
- **Правило:** код выхода снимается **отдельной командой сразу после проверяемой**, без подстановок и пайпов в той же строке (`cmd > /dev/null 2>&1; echo $?`). Иначе отчёт «команда с кодом выхода» — фикция, а именно на неё опирается Л37.
- **Закрепление: временное — `правило ds-rules §8`.** **Продлено 11.09.2026:** замер кода выхода — свойство среды, инструмент командную строку агента не видит.
- **Промоут:** не промоутить — распространяется через `ds-rules.md`.

### Л53. Сторож считал комментарий разметкой — ложный PASS на ровном месте
- **Дата/зона:** 06.09.2026 | `layout-check.mjs`, вскрыто фикстурами `lessons-cli verify`
- **Симптом:** на фикстуре, где дефект внесён намеренно, сенсор выдавал PASS. Б13 сообщал «компонент + хук ✓» при отсутствующем `data-table`, Б22 — «body { margin: 0 } сброшен» при отсутствующем сбросе.
- **Причина:** сенсор искал строки по всему файлу, включая HTML-комментарии. Слова `data-table` и `body { margin: 0 }` в комментарии-шапке фикстуры удовлетворяли проверке. На продуктовом экране тот же ложный PASS даёт любой комментарий вида `<!-- TODO: повесить data-table -->` — то есть **комментарий о ненаписанном коде закрывал проверку этого кода**.
- **Правило:** проверка присутствия конструкции ведётся по разметке БЕЗ комментариев И БЕЗ содержимого `<script>`. Тем же заходом вскрылась вторая половина: JS-шаблон `'<div class="tbl__row" style="grid-template-columns:' + GRID + '">'` сенсор считал готовой строкой таблицы, а `data-table` в строке кода закрывал Б13 так же, как комментарий. Открывающий тег `<script src>` при этом сохраняется — по нему считается подключение ds.js. Исключение из вырезания одно и осознанное: имя иконки в JS-шаблоне — настоящее употребление, поэтому Б2 валидирует имена по срезу «без комментариев, но со скриптами». Исключение — поиск заглушек (Б9): «TODO» в комментарии означает незаконченный экран ровно так же, поэтому там остаётся полный текст. Класс тот же, что Л48: правило верное, вход не отфильтрован.
- **Закрепление: исполняемое —** комментарии вырезаются в `checkMechanics` (с сохранением нумерации строк), Б9 читает `raw`. Доказано парами фикстур `Б13.bad.html` и `Б22.bad.html`: до правки — «не доказан», после — «падает на дефекте, молчит на эталоне».
- **Для приёмки:** побочно — на реальном экране `FundList.html` счёт иконок изменился 158 → 157 — одна `data-icon` лежала в закомментированном блоке и рендерилась только в воображении сенсора.
- **Для приёмки:** тот же класс во втором инструменте (06.09.2026, `ds-lint.js`, правило P4). Правило «рантайм вешает класс, которого нет в CSS» читало ВСЕ блоки `<script>` страницы, включая `<script type="text/plain" id="src-code-html">` — статичный образец кода для вкладки «Код». Итог: блокер «рантайм вешает `.demo-panel`» про строку из документации, которую никакой рантайм не исполняет. Подтверждение, что это класс, а не случай: один и тот же дефект входа независимо возник в двух сторожах, написанных в разное время. **Закрепление: исполняемое —** P4 исключает `type="text/plain"`, а находки из таких блоков уходят под P3 «код-панель предлагает» (находка не теряется, меняется классификация). Гейт `--parity`: BLOCKER 2 → 0.
- **Правило:** шире, чем «вырезать комментарии» — у любого сторожа надо назвать, что именно является его входом, и всё остальное отрезать. Комментарий, JS-шаблон, образец кода в `text/plain` — это три разных вида «текста, похожего на разметку», и каждый по очереди давал ложный вердикт.
- **Промоут:** не промоутить — закреплён кодом в обоих инструментах.

### Л54. Проверка без идентификатора в метке невидима для инвентаризации
- **Дата/зона:** 06.09.2026 | `layout-check.mjs`, блокер Б7
- **Симптом:** Б7 («сумма `grid-column:span` в ряду равна 12») числился незакрытым в отчёте покрытия и был бы записан в рабочий список «сторожа нет», хотя сторож есть и работает.
- **Причина:** метка выводилась без идентификатора — «ряд (строка 39): … ≠ 12». Инвентаризация (`--rules`, `coverage`) собирает идентификаторы из меток, поэтому безымянная проверка для неё не существует. Итог — риск написать второй сторож поверх работающего.
- **Правило:** **метка любой проверки начинается с её идентификатора.** Безымянная проверка не хуже работает, но выпадает из учёта, а учёт — единственный способ узнать, что закрывать дальше. Уточняет Л50: там проза расходилась с кодом, здесь код не представлен в собственной инвентаризации.
- **Закрепление: исполняемое —** метка получила префикс `Б7`; пункт перешёл из «посильно» в «закрыто сторожем» (21 → 22), фикстура `Б7.bad.html` доказывает откатом.
- **Промоут:** не промоутить — закреплён кодом.

### Л55. Хук проверялся «где-нибудь на странице», а не у компонента
- **Дата/зона:** 06.09.2026 | `layout-check.mjs`, блокер Б13
- **Симптом:** после того как в эталон фикстур добавили корректную таблицу с `data-table`, фикстура Б13 (вторая таблица БЕЗ хука) перестала ловиться: сторож рапортовал «компонент + хук ✓».
- **Причина:** проверка была устроена как пара присутствий — «на странице есть `class="tbl"`» и «на странице есть `data-table`». Совпадение этих двух фактов не означает, что хук стоит на той же таблице. Экран с тремя таблицами, где захукана одна, проходил приёмку, а две оставались мёртвыми: ни сортировки, ни выбора строк, ни тянущихся колонок.
- **Правило:** хук проверяется **на самом элементе компонента**, а не по факту присутствия строки где-то в файле. Пара присутствий — это не проверка соответствия, а совпадение. Родственно Л32 («хук стоит — поведение мёртвое»): там разметка обманывала приёмку, здесь — сама приёмка.
- **Закрепление: исполняемое —** Б13 для таблиц перебирает корни `.tbl` и требует `data-table` у каждого; в отчёт идут номера строк незахуканных. Доказано откатом (`fixtures/Б13.bad.html`). Проверено на трёх реальных экранах: у всех по одной таблице, все с хуком — ложных срабатываний нет.
- **Для приёмки:** осталось открытым — остальные пары в `hookPairs` (модалка, табы, сегмент-контрол, риск-метрика, аккордеон) по-прежнему проверяются присутствием. Класс дефекта тот же; поэлементная проверка для них — отдельная работа, записана в рабочий список покрытия.
- **Промоут:** не промоутить — закреплён кодом.

### Л56. `\b` в регулярке по кириллице не совпадает ни с чем: граница слова в JS считается по ASCII

- **Дата/зона:** 06.09.2026 · `lessons-cli.mjs`, правило `Л-ПРАВИЛО`
- **Симптом:** новая проверка формы записей объявила «с правилом 0» из 92 записей — при том, что ярлык `- **Правило:**` стоит в журнале 49 раз. Регулярка `/^Правил[оа]\b|^Решение\b/u` синтаксически верна, ошибок не бросает, совпадений даёт ноль.
- **Причина:** `\b` в JavaScript — граница между `\w` и не-`\w`, а `\w` это ASCII `[A-Za-z0-9_]`. Кириллическая «о» не входит в `\w`, поэтому после неё границы слова НЕТ никогда. Флаг `u` этого не меняет — юникодные классы включает только `\p{...}` при явном указании.
- **Правило:** в регулярках по русскому тексту `\b`, `\w`, `\W`, `\d` рядом с кириллицей не употреблять. Ограничитель писать явным классом (`[^а-яё]`, `[\s:.,]`) либо не писать вовсе. Это тот же класс, что Л51: регулярка проходит парсер и молча не ловит ничего.
- **Для приёмки:** новая регулярка, давшая **ноль** совпадений на заведомо непустом входе, — не «чисто», а подозрение. Счётчик совпадений печатается рядом с находками (`с правилом 87 · с закреплением 12 · якорей сверено 12`) именно поэтому: ноль в такой строке виден сразу, а список находок при нулевом совпадении выглядит как успех.
- **Закрепление:** временное — `правило ds-rules §8` (работа с файлами и кодировкой) плюс требование печатать счётчик совпадений рядом с вердиктом. Исполняемого сторожа нет: своих `.mjs` в контуре немного, а общий запрет `\b` дал бы ложные срабатывания на латинских паттернах — это суждение о том, по какому алфавиту идёт поиск. **Продлено 11.09.2026:** сторож на «`\b` рядом с кириллицей» требует суждения об алфавите паттерна и на латинице шумит; держится `ds-rules §8` и счётчиком совпадений в вердикте.
- **Промоут:** не промоутить — правило среды, а не дизайн-системы; живёт в `ds-rules §8`, выжимку читают перед приёмкой экрана.

### Л57. Правило «у процедуры один владелец» нарушается в первую очередь тем файлом, который это правило объявляет

- **Дата/зона:** 06.09.2026 · `.opencode/agents/ai-designer.md`, `.opencode/skills/screen-review/SKILL.md`
- **Симптом:** через сутки после того, как процедура самообучения была вынесена в единственного владельца (`skills/lessons/SKILL.md`), три уровня закрепления оказались перечислены ещё в двух файлах. В `ai-designer.md` дубль стоял **прямо под фразой** «Здесь её не пересказывают: копия правила у второго владельца расходится с оригиналом на первой же правке».
- **Причина:** вынос процедуры делался вручную, а «указатель» и «краткая выжимка для роли» на глаз неразличимы. Копия из трёх слов не выглядит копией. Разошлись они сразу: у `screen-review/SKILL.md` в перечне стоял якорь «каскад:К3» — пространства имён «каскад» не существует, канон `чек-лист:`.
- **Правило:** файл-указатель называет владельца и триггер, но **не перечисляет состав** процедуры — ни уровни, ни шаги, ни якоря по списку. Признак нарушения механический: файл, содержащий все элементы перечня, объявляет себя владельцем.
- **Закрепление:** исполняемое — `lessons-cli check`, находка `Л-ВЛАДЕЛЕЦ`: файлом-владельцем считается всякий `.md` в `.opencode/` и `AGENTS.md`, где встречаются все три названия уровней; журналы исключены — они уровни употребляют, а не определяют. Доказано откатом на живом репозитории: до правки БЛОКЕР 2 (оба дубля названы поимённо), после правки БЛОКЕР 0, владельцев ритуала 1. Заодно снят мёртвый якорь «каскад:К3». Мёртвый якорь цитируется в журнале ёлочками, а не обратными кавычками: кавычки означают «идентификатор живой», и `check` сверяет их с реестром.
- **Промоут:** не промоутить — закрыт исполняемым сторожем (`lessons-cli check`, `Л-ВЛАДЕЛЕЦ`); выжимка держит только незакрытое.

### Л58. Размер компонента берётся из его спеки, а не из сниппета чит-шита

- **Дата/зона:** 26.08.2026 | экран `DealPipelineCard.html`, разбор сборки
- **Симптом:** бургер панели навигации собран как `ibtn--s`.
- **Причина:** сниппет чит-шита взят как есть и не сверен с каркасным компонентом. В спеке и в расчёте оси в CSS у панели `ibtn--m` — чит-шит даёт обзор, а размер держит спека компонента.
- **Правило:** размерный модификатор (`--s`/`--m`/`--l`) берётся из спеки того компонента, в который элемент вставляется, а не из ближайшего похожего примера.
- **Закрепление:** исполняемое — `сенсор:Б15`, доказан парой фикстур `Б15.bad.html`. Уровень поднят 06.09.2026 по находке `Л-СРОК`: сторож написан в тот же день, что и урок, а поле осталось описывать долг, которого уже не было.
- **Промоут:** не промоутить — правило уже стоит пунктом чек-листа приёмки; выжимка держит то, что чек-листом не закрыто.

### Л59. Ширина тайла задаётся только `grid-column:span N`, свой класс ширины — изобретение

- **Дата/зона:** 26.08.2026 | экран `DealPipelineCard.html`, разбор сборки
- **Симптом:** ширина тайлов задана собственными классами `.dpc-half` / `.dpc-full`.
- **Причина:** паттерн `style="grid-column:span N"` на `.tile` описан прямо в чит-шите — он был прочитан и обойдён, отклонение нигде не помечено. Это тяжелее незнания: правило видели.
- **Правило:** своих классов ширины у тайла нет. Ширина — только `grid-column:span N`; отступление от прочитанного правила помечается в `screen.md`, а не делается молча.
- **Закрепление:** исполняемое — `сенсор:Б14`.
- **Промоут:** не промоутить — правило уже стоит пунктом чек-листа приёмки; выжимка держит то, что чек-листом не закрыто.

### Л60. Число колонок тайла выводится из объёма содержимого, а не ставится по умолчанию

- **Дата/зона:** 26.08.2026 | экран `DealPipelineCard.html`, разбор сборки
- **Симптом:** все тайлы получили 6 или 12 колонок, справа осталась пустота.
- **Причина:** объём контента с шириной не сверялся: блок из 3–4 коротких полей занимал полосу под 12 колонок вместо `span 4`, где три таких блока встают в ряд.
- **Правило:** ширина тайла выводится из числа и длины полей: 3–5 коротких полей — 4 колонки, 6–9 — 6, от 10 — 8; полная ширина — только под таблицу или график.
- **Закрепление:** временное — `чек-лист:K4` и `чек-лист:K1`. Сторожа НАПИСАНЫ (`сенсор:K4`, `сенсор:K1`), но откатом не доказаны: пары фикстур нет, а на эталоне `_base.ok.html` K1 уже даёт WARN — правило шумит на чистом входе и потому не может быть доказано в нынешнем виде (см. Л65). Повышение до «исполняемого» — предмет Э4. **Продлено 06.09.2026:** K1 срабатывает и на эталоне — правило шумит на чистом входе и потому не может быть доказано откатом, пока не перестанет (см. Л65). Чинится порог, а не заводится фикстура.
- **Промоут:** не промоутить — правило уже стоит пунктом чек-листа приёмки; выжимка держит то, что чек-листом не закрыто.

### Л61. Зазор, который держит компонент, руками не выставляется

- **Дата/зона:** 26.08.2026 | экран `DealPipelineCard.html`, разбор сборки
- **Симптом:** между рядами тайлов проставлен зазор 24px.
- **Причина:** взят токен соседнего уровня (`--layout-block-gap`) по аналогии, без проверки компонента. Зазор 16px между рядами держит само правило `.tile-row + .tile-row`.
- **Правило:** если расстояние задаёт компонент, экран его не переопределяет. Прежде чем ставить `gap`/`margin`, проверяется, нет ли уже правила у соседства элементов ДС.
- **Закрепление:** исполняемое — `сенсор:Б16`.
- **Промоут:** не промоутить — правило уже стоит пунктом чек-листа приёмки; выжимка держит то, что чек-листом не закрыто.

### Л62. Элемент на всю ширину сетки — это `.tile__grid-full` внутри сетки, а не блок рядом с ней

- **Дата/зона:** 26.08.2026 | экран `DealPipelineCard.html`, разбор сборки
- **Симптом:** поле «Описание» вынесено из `.tile__grid` отдельным блоком.
- **Причина:** паттерн «строка на всю ширину» не искался — вместо него изобретена замена. Готовый класс `.tile__grid-full` живёт внутри той же сетки и сохраняет выравнивание.
- **Правило:** прежде чем выносить элемент из сетки, ищется штатный модификатор «на всю ширину». Выход за сетку ломает вертикальный ритм соседних полей.
- **Закрепление:** исполняемое — `сенсор:Б18`, доказан парой фикстур `Б18.bad.html`. Уровень поднят 06.09.2026 по находке `Л-СРОК`, вместе с Л58.
- **Промоут:** не промоутить — правило уже стоит пунктом чек-листа приёмки; выжимка держит то, что чек-листом не закрыто.

### Л63. Прочитанный пример применяется в ту сторону, в какую подписан

- **Дата/зона:** 26.08.2026 | экран `DealPipelineCard.html`, разбор сборки
- **Симптом:** `pbar--floating` поставлен «Прогрессу», а «Вероятность» выведена текстом.
- **Причина:** пример прочитан верно и применён наоборот. Floating-вариант в ДС подписан как вариант для вероятности, обычный — для прогресса.
- **Правило:** у примера в спеке читается не только разметка, но и подпись «для чего». Пара «вариант — случай» переписывается в `screen.md` до сборки, тогда переворот виден на месте.
- **Закрепление:** исполняемое — `сенсор:Б17`.
- **Промоут:** не промоутить — правило уже стоит пунктом чек-листа приёмки; выжимка держит то, что чек-листом не закрыто.

### Л64. «Сторож написан» и «сторож доказан» — разные состояния, и второе до сих пор нигде не отмечалось

- **Дата/зона:** 06.09.2026 · `layout-check.mjs` (Б14, Б16, Б17), `lessons-cli verify`
- **Симптом:** три проверки сенсора — Б14 (свои классы ширины тайла), Б16 (ручной зазор ряда), Б17 (`pbar--floating` не в том контексте) — числились закрытыми с момента написания. Пары фикстур у них не было ни одной. Дыру назвал не человек и не прогон, а первый же урок, который сослался на них полем «Закрепление»: `check` выдал три `Л-ОРАКУЛ`.
- **Причина:** отчёт `coverage` отвечает на вопрос «есть ли у пункта сторож» и печатал их в графе «сторожем» — правильно. Вопроса «доказан ли он откатом» не задавал никто: `verify` перебирает существующие фикстуры и молчит о тех, которых нет. Отсутствие доказательства выглядело как отсутствие проблемы.
- **Правило:** сторож, у которого нет пары фикстур, закрытым пунктом не считается — он считается написанным. Проверка «падает на дефекте, молчит на эталоне» заводится **вместе** с проверкой, а не когда на неё сошлётся урок. Обратный порядок означает, что закрытие держится на памяти автора.
- **Закрепление:** исполняемое — `lessons-cli check`, находка `Л-ОРАКУЛ`: закрепление уровня «исполняемое», сославшееся на проверку сенсора, требует одноимённого файла в `fixtures/`. Доказано откатом на живом репозитории: до заведения фикстур БЛОКЕР 3 (Б14, Б16, Б17 поимённо), после — БЛОКЕР 0, фикстур 13 → 16, доказано 16.
- **Для приёмки:** дыра всё ещё шире находки. `Л-ОРАКУЛ` срабатывает только там, где на сторожа СОСЛАЛСЯ урок; сторожа, на которых не ссылается никто, остаются недоказанными молча. Полный перебор «реализовано против доказано» — отдельная проверка, она пока не написана.
- **Промоут:** не промоутить — закрыт исполняемым сторожем; выжимка держит незакрытое.

### Л65. Эталон оракула шумит — и правило, которое он должен был доказывать, доказать нельзя

- **Дата/зона:** 06.09.2026 · `tooling/fixtures/_base.ok.html`, проверка `сенсор:K1`
- **Симптом:** эталон фикстур `_base.ok.html` — файл, который по определению «без дефектов», — выдаёт `WARN K1 «Условия»: воздух ~352px (близко к порогу)`. Пары фикстур у K1 из-за этого завести нельзя: оракул требует, чтобы правило МОЛЧАЛО на эталоне, иначе оно шумит, а не ловит.
- **Причина:** K1 — эвристика: ширина текста оценивается по числу символов, а не меряется. Тайл эталона `span 12` с четырьмя колонками попадает в оценку «воздух у правой кромки», хотя поля в нём заполнены. Эталон при заведении проверялся по коду выхода (0), а код выхода даёт только FAIL — WARN на эталоне никто не смотрел.
- **Правило:** эталон обязан молчать **целиком**, а не только по коду выхода: любое WARN на `_base.ok.html` — либо дефект эталона, либо шум правила, и третьего не дано. Проверять эталон надо на пустой вывод, а не на код 0.
- **Закрепление:** временное — пункт приёмки этого файла. Исполняемого сторожа нет и он не тривиален: заставить `verify` падать на любом WARN эталона сейчас нельзя — сперва надо решить, чинится эталон или порог K1, и это суждение о геометрии, а не строковая проверка. **Продлено 11.09.2026:** эталон `_base.ok.html` по-прежнему даёт WARN K1 (проверено прогоном 11.09.2026); пока не решено, чинится эталон или порог эвристики, пары фикстур у K1/K4 быть не может.
- **Для приёмки:** пока эталон шумит, у `сенсор:K1` и `сенсор:K4` не может быть доказательства, и уроки на них честнее закреплять уровнем «временное» со ссылкой на эту запись (так сделано в Л60).
- **Промоут:** не промоутить — свойство оснастки самообучения, а не дизайн-системы; выжимку читают перед приёмкой экрана.

### Л66. Сторож, который проверяет наличие компонента ДС, берёт имя класса из спеки, а не по памяти

- **Дата/зона:** 06.09.2026 · `layout-check.mjs`, проверка `сенсор:З9`; экран `Portfolio.html`
- **Симптом:** новая проверка «пустая таблица без состояния „данных нет“» дала находку на `Portfolio.html` — экране, где EmptyState как раз описан: `.es es--m` с идентификатором `deals-empty`, рантайм его показывает и прячет. Ложное срабатывание на первом же прогоне по реальному экрану.
- **Причина:** две, и обе — про вход правила. Первая: искомый класс написан по памяти — `.empty-state` и `.dtable__empty`, тогда как в ДС компонент называется `.es` (`styles/empty-state.css`). Второе имя вообще ничему не принадлежит. Вторая: строки таблицы строит рантайм, а содержимое `<script>` сторож гасит намеренно — «строк в разметке нет» было прочитано как «таблица пуста».
- **Правило:** правило проекта «ничего не выдумывать» распространяется на сами сторожа. Имя класса, по которому сторож ищет компонент, берётся из спеки или из `styles/*.css`, а не пишется по догадке: сторож с выдуманным селектором не падает и не ошибается заметно — он тихо считает, что компонента нет. И отдельно: раз содержимое `<script>` погашено, любой вывод вида «в разметке этого нет» обязан быть перепроверен по неочищенному входу.
- **Закрепление:** исполняемое — `сенсор:З9` смотрит `class="… es …"` и сверяет число вхождений `tbl__row` в очищенном входе против неочищенного: строит ли строки рантайм. Доказано откатом (`fixtures/З9.bad.html`) плюс прогоном по трём реальным экранам — после правки новых находок нет.
- **Для приёмки:** это третий случай одного класса подряд (Л48 — комментарии, Л53 — `text/plain` в линтере, теперь З9). Общее у всех трёх: правило верное, вход не назван. У нового сторожа вход объявляется в комментарии рядом с кодом — что он читает и что намеренно не читает.
- **Промоут:** не промоутить — закрыт исполняемым сторожем; выжимка держит незакрытое.

### Л67. Инвентарь, который знает про один инструмент из двух, врёт в сторону недооценки — и это не безопасная ошибка

- **Дата/зона:** 06.09.2026 · `lessons-cli coverage`, `coverage.json`, пункт `чек-лист:К7`
- **Симптом:** отчёт покрытия держал К7 («`@import` по `file://` не считается подключением») в графе «посильно, сторож не написан» — то есть в очереди работ. Сторож существует: правило `линтер:A2` собирает классы из разметки и парного `page.js` и требует `<link>` на файл-владелец, а `@import`-цепочка подключением не считается по самому его устройству.
- **Причина:** `coverage` определял закрытость единственным вызовом — составом проверок сенсора (`layout-check --rules`). Второй инструмент, закрывающий часть тех же пунктов, в расчёт не входил вовсе. Ошибка «в безопасную сторону» только на вид: пункт занимает место в очереди работ, которую и так не успевают, и вытесняет оттуда пункт, у которого сторожа действительно нет.
- **Правило:** отчёт о покрытии перечисляет ВСЕ инструменты, способные закрыть пункт, и называет, каким именно он закрыт. Инвентарь, у которого источник один, а закрывающих инструментов несколько, показывает не покрытие, а покрытие одним инструментом — и заголовок обязан это говорить.
- **Закрепление:** исполняемое — в `coverage.json` заведена графа `byLinter` (пункт → правило ds-lint с обоснованием), `coverage` печатает её отдельной строкой «линтером». Проверено сменой отчёта: К7 переехал из «посильно» в «линтером», итог сошёлся — 67 пунктов, сумма граф без остатка.
- **Для приёмки:** доказательства откатом у этой графы нет и быть не может, пока у правил ds-lint нет фикстур: она объявляет, что сторож есть, но не проверяет, что он падает. Пары фикстур для линтера — отдельный заход.
- **Промоут:** не промоутить — свойство оснастки самообучения, а не дизайн-системы.

### Л68. Запрет без замены не исполняется: правило нарушено в тот же день, когда было записано

- **Дата/зона:** 06.09.2026 · `ds-rules §8`, замер кодов выхода сенсора
- **Симптом:** проверяя регресс по трём экранам, снял коды выхода циклом `for f in …; do node …; echo "$(basename $f): $?"; done` и получил 0 там, где инструмент печатал `ВЕРДИКТ: FAIL (1 блокер)`. Успел объявить это регрессом сторожа, прежде чем перемерил. Правило против ровно этой ошибки — с этим самым примером `$(basename $f)` — стояло в `ds-rules §8` с утра того же дня (урок Л52).
- **Причина:** правило было сформулировано как запрет: «код выхода снимается отдельной командой, без подстановок». Запрет говорит, чего не делать, но не даёт готового действия на месте задачи «проверить десять файлов подряд». Цикл — естественная форма этой задачи, а внутри цикла безопасного способа снять `$?` попросту нет; правило требовало отказаться от формы, ничего не предложив взамен.
- **Правило:** запрет в правилах пишется вместе с заменой — что делать ВМЕСТО. Для этого случая замена такая: в цикле по файлам код выхода не снимается вовсе, читается строка вердикта, которую инструмент печатает сам (`ВЕРДИКТ:` у сенсора, `BLOCKER n · WARN n` у линтера). Она не зависит ни от подстановок, ни от пайпов.
- **Закрепление:** временное — `правило ds-rules §8` дополнено заменой. Исполняемого сторожа нет и не будет: ни один инструмент контура не читает командную строку агента — это свойство среды, а не репозитория. **Продлено 11.09.2026:** замена («читать строку вердикта вместо кода выхода») записана в `ds-rules §8`; закрепить исполняемо нечем.
- **Для приёмки:** ценность записи не в самом правиле, а в замере. Проза в файле правил, называющая ошибку дословно и с примером, не удержала от повторения через несколько часов. Это довод в пользу того, что уровень «временное» — не мягкая версия закрепления, а честное признание, что закрепления нет.
- **Промоут:** не промоутить — правило среды, живёт в `ds-rules §8`; выжимку читают перед приёмкой экрана.

### Л69. Эталон тестового корпуса был размечен несуществующими классами — и молча искажал вычисления геометрии

- **Дата/зона:** 06.09.2026 · `tooling/fixtures/*.html`, проверки `сенсор:Б4`, `сенсор:K1`
- **Симптом:** первый же прогон новой проверки «класс существует в ДС» дал FAIL на `_base.ok.html` — файле, который по определению без дефектов: `.rof__label`, `.crumbs__link`, `.tile__head`. Ни одного из них в ДС нет: у ReadOnlyField подпись — `.ds-label__text`, у крошек пункт — `.crumbs__item`, шапка тайла — `.tile__header`. Все 22 фикстуры унаследовали разметку от эталона.
- **Причина:** эталон писался «на глаз, по образцу похожих компонентов», а сверять его было нечем — проверки «класс существует» тогда не существовало. Дефект не проявлялся, потому что остальные сторожа строковые: `Б13` ищет `data-table`, `Б22` — сброс `margin`, и им безразлично, как называется соседний класс.
- **Правило:** тестовый корпус — такая же разметка ДС, как экран, и подчиняется тому же правилу «ничего не выдумывать». Эталон, собранный по памяти, превращает весь корпус в проверку выдуманного языка: сторожа доказываются на разметке, которой в продукте не бывает.
- **Закрепление:** исполняемое — `сенсор:Б4` сверяет каждый класс со `styles/*.css`, собственным `<style>` страницы и хуками рантаймов; корпус переведён на каноническую разметку (22 файла), доказано откатом (`fixtures/Б4.bad.html`), все 25 пар оракула по-прежнему сходятся.
- **Для приёмки:** дефект был не косметическим. Парсер тайлов читает подпись поля из `.ds-label__text` — при разметке `.rof__label` он получал ПУСТУЮ подпись у каждого поля, поэтому оценка «воздуха у правой кромки» (K1) считалась по одному значению вместо «подпись против значения». Отсюда и вечное `WARN K1` на эталоне из Л65: правило не шумело — оно правильно считало по искажённому входу. После правки воздух 352px → 327px; порог всё ещё превышен, но причина теперь одна, а не две.
- **Промоут:** выведен из общей выжимки 06.09.2026 — scope «оснастка самообучения», переехал в `skills/lessons/references/lessons.md`. Запись нужна тому, кто пишет сторожа, а выжимку screen-review читают перед каждой сборкой экрана.

### Л70. Проверка «класс существует» обязана знать про хуки рантайма и про склейку строк в JS — иначе она объявляет дефектом рабочий код

- **Дата/зона:** 06.09.2026 · `сенсор:Б4`; экраны `FundList`, `DealPipelineCard`, `Portfolio`
- **Симптом:** первая версия проверки дала FAIL на всех трёх реальных экранах. Находки двух видов: `.nav__burger` — класс без единого правила в CSS, и мусор вида `.sel`, `.extraClass`, `.tone`, `.+`, `.===`.
- **Причина:** две разные, обе про природу входа. `.nav__burger` — **хук рантайма**: правил в CSS у него нет и не должно быть, но по нему работает `ds-nav-panel.js` (`querySelector('.nav__burger')`), и снятие класса ломает компонент. Мусор — **склейка строк в JS**: атрибут `class="tc' + (extra ? ' ' + extra : '') + '"` обрывается регуляркой на первой кавычке, и в разбор попадают имена переменных, синтаксически неотличимые от классов.
- **Правило:** проверка существования класса определяет «существует» по трём источникам сразу — CSS дизайн-системы, собственный `<style>` страницы и селекторы, по которым ищут рантаймы. И отсекает не подозрительный токен, а весь атрибут со следами конкатенации: половина JS-выражения не является списком классов, и чинить её потокенно значит гадать.
- **Закрепление:** исполняемое — `сенсор:Б4` собирает хуки паттернами `closest`, `querySelector(All)`, `classList.add/remove/toggle/contains` (зеркало `collectHooks` в `ds-lint.js`) и пропускает атрибуты, содержащие кавычку, `+`, скобку или `?`. Проверено на трёх экранах: ложных срабатываний не осталось, две настоящие находки уцелели.
- **Для приёмки:** цена решения названа честно — класс, собранный из кусков в JS, эта проверка не видит вовсе. Ловить его пришлось бы разбором JS, и это не сделано.
- **Промоут:** не промоутить — закрыт исполняемым сторожем; выжимка держит незакрытое.

### Л71. Перед тем как писать сторожа, проверяется, доходит ли до инструмента его предмет

- **Дата/зона:** 06.09.2026 · `layout-check.mjs` (К10), `ds-lint.js` (R2, R4)
- **Симптом:** проверка К10 («в `<style>` не осталось flex-раскладки старого конструктора») была написана в сенсоре экранов, доказана парой фикстур и прошла оракул. Сработать она не могла ни разу: её предмет — страница документации docs-split, а сенсор такую страницу не принимает вовсе — на ней первыми падают Б1 и Б5 («нет каркаса экрана»), и до К-проверок дело не доходит. Селектора `.pg__controls` на экране не бывает, значит правило гарантированно молчало.
- **Причина:** решение «где живёт правило» принималось по чек-листу, в котором пункт записан, а не по тому, какой инструмент читает файлы этого вида. Оракул этого не ловит по устройству: фикстура — файл того вида, который инструмент принимает, поэтому доказательство откатом проходит, а на настоящем входе правило не работает. Тот же промах рядом: первая версия R2 искала `segctrl` по всей странице и дала 20 замечаний там, где сегмент-контрол стоит в демо документируемого компонента, а не в конструкторе — «класс есть на странице» и «класс есть в этом узле» разные утверждения.
- **Правило:** перед написанием сторожа называется его ВХОД — какие файлы инструмент реально получает и какой узел внутри файла является предметом. Если инструмент этого вида файлов не читает, правило пишется в другой инструмент, а не «поближе к чек-листу». Доказательство откатом подтверждает, что правило РАБОТАЕТ, но не подтверждает, что оно ПРИМЕНЯЕТСЯ.
- **Закрепление:** исполняемое — К10 переехал в линтер правилом `линтер:R4`, R2 сузился до поддерева `#pg-controls` (помощник `tagSlice`); оба доказаны откатом на новом корпусе `DS-IBP/fixtures/`, `verify` гоняет теперь два корпуса. У сенсора К10 снят: у правила один владелец (Л43).
- **Для приёмки:** признак того, что предмет до инструмента не доходит, — правило, не срабатывающее НИ РАЗУ на всём корпусе (61 страница ДС, три экрана). Ноль находок сам по себе не доказывает ни чистоты, ни дефекта; он требует объяснения.
- **Промоут:** выведен из общей выжимки 06.09.2026 — scope «оснастка самообучения», переехал в `skills/lessons/references/lessons.md`. Запись нужна тому, кто пишет сторожа, а выжимку screen-review читают перед каждой сборкой экрана.

### Л72. Пункт чек-листа с условием, реализованный без условия, даёт блокеры на рабочих страницах

- **Дата/зона:** 06.09.2026 · `ds-lint.js`, снятое правило R3 (пункт `чек-лист:К6`)
- **Симптом:** правило «порядок скриптов: рантаймы → `page.js` → `docs-split.js`» выдало **11 блокеров** на исправных страницах ДС — Chip, Divider, Modal, NavPanel, RiskMetric и других. Все они работают, все прошли приёмку.
- **Причина:** пункт К6 звучит как «Порядок скриптов важен», но содержит условие: «если общий/страничный скрипт подключён раньше рантайма **и читает его состояние в момент события**». Реализована была первая половина. На деле последним на странице подключается `ds-nav.js` — рантайм навигации, к конструктору отношения не имеющий; его положение безразлично. Проверить вторую половину статикой нельзя: сигнатура, которую называет сам чек-лист (чтение `aria-selected` в обработчике), у шести `*.page.js` встречается на СВОИХ демо-элементах, а не на состоянии рантайма.
- **Правило:** пункт, сформулированный как «X важен, если Y», реализуется целиком или не реализуется. Половина условия — не строгая версия правила, а другое правило: оно запрещает то, что разрешено. Если вторая половина статике недоступна, пункт объявляется суждением с указанием, ЧТО именно неразрешимо, — и это честнее, чем сторож, который придётся глушить исключениями.
- **Закрепление:** неизмеримое — `чек-лист:К6` помечен суждением в `coverage.json` с причиной и с числом ложных блокеров: вторая половина условия (читает ли скрипт состояние рантайма в момент события) статике недоступна. Исполняемого сторожа нет и не предвидится.
- **Для приёмки:** порог, на котором я остановился, стоит назвать: 11 блокеров на заведомо рабочих страницах — это не «стало строже», а «правило не то». Новый сторож, давший блокеры на всём корпусе сразу, разбирается поимённо до того, как его увидит кто-то ещё.
- **Промоут:** выведен из общей выжимки 06.09.2026 — scope «оснастка самообучения», переехал в `skills/lessons/references/lessons.md`. Запись нужна тому, кто пишет сторожа, а выжимку screen-review читают перед каждой сборкой экрана.

### Л73. Схлопывание обратных слэшей повторилось в новом обличье: python-heredoc вместо bash-heredoc

- **Дата/зона:** 06.09.2026 · правка `ds-lint.js` через `python - <<PY`
- **Симптом:** регулярка, записанная в скрипте как `/segctrl/`, легла в файл как `/^Hsegctrl^H/` — с двумя литеральными символами возврата на позицию (0x08). Файл разбирается, ошибок нет, правило совпадает с нулём строк. Обнаружено не прогоном, а предупреждением интерпретатора про недопустимую escape-последовательность.
- **Причина:** урок Л51 был записан про **bash**-heredoc, и на python-heredoc я его не перенёс, хотя схлопывает слэши тот же слой — оболочка, а не язык. Правило запомнилось как «не писать регулярки через bash», а не как «не писать регулярки через шелл».
- **Правило:** любой код с обратными слэшами пишется РЕДАКТОРОМ в файл, каким бы языком ни был обёрнут вызов. Замена, которая работает, когда правку нельзя сделать редактором на месте: записать вставку отдельным файлом (редактором) и срастить её скриптом, который только читает и склеивает — тогда через шелл не проходит ни одного слэша.
- **Закрепление:** временное — `правило ds-rules §8`, формулировка расширена с «через bash» до «через шелл», плюс названа замена (вставка файлом). Исполняемого сторожа нет: инструмент не видит командной строки агента. **Продлено 11.09.2026:** свойство среды (шелл схлопывает слэши в любом heredoc), закрепить исполняемо нечем.
- **Для приёмки:** дешёвый признак того же дефекта — регулярка, дающая ноль совпадений на заведомо непустом входе (Л56), и предупреждение интерпретатора про escape-последовательность. Оба видны сразу и оба были на экране.
- **Промоут:** не промоутить — правило среды, живёт в `ds-rules §8`.

### Л74. Сторож читает объявленную связь, а не угадывает её по имени файла

- **Дата/зона:** 06.09.2026 · `сенсор:Б12`; экран `Projects/post/operations-did/mainPage/index.html`
- **Симптом:** проверка «рядом лежит `<Имя>.screen.md`» дала FAIL на экране, у которого спека есть и заполнена. Экран лежит в своей папке как `index.html`, а спека названа по экрану — `mainPage.screen.md`; правило искало `index.screen.md`.
- **Причина:** связь «страница ↔ спека» правило выводило из имени файла, хотя спека объявляет её сама: в её YAML-шапке стоит `file: Projects/post/operations-did/mainPage/index.html`. Угадывание по имени работает ровно до первого исключения, а исключение здесь штатное — экран в собственной папке под именем `index.html`.
- **Правило:** если связь между файлами где-то ОБЪЯВЛЕНА, сторож читает объявление, а не восстанавливает связь по соглашению об именах. Соглашение — подсказка для человека, объявление — факт. Порядок такой: сперва пара по имени (быстро и покрывает большинство), не нашлась — поиск того, кто объявил этот файл своим.
- **Закрепление:** исполняемое — `сенсор:Б12` при промахе по имени просматривает `*.screen.md` в той же папке и берёт ту, чья строка `file:` указывает на эту страницу. Проверено на двух живых экранах: `Portfolio.html` (пара по имени) и `mainPage/index.html` (пара по объявлению) — оба PASS.
- **Для приёмки:** тот же вопрос стоит задавать всякий раз, когда правило связывает два файла: реестр, спека или манифест обычно уже содержит связь явно.
- **Промоут:** не промоутить — закрыт исполняемым сторожем.

### Л75. Фикстуры доказывают, что сторож срабатывает; ложные срабатывания ловятся только настоящими файлами

- **Дата/зона:** 06.09.2026 · `сенсор:Б2`, `сенсор:Б4`; удаление экранов из `Projects/test/`
- **Симптом:** из тестовой папки удалили два экрана, регресс-корпус сократился с трёх файлов до двух. Прогон по оставшемуся, ранее не проверявшемуся экрану сразу дал ложный FAIL давнего сторожа Б2: «неизвестные иконки: `' + name + '`». Оракул при этом был зелёный — все 27 пар фикстур сходились.
- **Причина:** фикстура отвечает на вопрос «сработает ли правило на дефекте» и не отвечает на вопрос «промолчит ли оно на исправном файле, устроенном иначе». Эталон один, и он неизбежно один по устройству; разнообразие даёт только корпус реальных файлов. Б2 читает разметку вместе со скриптами, а атрибут, собранный склейкой (`data-icon="' + name + '"`), регулярка обрывает на первой кавычке — ровно тот же дефект входа, что днём раньше нашёлся у Б4.
- **Правило:** доказательство откатом и прогон по реальным файлам — две РАЗНЫЕ проверки, ни одна не заменяет другую. Сокращение корпуса реальных экранов — это потеря проверки, а не уборка. И: дефект входа не бывает у одного правила — чинится общим помощником, а не заплаткой по месту, иначе третий сторож повторит его снова.
- **Закрепление:** исполняемое — общий помощник `isLiteralAttr` в сенсоре, через него читают значения атрибутов и Б2, и Б4. Проверено: ложное срабатывание на `mainPage/index.html` снято, оракул 27 пар остался зелёным.
- **Для приёмки:** при уменьшении числа доступных экранов новые сторожа проверять строже — ложные срабатывания станет не на чем поймать. Живой корпус сейчас: `Portfolio.html` (код 0) и `mainPage/index.html` (код 1, настоящая находка Б6 — на странице нет `h1`).
- **Промоут:** выведен из общей выжимки 06.09.2026 — scope «оснастка самообучения», переехал в `skills/lessons/references/lessons.md`. Запись нужна тому, кто пишет сторожа, а выжимку screen-review читают перед каждой сборкой экрана.

### Л76. Единица учёта — то, что доказывается отдельно; пункт с двумя частями требует двух идентификаторов и явной строки об остатке

- **Дата/зона:** 06.09.2026 · `ds-lint.js` (R6, R8), `сенсор:K9`, `coverage.json`
- **Симптом:** пункт `чек-лист:К9` состоит из двух независимых требований — (а) бинарный селект обязан нести подпись в `.lbl` и (б) самописная кнопка-тумблер в конструкторе запрещена. Первая версия реализовала обе части под одним идентификатором R6. Пара фикстур у идентификатора одна, значит доказанной оказывалась ровно одна ветка, а вторая жила непроверенной под видом закрытой. Рядом другое: `сенсор:K9` закрывает числовую половину пункта («полей в тайле»), а половина «блоков в зоне не больше 7–8» не закрывается вовсе — «зона» структурно не определена. В отчёте пункт при этом выглядел закрытым целиком.
- **Причина:** идентификатор назначался по строке чек-листа, а не по тому, что можно доказать откатом по отдельности. Отчёт о покрытии знал ровно два состояния — «закрыт» и «не закрыт», — и всё, что закрыто наполовину, попадало в «закрыт»: оставшаяся половина исчезала из всех списков сразу, и из рабочего, и из суждений.
- **Правило:** идентификатор заводится на то, что доказывается отдельной парой фикстур. Строка чек-листа с частями (а)/(б) — это два правила, а не одно. И если правило закрывает пункт частично, остаток называется ЯВНО: «закрыто» без оговорки читается как «закрыто целиком», а невидимый остаток не попадает ни в один рабочий список.
- **Закрепление:** исполняемое — часть (б) выделена в `линтер:R8` со своей парой фикстур (доказаны обе); в `coverage.json` заведена графа `partial`, `lessons-cli coverage` печатает её отдельным блоком «ЗАКРЫТО ЧАСТИЧНО» с указанием, что именно осталось. Сейчас там два пункта: `K9` и `К13`.
- **Для приёмки:** признак пункта, который нельзя закрывать одним идентификатором, — союзы «и», «также», буквы (а)/(б) и оговорки «кроме случаев…» в его формулировке.
- **Промоут:** выведен из общей выжимки 06.09.2026 — scope «оснастка самообучения», переехал в `skills/lessons/references/lessons.md`. Запись нужна тому, кто пишет сторожа, а выжимку screen-review читают перед каждой сборкой экрана.

### Л77. Вызов несуществующей функции в короткозамкнутом условии не падает — прогон по эталону такой сторож проходит

- **Дата/зона:** 06.09.2026 · `layout-check.mjs`, проверка `сенсор:З10`
- **Симптом:** новое правило было написано как `if (рядов > 1 && !rx.test(styleSrcEarly(raw)))`, а функции `styleSrcEarly` в файле не существовало. Прогон по эталону дал чистый результат, и правило выглядело рабочим. Ошибка обнаружилась не прогоном, а грепом по имени.
- **Причина:** `&&` вычисляет правую часть только если левая истинна. На эталоне ряд тайлов один, условие обрывается на первом сравнении, до вызова дело не доходит — `ReferenceError` не возникает. Так ведёт себя любая проверка, у которой есть дешёвый предикат-предусловие: на «нормальном» входе тело правила не исполняется вовсе.
- **Правило:** прогон по эталону доказывает только то, что правило МОЛЧИТ. Он не доказывает, что правило исполняется: у сторожа с предусловием на эталоне тело кода не выполняется ни разу. Поэтому пара фикстур заводится ВМЕСТЕ с правилом, а не после проверки «на эталоне чисто» — падающая фикстура это единственный вход, на котором тело сторожа действительно работает.
- **Закрепление:** исполняемое — правило встроено в саму процедуру: `lessons-cli verify` требует пару фикстур, и `.bad`-фикстура по определению проходит через тело сторожа. Дополнительно `сенсор:Б4` и `Б29` переведены на общий `styleSrc`, объявленный один раз рядом с остальным входом, — второй разбор того же `<style>` больше не заводится.
- **Для приёмки:** «на эталоне чисто» и «сторож работает» — разные утверждения, и первое достигается в том числе синтаксически неверным кодом. Второй раз за заход: до этого регулярка с испорченным escape давала ноль совпадений и тоже выглядела чистым прогоном (Л56, Л73).
- **Промоут:** выведен из общей выжимки 06.09.2026 — scope «оснастка самообучения», переехал в `skills/lessons/references/lessons.md`. Запись нужна тому, кто пишет сторожа, а выжимку screen-review читают перед каждой сборкой экрана.

### Л78. Дефолт, притворяющийся модификатором: класс есть в разметке и в спеке, а в CSS его нет — и он ничего не делает

- **Дата/зона:** 06.09.2026 · `styles/input.css`, `scripts/input-kit.js`, спеки семейства Input, `_cheatsheet.md`
- **Симптом:** класс `.inp--m` стоял в разметке 45 раз, объявлялся в таблице анатомии спеки как пара к `.inp--s` и упоминался в двух смежных спеках. Селектора `.inp--m` в `input.css` не существовало: размер M был дефолтом базового `.inp` через `--inp-*`. Класс читался как «включает размер M» и не делал ничего — снять его было бы незаметно.
- **Причина:** дефолт и модификатор выглядят одинаково с точки зрения разметки, поэтому расхождение накапливалось молча и в трёх местах сразу с разным знаком. Рантайм `input-kit.js` знал правду и намеренно класс НЕ выдавал (`size === 'm' ? '' : ...`); чит-шит фиксировал её в строке 539 — и противоречил сам себе в строке 515, перечисляя `.inp--m` среди классов; таблица анатомии спеки объявляла симметричную пару. Ни один источник не был «главным», поэтому каждый читатель выбирал свой.
- **Правило:** ось, у которой в документации перечислены варианты, обязана иметь ВСЕ варианты явными классами. Молчаливый дефолт допустим только там, где ось не названа: как только спека пишет «M / S», обе величины должны существовать в CSS, иначе половина оси — фикция. Симметрия здесь не эстетика: дефолт нельзя ни переопределить в другом контексте, ни увидеть в разметке, ни проверить.
- **Закрепление:** исполняемое — `линтер:B12` («`.inp` без размерного модификатора»), вход — разметка страницы и парный `*.page.js`, доказано откатом (`fixtures/B12.bad.html`). Правило понадобилось потому, что цена пропуска после правки стала обратной: поле без размерного класса теряет высоту, паддинги, gap и шрифт МОЛЧА — `height: var(--inp-h)` без значения даёт `height: auto`.
- **Для приёмки:** сторож окупился сразу — поймал место, пропущенное ручной инвентаризацией: `class="inp tfm__span-2"` внутри `table-filter.page.js`. Инвентаризация шла по полным значениям атрибута из HTML и не видела разметку, собираемую строками в JS. Правило: ломающую замену класса делать ВМЕСТЕ со сторожем на пропуск, а не после неё.
- **Промоут:** выведен из общей выжимки 06.09.2026 — scope «оснастка самообучения», переехал в `skills/lessons/references/lessons.md`. Запись нужна тому, кто пишет сторожа, а выжимку screen-review читают перед каждой сборкой экрана.

### Л79. Предел выжимки меряет не объём журнала, а стоимость чтения на КОНКРЕТНОЙ задаче — поэтому он берётся шардами, а не только сторожами

- **Дата/зона:** 06.09.2026 · `screen-review/references/lessons.md`, шарды `docs-split` и `lessons`
- **Симптом:** выжимка держалась на 43 записях и 281 строке при пределе 20/150. Сторожа за день закрыли рабочий список целиком, но каждый закрытый урок уводил из выжимки одну запись, а каждый новый добавлял другую — счётчик почти не двигался. Механически предел был недостижим: 43 записи честного содержания не ужимаются до 20 без выбрасывания смысла.
- **Причина:** предел читался как «журнал не должен разрастаться», а меряет он другое — сколько уроков агент вынужден держать в памяти, приступая к задаче. Из 43 записей 7 относились к раскатке страниц документации и 7 к оснастке самообучения (как писать сторожа, чем доказывать закрепление). Приёмщику экрана не нужны ни те, ни другие, и он читал их на каждой приёмке. Отдельно: 46 строк «механизма» в шапке выжимки были дословной копией скилла `lessons` — тот самый дефект «у правила несколько владельцев», описанный уроком Л43, внутри файла, который на Л43 ссылается.
- **Правило:** запись живёт в выжимке той роли, которая её применяет. Общий архив один и номера сквозные, а выжимок столько, сколько разных задач: у урока есть не только уровень закрепления, но и АДРЕСАТ, и «полезно вообще» не значит «нужно здесь». Сокращение выжимки — это две разные операции: вывести закрытое сторожем и вынести чужое по адресату; вторая даёт больше и не требует ни строчки кода.
- **Закрепление:** исполняемое — `lessons-cli state` печатает объём выжимки против предела и код выхода по долгу курации; после курации 43 → 14 записей, 281 → 119 строк, закрепление объявили 14 из 14, долг 0. Шапка сокращена до указателя на скилл-владелец, шарды заведены в `skills/docs-split/references/` и `skills/lessons/references/` с обратными ссылками.
- **Для приёмки:** признак чужой записи в выжимке — она называет файлы, которых на этой задаче не бывает (`#pg-controls` у приёмщика экрана, `fixtures/` у сборщика). Сторожа на «адресат не тот» нет: это суждение о задаче.
- **Промоут:** в выжимке скилла `lessons` с 06.09.2026 — правило про адресата относится к каждой следующей курации.

### Л80. Проверка регресса читала не то поле и молчала бы всегда
- **Дата/зона:** `.opencode/skills/screen-review/tooling/lessons-cli.mjs`, команда `stats`
- **Симптом:** ветка «регресс» написана, реестр якорей полон, дат закрепления найдено 12 — а подброшенный прогон с кодом `B8` (Л29 закрыт `линтер:B8` 06.09.2026) регрессом не признался. Отчёт печатал `РЕГРЕСС: 0` и выходил с кодом 0, то есть выглядел как чистый.
- **Причина:** якорь закрепления брался только из поля «Закрепление». У ВЫВЕДЕННОЙ из выжимки записи этого поля может не быть вовсе: правило живёт прозой в поле «Правило», а якорь — в строке «Промоут: … закреплён `линтер:B8`». Разбор пропускал ровно те 14 записей, ради которых проверка и написана, — закрытые.
- **Правило:** закрытие урока объявляется ДВУМЯ полями, и второе важнее. «Закрепление: исполняемое» — намерение; «Промоут: … закреплён якорь» — свершившийся факт, после которого урок перестали держать в памяти. Любая проверка, спрашивающая «чем закрыт урок», обязана читать оба, а днём закрытия считать позднейшую из двух дат. После правки якорей с датой стало 22 против 12.
- **Закрепление:** исполняемое — `правило:stats-регресс`, доказано откатом: подброшенная строка прогона с кодом `B8` и датой позже дня закрепления даёт `РЕГРЕСС: 1` и код выхода 1, на эталонном журнале — `РЕГРЕСС: 0` и код 0. Откат прогнан вручную: собственного корпуса фикстур у проверок `lessons-cli` нет, и это названный пробел, а не умолчание. Плюс счётчик «якорей с датой закрепления» печатается в шапке отчёта — ноль в нём виден сразу (класс Л56).
- **Для приёмки:** новая проверка, чей результат «находок нет», доказывается подброшенным дефектом ДО того, как её объявят работающей. «Ноль находок» и «проверка не применилась» с виду одинаковы.

- **Промоут:** выведен в шард 06.09.2026 — scope «оснастка самообучения», переехал в `skills/lessons/references/lessons.md`. Запись нужна тому, кто пишет сторожа, а выжимку screen-review читают перед каждой сборкой экрана (Л79).

### Л81. Поле состояния, которое правят позже кода, превращается во враньё
- **Дата/зона:** `screen-review/references/lessons-raw.md`, записи Л1–Л3
- **Симптом:** новое правило «срок жизни временного закрепления» на первом же прогоне указало на Л1, Л2 и Л3. У всех трёх «Закрепление: временное — пункт чек-листа, сторож посилен, не написан», а строкой ниже, в «Промоуте» той же записи: «закреплён `линтер:R6`» (у Л2 — `линтер:R4`, у Л3 — `линтер:R7`). Сторожа написаны и доказаны фикстурами в тот же день.
- **Причина:** уровень закрепления правится не там, где пишется сторож. Написал правило линтера, завёл пару фикстур, прогнал `verify`, поставил «Промоут» — и не вернулся в поле «Закрепление» той же записи. Поле осталось описывать состояние, которого больше нет.
- **Правило:** «Закрепление» — поле СОСТОЯНИЯ, а не истории, и правится той же правкой, что и код сторожа. Уровень поднимается в момент, когда `verify` впервые сказал ДОКАЗАН, а не «когда-нибудь при курации». Запись, объявляющая долг, которого нет, обманывает дважды: показывает несуществующую работу в очереди и выводит якорь из-под проверки регресса (`stats` сторожит только объявленные закрепления).
- **Закрепление:** исполняемое — `правило:Л-СРОК` в `lessons-cli check`: временное закрепление старше 10 прогонов без явного «продлено: причина» даёт находку; временное без даты — тоже (иначе уровень становится вечным молча). Доказано откатом: на подброшенных десяти строках прогона правило даёт находки по Л58/Л60/Л62 и по трём записям выжимки, на эталонном журнале — ноль.
- **Для приёмки:** расхождение «Закрепление» и «Промоут» внутри одной записи — блокер приёмки самой записи, а не мелочь оформления.

- **Промоут:** выведен в шард 06.09.2026 — scope «оснастка самообучения», переехал в `skills/lessons/references/lessons.md`. Запись нужна тому, кто пишет сторожа, а выжимку screen-review читают перед каждой сборкой экрана (Л79).

### Л82. Комментарий, объясняющий правило, это правило и выключил
- **Дата/зона:** `DS-IBP/scripts/ds-lint.js`, корпус фикстур `DS-IBP/fixtures/`
- **Симптом:** две новые пары фикстур из десяти не сработали. `F3.bad.html` содержит `.tabs--horiz` без `data-tabs`, `A6.bad.html` — контейнер страницы не `<main class="page">`. Оба дефекта внесены, оба правила живы, находок нет. Отладка показала: `data-tabs` на странице ЕСТЬ, `<main class="page"` ЕСТЬ.
- **Причина:** искомые строки стояли в шапке-комментарии самой фикстуры («ДЕФЕКТ: `.tabs--horiz` без `data-tabs`»). Линтер вырезал из разметки `<style>` и `<script>`, но не комментарии, и читал пояснение как разметку. То есть файл, ОБЪЯСНЯЮЩИЙ дефект, тем же объяснением закрывал правило, которое дефект ищет.
- **Правило:** комментарий — не разметка, и вырезается наравне со `<style>` и `<script>` у ЛЮБОГО инструмента, работающего строковыми правилами по HTML. Сырой текст остаётся доступен только тем правилам, чей предмет — сам комментарий (C1 читает карточку `@dsCard` первой строкой, G1 ищет маркер гейта). Это не про фикстуры: страница документации, поясняющая класс в комментарии, глушила бы правило ровно так же и молча.
- **Закрепление:** исполняемое — `линтер:F3` и `линтер:A6` (пары фикстур `F3.bad.html`, `A6.bad.html`): до починки инструмента они не срабатывали, после — падают на дефекте и молчат на эталоне. На 61 странице ДС отчёт не изменился (BLOCKER 0 · WARN 72 · INFO 48) — правка не «стала строже», она убрала способ молча ослепить правило.
- **Для приёмки:** сенсор экранов вырезал комментарии с 06.09.2026, линтер — нет, и асимметрия прожила до первого корпуса фикстур. Починив вход одного инструмента, проверь тот же вход у соседнего: они читают одни и те же файлы.

- **Промоут:** выведен в шард 06.09.2026 — scope «оснастка самообучения», переехал в `skills/lessons/references/lessons.md`. Запись нужна тому, кто пишет сторожа, а выжимку screen-review читают перед каждой сборкой экрана (Л79).

### Л83. У правила есть не только идентификатор, но и вход — и корпус доказательств строится по входу
- **Дата/зона:** корпуса фикстур линтера, `lessons-cli verify` / `stats`
- **Симптом:** после первого захода `stats` показал 40 правил линтера из 59 без единого срабатывания и без фикстуры. Список читался как «рабочий список на 40 пунктов», то есть как долг. Попытка закрыть его подряд упёрлась в то, что для части правил фикстуру нельзя сделать вообще, а не «пока не сделали».
- **Причина:** правила линтера разложены по трём разным входам, и корпус фикстур покрывает не все. Вход СТРАНИЦА (`say` внутри `pageChecks`) — доказывается файлом в `DS-IBP/fixtures/`. Вход ЭКРАН — тот же файл, но по пути ВНЕ дерева ДС: A7, F6, L4, L5, G1 гоняются только когда путь начинается с `pages/screens/` или `../`, и в `DS-IBP/fixtures/` не срабатывают никогда. Вход РЕПОЗИТОРИЙ (`out.push` в глобальной секции) — реестры, все `styles/*.css`, все `scripts/*.js`, дерево файлов: файловой фикстурой не доказывается в принципе.
- **Правило:** прежде чем заводить пару фикстур, ответь, что у правила на входе — файл, файл-по-пути или репозиторий. Корпус заводится по входу, а не по инструменту: у одного линтера их два (`DS-IBP/fixtures/` для страниц, `Projects/test/fixtures/` для экранов). Правила репозиторного входа объявляются границей механизма, а не долгом, и различаются машинно — по способу отчёта (`out.push` против `say`), а не списком в файле.
- **Закрепление:** исполняемое — `правило:stats-вход`: `lessons-cli stats` печатает отдельную строку «ВХОД — РЕПОЗИТОРИЙ, файловой фикстурой не доказывается» и считает её из кода линтера каждым прогоном. Плюс третий корпус заведён и доказан: `verify` печатает три корпуса раздельно, рабочий список линтера 40 → 7.
- **Для приёмки:** «не доказано» и «этим механизмом не доказывается» — разные строки отчёта. Смешивать их значит держать вечный долг, который никто никогда не закроет, и прятать в нём настоящий.

- **Промоут:** выведен в шард 06.09.2026 — scope «оснастка самообучения», переехал в `skills/lessons/references/lessons.md`. Запись нужна тому, кто пишет сторожа, а выжимку screen-review читают перед каждой сборкой экрана (Л79).

### Л84. Дата fidelity-гейта сверяется только на перепрогоняемых экранах, а не на статичных фикстурах
- **Дата/зона:** 07.09.2026 | DS-IBP/scripts/ds-lint.js (правило G1), Projects/test/fixtures
- **Симптом:** `lessons-cli verify` отдавал «НЕ ДОКАЗАН G1 — правило срабатывает и на эталоне»: правка правила G1, «молчит на эталоне», не выполнялась.
- **Причина:** фикстура — статичный тестовый файл, fidelity-гейт у неё не перепрогоняется, но правило G1 сравнивало дату маркера с «сегодня». При смене календарного дня эталон (`Projects/test/fixtures/_base.ok.html`, дата 06.09.2026) начинал давать WARN по дате — но не потому, что экран «правился», а потому, что дата маркера физически отстала от календаря. Это артефакт инструмента проверки, а не регресс разметки.
- **Правило:** WARN по устаревшей дате fidelity-гейта применяется только к реальным перепрогоняемым экранам; к файлам в каталоге `fixtures/` не применяется. BLOCKER об отсутствии маркера действует на всех.
- **Закрепление:** исполняемое — «линтер:G1» (в условие даты добавлено `!p.split('/').includes('fixtures')`). Доказано откатом: `_base.ok.html` молчит на G1, `G1.bad.html` падает по G1, `lessons-cli verify` — 60/60.
- **Промоут:** не промоутить — закрыт исполняемым сторожем «линтер:G1», доказан откатом; держать в памяти ролей не нужно. Сторож снят 13.09.2026 вместе с гейтом фиделити (невыполним на рабочем контуре).

### Л85. Расстояние между блоками принадлежит каркасу — экран его не выдаёт себе сам
- **Дата/зона:** 10.09.2026 | `Projects/post/operations-did/Portfolio-did/Portfolio.html`, `DS-IBP/pages/foundations/Layout.html`, `DS-IBP/scripts/ds-lint.js`
- **Симптом:** у экрана «Текущий портфель ДИД» в собственном `<style>` жило `.dtable { margin-bottom: var(--space-8) }` — низ таблицы 32px против 24px у всех остальных экранов. Ни один гейт этого не видел: страница проходила линтер, сенсор раскладки давал OK, паспорт экрана честно описывал 32px как решение. Расхождение нашлось только при сверке экрана со страницей каркаса.
- **Причина:** каркас описывал зоны и поля, но не описывал раскладку экрана-реестра — состав блоков и расстояния между ними. Экран, которому нужно было расстояние, а в ДС его «нет», добрал недостающее своим `margin`. Величина при этом не проходит ни одной проверки: токены проверяются, шкала проверяется, а margin у чужого класса — нет. Правило L4 этот класс уже стерегло, но узко: только `padding` у `.screen__content` и переопределение `--layout-*`.
- **Правило:** расстояния МЕЖДУ блоками даёт каркас — зазор контентной области и её поля. Экран не выдаёт блоку ДС собственный `margin` ни при каких обстоятельствах: нужного расстояния нет в ДС — значит его нужно описать в каркасе, а не добрать локально. Собственных классов экрана правило не касается — там отступ внутренний.
- **Закрепление:** исполняемое — `линтер:L6` (пара фикстур `Projects/test/fixtures/L6.bad.html` / `_base.ok.html`). Правило срабатывает, только когда селектор в `<style>` экрана ссылается на класс, объявленный в CSS дизайн-системы; на четырёх реальных экранах и на эталоне молчит, на фикстуре падает — `lessons-cli verify`: 61/61. Первый цикл живёт как WARN по правилу расширения линтера (`ds-integrity-check.md`, «Как расширять», п.4). Чинился существующий сторож L4? Нет — у L4 другой предмет (поля и токены каркаса), поэтому заведена соседняя ветка той же группы, а не правило поверх.
- **Промоут:** не промоутить — закреплён сторожем `линтер:L6`, помнить правило наизусть больше не нужно
- **Для приёмки:** «в ДС нужного значения нет» — это не разрешение придумать значение, а сигнал, что в ДС не описан случай. Дефект такого рода не падает и выглядит нормально: он виден только при сверке экрана с документацией, поэтому и понадобился сторож.

### Л86. Объявленный в спеке компонент проверяется на существование кода, а не на упоминание
- **Дата/зона:** 10.09.2026 | DS-IBP/specs/Tile.md, pages/organisms/Tile.html, styles/tile.css, pages/organisms/Kanban.html
- **Симптом:** при проектировании канбана карточка едва не была заведена новой сущностью `.kcard`. Card уже объявлен в ДС как родственник Tile: указаны тип хэдера (TileHeader Card), Title `--type-h6-strong`, отступы контента 8/16/20/16 и полный набор состояний «Default / Hover / Active / Move / Disabled» — включая Move, то есть перетаскивание. При этом `grep -i card styles/tile.css` не находит ни одного правила: кода у Card нет и не было.
- **Причина:** объявление живёт в трёх местах (таблица «Tile · TileHeadless · Card» на странице, раздел «Когда не использовать», строка в спеке), и все три читаются как описание существующего компонента. Отличить «описано и реализовано» от «описано и забыто» можно только проверкой CSS, а её никто не делает: `spec-audit.mjs` ищет правила, объявленные внутри реализованных компонентов, и не проверяет, существует ли код у объявленного родственника. Дефект молчаливый: страница Tile выглядит полной и непротиворечивой.
- **Правило:** упоминание компонента в спеке или на странице соседа **не является доказательством его существования**. Прежде чем строить на объявленном компоненте или заводить взамен новый, проверяется наличие правил в `styles/<имя>.css`. Класс шире Card: любой «родственник», «вариант» и «режим», названный в прозе, но не имеющий своего CSS, — это дыра, а не компонент.
- **Закрепление:** исполняемое — `аудит:6` (11.09.2026): класс, названный в спеке в обратных кавычках или в примере `class="…"`, обязан иметь правила в `styles/*.css` ЛИБО роль в рантайме/на странице. Проход молчит, если класс есть в `scripts/*.js` (хук: `.nav__burger`, `.dpk__*`, `.preset-*`) или в разметке/`<style>` страницы (зона линтера `P5`); находка — то, чего нет нигде (случай Card/`.tile--card`). Доказано ручным откатом: зонд `.zzz-pass6-not-in-css` даёт находку, после удаления — нет. Первый прогон дал 23 находки; после уточнения входа и приведения трёх мёртвых ссылок в спеках к конвенции ёлочек (`dtable__edge` удалён, `chip--readonly` — не модификатор, `x--has-tooltip` — плейсхолдер) — 0. Экземпляр Card закрыт ранее: реализован как `.tile--card` в `styles/tile.css` (Tile 1.012).
- **Промоут:** выведен 11.09.2026 — закреплён `аудит:6`. В выжимке остаётся только ветка Л42 как ссылка на покрытие, отдельной записью не заводится: выжимка обязана сокращаться.

### Л87. Клавиатурный и указательный путь одного действия проверяются одним тестом на итоговое состояние
- **Дата/зона:** 10.09.2026 | DS-IBP/pages/rnd/Kanban.html, прототип перетаскивания
- **Симптом:** в прототипе канбана `Esc` при мышином перетаскивании возвращал карточку на место, а при клавиатурном — только снимал класс захвата и объявлял в `aria-live` «Перенос отменён». Карточка при этом оставалась там, куда её увели стрелки. Сообщение было враньём, отмены не происходило. Ничего не падало: оба пути по отдельности выглядели рабочими.
- **Причина:** мышиный и клавиатурный пути написаны как две независимые реализации одного действия. У мышиного состояние отката (`drag.from`, `drag.fromNext`) заводится в `beginDrag`, у клавиатурного аналога не завели вовсе — при захвате запоминался только сам элемент. Расхождение невидимо при чтении кода: обе ветки короткие и обе «про Esc». Проверка глазами его тоже не ловит — клавиатурный путь при ручной проверке обычно доводят до конца, а не отменяют на середине.
- **Правило:** если у действия есть клавиатурный эквивалент (а по WCAG 2.1.1 у перетаскивания он обязан быть), оба пути проверяются **одним тестом, сравнивающим итоговое состояние DOM**, а не наличием обработчика. Отмена проверяется отдельно от завершения: путь «начал → отменил» ломается чаще, чем «начал → закончил», потому что его реже проходят руками. Отмена обязана возвращать состояние, а не снимать флаг.
- **Закрепление:** неизмеримое — требует прогона обоих путей на живом DOM, что линтером и статическим сенсором не берётся. В заходе закреплено тестом `run-esc.mjs` (linkedom, скратчпад): берёт вторую карточку колонки, уводит стрелками на две колонки, жмёт Esc и сверяет и колонку, и позицию внутри неё. Тест захода, не сторож ДС: чтобы стать исполняемым, нужен компонент в ДС с рантаймом, которому такой тест можно приписать.
- **Промоут:** не промоутить — узкий случай: применим только к компоненту с клавиатурным эквивалентом указательного действия, сегодня такой один (прототип канбана). Промоутить, когда появится второй.

### Л88. Часть UI компонента живёт вне его поддерева — владельца нельзя искать от цели клика
- **Дата/зона:** 10.09.2026 | DS-IBP/scripts/ds-kanban.js, pages/organisms/Kanban.html
- **Симптом:** в рантайме канбана не работали три действия из меню — удалить карточку, удалить колонку, свернуть колонку. Звезда и «добавить карточку» при этом работали. Ничего не падало: обработчик молча выходил раньше, чем доходил до нужной ветки.
- **Причина:** делегированный обработчик начинался со строки `var board = boardOf(e.target); if (!board) return;` — доска бралась от цели клика. Но меню компонента живут не внутри него: `DSMenu` (как `DSPopover`, `DSTooltip` и вообще всё, что уходит в `DSFloat`) держит разметку в общем слое рядом с `</main>`, вне поддерева доски. Клик по пункту меню приходит с цели, у которой нет предка-владельца, и `closest('[data-kanban]')` возвращает null. Работали ровно те действия, чьи кнопки физически лежат в разметке компонента.
- **Правило:** в делегированном обработчике владелец берётся **от цели только для элементов, лежащих внутри компонента**. Для всего, что рантайм ДС выносит в общий слой — меню, поповеры, тултипы, модалки, — владелец восстанавливается из запомненного триггера открытия, а ветки этих действий стоят **до** проверки `if (!owner) return`. Признак дефекта: часть пунктов меню работает, часть молчит.
- **Закрепление:** неизмеримое — требует прогона на живом DOM: статикой не отличить «пункт меню внутри компонента» от «пункта в общем слое». В заходе закреплено тестом `t3-actions.mjs` (linkedom, скратчпад): кликает по кебабу, затем по пункту, и сверяет итог — удалилась ли карточка, переехали ли карточки удалённой колонки, встал ли класс свёрнутости. Тест захода, не сторож ДС.
- **Промоут:** в выжимке — класс шире канбана: касается любого компонента с меню, поповером или тултипом, то есть почти каждого нового организма.

### Л89. Клон шаблона очищается от всего, что идентифицирует оригинал, а не только от классов состояния
- **Дата/зона:** 10.09.2026 | DS-IBP/scripts/ds-kanban.js, функция addCard
- **Симптом:** после «Добавить карточку» удаление другой карточки удаляло не ту. На доске оказывались две карточки с одним `data-kb-card`: новая клонировалась из первой попавшейся и уносила её идентификатор. Визуально всё правильно — у клона свои заголовок и код в подвале, потому что их переписывали явно; расходился только атрибут.
- **Причина:** очистка клона делалась по списку «что бросается в глаза»: снял классы состояния (`--ghost`, `--selected`, `is-move`), переписал текстовые узлы. Идентифицирующий атрибут в этот список не попал, потому что он невидим. Классическая ошибка неполного перечисления: чистится то, что видно на экране, а не то, по чему работает код.
- **Правило:** клонируя узел-шаблон, чистить его по двум спискам сразу: **видимое** (классы состояния, тексты, `aria-pressed` и прочие переключатели) и **адресное** — `id`, `data-*`-идентификаторы, `for`/`aria-controls`/`aria-labelledby`. Второй список важнее: дубль идентификатора не виден глазами, но ломает любое действие, которое ищет узел по нему. Признак — «действие сработало не на том элементе» после добавления нового.
- **Закрепление:** неизмеримое — требует прогона: статикой не понять, какой атрибут в этом компоненте адресный. В заходе закреплено тестом `t3-actions.mjs`: добавляет карточку, удаляет другую по её идентификатору и проверяет, что исчезла именно она. Тест захода, не сторож ДС.
- **Промоут:** не промоутить — узкий случай: применим к рантайму, который клонирует узел-шаблон, а таких в ДС единицы. Промоутить, если повторится.

### Л90. Guard `window.DSX && DSX.method` при опечатке в имени метода не падает, а бесшумно выключает функциональность
- **Дата/зона:** 10.09.2026 | DS-IBP/scripts/kanban.page.js, scripts/ds-kanban.js, scripts/ds-lint.js
- **Симптом:** на живой странице Kanban у сегмент-контрола не появлялась белая «таблетка» под выбранным сегментом — контрол выглядел сломанным. В том же заходе нашлись ещё два таких же: модалка подтверждения удаления не закрывалась после нажатия «Удалить», иллюстрация пустого состояния не рисовалась. Три разных дефекта, одна причина. Ни один не давал ошибки в консоли, ни один не видел ни один гейт.
- **Причина:** принятая в ДС идиома отложенной привязки — `if (window.DSTabs && DSTabs.bindAll) DSTabs.bindAll(stage)`. Guard защищает от отсутствия рантайма, но при опечатке в ИМЕНИ МЕТОДА он тоже ложен, и ветка просто не исполняется. У `DSTabs` метода `bindAll` нет — экспорт `{tabs, segment, positionThumb, wireAll}`; у `DSModal` есть `closeTop`, а звали `close`; у `DSIllustrations` есть `render`, а звали `apply`. Имя метода — единственное, что не совпадало, и проверить его было некому: классы на месте, разметка валидна, рантайм подключён.
- **Правило:** имя метода рантайма берётся из его экспорта, а не из памяти. Идиома с guard'ом остаётся (она защищает от неподключённого рантайма), но само имя обязано быть сверено машинно: дефект этого класса невидим и в консоли, и на гейтах — только глазами на живой странице, то есть в лучшем случае у ревьюера, в худшем у пользователя.
- **Закрепление:** исполняемое — правило **A9** линтера (`DS-IBP/scripts/ds-lint.js`, `runtimeApiCheck`, уровень BLOCKER): собирает ключи из `window.DSX = {…}` каждого рантайма и сверяет с обращениями `DSX.method(` во всех `scripts/*.js` и `pages/**/*.html`; комментарии вырезаются (шапка `ds-float.js` законно упоминает приватный `DSModal.lockPage()`), неизвестный namespace пропускается. Правило репозиторного уровня (`out.push` в `globalChecks`), поэтому файловой фикстурой не доказывается — `lintRepoIds()` относит его к тем, у кого вход не файл, а репозиторий. Доказано ручным откатом: возврат `DSTabs.bindAll` в `kanban.page.js` даёт `BLOCKER A9`, откат обратно — `BLOCKER 0`. На момент заведения правило нашло в репозитории ровно три промаха (все три починены) и ни одного ложного срабатывания. Зафиксировано в `MAINTAINING.md` п. 12.
- **Промоут:** не промоутить — закрыт исполняемым сторожем A9, а выжимка держит только то, за что не отвечает код. Класс широкий (идиома употреблена в ДС десятки раз), но помнить его человеку больше не нужно: линтер падает сам.

### Л91. `position: sticky` держит элемент только внутри его контейнера — короткий контейнер отпускает шапку и отнимает цель попадания [ИСПРАВЛЕНО 10.09.2026, см. ниже]
- **Дата/зона:** 10.09.2026 | DS-IBP/styles/kanban.css, .kanban__track
- **Симптом:** на живой доске шапки колонок «то держатся, то слетают» при вертикальной прокрутке — в одной колонке шапка остаётся наверху, в соседней уезжает вместе с карточками. В том же месте вылез второй симптом, который выглядел отдельным дефектом: карточку нельзя было положить в колонку, если поднести её ниже последней карточки этой колонки — подсветки нет, дроп не срабатывает.
- **Причина:** у трека стояло `align-items: flex-start` с комментарием «колонки не равняют высоту друг с другом». Это давало каждой колонке высоту по её содержимому. Дальше сработали два разных механизма от одной причины: (1) `position: sticky` удерживает элемент в пределах его контейнера, и как только короткая колонка целиком уезжает вверх, шапке негде липнуть — колонка с двумя карточками отпускала её через 200px прокрутки, колонка с шестью держала до конца; (2) цель дропа ищется `elementFromPoint → closest('.kbcol')`, а ниже дна короткой колонки нет никакого `.kbcol` — там пустой трек, и класть карточку физически некуда.
- **Правило (ПЕРВАЯ ВЕРСИЯ, ОШИБОЧНАЯ):** ~~элемент с `position: sticky` живёт ровно столько, сколько его контейнер… в flex-раскладке это значит «не трогать `align-items`»~~. Рецепт неполон: он молчит про то, что сам скроллящийся контейнер тоже может быть flex-рядом. По этому рецепту в тот же день сделали правку, снявшую `flex-start`, — и получили ВТОРОЙ режим отказа того же места: `stretch` на flex-контейнере, который сам скроллпорт, растягивает детей по высоте ВИДИМОЙ ОБЛАСТИ, а не по высоте самого длинного из них (у скроллпорта высота определённая). Симптом стал хуже: раньше отваливались короткие колонки, теперь — все.
- **Правило (верное, 10.09.2026):** скроллпорт и ряд равной высоты ОБЯЗАНЫ быть разными элементами. `.kanban__track` развели на `.kanban__viewport` (скролл, `overflow: auto`, высота определённая — `flex: 1; min-height: 0`) и `.kanban__track` внутри него (ряд, высота `auto`). Растягивание по высоте самого длинного ребёнка (`stretch`) возможно ТОЛЬКО когда высота контейнера сама `auto` — то есть контейнер не может быть тем же элементом, что скроллпорт. «Не трогать `align-items`» остаётся верным, но недостаточным без этого разведения.
- **Закрепление:** исполняемое — правило `B13` линтера (`scripts/ds-lint.js`, `stickyInHorizontalScrollCheck`, WARN, репозиторного уровня): горизонтальный `display: flex` (без `flex-direction: column`) с `overflow: auto|scroll` в файле, где объявлен `position: sticky`, — подозрительная комбинация. Доказано ручным откатом: возврат `overflow: auto` на `.kanban__track` даёт `WARN B13`, откат — тихо. На момент заведения — 0 срабатываний на дереве (`.kanban__track` был единственным таким контейнером в ДС).
- **Промоут:** выведен 11.09.2026 — закреплён `линтер:B13` (исполняемое, репозиторного входа; доказано ручным откатом). Класс шире канбана, но помнить его ролям не нужно: сторож ловит сочетание «горизонтальный flex-скроллпорт + sticky» в любом файле `styles/*.css`.

### Л92. Модификатор состояния слабее `:hover` по специфичности — состояние объявлено, но никогда не видно
- **Дата/зона:** 10.09.2026 | DS-IBP/styles/kanban.css, .kbcol--drop / .kbcol--selected
- **Симптом:** при перетаскивании карточки колонка-цель не подсвечивалась вовсе. Рантайм класс ставил исправно, в DevTools он на элементе виден, правило в CSS написано — а тона нет.
- **Причина:** `.kbcol:hover` — это (0,2,0), голый модификатор `.kbcol--drop` — (0,1,0), и порядок в файле здесь ни при чём: специфичность решает раньше. Серый hover-тон затирал синий тон цели. Случай не редкий, а единственно возможный: летящий клон несёт `pointer-events: none`, поэтому колонка под курсором ВСЕГДА в `:hover` — то есть состояние, ради которого правило написано, и состояние, которое его гасит, наступают одновременно.
- **Правило:** модификатор состояния, конкурирующий за то же свойство, что и правило с псевдоклассом, обязан быть не слабее его: `.x.x--mod` вместо `.x--mod`. Плюс, если состояние взаимоисключающе с наведением (перетаскивание, захват), псевдокласс на время жеста гасится явно — `.host.is-dragging .x:hover { … }`. Признак дефекта: «класс на элементе есть, стиля нет», причём именно под курсором.
- **Закрепление:** временное — сторож не написан, но форма известна и проверена прототипом. Правило-кандидат: для каждого `styles/*.css` собрать свойства, объявленные в `.X:hover`, и сверить с `.X--mod` того же корня — пересечение свойств при меньшей специфичности есть подозрение. Прототип (`probe-spec.mjs`, скратчпад) на текущем дереве даёт 12 кандидатов в 7 файлах, и они разнородны: `.kbcol--selected` — настоящий дефект (починен здесь же), а `.ddl__item--selected` и `.nav__item--selected` скорее намеренны — наведение на выбранный пункт и должно перекрывать выбор. Поэтому правило заводится отдельным заходом, уровнем WARN и с белым списком причин (стиль `SHRINK_OK` / `SCROLL_GEOMETRY_OK`), после разбора всех 12 случаев. Записать его сейчас значило бы завести сторожа, который с первого дня шумит на девяти легальных местах. В этом заходе закреплено инвариантом в `specs/Kanban.md` и строкой диагностики в чит-шите. **Продлено 11.09.2026:** сторож требует белого списка после разбора 12 кандидатов (9 легальны) — заводится отдельным заходом уровнем WARN, до этого уровень остаётся временным.
- **Промоут:** в выжимке — класс общий для всей ДС: 12 мест на текущем дереве, и каждое новое состояние компонента добавляет кандидата.

### Л93. Разметка, пересобранная через `innerHTML`, теряет все привязки рантаймов — и молча
- **Дата/зона:** 10.09.2026 | DS-IBP/scripts/kanban.page.js, функция render
- **Симптом:** на странице Kanban не открывалось ни одно меню — ни кебаб доски, ни кебаб колонки, ни кебаб карточки. Разметка меню на странице есть, `ds-menu.js` подключён, кнопки с `data-menu` на месте. Ничего не падает, клик просто ничего не делает. Человек прочитал это как «меню убрали», а не как «меню сломаны», — и был прав по факту: функциональности не было.
- **Причина:** витрина пересобирает доску целиком (`stage.innerHTML = board(state)`) на каждое изменение конструктора. Рантаймы ДС связывают свою разметку один раз, на `DOMContentLoaded`, — новые узлы после этого никем не связаны. В `render()` были позваны `dsIcons.apply` и `DSKanban.bind`, а `DSMenu.bindAll` — нет. Забыть привязку легко именно потому, что часть жизни возвращается сама: иконки и сам компонент оживают, и страница выглядит рабочей.
- **Правило:** после любой пересборки поддерева через `innerHTML` вызывается повторная привязка ВСЕХ рантаймов, чья разметка попала внутрь, — не только «главного» компонента. Список для ДС: `dsIcons.apply(scope)`, `DSMenu.bindAll(scope)`, `DSModal.bindAll(scope)`, `DSDrawer.bindAll(scope)`, `DSTabs.wireAll(scope)`, `DSTooltip` не нужен (он делегирует на документ). Практика: заводить в витрине одну функцию `rebind(scope)` и звать её из единственного места, а не перечислять вызовы по месту — тогда новый рантайм добавляется одной строкой.
- **Закрепление:** временное — пункт чек-листа и правило `MAINTAINING.md` п. 13. Исполняемым не сделано: статикой не отличить «пересобрал и связал» от «пересобрал и забыл» — набор рантаймов внутри поддерева известен только в рантайме. Частично прикрыто соседним сторожем: правило **A9** ловит случай, когда привязку позвали, но именем несуществующего метода (`DSTabs.bindAll` вместо `wireAll` — второй дефект той же страницы, урок Л90). **Продлено 11.09.2026:** статикой не отличить «пересобрал и связал» от «пересобрал и забыл» — набор рантаймов внутри поддерева известен только в рантайме; правило живёт в `MAINTAINING.md` п. 13.
- **Промоут:** в выжимке — касается каждой витрины с конструктором, то есть каждой страницы ДС на docs-split.

### Л94. Номер сторожа берётся из реестра после того, как сторож написан, — а не назначается по ходу письма
- **Дата/зона:** 11.09.2026 | DS-IBP/styles/sub-tab.css, DS-IBP/specs/SubTab.md, DS-IBP/scripts/ds-lint.js
- **Симптом:** в шапке нового CSS и в инвариантах новой спеки стояло «охраняется правилом B13 линтера». Сторожа для этого класса дефекта не существовало вовсе, а номер `B13` был уже занят — им подписано правило про горизонтальный flex-скроллпорт со sticky. Обе записи выглядели как обычная ссылка на действующее правило и прошли бы в ДС незамеченными: страница собиралась, гейт был зелёный, `anchors` молчал — идентификатор-то живой.
- **Причина:** ссылка на сторожа писалась в тот момент, когда формулировалось правило применения компонента, то есть до кода сторожа. Номер в такой момент неоткуда взять, кроме как «следующий на вид свободный», а список занятых номеров в голове не держится: `B13` был заведён накануне и в шапке `ds-lint.js` прозой не перечислен. Существующие сторожа этот случай не ловят по устройству: `anchors` сверяет реестр с кодом и на живой идентификатор не реагирует, а совпадает ли ПРЕДМЕТ правила с тем, что о нём написано в чужом файле, не проверяет ничто.
- **Правило:** утверждение «охраняется правилом `<ID>`» пишется только после того, как сторож написан и прогнан, а `<ID>` берётся из `anchors --write`, а не назначается по памяти. Порядок внутри захода: сначала правило в `ds-lint.js` + пара фикстур, затем `verify`, затем `anchors --write`, и только потом ссылка на номер в CSS, спеке, чит-шите и правилах. Класс шире одного номера: это Л42 («правило объявлено — реализации нет») в обратную сторону — реализация есть, но чужая, и ссылка врёт молча.
- **Закрепление:** временное — MAINTAINING.md, «Новый компонент», шаг про сторожа; исполняемым не делается честно: проверить можно только существование идентификатора (`anchors` это уже делает, и на моём дефекте оно молчало бы — номер был живой), а совпадение предмета правила с текстом ссылки требует суждения. **Продлено 12.09.2026:** сторож невозможен по предмету, а не по нехватке рук — сверить, что `<ID>` в чужом файле охраняет ИМЕННО этот класс дефекта, машина не может; уровень «неизмеримое» тоже неверен, потому что пункт чек-листа существует и выполняется.
- **Промоут:** в шарде `lessons` (автор правила) — 11.09.2026

### Л95. Паддинг на элементе с inset-рейлом не сдвигает линию, а удлиняет её
- **Дата/зона:** 11.09.2026 | DS-IBP/styles/docs-split.css, styles/tab.css, scripts/ds-lint.js
- **Симптом:** ряд табов «Конструктор / Документация / Код» на всех страницах документации рисовался как полоска подчёркивания 24px, потом табы, потом снова полоска. Компонент Tab при этом чистый: боковых паддингов на `.tabs` в `tab.css` нет.
- **Причина:** рейл ряда — `box-shadow: inset 0 -1px 0 0 var(--tab-track)`, а inset-тень рисуется по PADDING-BOX. `docs-split.css` ставил `padding: 10px 24px 0` на сам `.tabs`: содержимое отодвинулось, линия — нет, она стала длиннее содержимого на 24px с каждой стороны. Правило «отступ ряда задаёт обёртка без рейла» в ДС было, но жило комментарием внутри `tab.css`, который автор хрома документации не читает.
- **Правило:** элемент, несущий рейл (inset-линию с одним ненулевым смещением), не принимает паддинг по оси этой линии — ни в CSS компонента, ни в хроме страницы, ни инлайном на экране. `inset 0 ±N` (линия горизонтальная) запрещает горизонтальный паддинг, `inset ±N 0` — вертикальный. Отступ задаётся обёрткой без рейла.
- **Закрепление:** исполняемое — `линтер:B15`. Вход правила — репозиторий (все `styles/*.css`), файловой фикстурой не доказывается по построению; доказано откатом на реальном файле: возврат `padding: 10px 24px 0` в `docs-split.css` ловится с указанием файла-источника рейла, на исправленном CSS правило молчит. Ложняки отсечены двумя признаками: кольцо (`inset 0 0 0 N`) не рейл, ветка селектора с комбинатором (`.btn-group--accent > .btn + .btn`) — разделитель НА элементе группы, он обязан идти во всю его сторону.
- **Для приёмки:** смотреть, что B15 в глобальном прогоне чист, а не разглядывать концы рейла на рендере.
- **Промоут:** выведен 11.09.2026 — закреплён `линтер:B15` (исполняемое); в выжимке не держим

### Л96. Предикат проверяется перед ПОСТРОЕНИЕМ разметки, а не перед показом
- **Дата/зона:** 11.09.2026 | DS-IBP/scripts/ds-tooltip.js, pages/{molecules/Tab,atoms/Chip,organisms/Entity,organisms/Table}.html, scripts/ds-lint.js
- **Симптом:** на странице Tab при наведении на любой таб рядом с подписью появлялась её копия и оставалась навсегда. Тултипа при этом не было — был его текст, набранный как обычный контент внутри компонента.
- **Причина:** две вещи, каждой по отдельности мало. Первая — `DSTooltip.truncated()` в ветке `pointerover` строил разметку (`.tip-anchor` вокруг подписи + `.tip` рядом) для ЛЮБОЙ подписи под селектором, а усечение проверял позже, в `allowed()` внутри `show()`: показ гасился, узлы оставались в DOM. Ветки `focusin` и `refresh()` того же механизма проверяли усечение ДО построения — три ветки одного предиката разошлись. Вторая — четыре страницы подключали `scripts/ds-tooltip.js` без `styles/tooltip.css`; рантайм объявляет зависимость в шапке файла, но не догружает её, и неоформленный `.tip` рисуется обычным блоком с текстом.
- **Правило:** рантайм, который СТРОИТ разметку по условию, проверяет это условие в точке построения, а не в точке показа — иначе отказ от показа оставляет мусор в чужом компоненте. И: страница, подключившая рантайм ДС, подключает его CSS; рантаймы стилей не догружают.
- **Закрепление:** исполняемое — `линтер:A1` (пара `ds-tooltip.js` → `tooltip.css` уровнем BLOCKER, доказана снятием `<link>` с `Tab.html`). Первая половина правила (точка проверки предиката) сторожем не закрывается: статикой не отличить «строит разметку» от «читает». Она закреплена структурно — `isTruncated` перед `attachTrunc` в `Tooltip 2.009` — и проверена откатом на linkedom-стенде: откаченный код даёт на неусечённой подписи 1 `.tip` и 1 `.tip-anchor`, исправленный — 0, усечённая подпись тултип сохраняет.
- **Для приёмки:** A1 в отчёте чист. Правку рантайма, которая вставляет узлы в чужую разметку, читать на предмет «под каким условием вставляем» — это суждение, не измерение.
- **Промоут:** выведен 11.09.2026 — закреплён `линтер:A1` (исполняемое). Половина про точку проверки предиката закрыта структурно (`Tooltip 2.009`) и остаётся неизмеримой: статикой не отличить рантайм, который строит разметку, от того, который читает

### Л97. Дата в `data-sort-value` — в формате, который парсит рантайм, а не ISO

- **Дата/зона:** 11.09.2026 | `Projects/test/post/tables/DealsTable.html`, `scripts/ds-table.js`, `сенсор:З5`
- **Симптом:** сортировка колонки «Дата сделки» (`data-sort-type="date"`) молча ничего не делала — клик по стрелке не менял порядок строк. Видимое значение ячейки при этом корректное, «01.09.2026».
- **Причина:** `cellText()` возвращает `data-sort-value` дословно, а `sortKey(value, 'date')` парсит только `DATE_RE = /^(\d{2})\.(\d{2})\.(\d{4})$/`. ISO `2026-09-01` под маску не подходит → ключ `NaN` у всех строк → компаратор возвращает 0 (тот же молчаливый класс вырождения, что Л33). Сенсор `З5` срабатывал на собранном `index.preview.html` ещё 07.09.2026, но регресс из `lessons-cli stats` не был разобран — находка дожила до аудита 11.09.2026.
- **Правило:** значение `data-sort-value` для даты пишется в том же порядке, который парсит рантайм (`ДД.ММ.ГГГГ`), а не ISO. ISO в атрибуте — не «машинный формат», а нечитаемый ключ: сортировка вырождается молча, ошибки нет. И: регресс из `lessons-cli stats` — находка, которую разбирают в том же заходе, а не строка сводки.
- **Для приёмки:** наличие `data-sort-value` не означает работающую сортировку — формат ключа обязан совпадать с парсером рантайма; проверять прогоном или сенсором, а не наличием атрибута.
- **Закрепление:** исполняемое — `сенсор:З5` (ловит ISO-дату в разметке; после правки молчит на `index.preview.html`, `ВЕРДИКТ: OK`). Вторая половина — процедурная: `stats` п. 8 «Порядок в заходе».
- **Промоут:** не промоутить — закрыт исполняемым сторожем `сенсор:З5`.

### Л98. Проверка долга читала заявку, а не факт присутствия — и молчала о неисполненном промоуте

- **Дата/зона:** 11.09.2026 | `.opencode/skills/screen-review/tooling/lessons-cli.mjs`, команда `state`
- **Симптом:** `state` печатал «долг курации: 0», хотя Л91 и Л94 объявляли «Промоут: в выжимке», а в `lessons.md` их не было (выжимка `updated: 10.09.2026` при записи от 11.09.2026). Неисполненный промоут выглядел исполненным, и отчёт о долге молчал.
- **Причина:** долг считался как «нет в выжимке **и** нет поля Промоут»: любая непустая строка в поле считалась решением, включая заявку на включение. Поле «Промоут» объявляет намерение; присутствие записи в выжимке — факт, и проверка читала первое вместо второго (тот же класс, что Л80 и Л81).
- **Правило:** проверка, спрашивающая «сделано ли», читает ФАКТ — заголовок записи в целевой выжимке, — а не объявление о намерении. Заявка «в выжимке / в шард …» без записи — долг; «не промоутить», «выведен … закреплён …», «слит в Лn», «не держим» — решения, долгом не являющиеся. Присутствие считается по ВСЕМ выжимкам (основная и шарды): вынесенная по адресату запись промоутирована (Л79).
- **Для приёмки:** отчёт «долг 0» и «проверка не применилась» с виду одинаковы — новая проверка долга доказывается подброшенной неисполненной заявкой ДО того, как ей поверят (Л80).
- **Закрепление:** исполняемое — `правило:state-долг` в `lessons-cli state`. Доказано откатом на живом репозитории: до правки `state` давал «долг 0» при отсутствующих Л91/Л94; после правки — «долг 2 (Л91 Л94)»; после курации — «долг 0».
- **Промоут:** в шарде `lessons` — правило про оснастку самообучения.

### Л99. Регресс — это ВОЗВРАТ кода, а не первое срабатывание сторожа на новом файле

- **Дата/зона:** 11.09.2026 | `.opencode/skills/screen-review/tooling/lessons-cli.mjs`, команда `stats`
- **Симптом:** `stats` показывал 6 «регрессов», из которых 5 — ложные: правило срабатывало на НОВОЙ странице (`линтер:A2` на Layout, `линтер:B8` на Kanban), на агрегатной цели («линтер:G1» на «62 файлах», `линтер:P4` на `--parity`) или единственный раз на файле, который после этого ни разу не запускался. РЕГРЕСС объявлялся по одному признаку — «код сработал позже дня закрепления».
- **Причина:** проверка не различала «сторож поймал новый экземпляр класса» и «закрытая починка сломалась». Первое — работа сторожа, второе — регресс; признак у них один и тот же (код есть), а различие — в предыстории файла. Плюс агрегатные цели («N файлов», режим `--parity`) вообще не файл, и пофайловый вопрос к ним неприменим.
- **Правило:** регресс — код **вернулся** на файле, который после закрепления уже проходил без него. Первое срабатывание на новом файле регрессом не является; агрегатные цели в пофайловый разбор не идут; если последний прогон файла чист, регресс закрыт и не показывается (иначе закрытый дефект висит вечно, а всегда красный отчёт не несёт сигнала — Л49). Проверка читает историю ФАЙЛА, а не только факт срабатывания.
- **Закрепление:** исполняемое — `правило:stats-регресс` (уточнено). Доказано откатом: подброшены два прогона одного файла — чистый, затем с `B8` → `РЕГРЕСС: 1` и код 1; без подброшенных строк на текущем журнале → `РЕГРЕСС: 0` и код 0.
- **Промоут:** в шарде `lessons` — правило про оснастку самообучения.

### Л100. Правило, у которого отняли вход, молчит — и молчание читается как чистота
- **Дата/зона:** 11.09.2026 | `layout-check.mjs`, `Projects/post/operations-did/**`, `skills/screen-assembly/references/`
- **Симптом:** сенсор давал `ВЕРДИКТ: OK, замечаний 0` на `Portfolio.html`, не прочитав 40 % файла (у `mainPage/index.html` — 56 %). Мутация `ibtn--m` → `ibtn--s` в трёх узлах навигации не поднимала Б15 вовсе. Параллельно тот же дефект жил в эталоне каркаса `references/skeleton.html`, по которому сенсор не запускали ни разу.
- **Причина:** содержимое `<script>` гасится пробелами до всех проверок разметки — это лечение ложных срабатываний (JS-шаблон принимали за готовую разметку). Но рабочие экраны собирают навигацию и таблицы строковыми шаблонами внутри `<script>`, и предмет правил разметки туда и уехал. Гашение входа сняло с правил половину экрана, не тронув ни одного правила: каждое осталось «рабочим» и доказанным на фикстуре.
- **Правило:** у правила разметки два входа — статическая разметка и строковые литералы из `<script>`. Гася вход ради ложных срабатываний, перечисли правила, у которых после этого пропал предмет, и заведи им именованный вход вместо общего. Зелёный вердикт на файле, часть которого не читалась, доказательством не является: доля непрочитанного — это и есть цена вердикта.
- **Для приёмки:** сторож жив на фикстуре — это ещё не «сторож применяется к этому файлу». Проверяется мутацией настоящего экрана: внести дефект, убедиться, что правило сработало, вернуть.
- **Закрепление:** исполняемое — `сенсор:Б15` (именованный вход `markupInScripts`), `сенсор:Б31` (анатомия NavPanel, читает оба входа)
- **Промоут:** выведен 11.09.2026 — закреплён `сенсор:Б15`, `сенсор:Б31`

### Л101. Эталон, из которого копируют, — предмет приёмки наравне с экраном
- **Дата/зона:** 11.09.2026 | `skills/screen-assembly/references/skeleton.html`, `references/patterns.md`, `Projects/test/post/`
- **Симптом:** в эталоне каркаса одновременно жили четыре расхождения с ДС: размер бургера и кнопки выхода, отсутствующий узел закрепления панели, строка пользователя дивом вместо ссылки. В рецептах — строка шапки таблицы без модификатора шапки и своя модалка настройки колонок вместо штатного атрибута. Часть уехала на экраны `Projects/test/post`.
- **Причина:** эталоны и рецепты правятся руками и не входят ни в один целевой набор проверок. Дефект в них не молчаливый, а невидимый: правило, способное его поймать, по этим файлам не запускается. Размер бургера починили в чит-шите 26.08.2026 — в эталоне он прожил ещё две недели и успел разойтись копированием.
- **Правило:** файл, из которого копируют разметку, обязан проходить те же проверки, что и его потомки. Правка эталона заканчивается прогоном сенсора по эталону, а не только по экрану, собранному из него. Проверки, мешающие по устройству (спутник-спека у эталона), отключаются явной веткой с печатью пропуска — молча пропускать нельзя.
- **Для приёмки:** расхождение эталона с чит-шитом ищется сверкой сниппетов, а не взглядом: чит-шит здесь старший источник, эталон — производный.
- **Закрепление:** исполняемое — режим layout-check.mjs --etalons: обход всех `.opencode/skills/*/references/*.html` сенсором, целевой набор строится из дерева, а не перечисляется руками. Вход правила — РЕПОЗИТОРИЙ (каталог, а не файл), файловой фикстурой не доказывается по построению, поэтому доказано откатом, как `линтер:B15` и vendor-scan: подложенный в копию эталона `ibtn--s` даёт FAIL Б15 и код 1, чистый эталон — OK; уведённый глоб даёт «эталонов найдено: 0» и FAIL, а не зелёный вердикт на пустом множестве. В журнал прогонов режим пишет одну сводную строку на обход — пофайловые прогоны отсечены, иначе они вытеснили бы настоящие экраны из окна `stats`. Неисполняемой остаётся вторая половина правила — привычка запускать: у сторожа состояния нет повода запуститься самому, и давность его прогона печатает `lessons-cli state`. Часть класса закрыта отдельно — `сенсор:Б31` ловит анатомию панели навигации в любом файле.
- **Промоут:** выведен 12.09.2026 — закреплён режимом --etalons (исполняемое); в выжимке не держим

### Л102. Сторож заводится там, откуда виден вход, а не там, где удобнее по теме
- **Дата/зона:** 11.09.2026 | `.opencode/skills/screen-review/tooling/vendor-scan.mjs`, `DS-IBP/scripts/spec-audit.mjs`
- **Симптом:** правило нейтральности репозитория (никаких следов посторонних агентных инструментов) держалось текстом и было нарушено трижды: абсолютный путь к внешнему плану в `Projects/post/…/data/`, два URL с именем вендора в скилле композиции. Нашлись они не проверкой, а чтением глазами при разборе другой задачи.
- **Причина:** проверку планировалось повесить проходом аудита ДС. У аудита корень — `DS-IBP/`: он не видит ни `.opencode/`, ни `Projects/`, то есть ровно те два места, где следы и лежали. Сторож получился бы зелёным навсегда, а зелёный сторож читается как отсутствие дефектов.
- **Правило:** место сторожа выбирается по его входу, а не по тематической близости к существующему инструменту. Прежде чем дописывать проход в готовый инструмент, проверь его корень и целевой набор: достаёт ли он до файлов, ради которых правило пишется. Не достаёт — сторож заводится там, откуда достаёт, даже если тема «чужая».
- **Для приёмки:** сторож, у которого стоп-слова записаны литералами, находит сам себя. Либо склейка из кусков, либо исключение собственного файла — и то, и другое объясняется в шапке, иначе следующий читатель «почистит» непонятное.
- **Закрепление:** исполняемое — vendor-scan (отдельный сторож в оснастке, корень репозитория, `node_modules` исключён), доказан откатом: подброшенные следы дают код 1 и шесть находок, после удаления — код 0. Исключения (имя протокола и имя npm-пакета шлюза) записаны в шапке с причиной и датой решения владельца, а не зашиты молча.
- **Промоут:** выведен 11.09.2026 — закреплён vendor-scan

### Л103. Заводя новый вход для старых правил, сохрани в нём улики, по которым они принимают решение
- **Дата/зона:** 12.09.2026 | `layout-check.mjs`, `collectScriptMarkup`, `сенсор:Б15`, `сенсор:Б31`
- **Симптом:** сенсор дал FAIL Б15 «размер не по спеке» на заведомо корректном экране, где размер подставляется переменной: `'<button class="ibtn ibtn--neutral ibtn--' + sz + ' nav__pin">'`. Дефекта нет, экран верный, вердикт красный.
- **Причина:** новый вход `markupInScripts` гасил код между строковыми литералами пробелами. Признак склейки у этой ДС один и давно принятый — `isLiteralAttr` отличает выражение от списка классов по символам `'`, `"`, `+`, `?`, `(`, `)`. Гашение пробелом стёрло ровно эту улику: разорванный атрибут доезжал до правила как `class="ibtn ibtn--   nav__pin"` — синтаксически литеральный и без `ibtn--m`. Вход был новый, а контракт, который его читает, — старый, и о потере улики никто не спросил.
- **Правило:** новый вход для существующих правил обязан нести те же признаки, по которым эти правила принимают решения. Гашение — это потеря информации: символ-заполнитель выбирается так, чтобы он оставался уликой для старшего контракта, а не маскировал её. Проверяется парой «дефект есть / дефекта нет», а не одной стороной: правило, доказанное только на срабатывании, ловит и то, чего нет.
- **Для приёмки:** ложное срабатывание опаснее пропуска. Пропуск оставляет дефект, ложный FAIL приучает не верить сторожу — и тогда перестаёт работать весь чек-лист, а не одно правило.
- **Закрепление:** исполняемое — `сенсор:Б15`: код между литералами заполняется `+`, `isLiteralAttr` применён к обоим входам у Б15 и `сенсор:Б31`. Пробел корпуса закрыт 12.09.2026: в эталон `_base.ok.html` внесён корректный JS-шаблон с атрибутом, разорванным выражением, — состояние БЕЗ дефекта, на котором эталон обязан молчать. Доказано откатом в обе стороны: возврат пробела вместо `+` в `collectScriptMarkup` воспроизводит ложный FAIL Б15 на эталоне, возврат `+` — молчание. Вход из JS-шаблона доказан отдельной фикстурой `Б15@js.bad.html` (тот же дефект, другой вход); сужение входа Б15 до статической разметки роняет её и не трогает статическую пару.
- **Промоут:** выведен 12.09.2026 — закреплён `сенсор:Б15`; в выжимке не держим

### Л104. Сторож состояния пишет в журнал прогонов одну строку на ОБХОД, а не строку на файл
- **Дата/зона:** 12.09.2026 | `layout-check.mjs --etalons`, `runlog.mjs`, `lessons-cli state`
- **Симптом:** два требования к одному режиму читались как несовместимые. «Эталоны в `runs.jsonl` не логировать» — иначе десяток прогонов по эталонам вытеснит настоящие экраны из окна, по которому `stats` делит коды на живые и исчезнувшие, и коды рабочих экранов уедут в «ИСЧЕЗ», то есть будут прочитаны как ОБУЧЕНИЕ. «`state` печатает, когда эталоны прогонялись в последний раз» — иначе пропуск ритуала невидим, а гейта на него нет намеренно. Без записи печатать нечего; с записью подделан сигнал живости правил.
- **Причина:** «прогон» в журнале означал ровно одно — проверку файла, потому что все три инструмента запускались по файлу. У сторожа, чей вход — состояние репозитория, единиц измерения две: РАБОТА (сколько файлов он посмотрел за обход) и РИТУАЛ (сколько раз его вообще позвали). Живость кодов считается по первой, давность ритуала — по второй. Записывая обе одной строкой на файл, приходится выбирать между слепым сигналом и отравленным; выбор кажется вынужденным, пока единицы не разведены.
- **Правило:** сторож, чей вход — состояние репозитория, а не файл, пишет в журнал прогонов ОДНУ сводную строку на обход, под собственным именем инструмента и без кодов; пофайловые прогоны такого сторожа отсекаются от журнала по пути. Гранулярность строки выбирается по тому, чем меряется сигнал, ради которого её пишут: давностью вызова — строка одна на обход; срабатыванием правила на файле — строка на файл. Имя инструмента берётся своё: под именем, которое читает классификатор живости, сводная строка станет данными о правилах, которыми не является.
- **Для приёмки:** проверяется не наличием строки в журнале, а приростом: обход по N файлам обязан дописать ровно одну строку при любом N, и прямой прогон того же файла — ни одной.
- **Закрепление:** исполняемое — `isEtalon` в `runlog.mjs` (прогон по файлу из `skills/*/references/` в журнал не идёт) плюс сводная строка `tool: эталоны`, которую пишет режим --etalons. Вход — репозиторий, файловой фикстурой не доказывается по построению; доказано откатом: со снятым `isEtalon` прямой прогон сенсора по `skeleton.html` дописывает строку (223→224), с ним — не дописывает (225→225), а обход `--etalons` даёт ровно +1 при любом числе эталонов. Тем же заходом поймана вторая утечка того же класса: копия экрана во временном каталоге (`../../…`) — это фикстура, созданная на ходу, и она занимала место настоящего экрана в окне; отсекается `isOutside`, доказано откатом (прогон копии вне корня журнал не растит, прогон настоящего экрана растит). Давность обоих сторожей состояния печатает `lessons-cli state`.
- **Промоут:** в шард `lessons` (автор правила) — 12.09.2026

### Л105. Эталон корпуса — общий вход всех правил: внося в него состояние ради одного, закрываешь фикстуру всем, чей предмет — это состояние
- **Дата/зона:** 12.09.2026 | `tooling/fixtures/_base.ok.html`, `сенсор:Б15`, `сенсор:З11`, `lessons-cli verify`
- **Симптом:** в том же заходе, где в эталон корпуса внесли корректный JS-шаблон с разорванным атрибутом (закрепление Л103 откатом), заводилось правило-замечание З11 «сколько атрибутов сенсор не прочитал». Пара фикстур для него оказалась невозможна: `verify` требует от правила МОЛЧАНИЯ на эталоне, а предмет З11 теперь лежит в эталоне по построению — и убрать его нельзя, на нём держится откат Л103.
- **Причина:** эталон — не фон, а ВХОД, общий у всех правил сразу. Любое состояние, внесённое в него ради одного правила, становится фактом для остальных, и правило, чей предмет — это состояние, теряет возможность быть доказанным парой. Обмена не видно в момент правки эталона: он выглядит добавлением одной строки, а на деле расходует общий ресурс. Проверить нечем — `verify` молчит, пока такого правила нет, а когда оно появляется, причина лежит на две недели раньше.
- **Правило:** прежде чем внести в `_base.ok.html` новое состояние, назови правило, для которого это состояние — ДЕФЕКТ или ПРЕДМЕТ. Есть такое правило или оно посильно — состояние идёт в фикстуру `<ID>[@вариант].bad.html`, а не в эталон. В эталон оно попадает только тогда, когда обязано быть там как состояние без дефекта, и цена — невозможность фикстуры для всякого правила об этом состоянии — называется в шапке эталона и в комментарии правила.
- **Для приёмки:** «у правила нет фикстуры» читается двояко: долг («посильно, не написано») или граница механизма. Различаются причиной, записанной рядом с правилом; правило без такой записи считается долгом.
- **Закрепление:** временное — комментарий у З11 в `layout-check.mjs` и шапка эталона; исполняемым не делается: чтобы поймать это машинно, инструменту пришлось бы знать, какие правила ЕЩЁ НЕ написаны, — а список ненаписанных правил не существует в коде по определению. Частично прикрыто соседом: `stats` делит «не встречался» на «доказан фикстурой» и «фикстуры нет», и второе видно в отчёте как рабочий список. **Продлено 12.09.2026:** предмет требует знания о ненаписанном; сторож невозможен по построению, а не по нехватке рук.
- **Промоут:** в шард `lessons` (автор правила) — 12.09.2026

### Л106. Коробка образца считается от ширины компонента, а не выбирается «на глаз»
- **Дата/зона:** 13.09.2026 | DS-IBP/pages/organisms/Kanban.html, scripts/kanban.page.js — блоки «Тон маркера», «Свёрнутая колонка», «Состояния колонки»
- **Симптом:** заливка hover колонки выходила за рамку образца `.sample__box`, в «Состояниях колонки» появлялся горизонтальный скролл. Линтер и `docs-split check` зелёные, дефект заметил пользователь.
- **Причина:** ширина образцов задана строками в JS (200px, 264px), а колонка ДС не сжимается (`flex: 0 0 --kb-col-w` = 288px). Внутри коробки оставалось 174/238px, лишнее торчало наружу или уходило в скроллпорт `.kanban__viewport`. Статика не видит, что число в строке JS меньше ширины компонента внутри.
- **Правило:** если внутри образца на странице документации стоит компонент фиксированной ширины (`flex: 0 0 N`, `width: N`), ширина коробки считается как N + паддинги + рамки коробки и заводится одной именованной константой с расчётом в комментарии. Сколько образцов в ряд — следствие этой ширины, а не наоборот.
- **Закрепление:** неизмеримое — переполнение образца видно только в раскладке: ширина коробки задаётся строкой в `*.page.js`, ширина компонента — токеном в CSS, а число образцов в ряду зависит от ширины области документации; проверяется замером `getBoundingClientRect` в рендере. Структурно закрыто в Kanban константой `COL_SAMPLE_W` с расчётом от 288px.
- **Промоут:** не промоутить — случай страниц документации с живыми образцами; на экраны `Projects/` не распространяется.

### Л107. Путь в журнал прогонов пишется от корня РЕПОЗИТОРИЯ, а не от корня инструмента
- **Дата/зона:** 13.09.2026 | `DS-IBP/scripts/ds-lint-cli.mjs`, `runlog.mjs` (`isOutside`), `lessons-cli gate`, `lessons-cli stats`
- **Симптом:** с 12.09.2026 ни один прогон линтера по экрану не попадал в `runs.jsonl`: прогон по `pages/…` дописывал строку, по `../Projects/…` — ноль. `stats` показывал коды экранов (A4, A7) как «не встречался, доказан фикстурой», хотя линтер выдавал их минуту назад. Ничего не падало. Найдено замером прироста журнала при первом полном прогоне гейта 13.09.2026.
- **Причина:** линтер отсчитывает пути от корня ДС и отдавал в журнал `../Projects/…` как есть; `runlog.relTarget` разрешает относительный путь от корня репозитория, получает `../Projects/…` — «вне репозитория» — и отсечение временных копий (`isOutside`, Л104) молча отбрасывает строку. Два правильных по отдельности решения — «пути линтера от ДС» и «внешние копии не логировать» — сошлись на одном входе. Писатель журнала не проверял, что запись состоялась: журнал «не имеет права уронить проверку», и промах любого вида глотается `try/catch`.
- **Правило:** инструмент, который пишет в журнал прогонов, передаёт путь в системе отсчёта журнала (корень репозитория) либо абсолютным; относительный путь в своей системе отсчёта не передаётся. Прогон по настоящему файлу обязан давать прирост журнала — отсутствие строки есть дефект, а не норма.
- **Закрепление:** исполняемое — `структурно:lessons-cli gate` (строка `ЖУРНАЛ:`: пофайловый шаг сенсора или линтера по файлу внутри репозитория без прироста `runs.jsonl` — FAIL). Вход — связка двух инструментов и журнала, файловой фикстурой не доказывается; доказано откатом: со старой строкой в `ds-lint-cli.mjs` гейт на `DealsTable.html` даёт `ЖУРНАЛ: прогон … не записан`, с исправлением — нет, и строка пишется с путём `Projects/test/post/tables/DealsTable.html`, как у сенсора; копия экрана вне репозитория по-прежнему не пишется (363→363).
- **Промоут:** в шард `lessons` (автор правила) — 13.09.2026

### Л108. Фрагмент, который вшивается в экран, проверяется в собранном файле, а не как экран
- **Дата/зона:** 13.09.2026 | `Projects/test/post/modals/*.html`, `tables/DealsTable.html`, `layout-check.mjs`, `DS-IBP/scripts/ds-lint-cli.mjs`, `tooling/fragments.mjs`, `lessons-cli gate`
- **Симптом:** четыре фрагмента пилота модульного экрана давали 21 блокер — «нет ds.css», «нет каркаса .nav-layout», «нет .screen.md», «нет FIDELITY-GATE». Первый полный гейт 13.09.2026 был красным из-за них; собранный `index.preview.html` при этом проходил сенсор.
- **Причина:** сенсор знал один вид «не экрана» — исходник с `<ds-include>`, линтер — ни одного: любой html вне дерева ДС для него экран. Кусок разметки, который экраном становится только после сборки, попадал под экранные правила. Определение по одному признаку «нет DOCTYPE» было бы дырой: настоящий экран с забытым DOCTYPE молча перестал бы проверяться.
- **Правило:** фрагмент — файл без `<!DOCTYPE`/`<html>`, на который ссылается `<ds-include src>` из html в той же папке или выше. Инструменты его пропускают строкой `ПРОПУЩЕН:`, а гейт вместо фрагмента проверяет источник и его собранный `*.preview.html`. Фрагмент-сирота проверяется как экран.
- **Закрепление:** исполняемое — `структурно:fragments.mjs` (`includersOf`, импортируют сенсор, `ds-lint-cli` и `gate`). Вход — связка файлов, файловой фикстурой корпуса не доказывается; доказано откатом: с меткой `<ds-include src="modals/DeleteConfirm.html">`, убранной в комментарий, сенсор проверяет `DeleteConfirm.html` как экран (FAIL, 10 блокеров), с меткой — `ПРОПУЩЕН`. Кандидатов «без DOCTYPE» среди 141 html репозитория — ровно эти 4 файла. `gate --dry --changed modals/EditDeal.html` даёт шаги на `index.html` и `index.preview.html`.
- **Промоут:** не промоутить — случай модульной сборки пилота; правило живёт в коде инструментов, помнить его агенту не нужно.

### Л109. Контракт анатомии читает узлы и в JS-шаблоне, а разметка очищается от комментариев первой
- **Дата/зона:** 13.09.2026 | `DS-IBP/scripts/ds-lint.js` (`линтер:F5`, очистка `markup`), `Projects/post/operations-did/mainPage/index.html`, `Portfolio-did/Portfolio.html`, `DS-IBP/fixtures/F5@js.bad.html`
- **Симптом:** F5 выносил BLOCKER «NavPanel использован не целиком: нет .nav__pin, .nav__user-text» на двух экранах Post, где анатомия полная. При заведении фикстуры `F5@js` линтер вместо F5 выдал A6 «нет `<main class="page">`», хотя `<main>` в файле был.
- **Причина:** первое — правило читало один вход из двух: экраны строят панель шаблоном по роли, в статике пустой хост, а `markup` вырезает `<script>` целиком. Второе — порядок очистки: скрипты вырезались раньше комментариев, и слово «<script>» в тексте комментария регулярка приняла за начало скрипта, съев разметку до первого `</script>`.
- **Правило:** правило, которое проверяет наличие узлов компонента, читает и разметку JS-шаблонов (атрибуты `class="…"` inline-скриптов; упоминание селектора в коде узлом не считается), а условие применения — по статическому хосту. Очистка разметки для правил начинается с комментариев: всё, что стоит в комментарии, не может ни включить, ни выключить правило.
- **Закрепление:** исполняемое — `линтер:F5`, пара `DS-IBP/fixtures/F5.bad.html` и `F5@js.bad.html` (`verify --only F5`: оба ДОКАЗАНЫ; на `F5@js` находка называет только `.nav__user-text` — шаблон прочитан). Откат: без чтения шаблонов F5 возвращается на обоих экранах Post. Порядок очистки: отчёты линтера по 109 html до и после совпали, кроме новой фикстуры (A6 ушёл, F5 появился).
- **Промоут:** в шард `lessons` (автор правила) — 13.09.2026

### Л110. Соглашение, которое ни разу не применялось, — непроверенный код: первое применение прогоняется через сторож
- **Дата/зона:** 13.09.2026 | .opencode/skills/screen-review/tooling/lessons-cli.mjs (check, Л-ЯКОРЬ), .opencode/skills/lessons/SKILL.md, снятие правила линтера G1
- **Симптом:** после снятия правила G1 цитаты «линтер:G1» в архиве переведены из обратных кавычек в ёлочки, как велит скилл lessons для мёртвых идентификаторов, — а `lessons-cli check` всё равно дал три БЛОКЕРА Л-ЯКОРЬ «сторожа с таким идентификатором нет».
- **Причина:** скилл объявлял: обратные кавычки — идентификатор живой, ёлочки — цитата снятого. Регулярка якорей в `check` искала пару «пространство имён, двоеточие, идентификатор» в любом обрамлении и ёлочки не исключала. До этого захода в журнале не было ни одной цитаты в ёлочках, поэтому расхождение молчало: соглашение не применялось ни разу и ни разу не проверялось.
- **Правило:** соглашение о форме записи, которое читает инструмент, считается действующим только после того, как инструмент хотя бы раз отработал на его применении. Первое применение соглашения — это прогон сторожа на нём, а не доверие тексту скилла; расхождение чинится в стороже, а не обходом формы записи.
- **Закрепление:** исполняемое — `правило:Л-ЯКОРЬ` в `lessons-cli check`: совпадение, перед которым стоит открывающая ёлочка, якорем не считается. Доказано откатом вручную: с ёлочками — БЛОКЕР 0; одна цитата возвращена в обратные кавычки — БЛОКЕР 1 (Л-ЯКОРЬ, Л84); ёлочки восстановлены — БЛОКЕР 0. Собственного корпуса фикстур у проверок `lessons-cli` нет — это названный ранее пробел (урок о `правило:stats-регресс`), не умолчание.
- **Промоут:** не промоутить — закрыт исполняемой проверкой; ролям помнить не нужно

### Л111. Новый вход (файл журнала, корпус) заводится у одного владельца списка — иначе его получают не все подкоманды
- **Дата/зона:** 13.09.2026 | .opencode/skills/screen-review/tooling/lessons-cli.mjs (`check`, `state`, `journalFiles`), шарды `docs-split` и `lessons`
- **Симптом:** одна и та же цитата снятого правила линтера в обратных кавычках дала три БЛОКЕРА Л-ЯКОРЬ в архиве и ноль — в шарде `lessons/references/lessons.md`. Шарды (25 записей) выпадали из `check` целиком: мёртвый якорь, чужой алфавит, закрепление без уровня там не ловились. Ничего не падало.
- **Причина:** список файлов журнала был объявлен трижды — свой `SHARDS` в `state`, `journalFiles()` для `stats` и захардкоженный `[RAW, CUR]` в `check`. Когда Л79 завёл шарды, их внесли в два списка из трёх. Класс Л43: у правила несколько владельцев, копии расходятся, и расхождение молчит, пока в третьей копии не окажется нужная запись.
- **Правило:** перечень входов инструмента (файлы журнала, корпуса фикстур, реестры) объявляется одной константой, и все подкоманды берут его оттуда. Заводя новый вход, ищи по коду каждое место, где читается старый, а не вспоминай, какие подкоманды его касаются.
- **Закрепление:** исполняемое — `структурно:lessons-cli check`: `SHARDS` объявлен один раз рядом с `RAW`/`CUR`, `journalFiles()` и `state` строятся из него, `check` обходит `journalFiles()`. Доказано откатом вручную: мёртвый якорь в обратных кавычках в шарде — со старым `[RAW, CUR]` БЛОКЕР 0, с правкой БЛОКЕР 1 (Л-ЯКОРЬ, Л99 в шарде); якорь убран — БЛОКЕР 0. Вскрытый долг шардов (Л-ЗАКР у шести записей `docs-split`, Л-СРОК у Л94) оставлен видимым замечаниями, не спрятан.
- **Промоут:** не промоутить — закрыт исполняемой проверкой; ролям помнить не нужно

### Л112. Копия реестра, которую читает агент, либо печатается командой, либо охраняется сторожем паритета
- **Дата/зона:** 13.09.2026 | `.opencode/rules/ds-rules.md` §6, `DS-IBP/specs/_index.md`, `DS-IBP/scripts/spec-audit.mjs` (проход 8)
- **Симптом:** Drawer и Kanban были в манифесте `specs/_index.md`, но отсутствовали в каталоге компонентов `ds-rules §6`, который загружен в контекст каждого агента. Нашлось ручной ревизией, не сторожем. Агент, которому запрещено изобретать компонент, по каталогу решил бы, что Drawer и Kanban в ДС нет.
- **Причина:** каталог — прозаическая копия манифеста. Сторож реестров (`линтер:D1`, `линтер:D5`, урок Л16) сверяет `ds-nav.js`, `index.html` и `_index.md`, а каталог правил агента в его вход не входит. Новый компонент регистрировался в охраняемых реестрах, неохраняемая копия отставала молча.
- **Правило:** у каждой копии реестра, которую читает агент, должен быть способ не отстать: либо её печатает команда (Л50), либо сторож сверяет её с источником в обе стороны. Проза без сторожа допустима только там, где расхождение не меняет решения агента. Сверка, давшая пустой разбор, — находка, а не «чисто».
- **Закрепление:** исполняемое — `аудит:8`: проход сверяет имена манифеста с каталогом §6 в обе стороны, расхождение или пустой разбор дают код 1, правка `ds-rules.md` запускает аудит в гейте. Доказано откатом: без Drawer в каталоге — находка «Drawer» и код 1, с ним — 58/58 и код 0. Таблица хуков `runtime-hooks.md` не охвачена осознанно: компоненты там названы описательно, сверка по именам дала бы около двадцати ложных находок.
- **Промоут:** не промоутить — закрыт исполняемым проходом аудита

### Л113. Спека экрана описывает состояние, а не историю: журнал правок платится заново каждой сессией
- **Дата/зона:** 14.09.2026 | `.opencode/skills/screen-review/tooling/layout-check.mjs` (Б32), `Projects/post/operations-did/Portfolio-did/Portfolio.screen.md`, `.opencode/agents/screen-builder.md`
- **Симптом:** `Portfolio.screen.md` — 852 строки, из них 500 (59 %, ≈12 500 токенов) занимают разделы «Правки 03.09.2026», «Правки 04.09.2026 (пагинация)» и ещё тринадцать таких же. Шаблон спеки такого раздела не предусматривает: в нём 13 разделов, и последний — «Открытые вопросы и допущения».
- **Причина:** правка дописывалась новым разделом, а не вносилась в тот раздел, который описывает изменённое поведение. Каждый заход добавлял свой, и файл превратился в журнал. Пока спеку читал только человек, это было незаметно; при окне сессии 250 000 токенов эти 12 500 платит **каждая** сессия, которая открывает файл, — приёмка, следующая правка, `/resume`. Правило «историю не копить» существовало, но только для handoff, и на спеку никто его не распространил.
- **Правило:** `<Имя>.screen.md` описывает текущее состояние экрана. Изменилось поведение — правится раздел, который его описывает, а не дописывается новый. Разделы вида «Правки ДД.ММ.ГГГГ», «История правок», «Changelog» в спеке запрещены: история живёт в git. Класс шире спеки: документ, который читают агенты, не ведёт журнал внутри себя — растущий хвост оплачивается на каждом чтении.
- **Закрепление:** исполняемое — сенсор:Б32 (пара фикстур `Б32.bad.html` + `Б32.bad.screen.md`, доказан откатом)
- **Промоут:** не промоутить — закреплён `сенсор:Б32`; помнить не нужно, сторож ловит журнал в спеке

### Л114. Оценочная модель доказывается обратным тестом на известном провале, а не правдоподобием числа
- **Дата/зона:** 14.09.2026 | `.opencode/skills/session-plan/tooling/ctx-budget.mjs`, `tooling/stages.json`
- **Симптом:** смета контекстного окна, написанная под заведомо не влезавший заход (сборка `Portfolio`, факт 220 000–235 000 токенов при окне 250 000), выдала на тех же входах `ВЕРДИКТ: OK — 148 486`. Инструмент, написанный, чтобы ловить этот случай, его не поймал и сообщил, что запас 51 514.
- **Причина:** круги правок были заложены плоской долей 25 % от сметы. Доля не видит, из чего круг состоит: главный расход не в самой правке, а в том, перечитывается ли экран целиком (29 720 токенов на экране в 1486 строк) или 20–40 строк вокруг якоря. Модель усредняла и промахивалась в безопасную сторону — то есть молчала ровно там, где должна была кричать. Число при этом выглядело правдоподобно, и на глаз ошибка не видна.
- **Правило:** модель, которая что-то оценивает и печатает вердикт, проверяется прогоном на ЗАВЕДОМО провалившемся входе с известным фактом, а не сверкой «похоже на правду». Оракул — известный провал и ожидаемый вердикт — хранится данными рядом с моделью, а не арифметикой в коде проверки: копия модели в её собственном тесте подтверждает сама себя. Величину, собранную из разнородных слагаемых, нельзя закладывать средней долей: доля прячет то самое слагаемое, ради которого строилась модель.
- **Закрепление:** исполняемое — `ctx-budget.mjs --selftest`, проверка 6 «обратный тест на известном провале»: входы и ожидаемый вердикт лежат в `stages.json` (`обратный_тест`), проверка гоняет ту же функцию сметы, что и CLI. Доказано откатом: коэффициент `перечитывание_доля_целиком` 1 → 0,05 роняет вердикт селфтеста в FAIL.
- **Промоут:** не промоутить — закреплён `ctx-budget --selftest` (обратный тест на известном провале)

### Л115. Сторож, которого никто не вызывает, — не сторож: новый инструмент подключается к гейту в том же заходе, где написан
- **Дата/зона:** 14.09.2026 | `.opencode/skills/screen-review/tooling/lessons-cli.mjs` (шаг гейта `ctx-budget`), `.opencode/skills/session-plan/tooling/ctx-budget.mjs`
- **Симптом:** заход 13.09.2026 написал смету контекста вместе с её сторожем — селфтестом с обратным тестом на известном провале — и закрылся зелёным гейтом. Назавтра выяснилось, что `gate --dry --changed .opencode/skills/session-plan/tooling/stages.json` поднимает ровно один шаг, `vendor-scan`. Селфтест не вызывал никто: коэффициент сметы можно было изменить мимоходом, и смета начала бы врать молча.
- **Причина:** матрица «изменилось → что гонять» (`gateStepsFor`) знает только те пути, которые в неё вписали. Новый инструмент со своим селфтестом кажется самодостаточным — команда есть, откат доказан, — и шаг «вписать в матрицу» выпадает: он не про инструмент, а про то, кто его позовёт. Зелёный гейт в конце захода это не ловит, потому что гейт гоняет то, что в матрице, и о пропущенной строке не знает.
- **Правило:** инструмент, у которого есть собственная проверка (`--selftest`, `check`, `verify`), в том же заходе получает строку в `gateStepsFor` и запись в `GATE_ORDER`/`GATE_FULL`. Закрепление считается сделанным не тогда, когда проверка написана, а когда доказано, что её кто-то запускает: `gate --dry --changed <файл инструмента>` обязан напечатать её шаг. Класс шире гейта: у любой проверки есть вызывающий, и он — часть закрепления, а не следующая задача.
- **Закрепление:** исполняемое — шаг гейта `ctx-budget` (`lessons-cli.mjs`, `gateStep` + `gateStepsFor`); доказано откатом: порча коэффициента `перечитывание_доля_целиком` (1 → 0,05) даёт `ВЕРДИКТ: FAIL` у `gate --changed`, возврат — `OK`. Матрица проверяется командой `gate --dry --changed`.
- **Промоут:** не промоутить — закреплён шагом гейта `ctx-budget`; матрица проверяется `gate --dry --changed`

### Л116. Эталон корпуса обязан нести канонический вход правила — иначе сторож доказан на дефекте и не проверен на норме
- **Дата/зона:** 14.09.2026 | `.opencode/skills/screen-review/tooling/layout-check.mjs` (Б8), `tooling/fixtures/_base.ok.html`, `Projects/test/StageRehearsal.html`
- **Симптом:** Б8 дал FAIL «нет краевых ячеек-разделителей» на строках, где разделители стоят на обоих краях. Ложным оказался сторож: захват строки шёл лоокэхедом до `</div></div>`, а канонический вид колонки действий из чит-шита — `<div class="tc"><div class="tc__hidden">…</div></div>` — этот лоокэхед и закрывает. Хвостовой `.tc--separator` в строку не попадал, последней ячейкой оказывался сам `.tc__hidden`. То есть правило краснело на любом реестре с кнопками в строке — а таких большинство.
- **Причина:** `Б8.bad.html` доказывал, что правило падает на дефекте, но эталон `_base.ok.html` нёс таблицу из четырёх простых ячеек, без единого вложенного узла. Корпус проверяет две вещи — «падает на дефекте» и «молчит на эталоне», — но вторая проверка стоит ровно столько, сколько разметки в эталоне: на бедном эталоне она пропускает ложные срабатывания целыми классами. Дефект прожил незамеченным, потому что продуктовые экраны с такой разметкой к сенсору не подносили.
- **Правило:** эталон корпуса несёт КАНОНИЧЕСКИЙ вид компонента из чит-шита, включая вложенность, а не упрощённый. Правило, разбирающее разметку, берёт границы элемента парным разбором (`tagRange`/`sliceTag`), а не лоокэхедом до закрывающих тегов: лоокэхед ломается на первой же вложенности, и ломается в сторону ложного FAIL. Второй разборщик при этом не заводится — у разметки один владелец (Л43).
- **Закрепление:** исполняемое — сенсор:Б8 переведён на `tagRange`, эталон `_base.ok.html` дополнен колонкой действий с `.tc__hidden`. Доказано откатом: на расширенном эталоне старая редакция правила даёт «срабатывает и на эталоне: оно шумит, а не ловит», новая — ДОКАЗАН; корпус целиком 33 из 33.
- **Промоут:** не промоутить — закреплён `сенсор:Б8` + канонический вход в эталоне корпуса

### Л117. Компонент, выносящий подложку отрицательным margin, сам поставляет контейнер списка — экран зазор не угадывает
- **Дата/зона:** 15.09.2026 | `DS-IBP/styles/entity.css` (Entity 1.008, `.entity-list`), `DS-IBP/specs/Entity.md`, `.opencode/skills/screen-review/tooling/layout-check.mjs` (Б33), корневой `index.html` (хаб проектов)
- **Симптом:** на хабе проектов строки Entity «Операции ДИД» и «Аналитические материалы» стояли в своём контейнере с зазором 4px, и подложка hover одной строки накрывала соседнюю. Нашёл пользователь глазами; сенсор, линтер и гейт были зелёными.
- **Причина:** у `.entity--interactive` `padding: 8px 10px` компенсирован `margin: -8px -10px`: контент стоит на одной вертикали с окружением, а подложка выходит за строку на 8px сверху и снизу. Соседние подложки не пересекаются только при зазоре ≥ 16px. Этот вынос жил только в CSS — ни спека, ни чит-шит, ни страница компонента о нём не говорили, штатного контейнера списка не было. Экран собирал список «на глаз» и не мог знать, что зазор задан компонентом. Класс — «код объявляет поведение, спека молчит» (обратный к Л42).
- **Правило:** если компонент выносит подложку, обводку или тень за свой бокс (отрицательный margin, outline-offset наружу, box-shadow), он же поставляет контейнер для соседства (`.entity-list`), а вынос записывается инвариантом спеки и чит-шита. Экран не подбирает зазор под геометрию чужого компонента. Где геометрию статикой не измерить — правило структурное: строк несколько — контейнер обязан быть.
- **Закрепление:** исполняемое — `сенсор:Б33` (пара `Б33.bad.html` + `Б33@js.bad.html`: две строки в разметке / строка в JS-шаблоне без `.entity-list`); доказан откатом `verify --only Б33`. Структурная гарантия — `.entity-list` в `entity.css` (Entity 1.008)
- **Промоут:** не промоутить — закреплён `сенсор:Б33` и контейнером `.entity-list` в ДС

### Л118. Порог сторожа выводится из определения пункта чек-листа — вне его входа правило молчит, а не подставляет значение по умолчанию
- **Дата/зона:** 15.09.2026 | `.opencode/skills/screen-review/tooling/layout-check.mjs` (K4), `tooling/fixtures/K4.bad.html`, корневой `index.html` (хаб проектов)
- **Симптом:** гейт дал FAIL `K4 «' + esc(g.label) + '»: 0 полей на 3 колонках — положено от 4` на хабе проектов: три колонки по 3 из 12, в каждой — список Entity, полей ReadOnlyField нет вовсе. Раскладку «три колонки по 3» попросил пользователь, и она повторяет рецепт стартовой страницы Layout.
- **Причина:** чек-лист composition-review задаёт K4 по объёму — 3–5 полей → 4 колонки, 6–9 → 6, 10+ → 8, таблица или график → 12. Про тайл без полей в пункте нет ничего. В коде тернарник заканчивался `fcount >= 3 ? 4 : 4`: для 0–2 полей подставлялся тот же минимум 4, то есть правило вышло за свой вход и стало требовать ширину от списков и колонок навигации. Строкой выше код сам признавал, что тайлу-списку колонки не нужны, но до K4 это знание не дошло. Пары фикстур у ветки FAIL не было, и выход за вход никто не видел.
- **Правило:** порог сторожа берётся из определения пункта чек-листа, включая его вход. Для входа, который пункт не описывает, сторож молчит или печатает «неприменимо», а не подставляет значение по умолчанию: такое значение неотличимо от требования. Ложный FAIL на законной раскладке чинится в стороже, а не обходом разметки экрана.
- **Закрепление:** исполняемое — сенсор:K4 (тайл без полей и без таблицы → строка «неприменимо», FAIL только по объёму); пара `K4.bad.html` (8 полей на 3 колонках) доказана откатом `verify --only K4`; хаб с тайлами-списками по 3 колонки — K4 молчит
- **Промоут:** не промоутить — закреплён `сенсор:K4` и парой фикстур

### Л119. Поведение внешнего инструмента утверждается по его установленной версии, а решение о его конфигурации держит сторож, а не абзац README
- **Дата/зона:** 15.09.2026 | `.opencode/opencode.json` (перенесён из корня), `.opencode/README.md` разделы 2 и 4, `tooling/agent-config.mjs`, `tooling/lessons-cli.mjs` (`GATE_ROOTS`, шаг `agent-config`)
- **Симптом:** README утверждал «конфиг внутри `.opencode/` подхвачен не будет» и «модель в конфиге намеренно не задана». Оба утверждения были ложью: opencode 1.18.30 читает `.opencode/opencode.json` и грузит его поверх корневого, а в конфиге стояли `agent.plan.model` и `agent.build.model`. Модель `deepseek-v4-pro` на рабочем контуре снята, провайдер там другой — агент `plan` остановился бы на `Model not found`. Попутно: гейт следил за конфигом по пути `opencode.json` в корне; после переноса `fingerprint()` проглотил бы отсутствующий путь, и правки прав молча ушли бы из-под гейта.
- **Причина:** поведение инструмента записано пересказом документации, где загрузка из `.opencode/` почти не описана, и ни разу не сверено с кодом установленной версии. Решение «модель не задаётся» жило только прозой — ключ дописали в конфиг, и ничто не покраснело. Список путей гейта — литералы: у переехавшего файла путь не обновляется сам, а отсутствующий путь `fingerprint()` пропускает без звука.
- **Правило:** утверждение «инструмент делает / не делает X» записывается только после проверки на установленной версии (код, `debug`-команда, прогон на копии), с номером версии рядом. Решение о конфигурации, нарушение которого не падает сразу (второй слой прав, модель, которой нет на другой машине), закрепляется сторожем в гейте в том же заходе. Файл, переехавший внутри репозитория, переносится и в литералы гейта; старый путь остаётся в слежении, если его повторное появление — дефект.
- **Закрепление:** исполняемое — `agent-config.mjs` (КФ1 конфиг в корне, КФ2 ровно один `.opencode/opencode.json`, КФ3 ключи модели), шаги гейта `agent-config` и `agent-config --selftest`; корневые и новые пути конфига в `GATE_ROOTS`. Доказано откатом: на репозитории до правки КФ3 дал `ВЕРДИКТ: FAIL` на `agent.plan.model` и `agent.build.model`, после удаления блока — `OK`; селфтест — 9 кейсов на временном дереве, эталон молчит. Матрица: `gate --dry --changed opencode.json,.opencode/opencode.json` печатает оба шага. Граница: шапки `.opencode/agents/*.md` на ключ `model` не проверяются — сейчас его там нет.
- **Промоут:** не промоутить — закреплён шагом гейта `agent-config`

### Л120. Состав строк меняет потребитель — рантайм узнаёт об этом наблюдателем, а не договорённостью о вызове хука
- **Дата/зона:** 16.09.2026 | DS-IBP/scripts/tbl-pin.js, DS-IBP/scripts/ds-table.js, DS-IBP/scripts/ds-table-settings.js, Projects/post/operations-did/Portfolio-did/Portfolio.html
- **Симптом:** в реестре сделок создаёшь сделку — у новой строки при горизонтальном скролле уезжают закреплённые колонки, у всех остальных держатся. Нашёл пользователь. Тем же пробелом объяснялись ещё три случая, до которых он не дошёл: после ресайза колонки новая строка разъезжается с шапкой, после переноса колонки её значения встают под чужие заголовки, а модалка настройки колонок новую строку не видит вовсе.
- **Причина:** удержание держит нативный `position: sticky`, а инсеты `left`/`right` пишет инлайном рантайм — по событиям закрепления, переноса, ресайза и ResizeObserver шапки. Вставка строки ни одного из них не вызывает, ширины шапки при этом не меняются. Ячейки новой строки получали `position: sticky` из CSS и оставались с `left: auto`: sticky без инсета стоит на своём месте и едет с контентом. Механизм дефекта шире симптома: рантаймы колонок (закрепление, ресайз, перенос, настройка) переписывают ТОЛЬКО те строки, что уже стоят в таблице, а строку дописывает экран — по разметке, снятой на момент авторства.
- **Правило:** рантайм, который правит все строки таблицы разом, обязан сам ловить появление новой строки (наблюдатель по составу контейнера), а не полагаться на то, что потребитель позовёт хук. Договорённость «после вставки строки позови `rowsChanged()`» закреплением не является: вызов забывают, а дефект молчаливый — строка выглядит правильной ровно до того, как тронут колонки. Ячейка дописанной строки сопоставляется с колонкой ключом (`data-col` на `.th` и `.tc`), а не позицией: после переноса и скрытия позиция врёт.
- **Закрепление:** исполняемое — `структурно:TableCell 2.019` (MutationObserver по составу `.tbl` в `tbl-pin.js` пересчитывает инсеты; `DSTable.adoptRow` приводит дописанную строку к форме шапки — порядок, треки, ячейка скрытой колонки на склад строки; `ds-table-settings.js` догоняет состав строк перед открытием модалки). Вход — рантайм и живая раскладка, файловой фикстурой корпуса не доказывается. Доказано откатом на настоящем экране в headless-браузере: при отключённом `window.MutationObserver` (обе новые ветки стоят за этой проверкой) сценарий «создать сделку» даёт 5 из 9 проверок — у новой строки инсеты пусты (`-/-` против `0px`/`8px`/`158px` у строк разметки), нет `data-pin-side`, после переноса колонки порядок ячеек остаётся исходным; с рантаймом — 9 из 9. Отдельным стендом: скрытая колонка и пересчёт на таблице вне потока.
- **Промоут:** не промоутить — закрепление исполняемое с момента записи: правило держит рантайм ДС, экрану помнить его не нужно. В выжимке запись занимала бы место под долг, которого нет.
